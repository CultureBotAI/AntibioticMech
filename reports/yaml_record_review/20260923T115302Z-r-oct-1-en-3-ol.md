# YAML Record Review: (R)-oct-1-en-3-ol

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml
- Started UTC: 2026-09-23T11:53:02Z
- Finished UTC: 2026-09-23T11:53:47Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | CHEBI:39932 |
| Label | (R)-oct-1-en-3-ol |
| Path | data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml |
| Class | ANTIMICROBIAL_UNSPECIFIED |
| Curation status | SEEDED |
| Grounding status | EXACT |
| Maintained owner | Generated from `data/raw/chebi_antimicrobials.tsv`; do not hand-edit the generated YAML |
| Source concept | CHEBI / CHEBI:39932 / `antibioticmech:chebi-fcb465dfd4` |

The entire target YAML was read before judging the record. The generated record contains ChEBI-seeded identity, definition, synonyms, parent, role, structure, xrefs, and source-concept fields only: it has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, `datasets`, `discussions`, or record-level `evidence`.

## Validation

| Check | Result |
|---|---|
| `rg --no-ignore --hidden --files -g 'AGENTS.md'` | Passed; no repo-local `AGENTS.md` was found, with hidden and ignored files included. |
| `rg --no-ignore --hidden '(^identifier: CHEBI:39932$|source_id: CHEBI:39932$|^CHEBI:39932\t|^review-readiness\tCHEBI:39932\t|r-oct-1-en-3-ol|oct-1-en-3-ol)' data/antibiotics data/raw curation reports/yaml_record_review` | Passed; resolved the target YAML, the raw ChEBI inventory row, the `PATHS.tsv` row, and the `review-readiness` row. The search also surfaced the broader achiral `CHEBI:34118` record and the `(S)` enantiomer `CHEBI:46735`, which are sibling records rather than the target. No prior `r-oct-1-en-3-ol` review report was found in `reports/yaml_record_review` with ignored files included. |
| `just validate data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml` | Passed; `linkml-validate` reported no issues. |
| `just validate-strict data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml --out /tmp/antibioticmech-r-oct-1-en-3-ol-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed; 2,939 records expected and present, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 120` | Passed; regenerated the review-readiness queue and showed `CHEBI:39932` immediately after `CHEBI:37907`. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-r-oct-1-en-3-ol-worklist.tsv` | Passed; wrote 8,252 rows. `CHEBI:39932` appears on the `mechanism` queue and the `review-readiness` queue only. |
| `just lint` | Passed; Ruff reported all checks passed. |
| `git diff --cached --check` | Passed after staging this ignored report. |
| Narrow single-record term/reference/history validators | Not available for plain ChEBI-seeded records; the repository exposes schema validation for a single record plus full-corpus generated-state and worklist checks. |

## Identity and Grounding

The record denotes the exact individual ChEBI structure named by the target:

- The official EBI OLS term detail for `CHEBI:39932` returned the non-obsolete defining ChEBI term `(R)-oct-1-en-3-ol` with definition `An oct-1-en-3-ol that has R-configuration.`
- The OLS term detail for `CHEBI:39932` matched the generated `smiles`, Standard InChI, Standard InChIKey `VSMOENVRRABVKN-QMMMGPOBSA-N`, formula `C8H16O`, charge `0`, average mass `128.215`, monoisotopic mass `128.12012`, CAS, ChemSpider, FoodDB, HMDB, LIPID MAPS, PDB-CCD, and Reaxys xrefs. The seeder normalizes OLS `foodb.food:FDB003350` to the schema namespace `foodb.compound:FDB003350`.
- The OLS parent lookup for `CHEBI:39932` returned `CHEBI:34118` / `oct-1-en-3-ol`; that parent has Standard InChIKey `VSMOENVRRABVKN-UHFFFAOYSA-N`, so the record's `parent_compounds: CHEBI:34118` is a broader achiral parent and not an exact xref.
- The ignored-inclusive YAML search for `standard_inchi_key: VSMOENVRRABVKN-QMMMGPOBSA-N`, `pdb-ccd:3OL`, and `antibioticmech:chebi-fcb465dfd4` under `data/antibiotics -g '*.yaml'` found only this record.

No identity, grounding, or seeded structure conflict was found. Seeded identity, definition, synonym, parent, activity-role, structure, xref, or source-concept fixes would be owned by `data/raw/chebi_antimicrobials.tsv`, `curation/decisions.tsv`, the ChEBI extractor, or the seeder rather than by direct generated-YAML edits.

## Evidence

Existing evidence-backed claim objects are absent, so there were no claim-level citations, snippets, molecular-target assertions, resistance mechanisms, activity observations, producer claims, datasets, or causal edges to verify.

Bounded literature checks found exact-enantiomer and achiral leads but no curated claim-level evidence already on the record:

- The raw ChEBI source row for `CHEBI:39932` carries PMID:11114310, PMID:19238325, PMID:26126831, PMID:26581402, PMID:28018630, PMID:30923972, PMID:32120462, and PMID:34023691 as source references. These are discovery leads, not record evidence items.
- `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '(R)-oct-1-en-3-ol antimicrobial mechanism fungus bacteria' --limit 20 --output /tmp/antibioticmech-r-oct-1-en-3-ol-pub.jsonl` found 0 PubMed candidates; Semantic Scholar returned HTTP 429.
- `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '"(R)-1-octen-3-ol" OR "(3R)-oct-1-en-3-ol" OR "FEMA 4492" OR "VSMOENVRRABVKN"' --limit 20 --output /tmp/antibioticmech-r-oct-1-en-3-ol-exact-pubmed.jsonl` returned exact-enantiomer leads, including PMID:32120462 for biosynthesis of `(R)-(-)-1-octen-3-ol` in recombinant *Saccharomyces cerevisiae* from *Tricholoma matsutake* genes and PMID:30923972 for R/S enantiomer effects on *Penicillium chrysogenum* gene expression.
- `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '"1-octen-3-ol" antimicrobial fungi bacteria' --limit 20 --output /tmp/antibioticmech-r-oct-1-en-3-ol-1-octen-pubmed.jsonl` returned 20 achiral `1-octen-3-ol` leads. Those may inform follow-up searches but must not be promoted onto the exact `(R)` record unless the inspected source reports enantiomeric identity or a racemic/achiral activity claim with clear structure scope.

## Completeness

The record is structurally complete enough for a seed-only exact ChEBI term:

- Identity and structure are grounded to ChEBI and reproduce from `data/raw/chebi_antimicrobials.tsv`.
- The broader achiral `CHEBI:34118` term is retained as `parent_compounds`, not mis-modeled as an exact `xrefs` value.
- The exact ChEBI-provided structure xrefs are present.
- The ChEBI `antimicrobial agent` role is retained as `activity_roles`; the file class `ANTIMICROBIAL_UNSPECIFIED` is appropriate because ChEBI does not assert a narrower antibacterial, antifungal, antiprotozoal, antiviral, or biocide role on `CHEBI:39932`.

Consequential gaps remain:

- No mode of action has been curated.
- No target scope has been curated.
- No molecular target or causal graph has been curated.
- No exact-enantiomer `ActivityObservation` has been curated.
- No producer claim has been curated despite exact-enantiomer biosynthesis literature leads.

The full worklist row for `CHEBI:39932` reports `MECHANISM_REVIEW: mechanism is absent; 8 source literature lead(s), 0 record evidence item(s), 0 target(s)`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | `CHEBI:39932` is not mechanism-reviewed: it lacks `mode_of_action`, target scope, molecular targets, and a causal graph for the exact `(R)` enantiomer. | The target YAML has no mechanism or target fields; `just worklist --limit 0 --tsv /tmp/antibioticmech-r-oct-1-en-3-ol-worklist.tsv` lists `mechanism	CHEBI:39932	(R)-oct-1-en-3-ol	CHEBI		0 CARD target(s), 0 resistance edge(s) to build on`; the review-readiness row reports `MECHANISM_REVIEW: mechanism is absent; 8 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | `data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml`, written through a validated one-off curation mutator that calls `record_curation_event` and `write_validated_antibiotic`. |
| Blocker | None found. Exact ChEBI grounding, definition, structure, parent placement, xref placement, source concept, and corpus reproduction all check out. | See `Identity and Grounding` and `Validation`. | Not applicable. |
| Minor | None found. | Not applicable. | Not applicable. |

## Recommended Edits

1. Curate exact `(R)-oct-1-en-3-ol` mechanism evidence from primary literature, or record a bounded `UNKNOWN`/`CURATION_TODO` discussion if the mechanism remains unresolved after exact-enantiomer source inspection.
   - Maintained path: `data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml` through a guarded `/tmp` mutator using `record_curation_event(..., curator="codex", llm_assisted=True)` and `write_validated_antibiotic`.
   - Candidate leads: PMID:30923972 for enantiomer-specific fungal gene-expression effects and the source PMIDs carried on the ChEBI raw row.

2. Evaluate whether exact-enantiomer biosynthesis and activity claims are supportable.
   - Maintained path: `data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml` through the same guarded write path.
   - Candidate producer lead: PMID:32120462, which reports biosynthesis of `(R)-(-)-1-octen-3-ol` from *Tricholoma matsutake* genes in recombinant *Saccharomyces cerevisiae*.
   - Any `ActivityObservation` must identify the assayed organism, outcome, assay, value qualifier, value, units for MIC measurements, and claim-level evidence; achiral `1-octen-3-ol` activity cannot be silently re-scoped to this exact enantiomer.

## Follow-up Checks

- Inspect full text for PMID:30923972 to decide whether the reported `(R)-(-)-1-octen-3-ol` effects on *Penicillium chrysogenum* can support a mechanism, exact activity observation, dataset, or nonmechanistic discussion.
- Inspect full text for PMID:32120462 to decide whether `CHEBI:39932` should receive a `producer_organisms` claim for *Tricholoma matsutake*.
- Retry Semantic Scholar for `"(R)-oct-1-en-3-ol" antimicrobial mechanism` after its HTTP 429 clears.
- After any curation edit, run `just validate-strict data/antibiotics/unspecified/r-oct-1-en-3-ol.yaml --out /tmp/antibioticmech-r-oct-1-en-3-ol-strict-after.tsv`.
- Run `just verify-corpus` and confirm curator-owned additions persist without drift.
- Run `just worklist --queue mechanism --limit 0 --tsv /tmp/antibioticmech-mechanism-after.tsv` and verify `CHEBI:39932` leaves the mechanism queue if a concrete mechanism is added.
- Run `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-after.tsv` and verify the row moves from `MECHANISM_REVIEW` to the next unresolved evidence gate.
- Run `just qc` before opening a curation PR.

## Additional Notes

- Search results, PubMed abstracts, and OLS responses were treated as leads or identifier checks only; no generated report asserts a new biological mechanism from them.
- Publication discovery was run with `NCBI_EMAIL` unset, as required by `.claude/skills/curate-yaml-record/SKILL.md`.
- Google Scholar was not queried because it uses SerpAPI and requires explicit approval.
- No generated YAML was edited.
