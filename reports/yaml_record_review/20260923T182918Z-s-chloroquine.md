# YAML Record Review: (S)-chloroquine

- Repository: AntibioticMech
- Record: `data/antibiotics/antiprotozoal/s-chloroquine.yaml`
- Started UTC: 2026-09-23T18:24:10Z
- Finished UTC: 2026-09-23T18:29:18Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiprotozoal/s-chloroquine.yaml` |
| Identifier | `CHEBI:39254` |
| Label | `(S)-chloroquine` |
| Class | `ANTIPROTOZOAL` |
| Grounding | `EXACT` |
| Status | `SEEDED` |
| Source concept | `CHEBI:39254` from ChEBI `2026-08-30` |
| Structure | `WHTVZRBIWZFKQO-AWEZNQCLSA-N`, `C18H26ClN3`, neutral |

The target resolves unambiguously through `data/antibiotics/PATHS.tsv`, which
maps `CHEBI:39254` to `ANTIPROTOZOAL/s-chloroquine`. The full target YAML was
read before review. It is a generated ChEBI-only seed with exact Beilstein and
CAS cross-references, ChEBI-derived `antimalarial` and `anticoronaviral agent`
activity roles, no `evidence`, no `mode_of_action`, no `molecular_targets`, no
`activity_observations`, no `causal_graphs`, and only seed/reseed curation
history.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiprotozoal/s-chloroquine.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/s-chloroquine.yaml --out /tmp/s-chloroquine-validate-strict.tsv` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, with no missing, unexpected, drifted, absent-from-`PATHS.tsv`, or stale-lockfile rows. |
| `just worklist --limit 0 --tsv /tmp/s-chloroquine-worklist.tsv` | Pass; `CHEBI:39254` appears only under `mechanism` and `review-readiness`. It is absent from minted-identity, xref, multi-component, producer-candidate, activity-candidate, structure-unreviewed, and target-evidence queues. |
| `just review-queue --limit 115` | Pass; `CHEBI:39254` appears as the next exact unreviewed record after the already-reviewed `(S)-butoconazole nitrate` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Pass; Ruff reported all checks passed. |
| `git diff --cached --check` | Pass after force-adding the ignored report. |
| Term/reference/history validators | No narrower single-record term, reference, or history validator is exposed for a plain ChEBI-seeded record in the justfile. The run used single-record schema checks plus full-corpus worklist and reproducibility checks instead. |

The full worklist also repeated the known unrelated `ARO:3000337`
cross-reference refusal for iclaprim and `CHEBI:31724`; it is not connected to
`CHEBI:39254` and did not affect this record.

## Identity and Grounding

The identity is internally consistent and matches the committed ChEBI inventory
row in `data/raw/chebi_antimicrobials.tsv`:

| Claim | Review |
|---|---|
| ChEBI identifier and label | `CHEBI:39254` is the live OLS ChEBI term for `(S)-chloroquine` and is not obsolete. |
| Parent | The local parent `CHEBI:3638` is broader, stereochemically unspecified chloroquine. The OLS `hierarchicalParents` endpoint also returns `CHEBI:3638` as the sole hierarchical parent of `CHEBI:39254`. |
| Opposite enantiomer | OLS exposes a reciprocal `is_enantiomer_of` relation from `CHEBI:39254` to `CHEBI:48811`, the distinct `(R)-chloroquine` ChEBI term. |
| Stereochemistry boundary | The target is not broader chloroquine `CHEBI:3638`, whose Standard InChIKey is `WHTVZRBIWZFKQO-UHFFFAOYSA-N`, and it is not `(R)-chloroquine` `CHEBI:48811`, whose Standard InChIKey is `WHTVZRBIWZFKQO-CQSZACIVSA-N`. This record has Standard InChIKey `WHTVZRBIWZFKQO-AWEZNQCLSA-N`. |
| Xrefs | `beilstein:7517230` and `cas:58175-86-3` are present on the OLS `CHEBI:39254` term and in the committed ChEBI inventory row for exact `CHEBI:39254`. PubChem resolves CAS `58175-86-3` to CID `639540` with the same Standard InChIKey and formula, which is consistent with retaining that same-structure xref. |
| Filing class | `ANTIPROTOZOAL` is consistent with ChEBI role `CHEBI:38068` / `antimalarial`. The additional `CHEBI:149553` / `anticoronaviral agent` role is retained in `activity_roles`; `conf/sources.yaml` gives `CHEBI:38068` higher filing priority than `CHEBI:149553`. |

The structural fields agree with the maintained ChEBI inventory, live OLS, and
PubChem for exact CAS `58175-86-3`: SMILES
`CCN(CC)CCC[C@H](C)Nc1ccnc2cc(Cl)ccc12`, Standard InChI
`InChI=1S/C18H26ClN3/c1-4-22(5-2)12-6-7-14(3)21-17-10-11-20-18-13-15(19)8-9-16(17)18/h8-11,13-14H,4-7,12H2,1-3H3,(H,20,21)/t14-/m0/s1`,
InChIKey `WHTVZRBIWZFKQO-AWEZNQCLSA-N`, formula `C18H26ClN3`,
charge `0`, average mass `319.880`, and monoisotopic mass `319.18153`.

Unlike the exact `(R)-chloroquine` ChEBI row, the exact `(S)-chloroquine` row
does not carry upstream `pdb-ccd:CLQ`. The broader racemic/unspecified
`CHEBI:3638` row does carry `pdb-ccd:CLQ`, but that broader identifier is
correctly not promoted onto exact `CHEBI:39254`.

## Evidence

The record has no local `evidence` objects. That is acceptable for generated
identity, structure, filing, and role imports because their provenance is the
ChEBI source concept and committed inventory row, but it is not enough to make
the antimalarial or anticoronaviral roles curator-reviewed and it is not enough
to assert an exact mode of action.

The relevant publication searches were bounded as follows, with
`NCBI_EMAIL` unset:

| Query | Result |
|---|---|
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '"(S)-chloroquine" OR "(+)-chloroquine" OR 58175-86-3 OR WHTVZRBIWZFKQO-AWEZNQCLSA-N' --limit 20 --output /tmp/s-chloroquine-publications-exact.jsonl` | PubMed returned 20 candidates; Semantic Scholar returned HTTP 429. The result set was dominated by recent broader chloroquine leads, not exact `(S)-chloroquine` primary evidence. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query 'chloroquine enantiomers Plasmodium falciparum IC50 stereoselective' --limit 20 --output /tmp/s-chloroquine-publications-enantiomer-pf.jsonl` | PubMed returned one mefloquine-analogue enantiomer lead, PMID:22401346; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '3527245 OR 3552292 OR 396700 OR "In vitro activity of chloroquine, the two enantiomers of chloroquine" OR "Antimalarial activity of the optical isomers of chloroquine diphosphate"' --limit 20 --output /tmp/s-chloroquine-publications-pmids.jsonl` | PubMed returned 7 candidates and Semantic Scholar returned HTTP 429. This retrieved exact enantiomer activity leads PMID:3527245, PMID:3552292, and PMID:396700. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '"(+)-chloroquine" "(-)-chloroquine" Plasmodium heme hemozoin beta-hematin' --limit 20 --output /tmp/s-chloroquine-publications-heme.jsonl` | PubMed returned 20 broader chloroquine, antimalarial-analogue, heme, or hemozoin leads; Semantic Scholar returned HTTP 429. The inspected public metadata did not identify an exact `(S)-chloroquine` heme/hemozoin assay. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '"(S)-chloroquine" OR "(+)-chloroquine" SARS-CoV-2 coronavirus anticoronaviral' --limit 20 --output /tmp/s-chloroquine-publications-antiviral.jsonl` | PubMed returned one broader COVID-19 management review mentioning chloroquine and hydroxychloroquine repurposing; Semantic Scholar returned HTTP 429. |

The PubMed searches found useful near misses and follow-up leads, not exact
curation support:

| Lead | Scope problem |
|---|---|
| `PMID:3527245` | Reports a 48-hour in vitro comparison of chloroquine, both chloroquine enantiomers, desethylchloroquine, and pyronaridine against two `Plasmodium falciparum` strains. The abstract does not expose an exact `(S)-chloroquine` strain/value row, so full-text table inspection is required before adding an `ActivityObservation`. |
| `PMID:3552292` | Appears to be a second report of the same in vitro enantiomer comparison but has no PubMed abstract. |
| `PMID:396700` | Reports optical-isomer chloroquine diphosphate efficacy against `Plasmodium berghei` in mice. The public abstract distinguishes d- and l-chloroquine rather than local `CHEBI:39254` / `CHEBI:48811` identifiers, so it needs full-text stereochemical mapping before any exact `(S)-chloroquine` claim is added. |
| `PMID:22401346` | Reports enantiopure 4-aminoalcohol quinoline analogues, not exact chloroquine. |
| `PMID:33966618` | Mentions broader chloroquine in a COVID-19 repurposing review, not exact `(S)-chloroquine` anticoronaviral evidence. |

## Completeness

- Identity, exact ChEBI grounding, stereospecific chemical structure, parent
  chloroquine relationship, Beilstein xref, CAS xref, and the ChEBI-derived
  role set are complete enough for a generated seed.
- The record correctly does not import broader chloroquine CAS, drug-level,
  KEGG, LINCS, PDB CCD, Reaxys, patent, or Wikipedia xrefs onto isolated
  `(S)-chloroquine`.
- The `mechanism` and `review-readiness` worklist entries are expected because
  exact `CHEBI:39254` still has no activity observations, mode of action,
  mechanism scope, molecular targets, or causal graph.
- Empty `molecular_targets`, `activity_observations`, `resistance_mechanisms`,
  `producers`, `clinical_status_assertions`, `datasets`, `causal_graphs`, and
  `discussions` are appropriate for now. The bounded searches found exact
  antimalarial enantiomer leads that need full-text follow-up, but no public
  abstract exposed an exact `(S)-chloroquine` value or mechanism row that can
  be safely curated from metadata alone.
- An ignored-inclusive exact search over `curation`, `data/raw`,
  `data/antibiotics`, `reports/yaml_record_review`, `docs`, `src`, `scripts`,
  and `tests` found `CHEBI:39254` only in the target YAML, `PATHS.tsv`, the raw
  ChEBI row, the review queue, and the newly added report. No exact prior
  review report for `CHEBI:39254` was found.

## Findings

| Severity | Finding | Evidence | Maintained owner for a future fix |
|---|---|---|---|
| blocker | None found | The exact ChEBI identity, stereospecific structure, parent relationship, role terms, exact xrefs, class, generated status, and schema shape are internally consistent. | Not applicable |
| major | Exact `(S)-chloroquine` still needs exact antimalarial activity and mechanism curation before this record can become `REVIEWED`. | The ChEBI row supplies `antimalarial` and `anticoronaviral agent` roles but no source PMIDs; `just worklist --limit 0 --tsv /tmp/s-chloroquine-worklist.tsv` keeps `CHEBI:39254` in `mechanism` and `review-readiness`; the YAML has no `evidence`, `activity_observations`, `mode_of_action`, `molecular_targets`, or `causal_graphs`; PubMed found PMID:3527245, PMID:3552292, and PMID:396700 as exact or likely exact enantiomer activity leads, but their public abstracts do not expose an exact `(S)-chloroquine` activity value or a heme/hemozoin mechanism claim. | Add future curator-owned exact claims to `data/antibiotics/antiprotozoal/s-chloroquine.yaml` only if exact `(S)` evidence is found, writing through `record_curation_event` and `write_validated_antibiotic`. If ChEBI later changes this role assertion, `data/raw/chebi_antimicrobials.tsv` and the ChEBI inventory extractor own that refresh. |
| minor | None found | No spelling, style, provenance, or non-blocking xref issue was found for this seed. | Not applicable |

## Recommended Edits

1. Leave the generated identity, structure, parent, activity-role, xref, and
   filing fields unchanged.
2. Inspect full text and tables for PMID:3527245 and PMID:3552292. If either
   reports an exact `(S)-chloroquine` value against a named `P. falciparum`
   strain, add an `ActivityObservation` with the method, units, strain, value,
   material identity, and PMID evidence.
3. Inspect PMID:396700 and map d-/l-chloroquine diphosphate to the exact ChEBI
   enantiomer before using it for an exact mouse-efficacy or activity claim.
4. Leave `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`,
   and `causal_graphs` absent unless an exact `(S)-chloroquine` primary source,
   or a source explicitly stating that the enantiomer shares racemic
   chloroquine's heme/hemozoin mechanism, supports the narrower claim.
5. Keep `curation_status: SEEDED` until the exact structure, ChEBI grounding,
   filing class, any added antimalarial or anticoronaviral activity, mode, and
   known molecular target all satisfy the repository `REVIEWED` gate.

## Follow-up Checks

After any future curator-owned exact mechanism edit, rerun:

- `just validate data/antibiotics/antiprotozoal/s-chloroquine.yaml`
- `just validate-strict data/antibiotics/antiprotozoal/s-chloroquine.yaml --out /tmp/s-chloroquine-validate-strict.tsv`
- `just verify-corpus --summary`
- `just worklist --limit 0 --tsv /tmp/s-chloroquine-worklist.tsv`
- `just review-queue --limit 115`
- `just lint`
- `git diff -- data/antibiotics/antiprotozoal/s-chloroquine.yaml curation/decisions.tsv src scripts tests`
- Manual full-text review that any cited PMID/DOI used for an activity
  observation, target, or causal edge identifies exact `(S)-chloroquine`, not
  broader racemic chloroquine, `(R)-chloroquine`, hydroxychloroquine, or a
  chloroquine analogue.

## Additional Notes

- This was a read-only review of one generated YAML record. No generated record,
  ChEBI inventory, queue TSV, rendered page, or curation history was edited.
- The review intentionally did not create a generic discussion for the missing
  mechanism. The gap is already expressed by the absent mechanism and worklist
  entries.
- The exact prior-review search included ignored files via
  `rg --no-ignore --hidden`; the file-presence check used `find`, which is
  also gitignore-independent.
