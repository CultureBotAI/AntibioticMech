# YAML Record Review: (−)-annonaine

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiprotozoal/annonaine.yaml`
- Started UTC: 2026-09-24T10:17:00Z
- Finished UTC: 2026-09-24T10:29:54Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiprotozoal/annonaine.yaml` |
| Class | `ANTIPROTOZOAL` |
| Identifier | `CHEBI:76` |
| Label | `(−)-annonaine` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | ChEBI `CHEBI:76`, version `2026-08-30`, minted key `antibioticmech:chebi-e3d5322349` |
| Standard InChIKey | `VZTUKBKUWSHDFM-CYBMUJFWSA-N` |
| Xrefs | `cas:1862-41-5`, `hmdb:HMDB0030348`, `kegg.compound:C09339`, `knapsack:C00001807`, `knapsack:C00027281`, `reaxys:90236` |

`annonaine.yaml` is a generated exact ChEBI record for the neutral aporphine
alkaloid `CHEBI:76`. It carries ChEBI-owned identity, structure, parentage,
equivalent xrefs, and the ChEBI trypanocidal and antiplasmodial roles that file
it under `ANTIPROTOZOAL`. It contains no record-level evidence, curated
`mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`,
`activity_spectrum`, `resistance_mechanisms`, `producer_organisms`,
`clinical_status`, `datasets`, `discussions`, or `causal_graphs`.

## Validation

| Command or check | Result |
|---|---|
| `find . -name AGENTS.md -print` | Passed before review; no local `AGENTS.md` files were present. `find` included hidden and ignored paths. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; exact `CHEBI:76` is the row after the just-reviewed `CHEBI:338412` anisomycin row and reports `MECHANISM_REVIEW: mechanism is absent; 4 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `rg --no-ignore --hidden -n -e '(^\|[^0-9])CHEBI:76([^0-9]\|$)' -e 'annonaine' reports/yaml_record_review` | Passed; the ignored/hidden-inclusive search found no prior exact `CHEBI:76` or annonaine review report. |
| `just validate data/antibiotics/antiprotozoal/annonaine.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/annonaine.yaml --out /tmp/annonaine-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2939 expected records were present on disk with no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` drift, and the known unrelated `ARO:3000337` iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/annonaine-worklist.tsv` | Passed; exact `CHEBI:76` appeared only on `mechanism` and `review-readiness`. The `mechanism` row reported 0 CARD targets and 0 resistance edges to build on. |
| `rg --no-ignore --hidden -n -e '(^\|[^0-9])CHEBI:76([^0-9]\|$)' -e 'antibioticmech:chebi-e3d5322349' -e 'VZTUKBKUWSHDFM-CYBMUJFWSA-N' -e '1862-41-5' -e 'HMDB0030348' -e 'C09339' -e 'C00001807' -e 'C00027281' -e 'reaxys:90236' -e 'annonaine' -e 'Anonaine' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed; this wider ignored/hidden-inclusive search found only the generated YAML, the ChEBI raw row, `PATHS.tsv`, and the checkpoint review-queue row for exact `CHEBI:76`/annonaine. It found no curator-owned local target, activity, resistance, producer, dataset, discussion, or causal-graph claim for exact `CHEBI:76`. |
| `rg --no-ignore --hidden -n -e '(^\|[^0-9])CHEBI:76([^0-9]\|$)' -e 'antibioticmech:chebi-e3d5322349' -e 'VZTUKBKUWSHDFM-CYBMUJFWSA-N' -e '1862-41-5' -e 'HMDB0030348' -e 'C09339' -e 'C00001807' -e 'C00027281' -e '90236' -e 'annonaine' -e 'Anonaine' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv` | Passed; the ignored/hidden-inclusive search found no ARO, BindingDB, Drugs@FDA, MIBiG, PHI-base, or grounding-decision row for exact `CHEBI:76` or its exact structure/xref identifiers. |
| OLS4 lookup for `CHEBI:76` | Passed; OLS resolved exactly one non-obsolete ChEBI class with identifier `CHEBI:76`, ASCII-equivalent label `(-)-annonaine`, and the same definition. |
| ChEBI page lookup for `CHEBI:76` | Passed; current ChEBI agreed with the generated label, definition, formula, average mass, monoisotopic mass, SMILES, Standard InChI, Standard InChIKey, trypanocidal and antiplasmodial roles, three parent terms, PubMed citations, and the HMDB, KEGG, KNApSAcK, CAS, and Reaxys xrefs. |
| OLS4 lookups for parent terms `CHEBI:134209`, `CHEBI:38104`, and `CHEBI:38164` | Passed; all three parent CURIEs resolved as non-obsolete ChEBI classes for `aporphine alkaloid`, `oxacycle`, and `organic heteropentacyclic compound`. |
| KEGG `C09339` lookup | Passed; KEGG resolved exact `(-)-Annonaine` / `Anonaine` with formula `C17H15NO2` and linked CAS `1862-41-5`, ChEBI `76`, and KNApSAcK `C00001807`/`C00027281`. |
| HMDB `HMDB0030348` lookup | Not independently checked: HMDB returned a Cloudflare browser challenge for `/metabolites/HMDB0030348.xml`. ChEBI lists this xref on current `CHEBI:76`. |
| KNApSAcK lookups for `C00001807` and `C00027281` | Partial pass; KEGG `C09339` corroborates both xrefs, and ChEBI lists both on current `CHEBI:76`, but KNApSAcK's own exact `C_ID` search returned 0 matched rows for both IDs. |
| Reaxys xref `90236` | Not independently checked: ChEBI lists this Reaxys registry number on exact `CHEBI:76`, but Reaxys had no public resolver available in this workflow. |
| PubMed esummary for ChEBI PMIDs `18305432`, `20826204`, `21361287`, and `22101086` | Passed; all four identifiers resolved. PMID:20826204 is the exact antiplasmodial aporphine-alkaloid report. The other three records are non-antimicrobial mammalian/cancer or extract-composition papers. |
| PubMed exact mechanism/activity search for `annonaine OR Anonaine` plus antiplasmodial, trypanocidal, antiprotozoal, mechanism, or target terms | Partial pass; PubMed returned 26 candidates. The top ten contained a review of anonaine pharmacology, the exact ChEBI antiplasmodial source PMID:20826204, cytotoxicity and extract studies, and recent computational target-prediction papers that were not direct primary evidence for an antimicrobial target. |
| PubMed Central lookup for `PMID:20826204` / `PMC3010440` | Passed; the open article text confirmed anonaine as compound 5 isolated from `Liriodendron tulipifera` bark and quantified in an in vitro `Plasmodium falciparum` pLDH assay. |
| Semantic Scholar search for `annonaine antiplasmodial trypanocidal mechanism` | Not checked: the public Graph API returned HTTP 429. |

No separate focused term, reference, or history validator is exposed in
`justfile`; the single-record strict validator, full-corpus `verify-corpus`, and
full-corpus `worklist` are the documented narrow checks for seeded-field drift
and queue placement.

## Identity and Grounding

| Assertion | Review |
|---|---|
| Label and identifier | Supported. OLS and current ChEBI resolve `CHEBI:76` to `(-)-annonaine`, the ASCII-equivalent spelling of the generated `(−)-annonaine`. |
| Structure | Supported. The generated SMILES, Standard InChI, Standard InChIKey `VZTUKBKUWSHDFM-CYBMUJFWSA-N`, formula `C17H15NO2`, neutral charge, average mass `265.312`, and monoisotopic mass `265.11028` match `data/raw/chebi_antimicrobials.tsv` and current ChEBI. |
| Source concept | Supported. The only source concept is ChEBI `CHEBI:76`; no CARD/ARO, curator, BindingDB, MIBiG, FDA, or PHI-base source row is attached to this record. |
| Filing class and roles | Supported. The committed ChEBI row and current ChEBI give exact `CHEBI:76` the roles `CHEBI:36335` `trypanocidal drug` and `CHEBI:64915` `antiplasmodial drug`, and `conf/sources.yaml` maps both roles to `ANTIPROTOZOAL`. |
| Parent terms | Supported. Current ChEBI resolves the generated parents as `CHEBI:134209` `aporphine alkaloid`, `CHEBI:38104` `oxacycle`, and `CHEBI:38164` `organic heteropentacyclic compound`, matching the record's structure and chemical class. |
| Xrefs | Mostly supported as ChEBI assertions. Current ChEBI lists CAS `1862-41-5`, HMDB `HMDB0030348`, KEGG `C09339`, KNApSAcK `C00001807`/`C00027281`, and Reaxys `90236` on exact `CHEBI:76`; KEGG independently corroborates exact `C09339`, ChEBI `76`, CAS `1862-41-5`, and both KNApSAcK IDs. |
| Grounding decision | Complete as empty. Exact ChEBI source concepts ground to their own CURIEs, and an ignored/hidden-inclusive search found no `curation/decisions.tsv` override for `CHEBI:76` or `antibioticmech:chebi-e3d5322349`. |

## Evidence

| Claim in generated YAML | Support |
|---|---|
| Exact `CHEBI:76` is a ChEBI-grounded record for neutral `(-)-annonaine` with the listed IUPAC synonym and structure. | Supported by the committed ChEBI inventory row for `CHEBI:76`, OLS, and the current ChEBI page for the same term. |
| Exact `CHEBI:76` has trypanocidal and antiplasmodial roles. | Supported as ChEBI assertions by the committed ChEBI row and current ChEBI. PMID:20826204 directly supports purified anonaine having antiplasmodial activity against `Plasmodium falciparum` D10 and Dd2 in vitro. |
| Exact `CHEBI:76` belongs to the generated structural parents. | Supported by current ChEBI resolving all three generated parent CURIEs and by the committed ChEBI row. |
| Exact `CHEBI:76` has a characterized antimicrobial mode of action or molecular target. | Unsupported as a populated claim only because the generated record asserts none. The inspected ChEBI PMIDs and the bounded PubMed search exposed activity, cytotoxicity, and computational target leads but no primary antimicrobial target or causal mechanism for exact annonaine. |

`PMID:20826204` could support future `ActivityObservation` curation if the
schema representation is acceptable for antiplasmodial IC50 data: its table
reports purified anonaine activity against `P. falciparum` D10 and Dd2 using a
parasite lactate dehydrogenase viability assay. The existing record does not
assert those strain-level observations, so there is no unsupported
`activity_spectrum` claim today.

The other three ChEBI PMIDs do resolve but should not be read as antimicrobial
activity evidence for this record. `PMID:18305432` studies dopamine biosynthesis
in PC12 cells, `PMID:21361287` studies human lung carcinoma H1299 cells, and
`PMID:22101086` studies an `Annona cherimolia` alkaloid extract in a mouse
forced-swim model with anonaine listed as one of several constituents.

## Completeness

| Area | Status |
|---|---|
| Identity and structure | Complete enough for read-only review; current ChEBI, OLS, the committed ChEBI row, `PATHS.tsv`, and the generated YAML agree on exact `CHEBI:76` and `VZTUKBKUWSHDFM-CYBMUJFWSA-N`. |
| Source concept | Complete. The only source concept is exact ChEBI `CHEBI:76`. |
| Activity roles and filing class | Complete enough for read-only review; ChEBI's trypanocidal and antiplasmodial roles map to `ANTIPROTOZOAL` without suppressing either role. |
| Mechanism | Incomplete. The record has no `mode_of_action`, no `mode_of_action_target_scope`, no `molecular_targets`, and no causal graph. |
| Activity observations | Incomplete but non-contradictory. The generated record has ChEBI roles only; exact antiplasmodial IC50 data from PMID:20826204 is available for possible future curator-owned `Plasmodium falciparum` observations. |
| Resistance mechanisms | Complete as empty. CARD contributes no resistance edges to this ChEBI-only record, and PHI-base has no exact row for this compound. |
| Producers | Complete as empty for the inspected local sources. MIBiG contributes no exact producer row for this InChIKey, and the ChEBI definition does not name a microbial producer candidate. |
| Clinical status | Complete as empty. Drugs@FDA contributes no exact GSRS/UNII row for this InChIKey or the inspected xrefs. |
| Datasets | Complete as empty for the inspected evidence. The record has no dataset assertion and the bounded checks did not expose a specific exact-annonaine public dataset accession to curate. |
| Discussions | Complete as empty for read-only review. The absent mechanism is already represented by the derived `mechanism` and `review-readiness` queues. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, current ChEBI agrees with exact `CHEBI:76`, and no existing target, activity, resistance, producer, or causal assertion overclaims support. | Not applicable. |
| Major | Exact `CHEBI:76` has no curated antiprotozoal mechanism, molecular target, or causal graph. | The generated record has no `mode_of_action`, `molecular_targets`, or `causal_graphs`; exact `CHEBI:76` is on the `mechanism` and `review-readiness` queues with 0 CARD targets and 0 CARD resistance edges to build on. | Future curator-owned fields on `data/antibiotics/antiprotozoal/annonaine.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI changes this term's seeded identity, roles, parentage, xrefs, or source PMIDs, `data/raw/chebi_antimicrobials.tsv` and the ChEBI extractor own the refresh. |
| Minor | Exact antiplasmodial assay evidence is available but not yet represented as a claim-level observation. | PMID:20826204 reports purified anonaine activity against `P. falciparum` D10 and Dd2, but `activity_spectrum` is empty. The record therefore retains only ChEBI's higher-level antiplasmodial role. | Future `activity_spectrum` entries on `data/antibiotics/antiprotozoal/annonaine.yaml`, if the IC50 values can be represented without abusing MIC-specific slots; otherwise the schema and its writer/renderer own any IC50 extension. |
| Minor | Several generated xrefs remain database assertions rather than independently resolved exact identities. | HMDB `HMDB0030348` returned a browser challenge, KNApSAcK exact `C_ID` lookups for `C00001807` and `C00027281` returned no rows despite ChEBI and KEGG both listing the IDs, and Reaxys has no public resolver available in this workflow. | No record edit required unless a curator later disproves an xref; exact xref assertions are seeded from `data/raw/chebi_antimicrobials.tsv`. |

## Recommended Edits

1. Inspect full primary reports, not just abstracts or search results, for exact
   `(-)-annonaine` mechanism claims in `Plasmodium`, `Trypanosoma`, or other
   protozoa before setting `mode_of_action` or adding a causal graph.

   Maintained owner: future curator-owned fields on
   `data/antibiotics/antiprotozoal/annonaine.yaml`.

2. Curate strain-level `Plasmodium falciparum` activity observations from
   PMID:20826204 if the current schema can honestly carry IC50 data. If it
   cannot, add a first-class IC50/EC50 measurement slot for non-bacterial assays
   before normalizing those values.

   Maintained owner: `data/antibiotics/antiprotozoal/annonaine.yaml` for
   qualitative activity with evidence, or
   `src/antibioticmech/schema/antibioticmech.yaml` plus writer/renderer support
   for a quantitative IC50 extension.

3. Re-check HMDB, KNApSAcK, and Reaxys before any future xref cleanup.
   Withhold an xref only if an authoritative source resolves it to a different
   structure; ChEBI and KEGG currently support keeping the KNApSAcK IDs.

   Maintained owner: `data/raw/chebi_antimicrobials.tsv` and the ChEBI
   extractor, or a future `curation/decisions.tsv` decision if a source
   concept needs grounding changes.

## Follow-up Checks

- Rerun `just validate-strict data/antibiotics/antiprotozoal/annonaine.yaml --out /tmp/annonaine-strict.tsv`.
- Rerun `just verify-corpus --summary` after any generated-source or
  curator-owned record edit.
- Rerun `just worklist --limit 0 --tsv /tmp/annonaine-worklist.tsv` and confirm
  exact `CHEBI:76` leaves any queue that a curation edit claims to resolve.
- Rerun exact ignored/hidden-inclusive searches for `CHEBI:76`,
  `antibioticmech:chebi-e3d5322349`, `VZTUKBKUWSHDFM-CYBMUJFWSA-N`, CAS
  `1862-41-5`, HMDB `HMDB0030348`, KEGG `C09339`, KNApSAcK `C00001807` and
  `C00027281`, Reaxys `90236`, `annonaine`, and `Anonaine` before asserting
  that any local source path is still absent.
- Inspect the post-write YAML and run `git diff -- data/antibiotics/antiprotozoal/annonaine.yaml curation/decisions.tsv src scripts tests` after any future mutator.
- For every proposed causal edge, confirm the edge's `evidence` cites a primary
  experiment rather than ChEBI's source list or a docking-only target-prediction
  paper.

## Additional Notes

- The raw row for exact `CHEBI:76` has four source PMIDs. They resolve, but only
  `PMID:20826204` directly supports the antiplasmodial activity role.
- The bounded PubMed mechanism search returned 26 candidates; the visible high
  ranks were either a pharmacology review of anonaine, the already seeded
  antiplasmodial activity paper, broader extract studies, cytotoxicity papers,
  or computational predictions. Semantic Scholar returned HTTP 429.
- The schema currently names its quantitative `ActivityObservation` value slot
  `mic_value`, with a MIC-specific description and rule. That is why the exact
  IC50 results from PMID:20826204 need a representation decision before they are
  normalized as numeric activity rows.
