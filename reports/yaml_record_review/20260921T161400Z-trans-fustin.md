# YAML Record Review: (+)-trans-fustin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/trans-fustin.yaml`
- Started UTC: 2026-09-21T16:08:00Z
- Finished UTC: 2026-09-21T16:14:00Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:228234` |
| Label | `(+)-trans-fustin` |
| Path | `data/antibiotics/antiviral/trans-fustin.yaml` |
| Class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | `CHEBI:228234` / `(+)-trans-fustin` |
| Minted source key | `antibioticmech:chebi-c713067564` |
| Generated or maintained | Generated from `data/raw/chebi_antimicrobials.tsv` plus `data/antibiotics/PATHS.tsv`; future seeded identity, structure, class, role, synonym, or xref changes belong upstream rather than as hand edits. |

Resolution:

- `curation/record_review_queue.tsv:24` names `CHEBI:228234`, label
  `(+)-trans-fustin`, and the review hint
  `MECHANISM_REVIEW: mechanism is absent`.
- `data/antibiotics/PATHS.tsv:534` maps `CHEBI:228234` to the `ANTIVIRAL`
  directory and `trans-fustin` slug.
- `data/raw/chebi_antimicrobials.tsv:2895` is the ChEBI source row that seeds
  the target record.
- The whole target YAML was read before judging generated identity, structure,
  xrefs, source concept, and curation history.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiviral/trans-fustin.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/trans-fustin.yaml --out /tmp/antibioticmech-trans-fustin-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2939 expected records, 2939 on disk, no missing, unexpected, or drifted records, no identifiers absent from `PATHS.tsv`, and no stale lockfile rows. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-trans-fustin.tsv` | Passed; `CHEBI:228234` appears in `mechanism` and `review-readiness`. |
| `just review-queue --limit 24` | Passed and confirmed `CHEBI:228234` remains in the review-readiness queue as `MECHANISM_REVIEW`, with zero source literature leads, zero record evidence items, and zero targets. |

No narrower term, reference, or curation-history validator is exposed for this
single ChEBI-seeded record. The focused strict validator covered closed schema
shape, and `verify-corpus` covered drift from the maintained raw and PATHS
inputs.

## Identity and Grounding

The seeded identity is consistent with the authoritative ChEBI record:

| Assertion | Record value | Check |
|---|---|---|
| ChEBI identity | `CHEBI:228234`, `(+)-trans-fustin` | Official ChEBI resolved `CHEBI:228234` to `(+)-trans-fustin`. |
| Definition | The `(2R,3R)` stereoisomer of fustin | Matched ChEBI. |
| Formula and charge | `C15H12O6`, charge `0` | Matched ChEBI. |
| Masses | average `288.255`, monoisotopic `288.06339` | Matched ChEBI and KEGG. |
| SMILES | `O=C1c2ccc(O)cc2O[C@H](c2ccc(O)c(O)c2)[C@H]1O` | Matched ChEBI. |
| Standard InChI | `InChI=1S/C15H12O6/c16-8-2-3-9-12(6-8)21-15(14(20)13(9)19)7-1-4-10(17)11(18)5-7/h1-6,14-18,20H/t14-,15+/m0/s1` | Matched ChEBI. |
| InChIKey | `FNUPUYFWZXZMIE-LSDHHAIUSA-N` | Matched ChEBI. |
| Antiviral role | `CHEBI:22587` | ChEBI asserts `has role` to `antiviral agent`. |
| Parent compound | `CHEBI:5202` | ChEBI asserts `(+)-trans-fustin is a fustin`. |
| Xrefs | `cas:4382-36-9`, `kegg.compound:C01378`, `knapsack:C00000963` | Matched the official ChEBI page. KEGG `C01378` resolves to `Fustin`, cross-links ChEBI `228234` and KNApSAcK `C00000963`, and carries the same formula and exact mass. |

The target is specifically the `(2R,3R)` and dextrorotatory stereoisomer, not
the sibling `CHEBI:228235` / `(-)-trans-fustin` record at
`data/antibiotics/antiviral/trans-fustin-228235.yaml`; the two records have
different ChEBI identifiers, CAS registry numbers, and Standard InChIKeys.
ChEBI also asserts `(+)-trans-fustin` as part of
`CHEBI:228236` / `(+-)-trans-fustin`, but that is a mixture/aggregate relation
and correctly does not appear in `parent_compounds`.

ChEBI additionally asserts the non-antimicrobial biological role
`plant metabolite` / `CHEBI:76924`. The seeded record retains only the ChEBI
`antiviral agent` antimicrobial role that drives inclusion in this repository,
and this review found no identity evidence that the record should move out of
the `ANTIVIRAL` filing class.

## Evidence

The target record has no record-level `evidence`. That is allowed for a
ChEBI-seeded record because the ChEBI source concept supplies provenance, and
there are no `activity_spectrum`, `molecular_targets`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status_assertions`,
`datasets`, or `causal_graphs` that would require claim-level evidence.

`data/raw/chebi_antimicrobials.tsv:2895` carries no PubMed or DOI leads for
the exact `CHEBI:228234` source concept. Targeted publication searches
resolved broad leads rather than record-ready `(+)-trans-fustin` claims:

| Search | Result |
|---|---|
| PubMed search for `(+)-trans-fustin`, `trans-fustin`, `(2R,3R)-fustin`, or `FNUPUYFWZXZMIE` | Returned 0 candidates. |
| PubMed search for `fustin antiviral OR fustin antimicrobial OR fustin antibacterial OR fustin antifungal` | Returned 16 candidates, including non-antimicrobial fustin studies and papers where `Fustin` was an author surname rather than a compound mention. |

The broader fustin search did surface several curation leads:

| PMID | Public abstract support |
|---|---|
| `PMID:22538659` | Reports activity-guided fractionation of *Rhus verniciflua* bark with isolation of fustin, fisetin, butin, and sulfuretin, and says fustin and sulfuretin showed antiviral `EC50` values against infectious hematopoietic necrosis virus and viral hemorrhagic septicemia virus. The public abstract does not identify the isolated fustin as exact `(2R,3R)` / `(+)-trans-fustin` or map each exact `EC50` value to fustin. |
| `PMID:30044823` | Reports antibacterial activity of *Rhus verniciflua* parts against fish-pathogenic bacteria and says methyl gallate and fustin showed antibacterial activity. The public abstract does not map exact MIC values, organism outcomes, or stereochemistry to `(+)-trans-fustin`. |
| `PMID:25027026` | Reports antifungal testing of *Hymenaea courbaril* xylem sap against *Cryptococcus neoformans* and dermatophytes; the filtrate contained a mixture of fisetinediol, fustin, 3-O-methyl-2,3-trans-fustin, and taxifolin, while the better-supported antifungal result in the abstract is for xylem sap and fisetin rather than exact `(+)-trans-fustin`. |
| `PMID:35241965` | Reports an in-silico *Shigella sonnei* DnaA screen in which Fustin was one of four top-ranked phytochemicals; the abstract does not provide an experimental activity observation or exact stereochemical target assertion for `(+)-trans-fustin`. |

## Completeness

Consequential gaps:

- `mode_of_action`, `mode_of_action_target_scope`, `mode_of_action_notes`,
  `molecular_targets`, and `causal_graphs` are absent, so the record has no
  curator-owned antiviral mechanism.
- No `activity_spectrum` entry captures an exact virus, bacterial organism, or
  fungus; assay; method; value; qualifier; unit; and per-observation evidence
  for exact `(+)-trans-fustin`.
- No `producer_organisms` entry captures a biosynthetic source organism for
  exact `CHEBI:228234`.

Correctly empty optional slots:

- No `resistance_mechanisms`: the record is ChEBI-only and CARD resistance
  routes do not apply.
- No `clinical_status` or `clinical_status_assertions`: the ChEBI-only source
  concept and inspected metadata do not assert regulatory use.
- No `datasets`: inspected ChEBI, PubMed metadata, KEGG, and exact-name PubMed
  searches did not surface a public screening, omics, or structural dataset
  accession for this exact structure.

Ignored-inclusive absence search:

- Ran `rg --hidden --no-ignore` over `curation/`, `data/raw/`,
  `data/antibiotics/`, `reports/yaml_record_review/`, and the full exported
  worklist for `CHEBI:228234`, `(+)-trans-fustin`, `trans-fustin`,
  `FNUPUYFWZXZMIE-LSDHHAIUSA-N`, `4382-36-9`, `C01378`, and `C00000963`.
- The search included ignored files and found only the expected generated
  target, the sibling `(-)-trans-fustin` record, `PATHS.tsv`, ChEBI raw rows,
  worklist rows, and the review-queue row. It found no prior review report,
  `curation/decisions.tsv` row, curated mechanism decision, or curated
  activity/provenance addition for this exact ChEBI concept.

## Findings

| Severity | ID | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| Major | TRANS-FUSTIN-M1 | The record is still a seeded exact-stereoisomer stub with an antiviral ChEBI role but no curator-owned activity spectrum, producer assertion, mode of action, mechanism scope, molecular target, or causal graph. | `just review-queue --limit 24` reports `MECHANISM_REVIEW: mechanism is absent` with zero source literature leads; `/tmp/antibioticmech-worklist-trans-fustin.tsv` lists `CHEBI:228234` only on `mechanism` and `review-readiness`. The exact `(+)-trans-fustin` PubMed query returned no candidates, and broader fustin searches returned useful activity leads but no existing claim from those sources has been curated onto the exact `(2R,3R)` record. | `data/antibiotics/antiviral/trans-fustin.yaml`, via guarded curator-owned record mutation plus a new `CurationEvent`; or `curation/decisions.tsv` if full-text inspection cannot verify exact `(+)-trans-fustin` antiviral activity. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect the full text of `PMID:22538659` to decide whether the isolated
   fustin compound is exact `(+)-trans-fustin` / `CHEBI:228234`, and whether
   its IHNV and VHSV `EC50` observations can be curated with virus, host-cell
   line, method, value, unit, and claim-local evidence.
2. Inspect `PMID:30044823`, `PMID:25027026`, and primary papers cited by
   ChEBI's stereochemically unspecified fustin parent before deciding whether
   antibacterial, antifungal, or non-viral claims can be narrowed to exact
   `(+)-trans-fustin`.
3. If exact primary evidence supports antiviral activity, add claim-local
   `activity_spectrum` observations and curate `mode_of_action`,
   `mode_of_action_target_scope`, `mode_of_action_notes`, molecular targets,
   and a causal graph only to the precision directly supported by the
   inspected mechanism source.
4. If exact `(+)-trans-fustin` antiviral activity cannot be verified, leave the
   record `SEEDED` and add either a curator discussion for the unsupported
   ChEBI role or a source-concept decision in `curation/decisions.tsv` if the
   role is clearly over-scoped from stereochemically unspecified fustin.
5. Preserve the seeded ChEBI identity, `(2R,3R)` structure, parent, xrefs, and
   source concept. This review found no grounded-identity correction that
   belongs in `data/raw/chebi_antimicrobials.tsv` or
   `data/antibiotics/PATHS.tsv`.

## Follow-up Checks

- `just validate-strict data/antibiotics/antiviral/trans-fustin.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus --summary`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-trans-fustin.tsv`
- `just review-queue --limit 24`
- `just qc`
- Manual diff review of
  `data/antibiotics/antiviral/trans-fustin.yaml` and `curation/decisions.tsv`
  to confirm every new exact-structure claim is claim-local and directly
  supported by an inspected primary source.

## Additional Notes

- `NCBI_EMAIL` was unset in the local environment before using the PubMed
  search script; no contact email was sent as NCBI API metadata.
- The review avoided SerpAPI/Google Scholar because that path can be billable
  and was not necessary to establish the bounded finding above.
- The direct KEGG check confirmed that `kegg.compound:C01378` is a fustin
  concept cross-linked to `ChEBI:228234` and `KNApSAcK:C00000963`. KEGG lists
  CAS `20725-03-5`, so this review did not treat the KEGG registry number as a
  confirmation of ChEBI's exact `cas:4382-36-9` xref.
- `knapsack:C00000963` was verified as a ChEBI manual xref and as a KEGG
  cross-link but was not independently resolved directly through KNApSAcK.
