# YAML Record Review: (+)-larreatricin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/larreatricin.yaml`
- Started UTC: 2026-09-21T11:04:00Z
- Finished UTC: 2026-09-21T11:08:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:67153` |
| Label | `(+)-larreatricin` |
| Path | `data/antibiotics/antiviral/larreatricin.yaml` |
| Class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | `CHEBI:67153` / `(+)-larreatricin` |
| Minted source key | `antibioticmech:chebi-9d7693930c` |
| Generated or maintained | Generated from `data/raw/chebi_antimicrobials.tsv` plus `data/antibiotics/PATHS.tsv`; future seeded identity, class, role, or xref changes belong upstream rather than as hand edits. |

Resolution:

- `curation/record_review_queue.tsv:14` names `CHEBI:67153`, label
  `(+)-larreatricin`, and the review hint `MECHANISM_REVIEW: mechanism is
  absent`.
- `data/antibiotics/PATHS.tsv:1911` maps `CHEBI:67153` to the `ANTIVIRAL`
  directory and `larreatricin` slug.
- `data/raw/chebi_antimicrobials.tsv:1656` is the ChEBI source row that seeds
  the target record.
- The whole target YAML was read before judging generated identity, structure,
  xrefs, source concept, and curation history.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiviral/larreatricin.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/larreatricin.yaml --out /tmp/antibioticmech-larreatricin-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2939 expected records, 2939 on disk, no missing, unexpected, or drifted records, no identifiers absent from `PATHS.tsv`, and no stale lockfile rows. |
| `just review-queue --limit 14` | Passed and confirmed `CHEBI:67153` appears in the review-readiness queue as `MECHANISM_REVIEW`. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-larreatricin.tsv` | Passed; `CHEBI:67153` appears in `mechanism`, `producer-candidate`, and `review-readiness`. |

No narrower term, reference, or curation-history validator is exposed for this
single ChEBI-seeded record. The focused strict validator covered closed schema
shape, and `verify-corpus` covered drift from the maintained raw and PATHS
inputs.

## Identity and Grounding

The seeded identity is consistent with the authoritative ChEBI record:

| Assertion | Record value | Check |
|---|---|---|
| ChEBI identity | `CHEBI:67153`, `(+)-larreatricin` | Official ChEBI resolved `CHEBI:67153` to `(+)-larreatricin`. |
| Definition | Antiviral lignan isolated from `Larrea tridentata`, with a 3,4-dimethyltetrahydrofuran skeleton and the `(2R,3R,4S,5R)` stereochemistry | Matched ChEBI's definition. |
| Formula and charge | `C18H20O3`, charge `0` | Matched ChEBI. |
| Masses | average `284.355`, monoisotopic `284.14124` | Matched ChEBI. |
| SMILES | `C[C@@H]1[C@H](C)[C@H](c2ccc(O)cc2)O[C@H]1c1ccc(O)cc1` | Matched ChEBI. |
| Standard InChI | `InChI=1S/C18H20O3/c1-11-12(2)18(14-5-9-16(20)10-6-14)21-17(11)13-3-7-15(19)8-4-13/h3-12,17-20H,1-2H3/t11-,12+,17-,18-/m1/s1` | Matched ChEBI. |
| InChIKey | `PIBJADPEZQHMQS-FVEFGIFQSA-N` | Matched ChEBI. |
| Antiviral role | `CHEBI:22587` | ChEBI asserts `has role` to `antiviral agent`. |
| Parent compounds | `CHEBI:25036`, `CHEBI:26912`, `CHEBI:33853` | ChEBI asserts `is a` edges to lignan, oxolanes, and phenols. |

The record correctly preserves the exact ChEBI source concept and keeps ChEBI's
alternate names as synonyms:

- `4,4'-[(2R,3R,4S,5R)-3,4-dimethyltetrahydrofuran-2,5-diyl]diphenol`
- `(2R,3R,4S,5R)-2,5-bis(4-hydroxyphenyl)-3,4-dimethyltetrahydrofuran`
- `(2R,3R,4S,5R)-2,5-di(4-hydroxyphenyl)-3,4-dimethyltetrahydrofuran`

The xref set is narrow but consistent:

| Xref | Check |
|---|---|
| `metacyc.compound:CPD-14400` | ChEBI lists `CPD-14400`; MetaCyc `CPD-14400` resolved as `(+)-larreatricin`. |
| `reaxys:9411082` | ChEBI lists this Reaxys registry number. Reaxys itself has no public resolver for this review to inspect. |

## Evidence

The target record has no record-level `evidence`. That is allowed for a
ChEBI-seeded record because the ChEBI source concept supplies provenance, and
there are no `activity_spectrum`, `molecular_targets`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status_assertions`,
`datasets`, or `causal_graphs` that would require claim-level evidence.

ChEBI's source row carries one PMID lead:

| PMID | DOI | What the resolved source covers for this review |
|---|---|---|
| `PMID:12960376` | `10.1073/pnas.1934562100` | Cho et al. 2003 reports `(+)-larreatricin` hydroxylase, `Larrea tridentata` polyphenol oxidase purification, the cloned encoding gene, and enzymatic conversion of `(+)-larreatricin` to `(+)-3'-hydroxylarreatricin`. The abstract and open PMC full text discuss antiviral reports only for nordihydroguaiaretic acid and its derivatives, not for exact `(+)-larreatricin`. |

Targeted literature searches:

| Query | Result |
|---|---|
| `(+)-larreatricin antiviral` | PubMed returned Cho et al. 2003 only; Semantic Scholar returned HTTP 429. |
| `PIBJADPEZQHMQS OR CPD-14400 OR 9411082` | PubMed returned one unrelated `PMID:9411082` hit because the bare Reaxys registry number is also a PMID; Semantic Scholar returned HTTP 429. |
| `larreatricin HIV antiviral` | PubMed returned 0 candidates; Semantic Scholar returned HTTP 429. |
| `"(+)-larreatricin" OR "larreatricin"` | PubMed returned 7 candidates, dominated by larreatricin stereochemistry, synthesis, crystal-structure, plant-source, and non-antimicrobial cytotoxicity papers. The one antimicrobial-looking hit was for `4-epi-larreatricin`, a different stereochemical form. Semantic Scholar returned HTTP 429. |

## Completeness

Consequential gaps:

- `activity_roles` and `antimicrobial_class` rest on ChEBI's antiviral role,
  but the only inspected ChEBI citation for this structure does not itself
  support an exact antiviral activity claim.
- `mode_of_action`, `mode_of_action_target_scope`, `mode_of_action_notes`,
  `molecular_targets`, and `causal_graphs` are absent. The exact mechanism
  remains unresolved because no inspected source reported how
  `(+)-larreatricin` inhibits a virus.
- No `activity_spectrum` entry captures an exact assay, organism, outcome,
  value, or unit for `(+)-larreatricin`.
- No `producer_organisms` entry captures `Larrea tridentata`
  (`NCBITaxon:66636`) as the organism that biosynthesizes `(+)-larreatricin`.
  The Cho et al. full text supports a biosynthesis claim, even though the
  definition phrase `isolated from` was correctly queued only as a candidate by
  the worklist.

Correctly empty optional slots:

- No `resistance_mechanisms`: the record is ChEBI-only and CARD resistance
  routes do not apply to an antiviral plant lignan.
- No `clinical_status` or `clinical_status_assertions`: the ChEBI-only source
  concept and inspected metadata do not assert regulatory use.
- No `datasets`: inspected ChEBI, MetaCyc, PubMed, Europe PMC, and exact-name
  PubMed metadata did not surface a public screening, omics, or structural
  dataset accession for this exact structure.

Ignored-inclusive absence search:

- Ran `rg --hidden --no-ignore` over `curation/`, `data/raw/`,
  `reports/yaml_record_review/`, the target YAML, and
  `data/antibiotics/PATHS.tsv` for `CHEBI:67153`,
  `antibioticmech:chebi-9d7693930c`, `PIBJADPEZQHMQS`, `CPD-14400`,
  `9411082`, `12960376`, and `larreatricin`.
- The search found only the expected generated target, `PATHS.tsv`, ChEBI raw
  row, worklist/review-queue rows, and adjacent existing records. It found no
  prior review report, `curation/decisions.tsv` row, or curated mechanism
  decision for this exact ChEBI concept.

## Findings

| Severity | ID | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| Major | LARREATRICIN-M1 | The seeded `ANTIVIRAL` filing role still needs primary exact-compound verification before this record can be trusted or mechanistically reviewed. | ChEBI asserts `CHEBI:22587` and cites `PMID:12960376`, but PubMed, Europe PMC, and the open PMC full text resolve that PMID as a `Larrea tridentata` biosynthesis paper. Its antiviral discussion is about nordihydroguaiaretic acid derivatives, and bounded PubMed searches did not find an exact `(+)-larreatricin` antiviral assay or mechanism paper. | `data/antibiotics/antiviral/larreatricin.yaml` if exact activity literature is found; otherwise a source-concept exclusion decision in `curation/decisions.tsv`. |
| Minor | LARREATRICIN-M2 | The record omits a supportable `Larrea tridentata` producer claim. | Cho et al. 2003 reports a `Larrea tridentata` enzyme that converts `(+)-larreatricin` to `(+)-3'-hydroxylarreatricin`, and NCBI Taxonomy resolves `Larrea tridentata` to active species `NCBITaxon:66636`. | `data/antibiotics/antiviral/larreatricin.yaml`, via guarded curator-owned record mutation plus a new `CurationEvent`. |

No blocker findings.

## Recommended Edits

1. Search beyond PubMed if necessary for a primary paper that directly reports
   `(+)-larreatricin` activity against a virus. If no exact activity source is
   found, consider excluding `antibioticmech:chebi-9d7693930c` in
   `curation/decisions.tsv` because the ChEBI role's only inspected citation is
   generic for antiviral activity.
2. If exact antiviral activity literature is found, curate organism-specific
   `activity_spectrum` entries with method, value or qualitative outcome, and
   evidence on each observation.
3. Curate `mode_of_action`, `mode_of_action_target_scope`, molecular targets,
   and a causal graph only after activity or mechanism evidence is found for
   the exact `(+)-larreatricin` structure.
4. Add a curator-owned `producer_organisms` entry for `Larrea tridentata`
   / `NCBITaxon:66636` with `PMID:12960376` evidence for plant biosynthesis.

## Follow-up Checks

- `just validate-strict data/antibiotics/antiviral/larreatricin.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus --summary`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-larreatricin.tsv`
- `just review-queue --limit 20`
- `just qc`
- Manual diff review of `data/antibiotics/antiviral/larreatricin.yaml` to
  confirm any added producer, activity, target, or causal claims carry
  claim-local evidence and do not overstate Cho et al. beyond biosynthesis.

## Additional Notes

- `NCBI_EMAIL` was unset in the local environment before using the PubMed
  search script; no contact email was sent as NCBI API metadata.
- The review avoided SerpAPI/Google Scholar because that path can be billable
  and was not necessary to establish the bounded findings above.
- The failed Semantic Scholar searches were rate-limit failures only. They did
  not affect identity validation because ChEBI, MetaCyc, PubMed, Europe PMC,
  and the open PMC article supplied the needed exact checks.
