# YAML Record Review: (+)-carvone

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/carvone.yaml`
- Started UTC: 2026-09-21T09:53:09Z
- Finished UTC: 2026-09-21T09:53:09Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antifungal/carvone.yaml` |
| Class | `ANTIFUNGAL` |
| ID | `CHEBI:15399` |
| Label | `(+)-carvone` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded `(+)-carvone` with InChIKey
`ULDHMXUKGWMISQ-VIFPVBQESA-N`. It has one ChEBI source concept, the ChEBI
antifungal-agent activity role `CHEBI:35718`, a strictly broader ChEBI parent
`CHEBI:38265` for generic `carvone`, seven same-structure xrefs, and no
`mode_of_action`, `mode_of_action_target_scope`, record evidence, molecular
target, activity observation, resistance mechanism, producer organism, clinical
assertion, causal graph, dataset, or `Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antifungal/carvone.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/carvone.yaml --out /tmp/antibioticmech-carvone-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Confirmed `CHEBI:15399` appears only in the `mechanism` and `review-readiness` queues, with no `moa-scope`, `target-evidence`, `xref-unverified`, `xref-span-conflict`, `multi-component`, `producer-candidate`, or `activity-candidate` row for this exact record. |
| `just review-queue --limit 12` | Confirmed `(+)-carvone` remains in `review-readiness`: `MECHANISM_REVIEW`, absent mechanism, one source literature lead, zero record evidence items, and zero targets. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '11380545' --limit 20 --output /tmp/antibioticmech-carvone-source-pmid-publications.jsonl` | PubMed resolved the exact ChEBI source PMID; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '"(+)-carvone" antifungal' --limit 20 --output /tmp/antibioticmech-carvone-label-publications.jsonl` | PubMed wrote 20 candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query 'ULDHMXUKGWMISQ OR 2244-16-8 OR C11383 OR C00010891' --limit 20 --output /tmp/antibioticmech-carvone-identifier-publications.jsonl` | PubMed wrote 20 candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '"S-carvone" OR "D-carvone" OR "(+)-carvone" MIC antifungal' --limit 20 --output /tmp/antibioticmech-carvone-mic-publications.jsonl` | PubMed wrote 15 candidates; Semantic Scholar returned HTTP 429. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`carvone.yaml` resolves unambiguously through the lock row
`CHEBI:15399	ANTIFUNGAL	carvone` in `data/antibiotics/PATHS.tsv`, despite the
nearby generated siblings `CHEBI:15400` for `(-)-carvone` and `CHEBI:38265` for
generic `carvone`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:15399`: SMILES, Standard InChI, InChIKey
`ULDHMXUKGWMISQ-VIFPVBQESA-N`, formula `C10H14O`, charge `0`, average mass, and
monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`. The official
ChEBI page for `CHEBI:15399` reports the same label, definition, 3-star status,
formula, charge, average mass, monoisotopic mass, SMILES, Standard InChI,
InChIKey, `CHEBI:35718` antifungal-agent role, `CHEBI:38265` generic carvone
parent, `CHEBI:15400` enantiomer relation, CAS `2244-16-8`, KEGG `C11383`,
KNApSAcK `C00010891`, and Reaxys `2042970` xref.

KEGG `C11383` independently names `(+)-(S)-Carvone`, reports formula `C10H14O`,
lists CAS `2244-16-8`, and cross-links back to ChEBI `15399` and KNApSAcK
`C00010891`. The NIST Chemistry WebBook page for CAS `2244-16-8` reports the
same formula and CAS and lists `(+)-(4S)-carvone` among names; its Standard
InChIKey uses `m1` rather than ChEBI's `m0` stereochemical layer, but the CAS
number is a seeded ChEBI xref also corroborated by KEGG.

No local grounding decision or prior review report was found for `CHEBI:15399`,
`antibioticmech:chebi-b48b01e65c`, `ULDHMXUKGWMISQ-VIFPVBQESA-N`,
`2244-16-8`, KEGG `C11383`, KNApSAcK `C00010891`, or `(+)-carvone` in a
gitignore-independent search over `curation/`, `data/raw/`,
`reports/yaml_record_review/`, and `data/antibiotics/`.

## Evidence

The generated identity, label, definition, exact and related synonyms,
structure, ChEBI parent, same-structure xrefs, antifungal filing class,
`CHEBI:35718` activity role, source concept, and grounding status reproduce from
the committed ChEBI inventory with no `verify-corpus` drift.

The ChEBI page still asserts `antifungal agent` as a biological role of
`CHEBI:15399`, so the generated `ANTIFUNGAL` filing class and retained
`CHEBI:35718` activity role agree with the upstream database assertion. The
record does not claim a literature-backed activity observation or MIC, and it
does not attach the ChEBI source PMID as record-level evidence.

The exact-PMID PubMed lookup resolves `PMID:11380545` to Nilsson et al. 2001,
`Mechanism of the antigen formation of carvone and related alpha, beta-unsaturated
ketones.` That article concerns carvone antigen formation and stereochemistry in
the allergy/dermatitis context; it does not report an antifungal assay or an
antimicrobial mode of action for `(+)-carvone`.

The bounded PubMed mechanism and MIC searches found antifungal activity leads
for generic `carvone`, `D-carvone`, carvone-rich oils, and L-carvone-derived
compounds, but the inspected abstracts do not establish a curated mechanism for
the exact `CHEBI:15399` compound. For example, recent abstracts report generic
carvone activity against `Botryosphaeria dothidea`, D-carvone activity against
`Fusarium oxysporum`, and monoterpene enantiomer MIC testing against Candida
species; each needs full-text review for exact material identity, units, assay
context, and whether the record's `(+)-carvone` stereoisomer is the active
tested material.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

## Completeness

The exact ChEBI identity, stereochemistry, structure, ChEBI grounding, source
concept, antifungal filing class, ChEBI antifungal-agent role, parent compound,
and public ChEBI/KEGG/KNApSAcK/CAS/Reaxys cross-references are defensible.

The consequential gap is the absent mechanism. `mode_of_action` is empty, there
is no target scope, and there is no molecular-target or causal-graph evidence
for a specific fungal process. `just worklist --limit 0` therefore keeps the
record on the `mechanism` queue, and `just review-queue --limit 12` keeps it in
`MECHANISM_REVIEW`.

No activity observations, producer claims, resistance assertions, clinical
assertions, or public datasets are present. The targeted searches found possible
activity and mechanism leads, but this read-only pass found no source that could
be promoted safely from its abstract alone, and no optional slot needs to be
filled before the unreviewed mechanism is resolved.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record cannot satisfy the `REVIEWED` gate because it has no curator-owned antimicrobial mode of action. | `data/antibiotics/antifungal/carvone.yaml` has no `mode_of_action`, no `mode_of_action_target_scope`, no `molecular_targets`, and no `causal_graphs`; `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` keeps `CHEBI:15399` on the `mechanism` queue, and `just review-queue --limit 12` reports `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | Future curator-owned mechanism, target, activity-observation, discussion, and causal-graph additions belong in `data/antibiotics/antifungal/carvone.yaml` and must be written through `write_validated_antibiotic`. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect primary full text for exact `(+)-carvone`, prioritizing papers that
   test isolated S/D enantiomers rather than essential oils, generic carvone, or
   carvone derivatives. Candidate PMIDs from this pass include `PMID:32506599`
   for monoterpene isomer MIC testing, `PMID:41520243` for generic carvone
   against `Botryosphaeria dothidea`, and `PMID:40657988` for D-carvone against
   `Fusarium oxysporum`.
2. Add `mode_of_action`, `mode_of_action_target_scope`, `MolecularTarget`,
   `ActivityObservation`, `Discussion`, or `causal_graphs` entries only when
   the inspected primary source supports the exact stereoisomer, fungal taxon,
   assay, MIC units, target or process, and causal direction at claim level.
3. Do not use ChEBI's source `PMID:11380545` as antimicrobial evidence. It is
   useful for carvone stereochemistry and antigen formation, not for the
   `CHEBI:35718` antifungal role or an antimicrobial mode of action.
4. If a mechanism or activity observation is supported, write it with a guarded
   mutator that asserts `identifier == "CHEBI:15399"`, updates only
   curator-owned fields, appends a `codex` `CurationEvent` with
   `llm_assisted: true`, and emits via `write_validated_antibiotic`.
5. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the new curator-checked mechanism satisfy the `docs/CURATION.md`
   sign-off criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antifungal/carvone.yaml --out /tmp/antibioticmech-carvone-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antifungal/carvone.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `rg --hidden --no-ignore` over `curation/`, `data/raw/`, and
  `reports/yaml_record_review/` for `CHEBI:15399`, `(+)-carvone`, and
  `carvone`.
- `rg --hidden --no-ignore` over `curation/`, `data/raw/`,
  `reports/yaml_record_review/`, and `data/antibiotics/` for `CHEBI:15399`,
  `antibioticmech:chebi-b48b01e65c`, `ULDHMXUKGWMISQ`, `2244-16-8`,
  `C11383`, `C00010891`, and `11380545`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned HTTP
429 for all public unauthenticated searches, so that provider's literature
leads were not available in this pass.

The browser-oriented PubMed HTML page for `PMID:11380545` returned a
proof-of-work/cookie challenge. NCBI E-utilities and Europe PMC both resolved
the PMID without that page and agreed on DOI `10.1034/j.1600-0536.2001.044006347.x`.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
