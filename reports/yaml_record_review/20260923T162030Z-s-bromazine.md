# YAML Record Review: (S)-bromazine

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/unspecified/s-bromazine.yaml`
- Started UTC: `2026-09-23T16:17:00Z`
- Finished UTC: `2026-09-23T16:20:30Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:59302` |
| Label | `(S)-bromazine` |
| Path | `data/antibiotics/unspecified/s-bromazine.yaml` |
| Class | `ANTIMICROBIAL_UNSPECIFIED` |
| Status | `SEEDED` |
| Grounding | `EXACT`, through the `CHEBI` source concept `CHEBI:59302` |
| Structure | `NUNIWXHYABYXKF-KRWDZBQOSA-N`, formula `C17H20BrNO`, charge `0` |
| Maintained input | `data/raw/chebi_antimicrobials.tsv` |
| Generated or maintained | Generated record under `data/antibiotics`; do not hand-edit seeded identity, class, structure, parent, xref, or source-concept fields |

The full target YAML was read before judgement. `CHEBI:59302` resolves through
`data/antibiotics/PATHS.tsv` to the unspecified antimicrobial record
`data/antibiotics/unspecified/s-bromazine.yaml`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/unspecified/s-bromazine.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/unspecified/s-bromazine.yaml --out /tmp/s-bromazine-validate-strict.tsv` | Pass; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 112` | Pass; `CHEBI:59302` remains queued immediately after the already-reviewed `(S)-1'-methyloctyl caffeate` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/s-bromazine-worklist.tsv` | Pass; the relevant entries were `mechanism` and `review-readiness`; the record was not queued for minted grounding, xref, multi-component, producer-candidate, activity-candidate, structure, scope, or target-evidence defects. |
| `uv run python scripts/search_publications.py --query '"(S)-bromazine" OR "(S)-bromodiphenhydramine" OR "NUNIWXHYABYXKF-KRWDZBQOSA-N"' --limit 20 --output /tmp/s-bromazine-publications.jsonl` | PubMed returned five general bromodiphenhydramine candidates; none of their abstracts named exact `(S)-bromazine`, exact `(S)-bromodiphenhydramine`, or the exact Standard InChIKey. Semantic Scholar returned an invalid response and was unavailable for this pass. |
| `uv run python scripts/search_publications.py --provider pubmed --query 'bromazine OR bromodiphenhydramine antimicrobial antibacterial antifungal mechanism MIC' --limit 20 --output /tmp/s-bromazine-broad-pubmed.jsonl` | PubMed returned older `ambodryl` / bromodiphenhydramine antimicrobial, synergy, and cross-resistance leads but no exact neutral `(S)-bromazine` activity, target, or mechanism abstract. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record. The bounded absence searches for a prior exact
review, curator decision, duplicate exact InChIKey, and the minted source
identifier used `rg --no-ignore --hidden`, so ignored `reports/` files were
included.

## Identity and Grounding

The ChEBI import row for `CHEBI:59302` exactly matches the generated identity:

- label `(S)-bromazine`
- 3-star ChEBI entry with `EXACT` grounding
- ChEBI role term `CHEBI:33281`
- broader parent `CHEBI:59177`
- Standard InChIKey `NUNIWXHYABYXKF-KRWDZBQOSA-N`
- neutral formula `C17H20BrNO` and charge `0`

OLS4 resolves `CHEBI:59302` as an active `(S)-bromazine` term, resolves its
hierarchical parent as `CHEBI:59177` / `bromazine`, and reports a symmetric
`is enantiomer of` graph edge with `CHEBI:59301` / `(R)-bromazine`. The local
`CHEBI:33281` role name is `antimicrobial agent`, and
`data/raw/chebi_role_names.tsv` classifies it as a generic `scope` role rather
than as a specific antibacterial, antifungal, antiprotozoal, antiviral, or
disinfectant class.

The record denotes the neutral `(S)` enantiomer of bromazine, not the racemate
`CHEBI:59177`, the opposite `(R)` enantiomer `CHEBI:59301`, or the separate
hydrochloride salt `(S)-bromazine hydrochloride` `CHEBI:59305`. The absence of
`xrefs`, `drug_xrefs`, and `document_xrefs` is consistent with the ChEBI source
row; the parent bromazine record carries the registry and drug-level xrefs, but
ChEBI did not assert them for the isolated `(S)` free base.

An ignored-inclusive search over `data/raw`, `data/antibiotics`, `curation`,
and `reports` found the exact InChIKey only in the ChEBI raw row and this
generated YAML record. The same bounded search found the minted source
identifier `antibioticmech:chebi-513dc21f02` only in the generated YAML, with
no `curation/decisions.tsv` override or prior exact review report.

## Evidence

Existing claim evidence is narrow:

| Claim | Evidence review |
|---|---|
| ChEBI identity, definition, synonyms, antimicrobial role, parent, and structure fields | Database-seeded from the `CHEBI:59302` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |
| `(S)-bromazine` is parented to bromazine | The ChEBI source row and live OLS4 graph list `CHEBI:59177`, the racemic bromazine record, as the broader parent. That is a strict broader relationship, not an exact xref. |
| `(S)-bromazine` is the enantiomeric counterpart of `(R)-bromazine` | OLS4 reports reciprocal `is enantiomer of` graph edges between `CHEBI:59302` and `CHEBI:59301`; the opposite enantiomer is a related term, not a duplicate identity. |

The record has no `mode_of_action`, `molecular_targets`,
`activity_observations`, `resistance_mechanisms`, `producer_organisms`,
`causal_graphs`, or record-level literature `evidence` items to overstate. No
existing citation is attached to a broader claim than it supports.

The bounded publication searches did not find a compound-specific mechanism or
antimicrobial assay for the isolated neutral `(S)-bromazine` enantiomer.
Broader PubMed results found antimicrobial leads for `ambodryl` or unspecified
bromodiphenhydramine, including penicillin synergy, promethazine synergy,
methdilazine synergy, and cross-resistance reports. Those abstracts do not
establish that the tested material was exact neutral `(S)-bromazine` rather
than the racemate, the hydrochloride salt, or another unspecified
bromodiphenhydramine form.

## Completeness

The record is not complete enough for `REVIEWED` because the mechanism gate is
unsettled:

- `mode_of_action` and `mode_of_action_target_scope` are absent.
- `molecular_targets` is absent.
- `causal_graphs` is absent.
- `activity_observations` is absent.
- `producer_organisms` is absent and the full worklist did not nominate a
  producer candidate for this ChEBI seed.
- `resistance_mechanisms`, `clinical_status`, and `datasets` are empty; the
  bounded review did not find local source rows proving these omissions are
  defects.

The exact ChEBI source row has no PMID or DOI lead. Future activity curation
needs primary full text or another exact source before copying MIC, synergy, or
cross-resistance values from `ambodryl` or bromodiphenhydramine papers, because
a racemic drug or salt assay is not automatically evidence for an isolated
enantiomeric free base.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| major | `mode_of_action`, `molecular_targets`, `causal_graphs`, and `activity_observations` are absent. ChEBI asserts the antimicrobial role for exact `CHEBI:59302`, but the local ChEBI row carries no source PMID/DOI and the bounded PubMed searches found only non-exact bromodiphenhydramine or `ambodryl` leads. | Curator-owned additions in `data/antibiotics/unspecified/s-bromazine.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blockers or minor findings were found.

## Recommended Edits

1. Inspect ChEBI's upstream provenance or primary literature for the isolated
   `(S)` enantiomer and decide whether exact `ActivityObservation` rows are
   supportable.
2. Inspect the `ambodryl` / bromodiphenhydramine antimicrobial leads in full
   text only as racemate or salt follow-up; curate them to `CHEBI:59177`,
   `CHEBI:59178`, `CHEBI:59305`, or another exact record only if the source
   compound form is explicit.
3. Leave `mode_of_action` unset unless a primary source supports a specific
   target or pathway for exact `(S)-bromazine`.
4. Add `molecular_targets` only if primary evidence supports a target beyond
   the source-seeded antimicrobial role.
5. Add a causal graph only after the activity and any mechanism interpretation
   are resolved; every edge needs its own primary-paper `EvidenceItem`.
6. Leave the seeded identity, role, parent, and structure untouched unless a
   ChEBI refresh or `curation/decisions.tsv` row changes the maintained import.

## Follow-up Checks

- After any curator-owned edit, rerun
  `just validate-strict data/antibiotics/unspecified/s-bromazine.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Rerun `just verify-corpus` to prove the generated record still reproduces
  from `data/raw/` plus curator inputs.
- Rerun `just worklist --limit 0 --tsv /tmp/s-bromazine-worklist.tsv` and
  confirm the `CHEBI:59302` `mechanism` and `review-readiness` rows changed as
  intended.
- Rerun `just lint`, `just test`, and `just render-check` through `just qc` if a
  later pass mutates the YAML rather than writing a review report only.

## Additional Notes

- `reports/` is gitignored. This report must be staged with `git add -f`.
- The bounded, ignored-inclusive local search found no committed PMID or DOI
  row for exact `CHEBI:59302`.
- The local exact-InChIKey search did not find any other source row or generated
  record with `NUNIWXHYABYXKF-KRWDZBQOSA-N`; ignored `reports` were included.
- Semantic Scholar returned an invalid response during the exact publication
  search and was not treated as a negative source.
