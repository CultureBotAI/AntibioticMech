# YAML Record Review: (+)-usnic acid

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/usnic-acid-38320.yaml`
- Started UTC: 2026-09-21T16:43:00Z
- Finished UTC: 2026-09-21T16:46:58Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:38320` |
| Label | `(+)-usnic acid` |
| Path | `data/antibiotics/antifungal/usnic-acid-38320.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | `CHEBI:38320` / `(+)-usnic acid` |
| Minted source key | `antibioticmech:chebi-8967c9728e` |
| Generated or maintained | Generated from `data/raw/chebi_antimicrobials.tsv` plus `data/antibiotics/PATHS.tsv`; future seeded identity, structure, class, role, synonym, or xref changes belong upstream rather than as hand edits. |

Resolution:

- `curation/record_review_queue.tsv:25` names `CHEBI:38320`, label
  `(+)-usnic acid`, and the review hint
  `MECHANISM_REVIEW: mechanism is absent`.
- `data/antibiotics/PATHS.tsv:968` maps `CHEBI:38320` to the `ANTIFUNGAL`
  directory and `usnic-acid-38320` slug.
- `data/raw/chebi_antimicrobials.tsv:774` is the ChEBI source row that seeds the
  target record.
- The whole 56-line target YAML was read before judging generated identity,
  structure, xrefs, source concept, and curation history.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/usnic-acid-38320.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/usnic-acid-38320.yaml --out /tmp/antibioticmech-usnic-acid-38320-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2939 expected records, 2939 on disk, no missing, unexpected, or drifted records, no identifiers absent from `PATHS.tsv`, and no stale lockfile rows. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-usnic-acid-38320.tsv` | Passed; `CHEBI:38320` appears in `mechanism` and `review-readiness`. |
| `just review-queue --limit 25` | Passed and confirmed `CHEBI:38320` remains in the review-readiness queue as `MECHANISM_REVIEW`, with zero source literature leads, zero record evidence items, and zero targets. |

No narrower term, reference, or curation-history validator is exposed for this
single ChEBI-seeded record. The focused strict validator covered closed schema
shape, and `verify-corpus` covered drift from the maintained raw and PATHS
inputs.

## Identity and Grounding

The seeded identity is consistent with the authoritative ChEBI record:

| Assertion | Record value | Check |
|---|---|---|
| ChEBI identity | `CHEBI:38320`, `(+)-usnic acid` | Official ChEBI resolved `CHEBI:38320` to `(+)-usnic acid`. |
| Formula and charge | `C18H16O7`, charge `0` | Matched ChEBI. |
| Masses | average `344.319`, monoisotopic `344.08960` | Matched ChEBI. |
| SMILES | `CC(=O)C1=C(O)C=C2Oc3c(C(C)=O)c(O)c(C)c(O)c3[C@@]2(C)C1=O` | Matched ChEBI. |
| Standard InChI | `InChI=1S/C18H16O7/c1-6-14(22)12(8(3)20)16-13(15(6)23)18(4)10(25-16)5-9(21)11(7(2)19)17(18)24/h5,21-23H,1-4H3/t18-/m0/s1` | Matched ChEBI. |
| InChIKey | `WEYVVCKOOFYHRW-SFHVURJKSA-N` | Matched ChEBI. |
| Antifungal role | `CHEBI:35718` | ChEBI asserts `has role` to `antifungal agent`. |
| Parent compound | `CHEBI:38319` | ChEBI asserts `(+)-usnic acid is a usnic acid`. |
| Xrefs | `beilstein:4272511`, `beilstein:4765696`, `cas:7562-61-0` | Matched the official ChEBI page. |

The target is specifically the `(9bR)` and dextrorotatory enantiomer of usnic
acid, not the sibling `CHEBI:122` / `(-)-usnic acid` record at
`data/antibiotics/antifungal/usnic-acid.yaml` and not the
stereochemically-unspecified parent `CHEBI:38319` / `usnic acid` record at
`data/antibiotics/antifungal/usnic-acid-38319.yaml`. Those three records have
different ChEBI identifiers, xrefs, and Standard InChIKeys.

ChEBI also asserts the non-antimicrobial `lichen metabolite` role
`CHEBI:78361` and the out-of-scope `acaricide` application `CHEBI:22153`. The
seeded record correctly retains only the ChEBI `antifungal agent`
antimicrobial role that drives inclusion in this repository, and this review
found no identity evidence that the record should move out of the `ANTIFUNGAL`
filing class.

Official ChEBI has no definition text on `CHEBI:38320`, matching the generated
record's intentionally absent `definition` and `definition_source`.

## Evidence

The target record has no record-level `evidence`. That is allowed for a
ChEBI-seeded record because the ChEBI source concept supplies provenance, and
there are no `activity_spectrum`, `molecular_targets`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status_assertions`,
`datasets`, or `causal_graphs` that would require claim-level evidence.

`data/raw/chebi_antimicrobials.tsv:774` carries no PubMed or DOI leads for the
exact `CHEBI:38320` source concept. Targeted publication searches resolved
broad leads rather than record-ready `(+)-usnic acid` claims:

| Search | Result |
|---|---|
| PubMed search for exact `(+)-usnic acid`, `(R)-usnic acid`, `d-Usnic acid`, `d-Usninic acid`, `WEYVVCKOOFYHRW`, or `7562-61-0` | Returned 20 candidates, but the inspected title/abstract metadata were mostly unrelated matches or non-antimicrobial exact `(+)-usnic acid` derivative work. |
| PubMed search for title/abstract `usnic acid` with `antifungal`, `Candida`, `Cryptococcus`, `dermatophyte`, or `Trichophyton` terms | Returned 20 candidates; the inspected abstracts surfaced lichen extracts, usnic acid derivatives, reviews, and non-fungal studies rather than claim-ready MIC or mechanism evidence for exact `CHEBI:38320`. |

The broader usnic-acid searches surfaced several curation leads:

| PMID | Public abstract support |
|---|---|
| `PMID:42653963` | Reports acetone-extracted lichen substances against the mycelial growth of *Heterobasidion parviporum* and associates fungistatic effects mainly with usnic acid, physodic acid, chloroatranorin, and atranorin. The public abstract describes extract complexes and does not map an exact MIC, assay, or isolated `(+)-usnic acid` observation to `CHEBI:38320`. |
| `PMID:41922928` | Reports synthesis and antifungal evaluation of acylhydrazide-modified usnic acid derivatives, with preliminary reactive-oxygen-species mechanism work on the derivative `A24`. That abstract supports a derivative scaffold lead, not parent exact `(+)-usnic acid`. |
| `PMID:42263183` | Reports validated chiral HPLC separation of `(+)-UA` and `(-)-UA` among usnic and isousnic acid enantiomers. The public abstract supports future enantiomer-specific source inspection, but reports no antifungal activity outcome. |
| `PMID:42186692` | Reports semi-synthetic modification of `(+)-usnic acid` and neuroprotective testing in scopolamine-injured C6 glial cells. The abstract names exact `(+)-usnic acid`, but the tested phenotype is host-cell neuroprotection rather than antimicrobial activity. |
| `PMID:39980171` | Reports isolation of usnic acid from an acetone extract of *Usnea steineri* and in vitro/in vivo trypanocidal activity against *Trypanosoma cruzi*. The abstract does not state `(+)-usnic acid`, and antiprotozoal activity would be an additional filing/activity question rather than evidence for the current antifungal gap. |

## Completeness

Consequential gaps:

- `mode_of_action`, `mode_of_action_target_scope`, `mode_of_action_notes`,
  `molecular_targets`, and `causal_graphs` are absent, so the record has no
  curator-owned antifungal mechanism.
- No `activity_spectrum` entry captures an exact fungus, strain, assay, value,
  qualifier, unit, and per-observation evidence for exact `(+)-usnic acid`.
- No `producer_organisms` entry captures the lichen or partner organism that
  biosynthesizes exact `CHEBI:38320`.

Correctly empty optional slots:

- No `resistance_mechanisms`: the record is ChEBI-only and CARD resistance
  routes do not apply.
- No `clinical_status` or `clinical_status_assertions`: the ChEBI-only source
  concept and inspected metadata do not assert regulatory use.
- No `datasets`: inspected ChEBI and PubMed metadata did not surface a public
  screening, omics, or structural dataset accession for this exact structure.

Ignored-inclusive absence search:

- Ran `rg --hidden --no-ignore` over `curation/`, `data/raw/`,
  `data/antibiotics/`, `reports/yaml_record_review/`, and the full exported
  worklist for `CHEBI:38320`, `(+)-usnic acid`, `usnic-acid-38320`,
  `WEYVVCKOOFYHRW-SFHVURJKSA-N`, and `7562-61-0`.
- The search included ignored files and found only the expected generated
  target, `PATHS.tsv`, ChEBI raw row, worklist rows, and the review-queue row.
  It found no prior review report, `curation/decisions.tsv` row, curated
  mechanism decision, or curated activity/provenance addition for this exact
  ChEBI concept.

## Findings

| Severity | ID | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| Major | USNIC-38320-M1 | The record is still a seeded exact-enantiomer stub with an antifungal ChEBI role but no curator-owned activity spectrum, producer assertion, mode of action, mechanism scope, molecular target, or causal graph. | `just review-queue --limit 25` reports `MECHANISM_REVIEW: mechanism is absent` with zero source literature leads; `/tmp/antibioticmech-worklist-usnic-acid-38320.tsv` lists `CHEBI:38320` only on `mechanism` and `review-readiness`. The targeted PubMed searches returned usnic-acid activity leads, but no existing exact `(+)-usnic acid` antifungal activity or mechanism claim has been curated onto the record. | `data/antibiotics/antifungal/usnic-acid-38320.yaml`, via guarded curator-owned record mutation plus a new `CurationEvent`; or `curation/decisions.tsv` if full-text inspection cannot verify exact `(+)-usnic acid` antifungal activity. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect the full text of source papers behind the broad usnic-acid
   antifungal leads, starting with `PMID:42653963` and earlier primary papers
   it cites, to decide which studies tested isolated `(+)-usnic acid`,
   stereochemically-unspecified usnic acid, `(-)-usnic acid`, mixtures, or
   derivatives.
2. If exact `(+)-usnic acid` antifungal primary evidence is found, add
   claim-local `activity_spectrum` observations with fungus, strain, assay,
   value, unit, and evidence; do not generalize extract-level or derivative
   activity onto the exact enantiomer.
3. Curate `mode_of_action`, `mode_of_action_target_scope`,
   `mode_of_action_notes`, molecular targets, and a causal graph only to the
   precision directly supported by inspected exact-compound mechanism sources.
4. If a lichen-biosynthesis source names the exact `(+)-usnic acid` enantiomer
   and a species or strain, add a `producer_organisms` assertion with
   claim-local evidence.
5. If exact `(+)-usnic acid` antifungal activity cannot be verified, leave the
   record `SEEDED` and add either a curator discussion for the unsupported
   ChEBI role or a source-concept decision in `curation/decisions.tsv` if the
   ChEBI role is clearly over-scoped from stereochemically unspecified usnic
   acid.
6. Preserve the seeded ChEBI identity, `(9bR)` structure, parent, xrefs, and
   source concept. This review found no grounded-identity correction that
   belongs in `data/raw/chebi_antimicrobials.tsv` or
   `data/antibiotics/PATHS.tsv`.

## Follow-up Checks

- `just validate-strict data/antibiotics/antifungal/usnic-acid-38320.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus --summary`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-usnic-acid-38320.tsv`
- `just review-queue --limit 25`
- `just qc`
- Manual diff review of
  `data/antibiotics/antifungal/usnic-acid-38320.yaml` and
  `curation/decisions.tsv` to confirm every new exact-structure claim is
  claim-local and directly supported by an inspected primary source.

## Additional Notes

- `NCBI_EMAIL` was unset in the local environment before using the PubMed
  search script; no contact email was sent as NCBI API metadata.
- The review avoided SerpAPI/Google Scholar because that path can be billable
  and was not necessary to establish the bounded finding above.
- The official ChEBI page also lists `(+)-usnic acid` as an acaricide, but
  antibiotic pesticides against metazoa remain out of scope for this corpus; the
  non-antimicrobial ChEBI role did not alter the record's `ANTIFUNGAL` filing
  class.
