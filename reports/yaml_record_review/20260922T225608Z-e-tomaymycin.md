# YAML Record Review: (E)-tomaymycin

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/e-tomaymycin.yaml`
- Started UTC: 2026-09-22T22:35:00Z
- Finished UTC: 2026-09-22T22:57:22Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:167317` |
| Label | `(E)-tomaymycin` |
| Path | `data/antibiotics/antibacterial/e-tomaymycin.yaml` |
| Class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding | `EXACT` |
| Source concept | `CHEBI:167317`, version `2026-08-30`, minted key `antibioticmech:chebi-50abd65978` |
| Source role | `CHEBI:33282` antibacterial agent |
| Parent compound | `CHEBI:157681` tomaymycin |
| Structure | `UQVNRKBFAXNOGA-LWTNMJDUSA-N`; formula `C16H20N2O4`; charge `0` |
| Source literature lead | `PMID:6833147` |
| Evidence-bearing objects | None |

This review covered one generated ChEBI-seeded exact `(E)-tomaymycin`
record. The YAML has exact ChEBI grounding, a ChEBI definition, one exact
ChEBI IUPAC synonym, one broader ChEBI parent, one antibacterial role,
chemical structure fields, and source-concept metadata. It has no generated or
curator-owned activity observation, molecular target, resistance mechanism,
mode of action, producer, discussion, dataset, causal graph, or claim-level
evidence.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/e-tomaymycin.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/e-tomaymycin.yaml --out /tmp/antibioticmech-e-tomaymycin-validation.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Pass: 2939 expected records, 2939 on disk, no missing records, no unexpected records, no drifted fields, no identifiers absent from `PATHS.tsv`, no stale lockfile rows. The unrelated known `ARO:3000337` CARD cross-reference warning was emitted. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-e-tomaymycin-worklist.tsv` | Pass: the full worklist TSV was written. Exact `CHEBI:167317` appears on `mechanism` and `review-readiness`; it was not found on any other queue in that TSV. |
| `just review-queue --limit 80` | Pass: `CHEBI:167317` is the first queued record after the records that already have `reports/yaml_record_review` reports; its row says `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `uv run runoak -i ols:chebi labels CHEBI:167317 CHEBI:157681 CHEBI:33282` | Pass: OLS resolved `CHEBI:167317` as `(E)-tomaymycin`, `CHEBI:157681` as `tomaymycin`, and `CHEBI:33282` as `antibacterial agent`. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '"E-tomaymycin" OR "tomaymycin"' --limit 20 --output /tmp/antibioticmech-e-tomaymycin-publications.jsonl` | Partial pass: PubMed returned 20 tomaymycin candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '6833147' --limit 5 --output /tmp/antibioticmech-e-tomaymycin-pmid6833147.jsonl` | Pass: PubMed resolved the ChEBI-seeded lead to PMID `6833147`, DOI `10.7164/antibiotics.36.276`. |
| `uv run --extra chemical-map python -c '<RDKit SMILES check>'` | Skipped: the repository pins `rdkit==2026.3.5`, whose wheel set does not include this macOS x86_64 platform. |
| `git diff --cached --check` | Pass: no staged whitespace or path errors. |
| `just lint` | Pass: Ruff reported `All checks passed!`. |

No narrower single-record term, reference, or history validator is exposed for
this plain ChEBI-seeded record; the schema/strict checks, `verify-corpus`, and
worklist are the narrowest documented local gates.

## Identity and Grounding

`data/raw/chebi_antimicrobials.tsv` seeds `CHEBI:167317` as a 3-star ChEBI
entry in antimicrobial role scope and gives the same label, exact definition,
SMILES, Standard InChI, Standard InChIKey, formula, charge, masses, ChEBI
parent, IUPAC name synonym, and source PMID that appear in or explain the
generated YAML. OAK through OLS resolved the identifier as `(E)-tomaymycin`, its
parent as `tomaymycin`, and its source role as `antibacterial agent`.

The record is correctly scoped to the E-isomer. ChEBI also has a broader
`CHEBI:157681` tomaymycin record with the Standard InChIKey
`UQVNRKBFAXNOGA-SWLSCSKDSA-N`, and a sibling `CHEBI:167318` `(Z)-tomaymycin`
record with the Standard InChIKey `UQVNRKBFAXNOGA-OHLDGCSVSA-N`. The reviewed
YAML, its `PATHS.tsv` row, and the exact source row all use
`UQVNRKBFAXNOGA-LWTNMJDUSA-N` and the `/b9-4+` Standard InChI bond layer, so
the generated record is not accidentally standing for the parent or Z-isomer.

The `ANTIBACTERIAL` filing class is consistent with the only generated ChEBI
role: `CHEBI:33282` maps to `ANTIBACTERIAL` with priority `2` in
`conf/sources.yaml`. Because the source row has no more-specific mechanism
role and no same-structure xrefs, the generated YAML correctly has no
`mode_of_action`, `mode_of_action_target_scope`, molecular target, activity
observation, causal graph, or xref block.

Before this review branch was created, ignored-inclusive searches found no
prior dedicated `CHEBI:167317`, `(E)-tomaymycin`, or `e-tomaymycin` review
report and no curation decision, curator-inventory row, generated activity
observation, molecular target, resistance edge, producer, dataset, or
causal-graph claim resolving this exact record. Exact branch lookups found no
local branch, remote-tracking branch, or remote
`review-chebi-167317-e-tomaymycin` branch.

## Evidence

The record's antibacterial class comes from the broad ChEBI `CHEBI:33282`
`antibacterial agent` role. That role is sufficient for the generator to file
the record under `ANTIBACTERIAL`, but it is not assertion-level primary
evidence for a particular bacterial organism, strain, MIC value, bacterial
molecular target, mode of action, resistance mechanism, or causal graph edge.

The focused PubMed lookup resolved ChEBI's only source-literature lead:
`PMID:6833147`, Tozuka et al. 1983, "Studies on tomaymycin. II. Total syntheses
of the antitumor antibiotics, E-and Z-tomaymycins." The abstract reports total
syntheses of naturally occurring E-tomaymycin and its olefinic Z geometrical
isomer and says the Z-isomer had the same antibacterial activity as the
E-isomer. That supports the source-level premise that exact E-tomaymycin is an
antibacterial compound, but the abstract does not supply a schema-ready
organism, strain, assay, MIC endpoint, unit, or molecular target.

The broader tomaymycin search found a 2009 `Streptomyces achromogenes`
gene-cluster lead for source-organism biosynthesis, a 2017 cell-free in vitro
NRPS reconstitution lead for pathway context, and DNA-adduct papers that
discuss tomaymycin's covalent reaction with DNA. The inspected abstracts either
discuss parent tomaymycin, oxo-tomaymycin, or pathway enzymology without
assigning a source-isolated producer, antibacterial endpoint, or microbial
target to exact `(E)-tomaymycin`. Full text is needed before the 2009 paper can
be mapped to `ProducerOrganism`; the 2017 in vitro lead should stay out of that
taxon-backed slot unless its full text also contains exact source-organism
evidence.

Semantic Scholar returned HTTP 429 for the repository publication-helper search;
PubMed returned the bounded search results and resolved the exact PMID lead.

## Completeness

The record is incomplete as a reviewed antibacterial record. It has exact ChEBI
identity, structure, one broader ChEBI parent, a ChEBI antibacterial role, and
one source-paper lead, but no claim-level evidence, no `activity_spectrum`, no
molecular target, no resistance mechanism, no mode of action, no dataset, and
no causal graph.

The empty molecular target, mode-of-action, resistance, and causal-graph slots
are correct for the current seed. The inspected PubMed abstracts provide
mechanistic leads for parent tomaymycin and pyrrolobenzodiazepine DNA adducts,
but not an exact E-isomer antimicrobial target assertion with organism context.

The empty `activity_spectrum` slot should be treated as missing curation, not
as negative activity. The 1983 seed citation is a direct E/Z tomaymycin lead,
but exact organism, strain, assay, endpoint, value, and unit details still need
full-paper extraction.

The empty `producers`, `clinical_status_assertions`, `datasets`, and
`discussions` slots are acceptable for the generated record. No clinical
assertion, public dataset accession, or discussion-worthy conflict with exact
`CHEBI:167317` claim-level evidence was identified in the bounded checks.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Exact `CHEBI:167317` has source-level ChEBI antibacterial classification, but no curator-owned primary evidence for exact antibacterial activity, no activity observation, no molecular target, no mode of action, and no causal graph. | The generated record has 1 source literature lead, zero record evidence items, zero `activity_spectrum` rows, zero targets, no resistance edges, no mode-of-action fields, and no causal graph. The worklist flags exact `CHEBI:167317` for `mechanism` and `review-readiness` only. `PMID:6833147` is a direct E/Z tomaymycin synthesis and antibacterial lead, but no activity endpoint or mechanistic claim from that paper is represented in the YAML. | Future curator-owned fields on `data/antibiotics/antibacterial/e-tomaymycin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI later changes this term's roles, parentage, or citations, the ChEBI inventory extractor owns that refresh. |

No blocker identity, structure, schema, xref, parentage, class, or
audit-history findings were found.

No minor findings.

## Recommended Edits

1. Inspect `PMID:6833147` in full and add exact `activity_spectrum` rows for
   source-reported E-tomaymycin observations only when the table text preserves
   organism, strain or isolate, endpoint type, value, unit, and assay method.
2. Inspect `PMID:19270147` in full as the source-organism biosynthesis lead and
   add an exact-E-isomer `ProducerOrganism` only if the paper supports that
   `Streptomyces achromogenes` produced `(E)-tomaymycin`, not only parent
   tomaymycin or a biosynthetic intermediate.
3. Treat `PMID:28890318` as in vitro pathway context only unless its full text
   contains an exact source-isolated E-isomer claim; a cell-free reconstituted
   NRPS system is not itself a `ProducerOrganism`.
4. Choose a `mode_of_action` only after full-paper review finds
   compound-specific evidence that exact `(E)-tomaymycin` has an antibacterial
   mechanism representable by `ModeOfActionEnum`. Do not infer an antimicrobial
   mode solely from parent-tomaymycin DNA-adduct papers or from antitumor PBD
   family behavior.
5. Add a `MolecularTarget` or `CausalEdge` only if the full text or a follow-on
   primary paper directly assays exact `(E)-tomaymycin` against a defined
   microbial target family, complex, or function.

## Follow-up Checks

After any exact-term curation or source refresh, rerun:

1. `just validate data/antibiotics/antibacterial/e-tomaymycin.yaml`
2. `just validate-strict data/antibiotics/antibacterial/e-tomaymycin.yaml --out /tmp/antibioticmech-e-tomaymycin-validation.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/antibioticmech-e-tomaymycin-worklist.tsv`
5. `just review-queue --limit 80`
6. `just qc`

Manually compare future activity, producer, mechanism, target, and causal-graph
curation against the full tomaymycin synthesis and biosynthesis papers to
confirm that every curated claim is attached to exact `(E)-tomaymycin` rather
than parent tomaymycin, `(Z)-tomaymycin`, oxo-tomaymycin, or a synthetic
pyrrolobenzodiazepine analog.

## Additional Notes

All absence checks in this report used `rg --no-ignore --hidden`, `find`, or
Git/GitHub ref listing, so they included ignored `reports/` files or otherwise
did not depend on Git's ignore filters.
