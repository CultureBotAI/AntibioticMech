# YAML Record Review: (−)-anisomycin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/anisomycin.yaml`
- Started UTC: 2026-09-24T09:48:48Z
- Finished UTC: 2026-09-24T09:49:36Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/anisomycin.yaml` |
| Class | `ANTIVIRAL` |
| Identifier | `CHEBI:338412` |
| Label | `(−)-anisomycin` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | ChEBI `CHEBI:338412`, version `2026-08-30`, minted key `antibioticmech:chebi-0bda73c132` |
| Standard InChIKey | `YKJYKKNCCRKFSL-RDBSUJKOSA-N` |
| Xrefs | `cas:22862-76-6`, `drugbank:DB07374`, `kegg.compound:C11281`, `lincs.smallmolecule:LSM-4047`, `pdb-ccd:ANM`, `reaxys:20705`, `wikipedia.en:Anisomycin` |

`anisomycin.yaml` is a generated exact ChEBI record for the neutral
`(2R,3S,4S)` anisomycin structure. Its generated content contains ChEBI-owned
identity, structure, parentage, xrefs, an anticoronaviral filing role, an
additional generic antimicrobial role, and a ChEBI-derived `MULTIPLE`
`mode_of_action` from protein-synthesis-inhibitor and DNA-synthesis-inhibitor
roles. It contains no record-level evidence, `molecular_targets`,
`activity_spectrum`, `resistance_mechanisms`, `producer_organisms`,
`clinical_status`, `datasets`, `discussions`, or `causal_graphs`.

## Validation

| Command or check | Result |
|---|---|
| `find . -name AGENTS.md -print` | Passed before review; no local `AGENTS.md` files were present. `find` included hidden and ignored paths. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; `CHEBI:338412` is row 139 of the scientific read-only review queue, immediately after the already reviewed `CHEBI:63687` erythro-mefloquine row. |
| `just validate data/antibiotics/antiviral/anisomycin.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/anisomycin.yaml --out /tmp/anisomycin-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2939 expected records were present on disk with no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` drift, and the known unrelated `ARO:3000337` iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/anisomycin-worklist.tsv` | Passed; exact `CHEBI:338412` appeared on `mechanism`, `producer-candidate`, and `review-readiness`. The `mechanism` row reported 0 CARD targets and 0 resistance edges to build on. The `producer-candidate` row captured the ChEBI-definition phrase `Streptomyces species` as a source-only isolation lead. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `rg --no-ignore --hidden -n -e 'CHEBI:338412' -e 'antibioticmech:chebi-0bda73c132' -e 'YKJYKKNCCRKFSL-RDBSUJKOSA-N' -e '22862-76-6' -e 'DB07374' -e 'C11281' -e 'LSM-4047' -e 'pdb-ccd:ANM' -e '20705' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed; this wider ignored/hidden-inclusive search found the exact generated YAML, ChEBI raw row, `PATHS.tsv` lock row, and `review-readiness` row. It found no prior exact anisomycin review report and no curator-owned activity, target, resistance, producer, or causal-graph claim for exact `CHEBI:338412`. |
| `rg --no-ignore --hidden -n -e 'CHEBI:338412' -e 'antibioticmech:chebi-0bda73c132' -e 'YKJYKKNCCRKFSL-RDBSUJKOSA-N' -e '22862-76-6' -e 'DB07374' -e 'C11281' -e 'LSM-4047' -e 'ANM' -e 'reaxys:20705' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv` | Passed with two irrelevant `ANM` substring hits in MIBiG InChIKeys; the ignored/hidden-inclusive search found no local ARO, BindingDB, Drugs@FDA, MIBiG, PHI-base, or grounding-decision row for exact `CHEBI:338412` or its exact structure/xref identifiers. |
| `rg --no-ignore --hidden -n -e 'CHEBI:149553' -e 'CHEBI:33281' -e 'CHEBI:48001' -e 'CHEBI:59517' data/raw/chebi_role_names.tsv conf src docs` | Passed; the exact ChEBI role names and local class/mode/scope maps agree with the seeded `ANTIVIRAL`, `MULTIPLE`, and `HOST_SHARED_TARGET` values. |
| OLS4 lookup for `CHEBI:338412` | Passed; OLS resolved exactly one non-obsolete ChEBI class with the same identifier, ASCII-equivalent label, definition, and synonyms. |
| ChEBI page lookup for `CHEBI:338412` | Passed; current ChEBI agreed with the generated label, 3-star status, definition, formula, charge, average mass, monoisotopic mass, SMILES, Standard InChI, Standard InChIKey, IUPAC synonym, four exact synonyms, `CHEBI:149553` and `CHEBI:33281` in-scope roles, `CHEBI:48001` and `CHEBI:59517` mode roles, `CHEBI:25558`/`CHEBI:46777` parentage, PubMed citations, CAS, Reaxys, and the DrugBank, KEGG, LINCS, PDB-CCD, and Wikipedia xrefs. |
| KEGG lookup for `C11281` | Passed; KEGG resolved anisomycin with formula `C14H19NO4` and linked ChEBI `338412`, PDB-CCD `ANM`, and CAS `22862-76-6`. |
| RCSB PDB CCD lookup for `ANM` | Passed; RCSB resolved `ANM` as `ANISOMYCIN` with formula `C14 H19 N O4`, charge 0, InChI `InChI=1S/C14H19NO4/c1-9(16)19-14-12(15-8-13(14)17)7-10-3-5-11(18-2)6-4-10/h3-6,12-15,17H,7-8H2,1-2H3/t12-,13+,14+/m1/s1`, InChIKey `YKJYKKNCCRKFSL-RDBSUJKOSA-N`, systematic name `(2R,3S,4S)-4-hydroxy-2-(4-methoxybenzyl)pyrrolidin-3-yl acetate`, and related DrugBank `DB07374`, CAS `22862-76-6`, and ChEBI `CHEBI:338412` mappings. |
| DrugBank `DB07374` lookup | Not independently checked: DrugBank returned a browser challenge page rather than the public compound record. RCSB links `ANM` to `DB07374` by matching InChIKey. |
| LINCS `LSM-4047` lookup | Not independently checked: the LINCS HMS resolver returned HTTP 500 in this workflow. ChEBI lists this exact xref on current `CHEBI:338412`. |
| Reaxys xref `20705` | Not independently checked: ChEBI lists this Reaxys registry number on exact `CHEBI:338412`, but Reaxys had no public resolver available in this workflow. |
| PubMed exact-source PMID lookup for `16005213`, `22004851`, `23192275`, `23525555`, `23582782`, `24333448`, and `6834379` | Passed; all seven PubMed identifiers from the committed ChEBI row and current ChEBI resolved. Their titles are dominated by anti-infective anisomycin-library, anisomycin-toxicology, and host cancer/cell-death studies rather than exact curated microbial targets. |
| PubMed search for `anisomycin` plus coronavirus/SARS/antiviral terms | Passed as discovery; PubMed returned 83 candidates. The first screened exact mechanistic/productive lead was PMID:38346922, a 2024 primary paper titled `Antiviral Mechanisms of Anisomycin Produced by Streptomyces albulus SN40 on Potato Virus Y`. |
| PubMed search for `anisomycin` plus protein-synthesis, peptidyl-transferase, 80S, or ribosome terms | Passed as discovery; PubMed returned 1029 broad anisomycin leads that would need manual triage before any molecular target can be promoted. |
| PubMed exact-identifier search for `CHEBI:338412`, `YKJYKKNCCRKFSL-RDBSUJKOSA-N`, `22862-76-6`, `DB07374`, `C11281`, and `LSM-4047` | Partial pass; the exact InChIKey, DrugBank, KEGG, and LINCS tokens were absent from PubMed, while the query returned many noisy matches because PubMed tokenized `CHEBI:338412` and CAS `22862-76-6`. |
| Semantic Scholar searches | Not checked: exact-identifier and mechanism searches both returned HTTP 429. |

No separate focused term, reference, or history validator is exposed in
`justfile`; the full-corpus `verify-corpus` and `worklist` checks above are the
documented narrow checks for seeded-field drift and queue placement.

## Identity and Grounding

| Assertion | Review |
|---|---|
| Label and identifier | Supported. OLS resolves `CHEBI:338412` to the ASCII-equivalent `(-)-anisomycin`, and the current ChEBI compound page resolves the same accession to the same exact structure. |
| Structure | Supported. The generated SMILES, Standard InChI, Standard InChIKey `YKJYKKNCCRKFSL-RDBSUJKOSA-N`, formula `C14H19NO4`, charge `0`, average mass `265.309`, and monoisotopic mass `265.13141` match `data/raw/chebi_antimicrobials.tsv` and current ChEBI. RCSB PDB CCD `ANM` independently reports the same InChI and InChIKey. |
| Source concept | Supported. The only source concept is ChEBI `CHEBI:338412`; no ARO, curator, BindingDB, MIBiG, FDA, or PHI-base source row is attached to this record. |
| Filing class and roles | Supported as ChEBI assertions. The exact ChEBI row and current ChEBI attach `CHEBI:149553` anticoronaviral agent and `CHEBI:33281` antimicrobial agent. `conf/sources.yaml` maps `CHEBI:149553` to `ANTIVIRAL`, which explains the generated filing class. |
| Seeded mode of action | Supported as a ChEBI-derived restatement, not as a curated mechanistic review. ChEBI attaches `CHEBI:48001` protein synthesis inhibitor and `CHEBI:59517` DNA synthesis inhibitor; `conf/sources.yaml` maps those to `PROTEIN_SYNTHESIS_INHIBITION`, `NUCLEIC_ACID_SYNTHESIS_INHIBITION`, and `HOST_SHARED_TARGET`, yielding the generated `MULTIPLE` note that explicitly says it is not curator-reviewed. |
| Parent terms | Supported. Current ChEBI attaches exact `CHEBI:338412` to `CHEBI:25558` and `CHEBI:46777` by `is a`, matching the generated `parent_compounds` field. |
| Xrefs | Mostly supported as ChEBI assertions. Current ChEBI lists `DB07374`, `C11281`, `LSM-4047`, `ANM`, `20705`, and `Anisomycin` on exact `CHEBI:338412`; KEGG `C11281` corroborates ChEBI, PDB-CCD, and CAS links; RCSB `ANM` corroborates the exact InChIKey and DrugBank/CAS/ChEBI mappings. DrugBank, LINCS, and Reaxys were not directly inspectable. |
| Grounding decision | Complete as empty. Exact ChEBI source concepts ground to their own CURIEs, and an ignored/hidden-inclusive search found no `curation/decisions.tsv` override for `CHEBI:338412` or `antibioticmech:chebi-0bda73c132`. |

The record denotes exact ChEBI `(−)-anisomycin`, not anisomycin analogues from
PMID:16005213 and PMID:6834379 and not the enantiomeric or stereochemically
unspecified labels that appear in synthesis literature.

## Evidence

| Claim in generated YAML | Support |
|---|---|
| `(−)-anisomycin` is a ChEBI-grounded exact record with the listed IUPAC name, exact structure, broader parents, PubMed citations, and xrefs. | Supported by the committed ChEBI inventory row for `CHEBI:338412` and by the current ChEBI page for the same term. |
| Exact `CHEBI:338412` has anticoronaviral and generic antimicrobial roles. | Supported as ChEBI assertions by the committed ChEBI row and by current ChEBI, both of which list `CHEBI:149553` and `CHEBI:33281` on exact `CHEBI:338412`. |
| Exact `CHEBI:338412` inhibits protein synthesis and DNA synthesis. | Supported only as ChEBI mechanism-role assertions by the committed ChEBI row and current ChEBI. The generated `mode_of_action_notes` correctly keep this on the seeder side and say that a curator must decide whether one mechanism is primary. |
| Exact `CHEBI:338412` is isolated from `Streptomyces` species. | Supported only at definition-string granularity by the committed ChEBI row and current ChEBI. No `ProducerOrganism` exists, and the `producer-candidate` queue correctly leaves the exact taxon/strain unresolved. |

No unsupported claim-level citation was found because no `MolecularTarget`,
`ResistanceMechanism`, `ActivityObservation`, `ProducerOrganism`, `CausalEdge`,
or `Dataset` is populated on `anisomycin.yaml`.

The local ARO target/resistance, BindingDB, FDA, MIBiG, and PHI-base
inventories have no exact row for `CHEBI:338412`, Standard InChIKey
`YKJYKKNCCRKFSL-RDBSUJKOSA-N`, or CAS `22862-76-6`. Those absence checks
included ignored and hidden files.

The bounded publication searches did not resolve a primary molecular target,
activity observation, producer, or causal graph to add in this read-only pass.
The current ChEBI PMID set includes anti-infective library, anisomycin
analogue, host toxicology, and host cancer/cell-death articles. PMID:38346922
is a newer primary lead that may support `Streptomyces albulus` SN40 production,
plant-virus activity against tobacco mosaic virus and Potato virus Y, and a
plant host-defense mechanism after full-text inspection, but its PubMed abstract
also says the viral-protein binding claims are molecular docking suggestions.
Do not promote the docking results to direct targets without inspecting the
paper and any underlying activity assays.

## Completeness

| Area | Status |
|---|---|
| Identity and structure | Complete enough for read-only review; current ChEBI, the committed ChEBI row, `PATHS.tsv`, RCSB `ANM`, and the generated YAML agree on exact `CHEBI:338412` and `YKJYKKNCCRKFSL-RDBSUJKOSA-N`. |
| Source concept | Complete. The only source concept is exact `CHEBI:338412`. |
| Filing class | Complete as a ChEBI-derived class. The anticoronaviral role maps to `ANTIVIRAL`; the additional generic antimicrobial role is retained in `activity_roles`. |
| Mode of action | Incomplete. `MULTIPLE` and `HOST_SHARED_TARGET` are source-seeded from ChEBI mechanism roles and have no curated target or causal graph. |
| Molecular targets | Complete as empty for imported source rows, but not scientifically resolved. CARD and BindingDB contribute no target; ChEBI and PDB/DrugBank point at host ribosome biology that needs primary evidence before a GO, ComplexPortal, InterPro, ARO, or UniProt-family target can be asserted. |
| Activity observations | Complete as empty for imported source rows, but not scientifically resolved. ChEBI supplies roles and PMIDs, not organismal assay values with units. |
| Resistance mechanisms | Complete as empty. CARD contributes no ARO resistance edges to this ChEBI-only record, and no PHI-base row names exact `CHEBI:338412`. |
| Producers | Incomplete. The ChEBI definition says anisomycin was isolated from various `Streptomyces` species; the worklist correctly flags this as a `producer-candidate`, and PMID:38346922 is a concrete follow-up lead for `Streptomyces albulus` SN40. |
| Clinical status | Complete as empty. Drugs@FDA contributes no exact GSRS/UNII row for this InChIKey. |
| Datasets | Complete as empty for the inspected evidence. The generated record has no dataset assertion, and the ChEBI/PubMed checks did not expose a specific exact-anisomycin public dataset accession to add. |
| Discussions | Complete as empty for current read-only review. The uncurated ChEBI-derived mechanism and unresolved producer candidate are already represented by derived `mechanism`, `producer-candidate`, and `review-readiness` queues. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, current ChEBI agrees with the exact identity and structure, and the record does not conflate exact `(−)-anisomycin` with anisomycin analogues. | Not applicable. |
| Major | Exact `CHEBI:338412` has a ChEBI-derived `MULTIPLE` mode of action and no curated molecular target or causal graph. | `mode_of_action_notes` says the mechanism is assigned from ChEBI roles `CHEBI:48001` and `CHEBI:59517` and is not curator-reviewed; the generated record has no `molecular_targets` or `causal_graphs`; exact `CHEBI:338412` is on the `mechanism` and `review-readiness` queues with 0 CARD targets and 0 CARD resistance edges. | Future curator-owned fields on `data/antibiotics/antiviral/anisomycin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI changes this term's roles, parentage, xrefs, or source PMIDs, `data/raw/chebi_antimicrobials.tsv` and the ChEBI extractor own the refresh. |
| Major | The natural producer signal is unresolved. | ChEBI's definition says anisomycin was isolated from various `Streptomyces` species, but the generated record has no `producer_organisms`; exact `CHEBI:338412` is on `producer-candidate`; PMID:38346922 reports anisomycin from `Streptomyces albulus` SN40 against plant viruses and should be inspected as a possible curated producer/activity source. | Future `producer_organisms` and, if supported, `activity_spectrum` entries on `data/antibiotics/antiviral/anisomycin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; future exact MIBiG imports would be owned by `data/raw/mibig_producers.tsv`. |
| Minor | Several direct external xrefs could only be checked through ChEBI or corroborating public resolvers in this review. | KEGG `C11281` and RCSB `ANM` corroborate exact ChEBI, CAS, PDB-CCD, and DrugBank mappings, but DrugBank itself served a browser challenge, LINCS returned HTTP 500, and Reaxys had no public resolver. | ChEBI extractor and `data/raw/chebi_antimicrobials.tsv`; future curation should either verify these sources externally or preserve them as upstream ChEBI assertions. |

## Recommended Edits

1. Leave the existing ChEBI-seeded `mode_of_action` block under seeder
   ownership until a curator inspects primary literature for exact anisomycin
   and decides whether protein-synthesis inhibition, DNA-synthesis inhibition,
   viral host-defense induction, or another represented mode is the defensible
   antimicrobial mechanism.
2. Inspect PMID:38346922 first for exact `(−)-anisomycin` production by
   `Streptomyces albulus` SN40, plant-virus activity assay details, and the
   strength of evidence for the TMV/PVY mechanistic assertions. If supported,
   add `ProducerOrganism` and `ActivityObservation` objects with claim-level
   evidence; treat docking-only target suggestions as hypotheses unless the
   paper reports direct target evidence.
3. Inspect ChEBI source PMIDs 16005213 and 6834379 for exact-anisomycin or
   exact-analogue whole-cell assay values before considering bacterial, fungal,
   or antiprotozoal activity observations. Do not generalize an anisomycin
   analogue's SAR result to exact `CHEBI:338412`.
4. If a molecular target is supported, write it with a guarded mutator that
   asserts `identifier == "CHEBI:338412"`, updates only curator-owned fields,
   appends a `codex` `CurationEvent` with `llm_assisted: true`, and emits via
   `write_validated_antibiotic`.
5. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the curated mode of action satisfy the `docs/CURATION.md`
   sign-off criteria.

## Follow-up Checks

After a future mechanism, producer, or activity curation edit:

1. `just validate-strict data/antibiotics/antiviral/anisomycin.yaml --out /tmp/anisomycin-strict.tsv`
2. `just verify-corpus`
3. `just worklist --limit 0 --tsv /tmp/anisomycin-worklist.tsv`
4. `just qc`
5. `git diff -- data/antibiotics/antiviral/anisomycin.yaml curation/decisions.tsv src scripts tests`
6. `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

Rerun exact ignored-inclusive searches for `CHEBI:338412`,
`antibioticmech:chebi-0bda73c132`, `YKJYKKNCCRKFSL-RDBSUJKOSA-N`,
`22862-76-6`, `DB07374`, `C11281`, `LSM-4047`, `pdb-ccd:ANM`, and `20705` to
confirm future evidence still lands on exact `(−)-anisomycin` and not on a
synthetic analogue.

## Additional Notes

Searches used to establish absence included ignored and hidden files:

- `find . -name AGENTS.md -print`
- `rg --no-ignore --hidden -n -e 'CHEBI:338412' -e 'antibioticmech:chebi-0bda73c132' -e 'YKJYKKNCCRKFSL-RDBSUJKOSA-N' -e '22862-76-6' -e 'DB07374' -e 'C11281' -e 'LSM-4047' -e 'pdb-ccd:ANM' -e '20705' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests`
- `rg --no-ignore --hidden -n -e 'CHEBI:338412' -e 'antibioticmech:chebi-0bda73c132' -e 'YKJYKKNCCRKFSL-RDBSUJKOSA-N' -e '22862-76-6' -e 'DB07374' -e 'C11281' -e 'LSM-4047' -e 'ANM' -e 'reaxys:20705' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv`

The repository does not have the stale `data/raw/card_antibiotics.tsv` and
`data/raw/card_literature.tsv` filenames used in an early exploratory raw-file
search; the repeated search used the actual `data/raw/aro_antibiotics.tsv`,
`data/raw/aro_target_edges.tsv`, and `data/raw/aro_resistance_edges.tsv`
inventories. Semantic Scholar returned HTTP 429, DrugBank was browser-gated,
and LINCS returned HTTP 500, so those provider records were not directly
available in this pass.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
