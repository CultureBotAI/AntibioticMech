# YAML Record Review: pefloxacin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antibacterial/pefloxacin.yaml`
- Started UTC: 2026-09-21T06:04:07Z
- Finished UTC: 2026-09-21T06:05:02Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antibacterial/pefloxacin.yaml` |
| Class | `ANTIBACTERIAL` |
| ID | `CHEBI:50199` |
| Label | `pefloxacin` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories plus curator-owned generated fields |

The record denotes the ChEBI-grounded pefloxacin structure with InChIKey
`FHFYDNQZQSQIAI-UHFFFAOYSA-N`, an ARO source concept `ARO:3000762`, a ChEBI
source concept `CHEBI:50199`, 22 CARD/ARO resistance assertions, two imported
CARD/ARO molecular-target assertions, one curator-added DNA gyrase causal graph,
no clinical status assertions, and no `Discussion` entries.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/pefloxacin.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/pefloxacin.yaml --out /tmp/antibioticmech-pefloxacin-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` drift. |
| `just review-queue --limit 10` | Confirmed `pefloxacin` remains in `review-readiness`: `TARGET_EVIDENCE_REVIEW`, two target assertions needing primary evidence. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`pefloxacin.yaml` resolves unambiguously under
`data/antibiotics/antibacterial/`. The generated lock row in
`data/antibiotics/PATHS.tsv` maps `CHEBI:50199` to
`ANTIBACTERIAL/pefloxacin`, and `source_concepts` carry the expected
`ARO:3000762` and `CHEBI:50199` upstream identities.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:50199`: SMILES, Standard InChI, InChIKey
`FHFYDNQZQSQIAI-UHFFFAOYSA-N`, formula `C17H20FN3O3`, charge `0`, average
mass, and monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`.

No conflicting local grounding decision was found for
`antibioticmech:aro-e4d6a66b1b` or `antibioticmech:chebi-0c977bad4e` in a
gitignore-independent search over `curation/`, `data/`, and
`reports/yaml_record_review/` with `rg --hidden --no-ignore`; the only matches
were the two `source_concepts` inside this record.

## Evidence

The generated identity, label, synonyms, exact structure, parent compounds,
xrefs, filing class, activity roles, structural class, source concepts,
resistance assertions, and target assertions reproduce from the committed
inventories with no `verify-corpus` drift.

The 22 `resistance_mechanisms` entries are imported CARD/ARO database
assertions from `data/raw/aro_resistance_edges.tsv`. Each item cites its own
`ARO:*` identifier and states that the edge is a CARD/ARO assertion rather than
a primary paper; this is honest database provenance, not a primary-literature
upgrade.

Both `molecular_targets` entries are source-owned CARD/ARO content:
`ARO:3000733`, `fluoroquinolone sensitive gyrA`, and `ARO:3000735`,
`Fluoroquinolone sensitive parC`, imported from
`data/raw/aro_target_edges.tsv`. Their `evidence_status:
PRIMARY_EVIDENCE_NEEDED`, `source: CARD_ARO`, and ARO evidence identifiers
accurately describe unresolved database assertions. Neither row has a direct
compound-level primary paper, target organism, strain, or assay attached to the
target claim.

No BindingDB quantitative target measurement was found for pefloxacin,
`CHEBI:50199`, `ARO:3000762`, or InChIKey `FHFYDNQZQSQIAI-UHFFFAOYSA-N` in the
local `data/raw/bindingdb_target_measurements.tsv` inventory.

The curator-owned `pefloxacin_dna_gyrase_supercoiling_inhibition` causal graph
carries edge-level `PMID:3010848` references. A gitignore-independent search
over the record, `data/raw/`, and `curation/` found that PMID only in the
generated pefloxacin YAML, so the graph is the local owner for that paper-backed
mechanism assertion. This pass did not re-inspect the paper text for that PMID
and therefore did not revalidate the exact wording of the graph edge notes
against the primary article.

## Completeness

The record has a defensible exact identity, structure, filing class, curated
mode of action, target scope, causal graph, and imported CARD/ARO resistance
rows.

The consequential remaining gap is sign-off of the two imported CARD/ARO target
rows. The causal graph reviews pefloxacin inhibition of bacterial DNA
gyrase-catalyzed DNA supercoiling, but it does not rewrite
`molecular_targets[0]` or `molecular_targets[1]`. Those two target labels remain
the coarse CARD/ARO `fluoroquinolone sensitive gyrA` and `Fluoroquinolone
sensitive parC` assertions with no organism, strain, assay, or primary article.

No activity observations, producer claims, public datasets, structural
observations, BindingDB target measurements, clinical assertions, or discussions
are present. The review found no specific local evidence that any of those
optional sections must be populated before the CARD/ARO target claims are
curated.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The two source-owned CARD/ARO molecular target assertions still need primary evidence before the record can be `REVIEWED`. | `molecular_targets[0]` is imported `ARO:3000733` / `fluoroquinolone sensitive gyrA`; `molecular_targets[1]` is imported `ARO:3000735` / `Fluoroquinolone sensitive parC`; both have `evidence_status: PRIMARY_EVIDENCE_NEEDED`, and `just review-queue --limit 10` keeps `pefloxacin` in `TARGET_EVIDENCE_REVIEW`. | Either the ARO target import/merge path that emits the `ARO:3000733` and `ARO:3000735` rows or curator-owned target replacements written to `data/antibiotics/antibacterial/pefloxacin.yaml` through `write_validated_antibiotic`. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Decide whether the coarse CARD/ARO `ARO:3000733` and `ARO:3000735` target
   rows should be replaced by curator-owned, primary-paper-backed bacterial DNA
   gyrase and topoisomerase IV targets or retained as queued database
   assertions.
2. If they are retained, leave `curation_status: SEEDED` and keep the record in
   `TARGET_EVIDENCE_REVIEW`.
3. If they are replaced or upgraded, write only through a guarded mutator that
   asserts `identifier == "CHEBI:50199"`, updates the `molecular_targets`
   slice, appends a `codex` `CurationEvent` with `llm_assisted: true`, and
   emits via `write_validated_antibiotic`.
4. Move `curation_status` to `REVIEWED` only after the upgraded targets plus
   the already curated identity, structure, class, mode of action, and causal
   graph satisfy the `docs/CURATION.md` sign-off criteria.

## Follow-up Checks

After a future target curation edit:

- `just validate-strict data/antibiotics/antibacterial/pefloxacin.yaml --out /tmp/antibioticmech-pefloxacin-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antibacterial/pefloxacin.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `rg --hidden --no-ignore` over `curation/`, `data/`, and
  `reports/yaml_record_review/` for `CHEBI:50199`, `ARO:3000762`,
  `pefloxacin`, `antibioticmech:aro-e4d6a66b1b`, and
  `antibioticmech:chebi-0c977bad4e`.
- `rg --hidden --no-ignore` over the target record, `data/raw/`, and
  `curation/` for `PMID:3010848`, with `reports/yaml_record_review/` included
  to confirm that only the already landed norfloxacin report also mentions that
  shared quinolone DNA gyrase paper.
- `rg --hidden --no-ignore` over `reports/yaml_record_review/` for an existing
  pefloxacin report.
- `rg --hidden --no-ignore` over `data/raw/bindingdb_target_measurements.tsv`
  for pefloxacin local identifiers.

Not checked: external paper text for `PMID:3010848`.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
