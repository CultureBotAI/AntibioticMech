# YAML Record Review: (R)-goitrin

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antiviral/r-goitrin.yaml
- Started UTC: 2026-09-23T08:40:16Z
- Finished UTC: 2026-09-23T08:40:16Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/r-goitrin.yaml` |
| Identifier | `CHEBI:183228` |
| Label | `(R)-goitrin` |
| Filing class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:183228` from ChEBI release `2026-08-30` |
| Minted source key | `antibioticmech:chebi-0a87e0e6e8` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, role, structure, and xref claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiviral/r-goitrin.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/antiviral/r-goitrin.yaml --out /tmp/r-goitrin-validate-strict.tsv` | Passed: 1 file checked, 0 files with `ERROR`, and 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed: all 2,939 expected records were on disk, no unexpected records were present, no generated fields drifted, no identifiers were absent from `PATHS.tsv`, and no stale lockfile rows were present. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/r-goitrin-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:183228` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 10 source literature leads, 0 record evidence items, and 0 targets. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just review-queue --limit 100` | Passed and listed `(R)-goitrin` as `MECHANISM_REVIEW: mechanism is absent; 10 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed before writing this report. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:183228`, `label: (R)-goitrin`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `UZQVYLOFLQICCT-SCSAIBSYSA-N`, formula, charge, masses, IUPAC name, synonyms, xrefs, and ChEBI antiviral role. |
| Structure | SMILES `[H][C@@]1(C=C)CNC(=S)O1`, Standard InChI, InChIKey, formula `C5H7NOS`, charge `0`, average mass `129.184`, and monoisotopic mass `129.02483` are the exact ChEBI-imported values. |
| Parent boundary | `CHEBI:183227` is the broader goitrin parent, and `CHEBI:80751` is the sibling `(S)-goitrin` enantiomer with a different InChIKey, so this record denotes the intended individual stereoisomer. |
| Filing class | The sole ChEBI role is `CHEBI:22587` / antiviral agent, which `conf/sources.yaml` maps to `ANTIVIRAL` at priority 5; filing the record under `data/antibiotics/antiviral/` is consistent with that mapping. |
| Xrefs | `cas:1072-93-1` and `chemspider:2297322` are direct ChEBI row imports. No other local xrefs are attached to the exact `(R)` record. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:183228` to `ANTIVIRAL` / `r-goitrin`, matching the current record path. |

Before this report, an ignored-inclusive search across `data/raw`, `curation`, `reports/yaml_record_review`, and `data/antibiotics` for `CHEBI:183228`, `antibioticmech:chebi-0a87e0e6e8`, `UZQVYLOFLQICCT-SCSAIBSYSA-N`, `cas:1072-93-1`, and `chemspider:2297322` found only the expected ChEBI raw row, path-lock row, review-queue row, and exact YAML record. It found no prior exact review report for this identifier.

## Evidence

There is no claim-level literature evidence to audit because this seeded record has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_observations`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, datasets, discussions, or record-level `evidence`.

The existing scientific assertions are seed-owned ChEBI database assertions:

| Assertion | Support |
|---|---|
| `(R)-goitrin` is a ChEBI-grounded exact record with the listed IUPAC name, synonyms, xrefs, and structure. | Supported by the committed ChEBI inventory row for `CHEBI:183228`. |
| The record carries the ChEBI antiviral-agent role term. | Supported by the same ChEBI row, which lists `CHEBI:22587` as the sole role term. |

Bounded publication discovery found primary literature that a future curator should inspect, but no claim has been curated into the record yet:

| Query | Result |
|---|---|
| Exact `(R)-goitrin` label through PubMed and Semantic Scholar | PubMed returned analytical, plant-metabolism, and rat-toxicology papers, including the ChEBI source lead `PMID:28894621` for stereospecific `(R)`/`(S)` quantification in Radix Isatidis and `PMID:27022502` for preparative chiral separation. Those hits support identity and purification context, not a curated antimicrobial mechanism, target, resistance, or activity claim. Semantic Scholar was rate-limited with HTTP 429. |
| Exact `UZQVYLOFLQICCT-SCSAIBSYSA-N` InChIKey through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Exact ChEBI IUPAC name through PubMed and Semantic Scholar | PubMed found `PMID:11170613`, a source-lead paper on epiprogoitrin-derived breakdown products and plant bioassays; it is not direct antimicrobial mechanism or activity evidence. Semantic Scholar was rate-limited with HTTP 429. |
| `epigoitrin antiviral mechanism` through PubMed and Semantic Scholar | PubMed found exact-synonym antiviral candidates, including `PMID:31918015` on Isatidis Radix glucosinolate isomers and breakdown products against influenza A virus and `PMID:30792656` on epigoitrin in H1N1 stress-susceptibility models. These are plausible future evidence sources for `(R)-goitrin` but require full-text inspection to decide whether future assertions should describe direct virus-particle activity, host innate-immunity modulation, or both. |
| `D-goitrin antiviral` through PubMed and Semantic Scholar | PubMed returned broad, mostly irrelevant antiviral and natural-product hits rather than additional exact mechanistic sources; Semantic Scholar was rate-limited with HTTP 429. |

## Completeness

The generated identity, source-concept, structure, parent, xref, antiviral role, and curation-history fields are complete enough for a ChEBI seed.

The record remains scientifically incomplete because it does not yet have a mechanism statement, target, resistance mechanism, direct activity observation, or evidence item for the exact `(R)` enantiomer. The derived worklist keeps it in both the `mechanism` and `review-readiness` queues with 10 ChEBI source literature leads, no record evidence, and no targets. That gap is consequential and blocks `REVIEWED` status under the local gate because the mode of action has not been checked.

The optional target, activity, resistance, producer, causal-graph, dataset, and discussion slots should stay absent until a curator reads the candidate epigoitrin influenza papers in full, chooses the right local mechanism and target-scope encoding, and adds claim-level evidence through the validated writer.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The record has no evidence-backed mode of action, target, resistance mechanism, direct activity observation, or causal graph for exact `(R)-goitrin`, so it cannot qualify as `REVIEWED`. | The YAML lacks `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `resistance_mechanisms`, `activity_observations`, and `causal_graphs`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:183228` in `MECHANISM_REVIEW` with 10 source literature leads, 0 record evidence items, and 0 targets. | Future curator-owned additions to `data/antibiotics/antiviral/r-goitrin.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded ChEBI identity, structure, antiviral role, parent, xrefs, source concept, and path unchanged.
2. Inspect the exact-synonym influenza literature, especially `PMID:31918015` and `PMID:30792656`, before adding a `mode_of_action`, `mode_of_action_target_scope`, activity observation, or causal graph for `CHEBI:183228`.
3. Keep direct virus-particle effects separate from host innate-immunity effects when curating epigoitrin evidence; these should not be collapsed into one antiviral mechanism if the primary papers report different experimental systems or causal paths.
4. Do not transfer claims from the unspecific `CHEBI:183227` goitrin parent or from the `(S)` enantiomer to this `(R)` record unless the source itself demonstrates that the measurement or mechanism applies to the exact same stereochemical form.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/antiviral/r-goitrin.yaml`
2. `just validate-strict data/antibiotics/antiviral/r-goitrin.yaml --out /tmp/r-goitrin-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/r-goitrin-worklist.tsv`
5. `just review-queue --limit 100`
6. `just lint`

Also re-run exact publication searches for `(R)-goitrin`, `epigoitrin`, `CHEBI:183228`, `UZQVYLOFLQICCT-SCSAIBSYSA-N`, and the exact ChEBI IUPAC name before deciding the final evidence model.

## Additional Notes

- The record has only seed and reseed curation-history events from `2026-08-30`, accurately matching its generated `SEEDED` state.
- `curation/record_review_queue.tsv` and `data/raw/chebi_antimicrobials.tsv` already carry ChEBI literature leads for this exact ChEBI row, but those leads have not been normalized into claim-level local evidence.
- The sibling `(S)-goitrin` record carries different CAS, ChemSpider, and KEGG xrefs and must remain distinct from this `(R)-specific record.
- Semantic Scholar returned HTTP 429 for exact-name, exact-structure, and synonym-oriented publication searches; PubMed carried the exact candidate discovery used in this report.
