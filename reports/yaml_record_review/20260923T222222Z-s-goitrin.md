# YAML Record Review: (S)-goitrin

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antiviral/s-goitrin.yaml
- Started UTC: 2026-09-23T22:22:22Z
- Finished UTC: 2026-09-23T22:22:22Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/s-goitrin.yaml` |
| Identifier | `CHEBI:80751` |
| Label | `(S)-goitrin` |
| Filing class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:80751` from ChEBI release `2026-08-30` |
| Minted source key | `antibioticmech:chebi-ad8a9f6ace` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, role, structure, and xref claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiviral/s-goitrin.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/antiviral/s-goitrin.yaml --out /tmp/s-goitrin-validate-strict.tsv` | Passed: 1 file checked, 0 files with `ERROR`, and 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed: all 2,939 expected records were on disk, no unexpected records were present, no generated fields drifted, no identifiers were absent from `PATHS.tsv`, and no stale lockfile rows were present. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-goitrin-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:80751` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 10 source literature leads, 0 record evidence items, and 0 targets. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just review-queue --limit 125` | Passed and listed `(S)-goitrin` as `MECHANISM_REVIEW: mechanism is absent; 10 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed before writing this report. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:80751`, `label: (S)-goitrin`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `UZQVYLOFLQICCT-BYPYZUCNSA-N`, formula, charge, masses, IUPAC name, synonyms, xrefs, and ChEBI antiviral role. |
| Live ChEBI/OLS | OLS confirmed an active, non-obsolete, ChEBI-defining, 3-star `CHEBI:80751` term with the same label, definition, formula, SMILES, InChI, InChIKey, CAS `500-12-9`, ChemSpider `5885349`, KEGG Compound `C16817`, and patent xrefs. |
| Structure | SMILES `[H][C@]1(C=C)CNC(=S)O1`, Standard InChI, InChIKey, formula `C5H7NOS`, charge `0`, average mass `129.184`, and monoisotopic mass `129.02483` are the exact ChEBI-imported values. |
| Parent boundary | OLS and the local seed both place `CHEBI:183227` as the one broader goitrin parent. OLS links the sibling `(R)-goitrin` / `CHEBI:183228` by `is_enantiomer_of` with the different InChIKey `UZQVYLOFLQICCT-SCSAIBSYSA-N`, so this record denotes the intended individual stereoisomer. |
| Filing class | The local ChEBI inventory imports `CHEBI:22587` / antiviral agent as the sole antimicrobial role term, which `conf/sources.yaml` maps to `ANTIVIRAL` at priority 5; filing the record under `data/antibiotics/antiviral/` is consistent with that mapping. |
| Xrefs | `cas:500-12-9`, `chemspider:5885349`, `kegg.compound:C16817`, and document xrefs `patent:CN101445490` and `patent:CN101863850` are direct ChEBI row imports and are present on the live OLS term. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:80751` to `ANTIVIRAL` / `s-goitrin`, matching the current record path. |

Before this report, an ignored-inclusive search across `data/raw`, `curation`, `reports/yaml_record_review`, and `data/antibiotics` for `CHEBI:80751`, `antibioticmech:chebi-ad8a9f6ace`, `UZQVYLOFLQICCT-BYPYZUCNSA-N`, `cas:500-12-9`, `chemspider:5885349`, `kegg.compound:C16817`, `patent:CN101445490`, and `patent:CN101863850` found only the expected ChEBI raw row, path-lock row, review-queue row, exact YAML record, and the prior `(R)-goitrin` sibling report's boundary note. An ignored-inclusive `find` for `*goitrin*` reports found only `reports/yaml_record_review/20260923T084016Z-r-goitrin.md` before writing this report, so there was no earlier exact `(S)-goitrin` review.

## Evidence

There is no claim-level literature evidence to audit because this seeded record has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_observations`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, datasets, discussions, or record-level `evidence`.

The existing scientific assertions are seed-owned ChEBI database assertions:

| Assertion | Support |
|---|---|
| `(S)-goitrin` is a ChEBI-grounded exact record with the listed IUPAC name, synonyms, xrefs, document xrefs, and structure. | Supported by the committed ChEBI inventory row for `CHEBI:80751` and by live OLS for the same term. |
| The record carries the ChEBI antiviral-agent role term. | Supported by the same ChEBI row, which lists `CHEBI:22587` as its imported antimicrobial role term. Live OLS also lists non-antimicrobial antithyroid-drug and plant-metabolite roles, which correctly do not appear in local `activity_roles`. |

Bounded publication discovery found future curation leads, but no claim has been curated into the record yet:

| Query | Result |
|---|---|
| Exact `(S)-goitrin` label through PubMed and Semantic Scholar | PubMed returned 16 candidates; Semantic Scholar was rate-limited with HTTP 429. The PubMed hits were mostly Radix Isatidis analytics, pharmacokinetics of racemic `(R,S)-goitrin`, and formula papers. `PMID:27022502` purified both `(R)-goitrin` and `(S)-goitrin`, and `PMID:28894621` measured both enantiomers in Radix Isatidis formulations, but those abstracts support stereospecific separation or quantification rather than an antiviral mechanism for the exact `S` enantiomer. |
| Exact `UZQVYLOFLQICCT-BYPYZUCNSA-N` InChIKey through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Exact ChEBI IUPAC name through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| `L-goitrin antiviral mechanism` through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| `goitrin influenza antiviral mechanism` through PubMed and Semantic Scholar | PubMed returned `PMID:31918015`, an exact candidate that tested Isatidis Radix glucosinolate isomers and breakdown products against influenza A virus, and `PMID:28314479`, a KangBingDu formula paper that identified `(R,S)-goitrin` as an effective component in mice. Semantic Scholar was rate-limited with HTTP 429. |
| Direct PubMed summary fetch for the 10 local ChEBI source literature leads | Source leads covered human bitter-taste genetics for goitrin, Radix Isatidis glucosinolate biotransformation, Brassica thyroid-risk chemistry, chiral separation and stereospecific assay papers, the 2020 influenza candidate, and older antithyroid chick/rat/human studies. The inspected titles and abstracts do not contradict the seeded exact identity. |

## Completeness

The generated identity, source-concept, structure, parent, xref, document-xref, antiviral role, and curation-history fields are complete enough for a ChEBI seed.

The record remains scientifically incomplete because it does not yet have a mechanism statement, target, resistance mechanism, direct activity observation, or evidence item for the exact `(S)` enantiomer. The derived worklist keeps it in both the `mechanism` and `review-readiness` queues with 10 ChEBI source literature leads, no record evidence, and no targets. That gap is consequential and blocks `REVIEWED` status under the local gate because the mode of action has not been checked.

The optional target, activity, resistance, producer, causal-graph, dataset, and discussion slots should stay absent until a curator reads the exact goitrin and influenza leads in full, decides whether the tested compound was resolved `(S)-goitrin` or an `R,S` mixture, chooses the right local mechanism and target-scope encoding, and adds claim-level evidence through the validated writer.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The record has no evidence-backed mode of action, target, resistance mechanism, direct activity observation, or causal graph for exact `(S)-goitrin`, so it cannot qualify as `REVIEWED`. | The YAML lacks `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `resistance_mechanisms`, `activity_observations`, and `causal_graphs`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:80751` in `MECHANISM_REVIEW` with 10 source literature leads, 0 record evidence items, and 0 targets. | Future curator-owned additions to `data/antibiotics/antiviral/s-goitrin.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded ChEBI identity, structure, antiviral role, parent, xrefs, document xrefs, source concept, and path unchanged.
2. Inspect the full text of `PMID:31918015` before adding a `mode_of_action`, `mode_of_action_target_scope`, activity observation, or causal graph for `CHEBI:80751`; the PubMed abstract names goitrin among the compounds tested against influenza A virus but is not enough to extract claim-level effect direction, target, assay, and stereochemical boundary.
3. Treat `PMID:27022502` and `PMID:28894621` as stereospecific separation and quantification support, not as direct antiviral mechanism evidence.
4. Do not transfer claims from the unspecific `CHEBI:183227` goitrin parent, the `R,S` racemate, or the `(R)` enantiomer to this `(S)` record unless the source itself demonstrates that the measurement or mechanism applies to the exact same stereochemical form.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/antiviral/s-goitrin.yaml`
2. `just validate-strict data/antibiotics/antiviral/s-goitrin.yaml --out /tmp/s-goitrin-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/s-goitrin-worklist.tsv`
5. `just review-queue --limit 125`
6. `just lint`

Also re-run exact publication searches for `(S)-goitrin`, `L-goitrin`, `CHEBI:80751`, `UZQVYLOFLQICCT-BYPYZUCNSA-N`, the exact ChEBI IUPAC name, and `goitrin influenza antiviral mechanism` before deciding the final evidence model.

## Additional Notes

- The record has only seed and reseed curation-history events from August and September 2026, accurately matching its generated `SEEDED` state.
- `curation/record_review_queue.tsv` and `data/raw/chebi_antimicrobials.tsv` already carry ChEBI literature leads for this exact ChEBI row, but those leads have not been normalized into claim-level local evidence.
- Live OLS has more PubMed xrefs for `CHEBI:80751` than the committed 2026-08-30 ChEBI inventory row has. This review treated those as discovery leads only.
- The sibling `(R)-goitrin` record carries a different CAS, ChemSpider xref, InChI stereolayer, and InChIKey and must remain distinct from this `(S)`-specific record.
- Semantic Scholar returned HTTP 429 for exact-name, exact-structure, and mechanism-oriented publication searches; PubMed carried the candidate discovery used in this report.
