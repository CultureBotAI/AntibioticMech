# YAML Record Review: (R)-(+)-citronellal

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antifungal/r-citronellal.yaml`
- Started UTC: `2026-09-23T01:06:08Z`
- Finished UTC: `2026-09-23T01:07:30Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:299` |
| Label | `(R)-(+)-citronellal` |
| Path | `data/antibiotics/antifungal/r-citronellal.yaml` |
| Class | `ANTIFUNGAL` |
| Status | `SEEDED` |
| Grounding | `EXACT`, through the `CHEBI` source concept `CHEBI:299` |
| Structure | `NEHNMFOYXAPHSD-SNVBAGLBSA-N`, formula `C10H18O`, charge `0` |
| Maintained input | `data/raw/chebi_antimicrobials.tsv` |
| Generated or maintained | Generated record under `data/antibiotics`; do not hand-edit seeded identity, class, structure, parent, xref, or source-concept fields |

The full target YAML was read before judgement. `CHEBI:299` resolves through
`data/antibiotics/PATHS.tsv` to the antifungal record
`data/antibiotics/antifungal/r-citronellal.yaml`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/r-citronellal.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/r-citronellal.yaml --out /tmp/r-citronellal-validate-strict.tsv` | Pass; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 90` | Pass; `CHEBI:299` remains next after the already-reviewed `(M)-viriditoxin` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/r-citronellal-worklist.tsv` | Pass; the relevant entries were `mechanism` and `review-readiness`, and the record was not queued for minted grounding, xref, multi-component, structure, scope, or target-evidence defects. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '"(R)-(+)-citronellal" OR "(3R)-(+)-citronellal" OR "2385-77-5"' --limit 20 --output /tmp/r-citronellal-exact-publications.jsonl` | PubMed returned 20 candidates; Semantic Scholar returned HTTP 429 and was unavailable for this pass. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query 'citronellal antifungal MIC Candida mechanism' --limit 20 --output /tmp/r-citronellal-broad-publications.jsonl` | PubMed returned 3 candidates; Semantic Scholar returned HTTP 429 and was unavailable for this pass. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record. The bounded absence searches for a prior review,
curator decision, duplicate exact InChIKey, and exact-enantiomer PMIDs used
`rg --no-ignore --hidden` or `find`, so ignored `reports/` files were included.

## Identity and Grounding

The ChEBI import row for `CHEBI:299` exactly matches the generated identity:

- label `(R)-(+)-citronellal`
- 3-star ChEBI entry with `EXACT` grounding
- antifungal role `CHEBI:35718`
- broader parent `CHEBI:47856`
- Standard InChIKey `NEHNMFOYXAPHSD-SNVBAGLBSA-N`
- xrefs `beilstein:1720791`, `beilstein:4654384`, `cas:2385-77-5`,
  `gmelin:985290`, `kegg.compound:C09848`, `knapsack:C00003037`, and
  `lipidmaps:LMPR0102010007`

The stereochemical scope is represented correctly. The broader achiral
`citronellal` record lives at `CHEBI:47856` with Standard InChIKey
`NEHNMFOYXAPHSD-UHFFFAOYSA-N`, and the opposite enantiomer
`(S)-(−)-citronellal` lives at `CHEBI:368` with Standard InChIKey
`NEHNMFOYXAPHSD-JTQLQIEISA-N`. The `CHEBI:47856` row is a
`parent_compounds` value for both enantiomers, not an exact `xrefs` value.

An ignored-inclusive search over `curation`, `data/raw`, `data/antibiotics`,
and `reports` found the exact R-enantiomer InChIKey only in the ChEBI raw row
and `r-citronellal.yaml`; it found no `curation/decisions.tsv` override for
`antibioticmech:chebi-16c7adecd7` and no prior `r-citronellal` review report.

## Evidence

Existing claim evidence is narrow:

| Claim | Evidence review |
|---|---|
| ChEBI identity, definition, synonyms, role, parent, xrefs, and structure fields | Database-seeded from the `CHEBI:299` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |

The record has no `molecular_targets`, `activity_observations`,
`resistance_mechanisms`, `producer_organisms`, `causal_graphs`, or record-level
literature `evidence` items to overstate. No existing citation is attached to a
broader claim than it supports.

## Completeness

The record is not complete enough for `REVIEWED` because the mechanism gate is
unsettled:

- `mode_of_action` and `mode_of_action_target_scope` are absent.
- `molecular_targets` is absent even though exact `(R)-(+)-citronellal`
  antifungal leads now exist.
- `causal_graphs` is absent.
- `activity_observations` is absent; exact-enantiomer PubMed leads should be
  inspected for MIC units, assay methods, and strain scope before an observation
  is added.
- `resistance_mechanisms`, `producer_organisms`, `clinical_status`, and
  `datasets` are empty; the bounded review did not find local source rows
  proving these omissions are defects.

PubMed searches found exact-enantiomer discovery leads that a future curation
pass must inspect in full text before asserting a target, activity observation,
or mode of action:

| PMID | Relevance |
|---|---|
| `PMID:41738628` | Exact `(R)-(+)-citronellal` paper against clinical `Trichophyton rubrum` isolates reporting MIC/MFC values and ergosterol/cholesterol/sorbitol mechanism assays. |
| `PMID:37222371` | Exact `(R)` and `(S)` citronellal enantiomer paper against voriconazole-resistant `Candida albicans` from onychomycoses with broth microdilution, sorbitol and ergosterol assays, and predictive docking. |
| `PMID:35386096` | Non-enantiospecific citronellal and `Cymbopogon nardus` essential-oil paper against clinical `Candida albicans` strains; useful only as an achiral citronellal lead unless full text proves the tested form. |

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| major | `mode_of_action`, `molecular_targets`, and `causal_graphs` are absent even though exact `(R)-(+)-citronellal` antifungal literature exists, so `CHEBI:299` cannot pass the mechanism criterion for `REVIEWED`. PubMed leads point to fungal cell membrane/cell wall assays and docking hypotheses, but those must be full-text-checked before the record chooses `CELL_WALL_SYNTHESIS_INHIBITION`, `CELL_MEMBRANE_SYNTHESIS_INHIBITION`, `ERGOSTEROL_PATHWAY_INHIBITION`, `MULTIPLE`, `OTHER`, or `UNKNOWN`. | Curator-owned additions in `data/antibiotics/antifungal/r-citronellal.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blockers or minor findings were found.

## Recommended Edits

1. Inspect `PMID:41738628` and `PMID:37222371` in full text and decide whether
   `(R)-(+)-citronellal` supports a specific fungal membrane, fungal cell-wall,
   ergosterol-pathway, multi-mechanism, `OTHER`, or `UNKNOWN` `mode_of_action`.
2. Add `ActivityObservation` entries from those exact-enantiomer papers only for
   claims with organism or isolate scope, activity calls, assay method, MIC
   values, and MIC units.
3. Add `molecular_targets` only when the source supports a target beyond docking
   or rescue-assay inference. Keep a docking hypothesis in notes or a discussion
   rather than asserting direct binding if no biochemical target assay was run.
4. Add a causal graph only after the mode and target interpretation is
   resolved; every edge needs its own primary-paper `EvidenceItem`.
5. Leave the seeded identity, xrefs, `parent_compounds`, and class untouched
   unless a ChEBI refresh or `curation/decisions.tsv` row changes the maintained
   import.

## Follow-up Checks

- After any curator-owned edit, rerun
  `just validate-strict data/antibiotics/antifungal/r-citronellal.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Rerun `just verify-corpus` to prove the generated record still reproduces
  from `data/raw/` plus curator inputs.
- Rerun `just worklist --limit 0 --tsv /tmp/r-citronellal-worklist.tsv` and
  confirm the `CHEBI:299` `mechanism` and `review-readiness` rows changed as
  intended.
- Rerun `just lint`, `just test`, and `just render-check` through `just qc` if a
  later pass mutates the YAML rather than writing a review report only.

## Additional Notes

- `reports/` is gitignored. This report must be staged with `git add -f`.
- The two searched PMIDs most directly relevant to exact `(R)-(+)-citronellal`,
  `PMID:41738628` and `PMID:37222371`, were absent from `curation`,
  `data/raw`, `data/antibiotics`, and ignored `reports` at review time.
- The local exact-InChIKey search did not find any other source row or generated
  record with `NEHNMFOYXAPHSD-SNVBAGLBSA-N`; ignored `reports` were included.
- Semantic Scholar was rate-limited with HTTP 429 and was not treated as a
  negative source.
