# YAML Record Review: (+)-artemisinin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiprotozoal/artemisinin.yaml`
- Started UTC: 2026-09-21T08:14:24Z
- Finished UTC: 2026-09-21T08:14:24Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiprotozoal/artemisinin.yaml` |
| Class | `ANTIPROTOZOAL` |
| ID | `CHEBI:223316` |
| Label | `(+)-artemisinin` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded `(+)-artemisinin` with InChIKey
`BLUAFEHZUWYNDE-NNWCWBAJSA-N`. It has one ChEBI source concept, one
antimalarial activity role, two broader ChEBI parent compounds, multiple
same-structure xrefs, drug/document xrefs, and no record evidence, mode of
action, molecular target, activity observation, resistance mechanism, producer
organism, clinical assertion, causal graph, dataset, or `Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antiprotozoal/artemisinin.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/artemisinin.yaml --out /tmp/antibioticmech-artemisinin-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just review-queue --limit 10` | Confirmed `(+)-artemisinin` remains in `review-readiness`: `MECHANISM_REVIEW`, no mechanism, six source literature leads, zero record evidence items, and zero targets. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "artemisinin antimalarial mechanism heme PfATP6" --limit 20 --output /tmp/antibioticmech-artemisinin-pubs.jsonl` | PubMed wrote 4 broad candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "CHEBI:223316 Qinghaosu artemisinin" --limit 20 --output /tmp/antibioticmech-artemisinin-identity-pubs.jsonl` | PubMed wrote 0 candidates; Semantic Scholar returned HTTP 429. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`artemisinin.yaml` resolves unambiguously under
`data/antibiotics/antiprotozoal/`. The generated lock row in
`data/antibiotics/PATHS.tsv` maps `CHEBI:223316` to
`ANTIPROTOZOAL/artemisinin`, and `source_concepts` carries only the expected
ChEBI upstream identity with minted ID `antibioticmech:chebi-ab983ede98`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:223316`: SMILES, Standard InChI, InChIKey
`BLUAFEHZUWYNDE-NNWCWBAJSA-N`, formula `C15H22O5`, charge `0`, average mass,
and monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`. The
official ChEBI page for `CHEBI:223316` also reports the same label, formula,
SMILES, Standard InChI, InChIKey, CAS `63968-64-9`, KEGG, KNApSAcK, LIPID
MAPS, MetaCyc, and Reaxys xrefs, and the ChEBI antimalarial role.

The target is distinct from adjacent artemisinin derivatives already present in
the corpus, including `CHEBI:207229` dihydroartemisinin, `CHEBI:195280`
artemether, and `CHEBI:63918` artesunate. Those records have different
identifiers, structures, formulas, InChIKeys, xrefs, and source-literature
leads.

No local grounding decision was found for `CHEBI:223316` or
`antibioticmech:chebi-ab983ede98` in a gitignore-independent search over
`curation/decisions.tsv` with `grep -R`.

## Evidence

The generated identity, label, definition, exact synonyms, exact structure,
parent compounds, xrefs, drug xrefs, document xref, filing class, antimalarial
activity role, source concept, and grounding status reproduce from the
committed ChEBI inventory with no `verify-corpus` drift.

The retained ChEBI role is `CHEBI:38068`, which this repo's raw role-name
inventory maps to `antimalarial`. That role explains why the record is filed as
`ANTIPROTOZOAL`.

The committed ChEBI row names six PMID source rows, but they are
source-level leads rather than curated record evidence. A PubMed summary pass
over those PMIDs found derivative activity papers, production papers, and
secondary or anticancer-oriented artemisinin papers; this read-only review did
not find one among them that could be promoted directly to a
compound-specific antimalarial mode of action, molecular target, or causal
graph.

The targeted mechanism discovery query surfaced four broad candidates around
heme, PfATP6, artemisinin drug action, resistance, and non-malarial
heme-catalyzed reactive oxygen species. These are future leads only, not
claim-level support in this report.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

The committed BindingDB, Drugs@FDA, MIBiG, and PHI-base inventories contain no
local row for `CHEBI:223316`, InChIKey `BLUAFEHZUWYNDE-NNWCWBAJSA-N`, CAS
`63968-64-9`, or exact `(+)-artemisinin`.

## Completeness

The exact ChEBI identity, stereochemistry, structure, ChEBI grounding, source
concept, antiprotozoal filing class, ChEBI antimalarial role, parent compounds,
and same-structure xrefs are defensible.

The consequential gap is mechanism sign-off. The YAML has no
`mode_of_action`, no `mode_of_action_target_scope`, no `molecular_targets`, and
no `causal_graphs`; it also has no record-level evidence or discussion of the
well-known uncertainty around exact artemisinin drug action. `just review-queue
--limit 10` therefore keeps the record in `MECHANISM_REVIEW`.

No activity observations, producer claims, resistance assertions, clinical
assertions, or public datasets are present. The review found no specific local
evidence that any of those optional sections must be populated before the
absent mechanism is resolved.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has no curated antimalarial mechanism and cannot satisfy the `REVIEWED` gate. | The target YAML has no `record_evidence`, `mode_of_action`, `molecular_targets`, or `causal_graphs`; the source-literature rows in `data/raw/chebi_antimicrobials.tsv` are discovery leads, not curated evidence; `just review-queue --limit 10` reports `MECHANISM_REVIEW: mechanism is absent; 6 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | Curator-owned evidence, mechanism, target, discussion, or causal-graph additions written to `data/antibiotics/antiprotozoal/artemisinin.yaml` through `write_validated_antibiotic`, if primary literature supports them. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Review primary literature for exact `(+)-artemisinin` antimalarial drug
   action and resistance, prioritizing heme-dependent activation, PfATP6/SERCA,
   parasite proteostasis or stress-response models, and K13-associated
   resistance papers as discovery trails.
2. Do not promote derivative-only evidence from dihydroartemisinin,
   artemether, artesunate, artemisinin dimers, or synthetic analogues unless
   the inspected paper explicitly supports the same assertion for
   `CHEBI:223316`.
3. If the mechanism remains genuinely unresolved after the primary-paper
   review, add a scoped `Discussion` that records the controversy and bounded
   searches rather than asserting a speculative molecular target.
4. If a mode of action or target is supported, write it with a guarded mutator
   that asserts `identifier == "CHEBI:223316"`, updates only the
   curator-owned mechanism fields, appends a `codex` `CurationEvent` with
   `llm_assisted: true`, and emits via `write_validated_antibiotic`.
5. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the newly curated mechanism or unresolved-mechanism discussion
   satisfy the `docs/CURATION.md` sign-off criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antiprotozoal/artemisinin.yaml --out /tmp/antibioticmech-artemisinin-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antiprotozoal/artemisinin.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `grep -R` over `curation/`, `data/antibiotics/`, `data/raw/`, and
  `reports/yaml_record_review/` for `CHEBI:223316`,
  `BLUAFEHZUWYNDE`, `63968-64-9`, `CPD-7561`, `C00003359`,
  `LMPR0103190003`, `D02481`, and `C09538`.
- `grep -R` over `reports/yaml_record_review/` for an existing `CHEBI:223316`,
  `BLUAFEHZUWYNDE`, or `(+)-artemisinin` review report.
- `grep -R` over `data/raw/bindingdb_target_measurements.tsv`,
  `data/raw/fda_clinical_status.tsv`, `data/raw/mibig_producers.tsv`, and
  `data/raw/phibase_amr.tsv` for `CHEBI:223316`,
  `BLUAFEHZUWYNDE`, `63968-64-9`, and `artemisinin`.
- `grep -R` over `curation/decisions.tsv` for `CHEBI:223316` and
  `antibioticmech:chebi-ab983ede98`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned HTTP
429 for the targeted publication searches, so that provider's literature leads
were not available in this pass.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
