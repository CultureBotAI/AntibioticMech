# YAML Record Review: (S)-(−)-citronellal

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antifungal/s-citronellal.yaml`
- Started UTC: `2026-09-23T14:48:29Z`
- Finished UTC: `2026-09-23T14:49:21Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:368` |
| Label | `(S)-(−)-citronellal` |
| Path | `data/antibiotics/antifungal/s-citronellal.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated status | Generated from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv`; no `curation/decisions.tsv` override owns this ChEBI seed. |
| Source concept | `CHEBI:368` / `(S)-(−)-citronellal`, version `2026-08-30`, role `CHEBI:35718` |
| Standard InChIKey | `NEHNMFOYXAPHSD-JTQLQIEISA-N` |

This review read the entire generated record at
`data/antibiotics/antifungal/s-citronellal.yaml`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/s-citronellal.yaml` | Passed; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/s-citronellal.yaml --out /tmp/s-citronellal-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, and 0 stale lockfile rows. The only warning was the known unrelated `ARO:3000337` / iclaprim CARD cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-citronellal-worklist.tsv` | Passed; `CHEBI:368` appears only in the `mechanism` and `review-readiness` queues, both because the mechanism is absent. It is not queued for minted grounding, xref, multi-component, structure, scope, or target-evidence defects. |
| `just review-queue --limit 106` | Passed; `CHEBI:368` is still queued immediately after the reviewed `(R,R,R)-α-tocopherol` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| OLS4 ChEBI `CHEBI:368` term lookup | Passed; the live ChEBI term is active and matched the generated definition, SMILES, Standard InChI, Standard InChIKey, formula, charge, masses, and xrefs. |
| OLS4 ChEBI parent, relation, and role lookups | Passed; `CHEBI:47856` is the only direct parent, `CHEBI:299` is the opposite enantiomer, and `CHEBI:35718` resolves to `antifungal agent`. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '"NEHNMFOYXAPHSD-JTQLQIEISA-N"[All Fields] OR "CHEBI:368"[All Fields]' --limit 10 --output /tmp/s-citronellal-pubmed-exact.jsonl` | PubMed returned 0 exact identifier candidates. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '"(S)-citronellal" OR "(S)-(-)-citronellal" OR "(S)-(−)-citronellal" OR "(3S)-citronellal"' --limit 20 --output /tmp/s-citronellal-pubmed-name.jsonl` | PubMed returned 20 exact-name candidates, mostly synthesis or biotransformation papers plus one exact antifungal hit, `PMID:37222371`. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '(citronellal OR "(S)-citronellal") AND (Candida OR antifungal OR ergosterol OR sorbitol OR MIC)' --limit 20 --output /tmp/s-citronellal-pubmed-antifungal.jsonl` | PubMed returned 20 antifungal or mechanism candidates; only the `PMID:37222371` abstract was exact to the `(S)` enantiomer. |
| Single-record term, reference, or history validators | Not available for this plain ChEBI-seeded generated record. The narrowest repository validators exposed by `justfile` for these concerns are the full-corpus `verify-corpus`, `worklist`, and `review-queue` checks above. |
| `just lint` | Passed after writing this ignored report. |
| `git diff --cached --check` | Passed after staging this ignored report. |

## Identity and Grounding

Identity is internally consistent and agrees with ChEBI:

- `data/raw/chebi_antimicrobials.tsv` row 9 is the maintained ChEBI input for
  `CHEBI:368`; its label, definition, role, parent, SMILES, Standard InChI,
  Standard InChIKey, formula, charge, masses, synonyms, and xrefs reproduce
  the generated YAML exactly.
- The OLS4 ChEBI `CHEBI:368` term is not obsolete and has the same Standard
  InChIKey, `NEHNMFOYXAPHSD-JTQLQIEISA-N`.
- ChEBI describes `CHEBI:368` as the `(3S)` stereoisomer of
  3,7-dimethyloct-6-enal, matching the local definition and the local
  stereospecific SMILES `[H]C(=O)C[C@@H](C)CCC=C(C)C`.
- The parent `CHEBI:47856` is achiral `citronellal` and has Standard InChIKey
  `NEHNMFOYXAPHSD-UHFFFAOYSA-N`, so it is broader than this record and belongs
  in `parent_compounds`.
- The opposite enantiomer `(R)-(+)-citronellal` is `CHEBI:299` with Standard
  InChIKey `NEHNMFOYXAPHSD-SNVBAGLBSA-N`; it is not asserted as equivalent to
  this record.
- `CHEBI:156466`, `(S)-citronellalthiosemicarbazonate`, appears downstream of
  `CHEBI:368` by `has functional parent` in the OLS4 graph, so its absence
  from this record is correct.
- The seven generated `xrefs` are the seven exact database cross-references on
  the ChEBI `CHEBI:368` term: `beilstein:1720790`,
  `beilstein:4654383`, `cas:5949-05-3`, `gmelin:2038127`,
  `kegg.compound:C11384`, `knapsack:C00010309`, and
  `lipidmaps:LMPR0102010011`.
- `CHEBI:35718` resolves to `antifungal agent`, so the `ANTIFUNGAL` filing
  class and the retained `activity_roles` entry agree with the sole
  ChEBI-seeded antimicrobial role.

The generated Unicode minus in the local preferred label is a typography
normalization difference from the OLS4 API label `(S)-(-)-citronellal`, not an
identity difference.

## Evidence

Existing claim evidence is narrow:

| Claim | Evidence review |
|---|---|
| ChEBI identity, definition, synonyms, antifungal role, parent, xrefs, and structure fields | Database-seeded from the `CHEBI:368` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |

The record has no `mode_of_action`, `molecular_targets`,
`activity_observations`, `resistance_mechanisms`, `producer_organisms`,
`causal_graphs`, or record-level literature `evidence` items. No citation is
therefore attached to a broader claim than it supports.

## Completeness

The record is structurally grounded but not complete enough for `REVIEWED`:

- `mode_of_action` and `mode_of_action_target_scope` are absent.
- `molecular_targets` is absent even though exact `(S)-citronellal` antifungal
  mechanism leads now exist.
- `causal_graphs` is absent.
- `activity_observations` is absent; exact-enantiomer PubMed leads should be
  inspected in full text for MIC units, assay method, isolate count, and strain
  scope before an observation is added.
- `resistance_mechanisms`, `producer_organisms`, `clinical_status`, and
  `datasets` are empty; this bounded review found no local source rows proving
  these omissions are defects.

PubMed searches found one exact-enantiomer discovery lead that a future
curation pass must inspect in full text before asserting a target, activity
observation, or mode of action:

| PMID | Relevance |
|---|---|
| `PMID:37222371` | Exact `(R)` and `(S)-citronellal` enantiomer paper against voriconazole-resistant `Candida albicans` from onychomycoses; the PubMed abstract reports broth microdilution, sorbitol and ergosterol assays, predictive docking against 1,3-β-glucan synthase and lanosterol 14α-demethylase, and a `(S)` MIC of 32 µg/mL. |
| `PMID:41738628` | Opposite-enantiomer `(R)-(+)-citronellal` paper against clinical `Trichophyton rubrum` isolates; not evidence for `CHEBI:368` except as a sibling-record lead. |
| `PMID:35386096` | Non-enantiospecific citronellal and `Cymbopogon nardus` essential-oil paper against clinical `Candida albicans` strains; useful only as an achiral citronellal lead unless full text proves the exact tested form. |

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| major | `mode_of_action`, `molecular_targets`, `activity_observations`, and `causal_graphs` are absent even though `PMID:37222371` is an exact `(S)-citronellal` antifungal lead for voriconazole-resistant `Candida albicans`. The abstract points to fungal cell wall and membrane experiments plus docking hypotheses, but the full text must be inspected before the record chooses `CELL_WALL_SYNTHESIS_INHIBITION`, `MEMBRANE_DISRUPTION`, `ERGOSTEROL_PATHWAY_INHIBITION`, `MULTIPLE`, `OTHER`, or `UNKNOWN` and before it can add MIC or organism-scope details. | Curator-owned additions in `data/antibiotics/antifungal/s-citronellal.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blockers or minor findings were found.

## Recommended Edits

1. Inspect `PMID:37222371` in full text and decide whether
   `(S)-citronellal` supports a fungal membrane, fungal cell-wall, ergosterol
   pathway, multi-target, other, or unknown mode of action. Make any update
   through a guarded mutator that loads
   `data/antibiotics/antifungal/s-citronellal.yaml`, asserts `CHEBI:368`,
   appends a `codex` `record_curation_event`, and writes with
   `write_validated_antibiotic`.
2. If the full text reports strain-level data, add `ActivityObservation`
   entries for the tested `Candida albicans` isolates with MIC values, units,
   assay, and `PMID:37222371` evidence on each observation.
3. Add `MolecularTarget` records or a `causal_graphs` entry only for claims
   supported by the full text. Keep 1,3-β-glucan synthase or lanosterol
   14α-demethylase as predictive docking targets unless the paper proves a
   direct experimental target relation.

## Follow-up Checks

- Rerun `just validate data/antibiotics/antifungal/s-citronellal.yaml` and
  `just validate-strict data/antibiotics/antifungal/s-citronellal.yaml --out
  /tmp/s-citronellal-strict.tsv`.
- Rerun `just verify-corpus --summary` to prove the generated record still
  reproduces exactly from maintained sources plus curator-owned additions.
- Rerun `just worklist --limit 0 --tsv /tmp/s-citronellal-worklist.tsv` and
  verify that `CHEBI:368` leaves the `mechanism` and `review-readiness` queues
  only when the curated mechanism satisfies the local `REVIEWED` gate.
- Run `just qc` before opening any curation PR.

## Additional Notes

- An ignored/hidden-inclusive search of `reports/yaml_record_review` found no
  prior `s-citronellal` report. It found only the
  `20260923T010730Z-r-citronellal.md` report, where `CHEBI:368` was mentioned
  to distinguish the `(S)` enantiomer from reviewed `CHEBI:299`.
- An ignored/hidden-inclusive search of `curation`, `data/raw`,
  `data/antibiotics/PATHS.tsv`, and `/tmp/s-citronellal-worklist.tsv` found
  `CHEBI:368` only in the ChEBI raw row, the generated path lockfile, and the
  expected `mechanism` / `review-readiness` worklist rows.
- An ignored/hidden-inclusive `AGENTS.md` search, excluding `.git`, found no
  local AGENTS guidance files; only the chat-provided exhaustive-search rule
  applied.
