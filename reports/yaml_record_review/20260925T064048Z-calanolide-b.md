# YAML Record Review: (−)-calanolide B

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/calanolide-b.yaml`
- Started UTC: 2026-09-25T06:40:48Z
- Finished UTC: 2026-09-25T06:40:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/calanolide-b.yaml` |
| Class | `ANTIVIRAL` |
| ID | `CHEBI:65553` |
| Label | `(−)-calanolide B` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded `(−)-calanolide B`, also called
`costatolide`, with InChIKey `NIDRYBLTWYFCFV-PZROIBLQSA-N`. It has one ChEBI
source concept, a ChEBI HIV-1 reverse-transcriptase-inhibitor activity role, a
seeded `VIRAL_POLYMERASE_INHIBITION` mode of action with `MICROBIAL_TARGET`
scope, four broader ChEBI parent compounds, two same-structure chemical xrefs,
one patent document xref, and no record evidence, molecular target, activity
observation, resistance mechanism, producer organism, clinical assertion,
causal graph, dataset, or `Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antiviral/calanolide-b.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/calanolide-b.yaml --out /tmp/calanolide-b-validate-strict.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just worklist --limit 0 --tsv /tmp/calanolide-b-worklist.tsv` | Confirmed `CHEBI:65553` appears in `mechanism`, `producer-candidate`, `activity-candidate`, and `review-readiness`; the review row reports `MECHANISM_REVIEW`, five source literature leads, zero record evidence items, and zero targets. |
| `uvx --from oaklib runoak -i ols:chebi info CHEBI:65553 CHEBI:53756 CHEBI:18946 CHEBI:35681 CHEBI:37407 CHEBI:38163` | Resolved the compound, HIV-1 reverse transcriptase inhibitor role, and all four parent terms through OLS/ChEBI. |
| `curl -L 'https://www.ebi.ac.uk/ols4/api/ontologies/chebi/terms?obo_id=CHEBI:65553'` | Resolved the official ChEBI term with the same label, definition, formula, SMILES, Standard InChI, InChIKey, CAS `909-14-8`, Reaxys `7257193`, patent `WO9932492`, and five PubMed xrefs. |
| `curl -L 'https://www.ebi.ac.uk/ols4/api/ontologies/chebi/terms?obo_id=CHEBI:53756'` | Resolved `HIV-1 reverse transcriptase inhibitor` as a current ChEBI role term. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '10428899[uid] OR 11142630[uid] OR 15340243[uid] OR 9784162[uid] OR 9934455[uid]' --limit 20 --output /tmp/antibioticmech-calanolide-b-source-pmids.jsonl` | Wrote the five exact ChEBI source-PMID candidates. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '"calanolide B" "HIV-1 reverse transcriptase"' --limit 20 --output /tmp/antibioticmech-calanolide-b-mechanism-publications.jsonl` | PubMed wrote one candidate; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query 'NIDRYBLTWYFCFV OR 909-14-8 OR 7257193 OR costatolide' --limit 20 --output /tmp/antibioticmech-calanolide-b-ids-publications.jsonl` | PubMed wrote 20 calanolide-family or identifier candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '"calanolide B" "Calophyllum lanigerum"' --limit 20 --output /tmp/antibioticmech-calanolide-b-lanigerum-publications.jsonl` | Wrote two PubMed candidates useful for separating `(+)-calanolide A` and `(-)-calanolide B` plant-source claims. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`calanolide-b.yaml` resolves unambiguously under
`data/antibiotics/antiviral/`. The generated lock row in
`data/antibiotics/PATHS.tsv` maps `CHEBI:65553` to
`ANTIVIRAL/calanolide-b`, and `source_concepts` carries only the expected ChEBI
upstream identity with minted ID `antibioticmech:chebi-df521c82f4`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:65553`: SMILES, Standard InChI, InChIKey
`NIDRYBLTWYFCFV-PZROIBLQSA-N`, formula `C22H26O5`, charge `0`, average mass,
monoisotopic mass, CAS, Reaxys, patent, and all five source PMIDs agree with
`data/raw/chebi_antimicrobials.tsv`. The official ChEBI OLS term for
`CHEBI:65553` reports the same label, definition, formula, SMILES, Standard
InChI, InChIKey, CAS `909-14-8`, Reaxys `7257193`, patent `WO9932492`, and
PubMed xrefs `10428899`, `11142630`, `15340243`, `9784162`, and `9934455`.

The target is distinct from `CHEBI:65552` `(+)-calanolide A`: that sibling has a
different label, stereochemical layers in its Standard InChI, external xrefs,
and InChIKey `NIDRYBLTWYFCFV-FMTVUPSXSA-N`.

No local grounding decision or prior review report was found for `CHEBI:65553`,
`antibioticmech:chebi-df521c82f4`, `NIDRYBLTWYFCFV-PZROIBLQSA-N`,
`909-14-8`, `WO9932492`, Reaxys `7257193`, or `calanolide-b` in
gitignore-independent searches over `curation/`, `data/raw/`,
`data/antibiotics/`, and `reports/yaml_record_review/`.

## Evidence

The generated identity, label, definition, exact synonyms, exact structure,
parent compounds, same-structure xrefs, filing class, HIV-1 reverse
transcriptase inhibitor role, source concept, and grounding status reproduce
from the committed ChEBI inventory with no `verify-corpus` drift.

The seeded `mode_of_action` and `mode_of_action_target_scope` are honest about
their provenance: the note states they are assigned from ChEBI role
`CHEBI:53756`, that ChEBI asserts that role on the compound, and that the field
is not a curator's mechanistic review. The corresponding ChEBI OLS terms for
`CHEBI:65553` and `CHEBI:53756` still resolve, and the local raw inventory
still names `CHEBI:53756` as the role that seeded the mechanism.

The exact-PMID PubMed lookup resolved all five ChEBI literature leads:
`PMID:10428899` is a primary HIV-1 reverse-transcriptase and cross-resistance
paper for `(-)-calanolide B`/costatolide; `PMID:11142630` reports combination
activity for calanolides including costatolide; `PMID:15340243` reports
Calophyllum brasiliense bioassay-guided isolation of anti-HIV
dipyranocoumarins including calanolide B; `PMID:9784162` reports a
Calophyllum chemotaxonomic survey that distinguishes `(+)-calanolide A` from
`(-)-calanolide B`; and `PMID:9934455` reports `(-)-calanolide B` in
Calophyllum seed oil.

The targeted mechanism and organism queries also found `PMID:1379639`, a
primary Calophyllum lanigerum bioassay-guided fractionation paper that reports
calanolide B structure determination, anti-HIV-1 activity, and HIV-1-specific
reverse-transcriptase inhibition for calanolides. That is a promising
claim-level mechanism and plant-source lead, not evidence currently present in
the YAML.

The identifier query did not find a registry-identifier-only source that
changed the record identity or introduced a more exact upstream structure
source; most candidates were calanolide-family reviews, analog papers,
docking-only studies, or sources already discoverable by compound name.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

## Completeness

The exact ChEBI identity, stereochemistry, structure, ChEBI grounding, source
concept, antiviral filing class, ChEBI HIV-1 reverse-transcriptase-inhibitor
role, parent compounds, chemical xrefs, and patent xref are defensible.

The consequential gap is mechanism sign-off. The YAML carries only a
source-seeded mode of action and scope, has no `molecular_targets` item for
HIV-1 reverse transcriptase, and has no target-specific `EvidenceItem`.
`just worklist --limit 0 --tsv /tmp/calanolide-b-worklist.tsv` therefore keeps
the record in `mechanism` and `review-readiness`.

No activity observations, producer claims, resistance assertions, clinical
assertions, or public datasets are present. The PubMed source leads include
activity and producer literature that could support future optional additions,
but this read-only pass found no local evidence that any of those optional
sections must be populated before the unreviewed mechanism is resolved.

The ChEBI definition embeds source-organism and anti-HIV activity prose that
`just worklist` correctly exposes as `producer-candidate` and
`activity-candidate` rows. Those phrases are discovery signals, not curated
producer or assay claims, because the record does not yet name a taxon,
strain, assay, endpoint, units, reference, or evidence object for either.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has only a source-seeded antiviral mechanism and cannot satisfy the `REVIEWED` gate until a curator reviews the HIV-1 reverse transcriptase claim and adds target-level evidence. | The target YAML has a ChEBI-seeded `mode_of_action` note ending `Not a curator's mechanistic review`, no record evidence, no `molecular_targets`, and no `causal_graphs`; `just worklist --limit 0 --tsv /tmp/calanolide-b-worklist.tsv` reports `0 CARD target(s), 0 resistance edge(s) to build on` for the `mechanism` queue and `MECHANISM_REVIEW: mechanism is source-seeded and not curator-checked; 5 source literature lead(s), 0 record evidence item(s), 0 target(s)` for the `review-readiness` queue. | Curator-owned evidence, molecular target, activity observation, producer, discussion, or causal-graph additions written to `data/antibiotics/antiviral/calanolide-b.yaml` through `write_validated_antibiotic`, if primary literature supports them. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect primary HIV-1 reverse-transcriptase papers for exact
   `(-)-calanolide B`/costatolide, prioritizing `PMID:10428899` and
   `PMID:1379639`, then add a curator-owned `MolecularTarget` for HIV-1 reverse
   transcriptase only if the source supports the target identity, direction,
   evidence status, and any resistance context at claim level.
2. Promote the source-seeded `VIRAL_POLYMERASE_INHIBITION` block to a
   curator-owned mechanism only when the selected paper supports inhibition of
   HIV-1 reverse transcriptase by this exact stereoisomer; keep
   `mode_of_action_target_scope: MICROBIAL_TARGET` because the target is viral.
3. Review `PMID:15340243`, `PMID:9784162`, `PMID:9934455`, and
   `PMID:1379639` for optional activity and producer claims. Keep the
   `(+)-calanolide A`, `(-)-calanolide B`, and source-taxon assertions separate
   unless full text supports a broader Calophyllum or calanolide-family claim.
4. Review `PMID:10428899` before adding resistance observations from NNRTI
   cross-resistance experiments; record exact RT mutations or viral strains at
   the claim level and do not convert a phenotype into an organism-independent
   biochemical resistance mechanism.
5. Do not promote evidence from `(+)-calanolide A`, `(-)-dihydrocalanolide B`,
   `(+)-12-oxo(+)-calanolide A`, F18, ring-opened analogs, calanolide-family
   reviews, or docking-only papers unless the inspected source explicitly
   supports the same assertion for `(-)-calanolide B`.
6. If a mode of action, target, activity observation, producer claim, or causal
   graph is supported, write it with a guarded mutator that asserts
   `identifier == "CHEBI:65553"`, updates only curator-owned fields, appends a
   `codex` `CurationEvent` with `llm_assisted: true`, and emits via
   `write_validated_antibiotic`.
7. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the newly curator-checked mechanism satisfy the
   `docs/CURATION.md` sign-off criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antiviral/calanolide-b.yaml --out /tmp/calanolide-b-validate-strict.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antiviral/calanolide-b.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `rg --no-ignore --hidden` over `data/antibiotics`, `data/raw`, `curation`,
  `reports/yaml_record_review`, `.claude`, `docs`, `README.md`, `conf`, `src`,
  `scripts`, `tests`, `justfile`, and `pyproject.toml` for `CHEBI:65553`,
  `calanolide B`, and `calanolide-b`.
- `find` over `data/antibiotics` and `reports/yaml_record_review` for
  `*calanolide*`.
- `rg --no-ignore --hidden` over `curation/`, `data/raw/`, and
  `reports/yaml_record_review/` for `antibioticmech:chebi-df521c82f4`,
  `NIDRYBLTWYFCFV-PZROIBLQSA-N`, `909-14-8`, `reaxys:7257193`, and
  `WO9932492`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned HTTP
429 for both non-PMID searches, so that provider's literature leads were not
available in this pass.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
