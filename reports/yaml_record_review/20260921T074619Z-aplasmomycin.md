# YAML Record Review: (+)-aplasmomycin

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antibacterial/aplasmomycin.yaml`
- Started UTC: 2026-09-21T07:46:19Z
- Finished UTC: 2026-09-21T07:46:19Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antibacterial/aplasmomycin.yaml` |
| Class | `ANTIBACTERIAL` |
| ID | `CHEBI:32617` |
| Label | `(+)-aplasmomycin` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded sodium `(+)-aplasmomycin` with InChIKey
`GEIYDRPAWIMINR-ZDKANEFWSA-N`. It merges one CARD/ARO source concept and one
ChEBI source concept, carries ARO, Beilstein, CAS, and PubChem xrefs, and has
no record evidence, mode of action, molecular target, activity observation,
resistance mechanism, producer organism, clinical assertion, causal graph,
dataset, or `Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/aplasmomycin.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/aplasmomycin.yaml --out /tmp/antibioticmech-aplasmomycin-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just review-queue --limit 10` | Confirmed `(+)-aplasmomycin` remains in `review-readiness`: `MECHANISM_REVIEW`, no mechanism, one source literature lead, zero record evidence items, and zero targets. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "aplasmomycin ionophore antibacterial mechanism" --limit 20 --output /tmp/antibioticmech-aplasmomycin-pubs.jsonl` | PubMed wrote 0 candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "aplasmomycin" --limit 20 --output /tmp/antibioticmech-aplasmomycin-exact-pubs.jsonl` | PubMed wrote 11 candidates; Semantic Scholar returned HTTP 429. The candidates included the 1976 discovery paper, the 1977 structure paper, biosynthesis and analogue papers, and `PMID:30089869`, the 2018 futalosine-pathway mechanism lead already present in the review queue. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`aplasmomycin.yaml` resolves unambiguously under
`data/antibiotics/antibacterial/`. The generated lock row in
`data/antibiotics/PATHS.tsv` maps `CHEBI:32617` to
`ANTIBACTERIAL/aplasmomycin`.

Both upstream source concepts converge on this ChEBI-grounded identity:
`ARO:3007792` cross-references `CHEBI:32617` and PubChem CID `23697358`, while
the record's ChEBI source concept directly names `CHEBI:32617`
`(+)-aplasmomycin`. No local grounding decision was found for `CHEBI:32617`,
`ARO:3007792`, or `aplasmomycin` in a gitignore-independent search over
`curation/decisions.tsv` with `grep -R`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:32617`: SMILES, Standard InChI, InChIKey
`GEIYDRPAWIMINR-ZDKANEFWSA-N`, formula `C40H60BO14.Na`, neutral charge, average
mass, and monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`.
The official ChEBI page for `CHEBI:32617` also reports the same label, formula,
SMILES, Standard InChI, InChIKey, CAS `61230-25-9`, Beilstein `5801225`, and
broader parents `CHEBI:25105` and `CHEBI:33619`.

The ARO-supplied PubChem xref also lands on the same structure: PubChem CID
`23697358` reports the same Standard InChI and InChIKey
`GEIYDRPAWIMINR-ZDKANEFWSA-N`, with sodium represented in Hill formula order as
`C40H60BNaO14`.

The record is filed as `ANTIBACTERIAL` because the ARO source concept is in
CARD's antibiotic subtree. The retained ChEBI role is the broader
`CHEBI:33281` `antimicrobial agent` assertion.

## Evidence

The generated identity, label, definition, exact synonyms, structure,
structural class, parent compounds, xrefs, filing class, activity role, source
concepts, and grounding status reproduce from the committed ARO and ChEBI
inventories with no `verify-corpus` drift.

The ARO definition is supported at source-inventory level by `PMID:30089869`,
but that paper has not been promoted to claim-level evidence. PubMed's record
for `PMID:30089869` identifies it as the 2018 paper "Aplasmomycin and boromycin
are specific inhibitors of the futalosine pathway"; its abstract states that
aplasmomycin, a boron-containing macrodiolide, was isolated from
`Streptomyces sp. K15-0223` as a specific futalosine-pathway inhibitor. The
abstract is a strong future lead for mechanism and producer curation, but this
read-only review did not inspect enough full text to attach molecular-target,
causal-edge, activity-observation, or producer claims to the YAML.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

The committed BindingDB, Drugs@FDA, MIBiG, and PHI-base inventories contain no
local row for `CHEBI:32617`, `ARO:3007792`, InChIKey
`GEIYDRPAWIMINR-ZDKANEFWSA-N`, CAS `61230-25-9`, PubChem CID `23697358`,
Beilstein `5801225`, or the label `aplasmomycin`.

## Completeness

The exact ChEBI identity, sodium structure, merged ARO and ChEBI source
concepts, ARO antibacterial filing, ChEBI role, ChEBI parents, and
same-structure xrefs are defensible.

The consequential gap is mechanism sign-off. The YAML has no
`mode_of_action`, no `mode_of_action_target_scope`, no `molecular_targets`, and
no `causal_graphs`; it also has no record-level evidence even though the ARO
inventory row points at a specific futalosine-pathway inhibitor paper. `just
review-queue --limit 10` therefore keeps the record in `MECHANISM_REVIEW`.

The record also lacks a producer assertion for the `Streptomyces sp. K15-0223`
source named by `PMID:30089869`, but the producer field is optional and the
unresolved mechanism is the sign-off gate that currently blocks `REVIEWED`.

No activity observations, resistance assertions, clinical assertions, or
datasets are present. The review found no specific local evidence that any of
those optional sections must be populated before the missing mechanism is
resolved.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has no curated antibacterial mechanism and cannot satisfy the `REVIEWED` gate. | The target YAML has no `record_evidence`, `mode_of_action`, `molecular_targets`, or `causal_graphs`; the ARO inventory row lists `PMID:30089869`; that PubMed record describes aplasmomycin as a futalosine-pathway inhibitor; `just review-queue --limit 10` reports `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | Curator-owned evidence, mechanism, target, producer, discussion, or causal-graph additions written to `data/antibiotics/antibacterial/aplasmomycin.yaml` through `write_validated_antibiotic`, if primary literature supports them. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Review `PMID:30089869` for exact aplasmomycin measurements, futalosine-pathway
   inhibition experiments, the concrete microbial enzyme or pathway step
   assayed, and the isolation context for `Streptomyces sp. K15-0223`.
2. If the full paper supports a futalosine-pathway mechanism for exact
   `CHEBI:32617`, add `record_evidence`, `mode_of_action`,
   `mode_of_action_target_scope`, molecular target or pathway context, and a
   causal graph whose edges each carry narrow primary-paper evidence.
3. If `Streptomyces sp. K15-0223` is reported as the producer of
   aplasmomycin rather than only a screening source, add a scoped
   `ProducerOrganism` with claim-level evidence from the inspected paper.
4. If the paper supports no precise molecular target beyond a pathway-level
   phenotype, keep target-specific fields blank and add a scoped `Discussion`
   explaining what was checked and why the exact mechanism remains unresolved.
5. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, activity role, and the newly curated mechanism or
   unresolved-mechanism discussion satisfy the `docs/CURATION.md` sign-off
   criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antibacterial/aplasmomycin.yaml --out /tmp/antibioticmech-aplasmomycin-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antibacterial/aplasmomycin.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `grep -R` over `curation/`, `data/antibiotics/`, `data/raw/`, and
  `reports/yaml_record_review/` for `CHEBI:32617`, `aplasmomycin`,
  `ARO:3007792`, `61230-25-9`, `GEIYDRPAWIMINR`, `23697358`, and `5801225`.
- `grep -R` over `reports/yaml_record_review/` for an existing `CHEBI:32617`,
  `aplasmomycin`, or `GEIYDRPAWIMINR` review report.
- `grep -R` over `data/raw/bindingdb_target_measurements.tsv`,
  `data/raw/fda_clinical_status.tsv`, `data/raw/mibig_producers.tsv`, and
  `data/raw/phibase_amr.tsv` for `CHEBI:32617`, `ARO:3007792`,
  `GEIYDRPAWIMINR`, `61230-25-9`, `23697358`, `5801225`, and `aplasmomycin`.
- `grep -R` over `curation/decisions.tsv` for `CHEBI:32617`, `ARO:3007792`,
  and `aplasmomycin`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned HTTP
429 for the targeted publication searches, so that provider's literature leads
were not available in this pass.

An initial publication search for `CHEBI:32617 OR ARO:3007792 OR aplasmomycin`
returned unrelated PubMed rows because `OR` and `ARO` made the query too broad.
It was superseded by the literal `aplasmomycin` search, which returned 11
on-topic PubMed candidates.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
