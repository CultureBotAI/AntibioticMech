# YAML Record Review: (R)-bromazine

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/unspecified/r-bromazine.yaml`
- Started UTC: `2026-09-23T03:51:00Z`
- Finished UTC: `2026-09-23T03:56:00Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:59301` |
| Label | `(R)-bromazine` |
| Path | `data/antibiotics/unspecified/r-bromazine.yaml` |
| Class | `ANTIMICROBIAL_UNSPECIFIED` |
| Status | `SEEDED` |
| Grounding | `EXACT`, through the `CHEBI` source concept `CHEBI:59301` |
| Structure | `NUNIWXHYABYXKF-QGZVFWFLSA-N`, formula `C17H20BrNO`, charge `0` |
| Maintained input | `data/raw/chebi_antimicrobials.tsv` |
| Generated or maintained | Generated record under `data/antibiotics`; do not hand-edit seeded identity, class, structure, parent, xref, or source-concept fields |

The full target YAML was read before judgement. `CHEBI:59301` resolves through
`data/antibiotics/PATHS.tsv` to the unspecified antimicrobial record
`data/antibiotics/unspecified/r-bromazine.yaml`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/unspecified/r-bromazine.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/unspecified/r-bromazine.yaml --out /tmp/r-bromazine-validate-strict.tsv` | Pass; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 110` | Pass; `CHEBI:59301` is queued immediately after the already-reviewed `(R)-7-butyl-6,8-dihydroxy-3-[(3E)-pent-3-en-1-yl]-3,4-dihydroisochromen-1-one` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/r-bromazine-worklist.tsv` | Pass; the relevant entries were `mechanism` and `review-readiness`; the record was not queued for minted grounding, xref, multi-component, producer-candidate, activity-candidate, structure, scope, or target-evidence defects. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query '"(R)-bromazine" OR "(R)-bromodiphenhydramine" OR "NUNIWXHYABYXKF-QGZVFWFLSA-N"' --limit 20 --output /tmp/r-bromazine-publications.jsonl` | PubMed returned one non-antimicrobial rabbit disposition paper for 4-bromodiphenhydramine; Semantic Scholar returned HTTP 429 and was unavailable for this pass. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query 'bromazine OR bromodiphenhydramine antimicrobial antibacterial antifungal mechanism MIC' --limit 20 --output /tmp/r-bromazine-broad-pubmed.jsonl` | PubMed returned 20 broad candidates, including antimicrobial reports for `ambodryl` / bromodiphenhydramine hydrochloride but no exact `(R)-bromazine` free-base activity, target, or mechanism abstract. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record. The bounded absence searches for a prior review,
curator decision, duplicate exact InChIKey, and the minted source identifier
used `rg --no-ignore --hidden`, so ignored `reports/` files were included.

## Identity and Grounding

The ChEBI import row for `CHEBI:59301` exactly matches the generated identity:

- label `(R)-bromazine`
- 3-star ChEBI entry with `EXACT` grounding
- ChEBI role term `CHEBI:33281`
- broader parent `CHEBI:59177`
- Standard InChIKey `NUNIWXHYABYXKF-QGZVFWFLSA-N`
- neutral formula `C17H20BrNO` and charge `0`

The record denotes the neutral `(R)` enantiomer of bromazine, not the racemate
`CHEBI:59177`, the opposite `(S)` enantiomer `CHEBI:59302`, or the hydrochloride
salt `CHEBI:59304`. The absence of `xrefs`, `drug_xrefs`, and
`document_xrefs` is consistent with the ChEBI source row; parent bromazine and
bromazine hydrochloride carry registry xrefs, but ChEBI did not assert any
external xref for the isolated `(R)` free base.

An ignored-inclusive search over `data/raw`, `data/antibiotics`, `curation`, and
`reports` found the exact InChIKey only in the ChEBI raw row and this generated
YAML record. The same bounded search found the minted source identifier
`antibioticmech:chebi-4de00276c7` only in the generated YAML, with no
`curation/decisions.tsv` override or prior review report.

## Evidence

Existing claim evidence is narrow:

| Claim | Evidence review |
|---|---|
| ChEBI identity, definition, synonyms, antimicrobial role, parent, and structure fields | Database-seeded from the `CHEBI:59301` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |
| `(R)-bromazine` is parented to bromazine | The ChEBI source row lists `CHEBI:59177`, the racemic bromazine record, as the broader parent. That is a strict broader relationship, not an exact xref. |

The record has no `mode_of_action`, `molecular_targets`,
`activity_observations`, `resistance_mechanisms`, `producer_organisms`,
`causal_graphs`, or record-level literature `evidence` items to overstate. No
existing citation is attached to a broader claim than it supports.

The exact-label and exact-InChIKey publication search did not find a
compound-specific mechanism or antimicrobial assay for the isolated neutral
`(R)` enantiomer. It returned `PMID:7139065`, a rabbit disposition study that
mentions 4-bromodiphenhydramine but does not report antimicrobial activity. A
broader PubMed search found nearby antimicrobial reports for
`ambodryl` / bromodiphenhydramine hydrochloride, including synergy and
cross-resistance studies, but those abstracts do not establish whether the
tested material was this exact `(R)` free base rather than the racemate,
unspecified bromodiphenhydramine, or the hydrochloride salt.

## Completeness

The record is not complete enough for `REVIEWED` because the mechanism gate is
unsettled:

- `mode_of_action` and `mode_of_action_target_scope` are absent.
- `molecular_targets` is absent.
- `causal_graphs` is absent.
- `activity_observations` is absent.
- `producer_organisms` is absent and the full worklist did not nominate a
  producer candidate for this ChEBI seed.
- `resistance_mechanisms`, `clinical_status`, and `datasets` are empty; the
  bounded review did not find local source rows proving these omissions are
  defects.

The exact ChEBI source row has no PMID or DOI lead. Future activity curation
needs primary full text or another exact source before copying MIC or synergy
values from bromodiphenhydramine hydrochloride papers, because a racemic salt
or parent-drug assay is not automatically evidence for an isolated enantiomeric
free base.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| major | `mode_of_action`, `molecular_targets`, `causal_graphs`, and `activity_observations` are absent. ChEBI asserts the antimicrobial role for exact `CHEBI:59301`, but the local ChEBI row carries no source PMID/DOI and the bounded PubMed searches found only non-exact bromodiphenhydramine or bromodiphenhydramine hydrochloride leads. | Curator-owned additions in `data/antibiotics/unspecified/r-bromazine.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blockers or minor findings were found.

## Recommended Edits

1. Inspect ChEBI's upstream provenance or primary literature for the isolated
   `(R)` enantiomer and decide whether exact `ActivityObservation` rows are
   supportable.
2. Inspect the non-exact bromodiphenhydramine hydrochloride antimicrobial leads
   only as parent or salt follow-up; curate them to `CHEBI:59178`,
   `CHEBI:59304`, or another exact record only if the source compound form is
   explicit.
3. Leave `mode_of_action` unset unless a primary source supports a specific
   target or pathway for exact `(R)-bromazine`.
4. Add `molecular_targets` only if primary evidence supports a target beyond
   the source-seeded antimicrobial role.
5. Add a causal graph only after the activity and any mechanism interpretation
   are resolved; every edge needs its own primary-paper `EvidenceItem`.
6. Leave the seeded identity, role, parent, and structure untouched unless a
   ChEBI refresh or `curation/decisions.tsv` row changes the maintained import.

## Follow-up Checks

- After any curator-owned edit, rerun
  `just validate-strict data/antibiotics/unspecified/r-bromazine.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Rerun `just verify-corpus` to prove the generated record still reproduces
  from `data/raw/` plus curator inputs.
- Rerun `just worklist --limit 0 --tsv /tmp/r-bromazine-worklist.tsv` and
  confirm the `CHEBI:59301` `mechanism` and `review-readiness` rows changed as
  intended.
- Rerun `just lint`, `just test`, and `just render-check` through `just qc` if a
  later pass mutates the YAML rather than writing a review report only.

## Additional Notes

- `reports/` is gitignored. This report must be staged with `git add -f`.
- The bounded, ignored-inclusive local search found no committed PMID or DOI
  row for exact `CHEBI:59301`.
- The local exact-InChIKey search did not find any other source row or generated
  record with `NUNIWXHYABYXKF-QGZVFWFLSA-N`; ignored `reports` were included.
- Semantic Scholar was rate-limited with HTTP 429 and was not treated as a
  negative source.
