# YAML Record Review: (13R)-epi-8,13-epoxylabd-14-ene

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml`
- Started UTC: `2026-09-21T19:12:38Z`
- Finished UTC: `2026-09-21T19:13:47Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:138224` |
| Label | `(13R)-epi-8,13-epoxylabd-14-ene` |
| Path | `data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml` |
| Class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | `CHEBI:138224`, minted key `antibioticmech:chebi-04cfd8e533`, ChEBI release `2026-08-30` |
| Source role | `CHEBI:33282` antibacterial agent |

This is a generated, ChEBI-seeded record. The seeder owns its identity,
definition, synonyms, parent terms, ChEBI xrefs, `activity_roles`,
`chemical_structure`, `source_concepts`, and audit entries. Future record-level
mechanism, activity, producer, resistance, dataset, discussion, or causal-graph
additions belong in this YAML through a guarded mutator that calls
`record_curation_event` and `write_validated_antibiotic`; seeded identity fixes
would belong in `data/raw/chebi_antimicrobials.tsv`, the ChEBI extractor, or a
`curation/decisions.tsv` row followed by `just seed-apply`.

The full target YAML was read before judging the record. It contains ChEBI's
definition, one IUPAC name, two ChEBI-derived synonyms, four broader
`parent_compounds`, a neutral `C20H34O` structure with InChIKey
`IGGWKHQYMAJOHK-QVHQYWGISA-N`, four structural xrefs, one ChEBI source concept,
and three seed-time history events. It contains no record-level `evidence`,
`mode_of_action`, `molecular_targets`, `activity_observations`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status`, `datasets`, or
`causal_graph`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml --out /tmp/antibioticmech-13r-epi-138224-validation.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 error rows. |
| `just verify-corpus --summary` | Passed; 2939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 35` | Confirmed this record is the next unreviewed ChEBI mechanism item after `CHEBI:31058`, with `MECHANISM_REVIEW`, 6 source literature leads, 0 evidence items, and 0 targets. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-13r-epi-138224.tsv` | Wrote the full worklist. `CHEBI:138224` appears on `mechanism`, `producer-candidate`, and `review-readiness` only. |
| Single-record term/reference/history validators | Not checked: no narrower term, reference, or history validator is exposed for a single ChEBI-seeded record. The documented focused checks are the single-record schema gates plus the full-corpus `verify-corpus`, `worklist`, and `review-queue` diagnostics. |

`printenv NCBI_EMAIL` was unset, so the PubMed helper was run as
`env -u NCBI_EMAIL uv run python scripts/search_publications.py ...`.

## Identity and Grounding

The record identity is coherent. The live ChEBI entry for `CHEBI:138224`
matches the record's label, ASCII name, formula `C20H34O`, neutral charge,
average mass `290.491`, monoisotopic mass `290.26097`, SMILES, Standard InChI,
and InChIKey `IGGWKHQYMAJOHK-QVHQYWGISA-N`.

ChEBI also supports the seeded aliases and broader parents:

| Seeded item | ChEBI support |
|---|---|
| IUPAC synonym | ChEBI lists `(3R,4aS,6aR,10aR,10bS)-3-ethenyl-3,4a,7,7,10a-pentamethyldodecahydro-1H-naphtho[2,1-b]pyran`. |
| `(-)-ent-13-epi-Manoyl oxide` | ChEBI lists this as a KNApSAcK synonym. |
| `ent-13-epi-manoyl oxide` | ChEBI lists this as a UniProt name. |
| `CHEBI:26979` | ChEBI asserts `is a` organic heterotricyclic compound. |
| `CHEBI:36770` | ChEBI asserts `is a` labdane diterpenoid. |
| `CHEBI:37407` | ChEBI asserts `is a` cyclic ether. |
| `CHEBI:79084` | ChEBI asserts `is a` tricyclic diterpenoid. |
| `CHEBI:33282` | ChEBI asserts `has role` antibacterial agent. |

The official ChEBI page also lists the same same-structure accessions carried in
the record: `knapsack:C00043487`, `metacyc.compound:CPD-20258`,
`cas:27642-41-7`, and `reaxys:7269026`.

Grounding status `EXACT` is appropriate for a record whose sole source concept
is its own ChEBI entry. There is no `curation/decisions.tsv` row for
`antibioticmech:chebi-04cfd8e533`, and none is needed.

## Evidence

The record currently has no curated evidence items. That is allowed for a
seeded record because ChEBI supplies the source concept and the antimicrobial
role, but it means no downstream activity, target, resistance, producer, or
causal assertions have been reviewed.

The ChEBI inventory row carries six publication leads:

| Lead | What the inspected PubMed text supports |
|---|---|
| `PMID:11301869` | Isolation and structural determination of ent-13-epi-manoyl oxide from `Cistus monspeliensis`; the abstract reports slight cytotoxic growth inhibition, not an antibacterial target or mechanism. |
| `PMID:12443786` | Exact epimer characterization by GC-MS and NMR; the abstract reports that mixtures with different C13 epimeric composition had antibacterial activity and that ent-13-epi-manoyl oxide appeared more active than manoyl oxide against Gram-positive bacteria. |
| `PMID:15564689` | Isolation of `(-)-ent-13-epi-manoyl oxide` from the culture filtrate of `Phaeosphaeria` sp. L487; the abstract is producer/origin evidence, not antibacterial mechanism evidence. |
| `PMID:17236091` | A no-abstract PubMed entry for natural metabolites of ent-13-epi-manoyl oxide from `Cistus creticus` resin; the PubMed metadata alone is not enough to support an activity, producer, or mechanism claim. |
| `PMID:26077765` | Biosynthetic-enzyme evidence that `Salvia miltiorrhiza` SmCPS4 and kaurene synthase-like 2 produce ent-13-epi-manoyl oxide in floral sepals; no antimicrobial mechanism is reported in the abstract. |
| `PMID:7765688` | Biotransformation of ent-13-epi-manoyl-oxide derivatives by `Curvularia lunata`; the abstract reports antileishmanial activity for related hydroxy and delta-1 derivatives, not for this exact record structure. |

The targeted PubMed searches by exact label, synonyms, InChIKey, CAS number,
and `manoyl oxide` plus antibacterial terms found plausible activity leads,
including pure `epi-manoyloxide` anti-`Borrelia burgdorferi` testing in
`PMID:31474477` and mixture or essential-oil studies in `PMID:20432627`,
`PMID:42067804`, and `PMID:10630116`. The inspected abstracts do not resolve an
exact molecular target or causal graph.

## Completeness

The `mechanism` worklist row is the most important gap: the record has a
ChEBI-supported antibacterial role but no curated `mode_of_action`,
`mode_of_action_target_scope`, `molecular_targets`, or `causal_graph`.

The `producer-candidate` row was triggered only by the definition phrase
`isolated from plants and fungi`; the candidate subject is `(no binomial)` and
the worklist correctly warns that this phrase is `SOURCE only`. ChEBI's live
entry names concrete species origins for `Cistus creticus`,
`Cistus incanus subsp. creticus`, `Cistus monspeliensis`, and `Phaeosphaeria`
sp. L487, but those sources still need paper-level inspection before any exact
`producer_organisms` entries are added.

The empty `resistance_mechanisms` field is expected for this ChEBI-only natural
product: the mechanism worklist reports 0 CARD targets and 0 CARD resistance
edges to build on. The empty `clinical_status`, `datasets`, and
`molecular_targets` fields are not schema errors and should remain empty until
an inspected source supports exact assertions.

Exhaustive local searches included ignored files where an absence mattered:

- `find reports/yaml_record_review -maxdepth 1 -name '*13r*epoxylabd*' -print`
  found no prior report for this record; `find` includes ignored reports.
- `rg --no-ignore --hidden` over `curation`, `data/raw`, and
  `data/antibiotics` for `138224`, the slug, `ent-13-epi-manoyl`,
  `IGGWKHQYMAJOHK`, `C00043487`, `CPD-20258`, and `27642-41-7` found only the
  expected ChEBI inventory row, `PATHS.tsv` row, review-queue row, and target
  YAML. It found no curated decision, secondary maintained record source, or
  existing local curation for this compound.

## Findings

| ID | Severity | Finding | Maintained owner |
|---|---|---|---|
| `EPOXYLABD-138224-M1` | major | The record is still a ChEBI seed stub for a structure with reported antibacterial activity leads. It has no curated activity observation, producer assertion, microbial target, mode-of-action disposition, or causal graph, so the reader can verify the ChEBI identity but not why this exact diterpenoid inhibits bacteria or which organisms and assays support the role. | `data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml` via a guarded curator mutator; seeded identity corrections, if any are later found, must go through `data/raw/chebi_antimicrobials.tsv` or the ChEBI extractor. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect the full text and tables for `PMID:12443786`; if it reports assay
   organisms, methods, and MIC values for purified ent-13-epi-manoyl oxide, add
   one `ActivityObservation` per organism with the source's original MIC value,
   qualifier, units, assay, strain where available, and a claim-level
   `EvidenceItem`.
2. Inspect `PMID:31474477`, `PMID:20432627`, and `PMID:10630116` to separate
   exact purified 13-epi/ent-13-epi-manoyl oxide measurements from essential-oil
   or epimer-mixture measurements. Promote only exact-compound measurements to
   `activity_observations`; keep mixture results out of the record or mention
   them only in a bounded `Discussion`.
3. Inspect `PMID:11301869`, `PMID:15564689`, `PMID:17236091`, `PMID:26077765`,
   and DOI `10.1080/10412905.1994.9698322` for exact source-organism claims.
   Add `producer_organisms` only where the source names a taxon and establishes
   isolation or biosynthesis of this exact structure.
4. Continue backward citation tracing from the antibacterial leads for direct
   target or mechanism evidence. If no target is known after bounded full-text
   review, keep `mode_of_action` empty and add a `CURATION_TODO` discussion
   summarizing the searched terms and unresolved target question.

## Follow-up Checks

- Re-run
  `just validate-strict data/antibiotics/antibacterial/13r-epi-8-13-epoxylabd-14-ene.yaml --out /tmp/antibioticmech-13r-epi-138224-validation.tsv`
  after any curator-owned edit.
- Re-run `just verify-corpus --summary` to prove the guarded curator additions
  survive reseeding and no generated seed-owned field drifted.
- Re-run `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-13r-epi-138224.tsv`
  and confirm `CHEBI:138224` leaves `mechanism` only when a curator-owned
  mechanism or explicit target-unknown discussion has been added.
- Re-run `just lint` and `git diff --check` before committing the future
  curation change.

## Additional Notes

ChEBI's definition currently says `anti-bacterial protperties`; the misspelling
is upstream ChEBI text copied into `data/raw/chebi_antimicrobials.tsv`, not a
hand-editing opportunity for the generated YAML.

`curation/record_review_queue.tsv` stores only the first five source literature
leads in its `source_id` column by design while the hint counts all six ChEBI
PMID leads. The missing sixth PMID in the display column is therefore not a data
loss signal.
