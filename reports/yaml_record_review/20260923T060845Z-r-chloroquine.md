# YAML Record Review: (R)-chloroquine

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antiprotozoal/r-chloroquine.yaml`
- Started UTC: `2026-09-23T06:02:00Z`
- Finished UTC: `2026-09-23T06:08:45Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:48811` |
| Label | `(R)-chloroquine` |
| Path | `data/antibiotics/antiprotozoal/r-chloroquine.yaml` |
| Class | `ANTIPROTOZOAL` |
| Status | `SEEDED` |
| Grounding | `EXACT`, through the `CHEBI` source concept `CHEBI:48811` |
| Generated? | Yes. This record is emitted from committed source inventories; future identity or source-role corrections must go through the ChEBI inventory/extractor path, and future curator-owned assertions must be written through `record_curation_event` plus `write_validated_antibiotic`. |

The full target YAML was read before judgement. `CHEBI:48811` resolves through
one source concept:

| Source | Source ID | Source label | Minted ID | Source version | Role terms |
|---|---|---|---|---|---|
| `CHEBI` | `CHEBI:48811` | `(R)-chloroquine` | `antibioticmech:chebi-4a24f1b9c0` | `2026-08-30` | `CHEBI:149553`, `CHEBI:38068` |

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiprotozoal/r-chloroquine.yaml` | Pass; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/r-chloroquine.yaml --out /tmp/r-chloroquine-validate-strict.tsv` | Pass; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, with 0 missing records, unexpected records, drifted fields, identifiers absent from `PATHS.tsv`, or stale lockfile rows. The corpus reproduces exactly from `data/raw/` plus curator inputs. |
| `just review-queue --limit 135` | Pass; `CHEBI:48811` is queued immediately after the already-reviewed `(R)-butoconazole nitrate` row with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/r-chloroquine-worklist.tsv` | Pass; exact `CHEBI:48811` rows are limited to the missing `mechanism`, the `review-readiness` row, and `xref-span-conflict` for upstream `pdb-ccd:CLQ`, which is deliberately absent from the published `xrefs` because the same PDB CCD accession also appears on broader `CHEBI:3638`. No exact minted-identity, unverified-published-xref, multi-component, producer-candidate, activity-candidate, target-evidence, or structure-unreviewed row is present. |
| Focused term/reference/history validators | Not exposed for this plain ChEBI-seeded record; the narrowest documented structural checks are the one-record schema checks above plus full-corpus `just verify-corpus` and `just worklist`. |
| `git diff --cached --check` | Pass after force-adding the ignored report. |
| `just lint` | Pass. |

## Identity and Grounding

The ChEBI import row for `CHEBI:48811` exactly matches the generated identity:

| Aspect | Local ChEBI row / generated record |
|---|---|
| Label | `(R)-chloroquine` |
| ChEBI stars | 3 |
| Scope flag | `true` |
| Roles | `CHEBI:149553`, `CHEBI:38068` |
| Parent | `CHEBI:3638` |
| SMILES | `CCN(CC)CCC[C@@H](C)Nc1ccnc2cc(Cl)ccc12` |
| Standard InChIKey | `WHTVZRBIWZFKQO-CQSZACIVSA-N` |
| Formula / charge | `C18H26ClN3` / `0` |
| Xrefs imported from exact row | `beilstein:5051460`, `cas:58175-87-4`, `pdb-ccd:CLQ` |

`CHEBI:3638` is the broader stereochemically unspecified chloroquine record,
with the racemic/unspecified InChIKey `WHTVZRBIWZFKQO-UHFFFAOYSA-N`, so it is
correctly a `parent_compounds` entry rather than an exact `xrefs` entry.
PubChem resolves the exact CAS `58175-87-4` to CID `444810`, the same
stereospecific Standard InChIKey `WHTVZRBIWZFKQO-CQSZACIVSA-N`, and formula
`C18H26ClN3`, which is consistent with the retained `cas:` xref. The Beilstein
xref could only be checked as the accession on the exact local ChEBI row; no
public Beilstein endpoint was available during this review.

The `pdb-ccd:CLQ` source xref is intentionally not present in the generated
record. `just worklist --queue xref-span-conflict` flags that accession on both
exact `(R)-chloroquine` and broader `chloroquine`; withholding it avoids
asserting one PDB CCD accession as exact identity for two different structures.

`CHEBI:38068` maps to `ANTIPROTOZOAL`, while `CHEBI:149553` maps to `ANTIVIRAL`
in `conf/sources.yaml`. Filing under `ANTIPROTOZOAL` follows the class-priority
rule, and keeping both role CURIEs in `activity_roles` preserves the full ChEBI
role set.

## Evidence

| Claim | Review |
|---|---|
| ChEBI identity, parent, role, synonym, structure, same-structure `cas:`/`beilstein:` xref fields | Database-seeded from the `CHEBI:48811` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |
| `pdb-ccd:CLQ` not present in generated `xrefs` | Supported by the exact full-worklist row for `CHEBI:48811`: the same PDB CCD accession is also published on broader `CHEBI:3638`, so the seeder withholds it instead of asserting exact equivalence. |
| Antimalarial activity | Plausible for the exact enantiomer but not yet structured. PubMed search found PMID:3527245, whose abstract reports a 48-hour in vitro comparison of chloroquine, the two chloroquine enantiomers, desethylchloroquine, and pyronaridine against two `Plasmodium falciparum` strains. The abstract does not expose the exact strain/value row for `(R)-chloroquine`, so it is a lead for a future `ActivityObservation`, not enough by itself for a fully scoped activity assertion. |
| Anticoronaviral activity | Still database-seeded only. ChEBI imports `CHEBI:149553` on the exact `(R)-chloroquine` row, but the local row carries no PMID/DOI, and an exact PubMed/Semantic Scholar search for the R enantiomer plus SARS-CoV-2/coronavirus terms found only broader chloroquine, hydroxychloroquine, 4-aminoquinoline derivative, or review leads. |
| Mode of action | Absent. Searches for exact `(R)-chloroquine`, `(-)-chloroquine`, CAS `58175-87-4`, the exact InChIKey, `Plasmodium falciparum`, and heme/hemozoin terms found exact antimalarial-activity leads or broader chloroquine/analogue hemozoin leads, but no inspected source that justifies a mechanism for exact `(R)-chloroquine`. |

Searches performed with `NCBI_EMAIL` unset:

| Output | Providers | Query | Result |
|---|---|---|---|
| `/tmp/r-chloroquine-publications-exact.jsonl` | PubMed; Semantic Scholar attempted | `"(R)-chloroquine" OR "(R)-(−)-chloroquine" OR "(−)-chloroquine" OR 58175-87-4 OR WHTVZRBIWZFKQO-CQSZACIVSA-N` | PubMed wrote 20 mostly generic chloroquine candidates; Semantic Scholar returned HTTP 429. |
| `/tmp/r-chloroquine-publications-enantiomer-pf.jsonl` | PubMed; Semantic Scholar attempted | `chloroquine enantiomers Plasmodium falciparum IC50 stereoselective` | PubMed wrote 1 exact enantiomer-series lead, PMID:22401346, about mefloquine analogue enantiomers rather than exact `CHEBI:48811`; Semantic Scholar returned HTTP 429. |
| `/tmp/r-chloroquine-publications-heme.jsonl` | PubMed; Semantic Scholar attempted | `"(-)-chloroquine" "(+)-chloroquine" Plasmodium heme polymerization beta-hematin` | PubMed wrote 20 heme or hemozoin-pathway leads for broader chloroquine or comparator analogues; Semantic Scholar returned HTTP 429. |
| `/tmp/r-chloroquine-publications-pmids.jsonl` | PubMed | `3552292 OR 396700 OR "In vitro activity of chloroquine, the two enantiomers of chloroquine" OR "Antimalarial activity of the optical isomers of chloroquine diphosphate"` | PubMed wrote 7 candidates, including exact enantiomer activity leads PMID:3527245, PMID:3552292, and PMID:396700. |
| `/tmp/r-chloroquine-publications-antiviral.jsonl` | PubMed and Semantic Scholar | `"(R)-chloroquine" OR "(-)-chloroquine" SARS-CoV-2 coronavirus anticoronaviral` | 21 candidates, none inspected as exact `(R)-chloroquine` anticoronaviral evidence. |

## Completeness

The record is complete enough for a generated exact ChEBI identity: identifier,
label, source concept, synonyms, parent, roles, charge, formula, masses, SMILES,
Standard InChI, and Standard InChIKey are mutually consistent with the committed
ChEBI inventory. Empty record-level `evidence` is acceptable for a seeded
database record.

Consequential gaps remain:

- No `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, or
  `causal_graphs` are present. This is correctly queued by both `mechanism` and
  `review-readiness`.
- No `activity_observations` are present even though PMID:3527245 is a likely
  exact lead for `P. falciparum` activity of both chloroquine enantiomers.
- No `resistance_mechanisms`, `producer_organisms`, `clinical_statuses`, or
  `datasets` are present. The bounded searches above did not expose an exact
  source that should be added during a read-only review.

The ignored-inclusive search across `reports/yaml_record_review`,
`data/antibiotics`, `data/raw`, `curation`, and
`/tmp/r-chloroquine-worklist.tsv` for exact `CHEBI:48811`, exact InChIKey
`WHTVZRBIWZFKQO-CQSZACIVSA-N`, exact xrefs, and
`antibioticmech:chebi-4a24f1b9c0` found this YAML, its `PATHS.tsv` and raw
ChEBI provenance rows, exact worklist rows, and no prior exact review report.

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| blocker | None found. | Not applicable. |
| major | Exact `(R)-chloroquine` still needs mechanism and activity curation before this record can become `REVIEWED`. ChEBI supplies antimalarial and anticoronaviral roles for exact `CHEBI:48811`, but the local ChEBI row has no source PMID/DOI, the record has no exact structured `ActivityObservation`, no `mode_of_action`, no target, and no causal graph. The bounded search did find PMID:3527245 as an exact in vitro `P. falciparum` enantiomer lead, but the abstract does not expose the exact R-enantiomer strain/value row that the schema needs. | Curator-owned additions in `data/antibiotics/antiprotozoal/r-chloroquine.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI later changes the exact row's source roles or xrefs, the ChEBI inventory extractor owns that refresh. |
| minor | None found. | Not applicable. |

## Recommended Edits

1. Inspect PMID:3527245 full text and tables. If it reports an exact
   `(R)-chloroquine` value against a named `P. falciparum` strain, add an
   `ActivityObservation` with the strain, assay, value or qualitative outcome,
   and PMID evidence attached to the observation.
2. Inspect the older exact enantiomer leads PMID:3552292 and PMID:396700 to
   decide whether either supplies complementary exact activity rows for
   `(R)-chloroquine`.
3. Inspect ChEBI's upstream provenance for exact `CHEBI:48811`. If its
   `anticoronaviral agent` role is inherited from broader chloroquine evidence,
   decide whether to leave that database-seeded role untouched, add a
   curator-owned discussion flagging the exact-enantiomer gap, or correct the
   source inventory after an upstream refresh.
4. Leave `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`,
   and `causal_graphs` absent unless an exact `(R)-chloroquine` primary source,
   or a source that explicitly states the enantiomer shares racemic
   chloroquine's heme/hemozoin mechanism, supports the narrower claim.

## Follow-up Checks

- Re-run `just validate data/antibiotics/antiprotozoal/r-chloroquine.yaml`.
- Re-run `just validate-strict data/antibiotics/antiprotozoal/r-chloroquine.yaml
  --out /tmp/r-chloroquine-validate-strict.tsv`.
- Re-run `just verify-corpus --summary` and confirm the generated record still
  reproduces from the committed source inventories plus curator inputs.
- Re-run `just worklist --limit 0 --tsv /tmp/r-chloroquine-worklist.tsv` and
  confirm the `CHEBI:48811` `mechanism` and `review-readiness` rows changed as
  expected after any curated mechanism or activity update. If no mechanism is
  supportable, the `mechanism` queue entry should remain.
- Manually re-check `xref-span-conflict` for `pdb-ccd:CLQ` after any ChEBI
  upstream refresh and confirm that the ambiguous PDB CCD accession is still
  absent from published `xrefs` unless it resolves to one exact structure only.

## Additional Notes

- `data/raw/chebi_antimicrobials.tsv` also contains the broader
  stereochemically unspecified `CHEBI:3638` chloroquine row. That broader row
  carries its own `drugbank:DB00608`, `drugcentral:607`,
  `kegg.compound:C07625`, `kegg.drug:D02366`, `pdb-ccd:CLQ`, `reaxys:482809`,
  and `wikipedia.en:Chloroquine` accessions; none of those broader/drug-level
  identifiers appears as a same-structure xref on exact `CHEBI:48811`.
- Semantic Scholar was available for the final antiviral search but
  rate-limited during the other three direct PubMed/Semantic exact searches.
  PubMed succeeded in every repository-helper search.
