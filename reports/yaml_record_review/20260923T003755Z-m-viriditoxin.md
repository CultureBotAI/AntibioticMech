# YAML Record Review: (M)-viriditoxin

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/m-viriditoxin.yaml`
- Started UTC: `2026-09-23T00:36:11Z`
- Finished UTC: `2026-09-23T00:37:55Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:146007` |
| Label | `(M)-viriditoxin` |
| Path | `data/antibiotics/antibacterial/m-viriditoxin.yaml` |
| Class | `ANTIBACTERIAL` |
| Status | `SEEDED` |
| Grounding | `EXACT`, through the `CHEBI` source concept `CHEBI:146007` |
| Structure | `GMCZVCXZGZGZPX-HOTGVXAUSA-N`, formula `C34H30O14`, charge `0` |
| Maintained inputs | `data/raw/chebi_antimicrobials.tsv` and `data/raw/mibig_producers.tsv` |
| Generated or maintained | Generated record under `data/antibiotics`; do not hand-edit seeded identity or MIBiG slices |

The full target YAML was read before judgement. The record is a generated
ChEBI-seeded individual structure with one MIBiG-derived producer claim for
`BGC0001866` in `Byssochlamys spectabilis`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/m-viriditoxin.yaml` | Pass; LinkML reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/m-viriditoxin.yaml --out /tmp/m-viriditoxin-validate-strict.tsv` | Pass; 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Pass; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just review-queue --limit 87` | Pass; `CHEBI:146007` remains the first unreviewed record after `CHEBI:46762` with `MECHANISM_REVIEW: mechanism is absent; 6 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/m-viriditoxin-worklist.tsv` | Pass; the relevant entries were `mechanism` and `review-readiness`, and the record was not queued for minted grounding, xref, multi-component, structure, scope, or target-evidence defects. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --query viriditoxin --limit 20 --output /tmp/m-viriditoxin-publications.jsonl` | PubMed returned 20 candidates; Semantic Scholar returned HTTP 429 and was unavailable for this pass. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record. The bounded absence searches for a prior review,
curator decision, BGC mirror, and additional exact-structure rows used
`rg --no-ignore --hidden` or `find`, so ignored `reports/` files were included.

## Identity and Grounding

The ChEBI import row for `CHEBI:146007` exactly matches the generated identity:

- label `(M)-viriditoxin`
- 3-star ChEBI entry with `EXACT` grounding
- antibacterial role `CHEBI:33282`
- broader parent `CHEBI:146015`
- CAS xref `cas:1381782-08-6`
- Standard InChIKey `GMCZVCXZGZGZPX-HOTGVXAUSA-N`

The same Standard InChIKey appears in the MIBiG producer inventory row for
`BGC0001866`, where it is joined as compound-specific viriditoxin. An
ignored-inclusive search over `curation`, `data/raw`, `data/antibiotics`, and
`reports` found no `curation/decisions.tsv` override for `CHEBI:146007` and no
prior viriditoxin review report.

## Evidence

Existing claim evidence is narrow and honest:

| Claim | Evidence review |
|---|---|
| ChEBI identity, role, parent, synonym, CAS xref, and structure fields | Database-seeded from the `CHEBI:146007` row in `data/raw/chebi_antimicrobials.tsv`; no curator-owned literature citation is required at record level. |
| `Byssochlamys spectabilis` producer claim for `BGC0001866` | Database-seeded from `data/raw/mibig_producers.tsv`, with exact InChIKey agreement and `source_quality: questionable`; the generated note correctly says `PMID:31045362` is MIBiG's first legacy reference for the entry rather than compound-specific support. |

The record has no `molecular_targets`, `activity_observations`,
`resistance_mechanisms`, `causal_graphs`, or record-level literature `evidence`
items to overstate. No existing citation is attached to a broader claim than it
supports.

## Completeness

The record is not complete enough for `REVIEWED` because the mechanism gate is
unsettled:

- `mode_of_action` and `mode_of_action_target_scope` are absent.
- `molecular_targets` is absent even though exact viriditoxin literature
  includes FtsZ-target papers and later reports that qualify that target model.
- `causal_graphs` is absent.
- `activity_observations` is absent; PubMed returned viriditoxin activity leads
  that should be mined for assay, strain, MIC value, units, and method before a
  claim is added.
- `resistance_mechanisms`, `clinical_status`, and `datasets` are empty; the
  bounded review did not find local source rows proving these omissions are
  defects.

The PubMed query `viriditoxin` found mechanistic leads that a future curation
pass must inspect in full text before asserting a target:

| PMID | Relevance |
|---|---|
| `PMID:21413107` | Exact `(-)-viriditoxin` synthesis paper whose title names bacterial FtsZ as a target. |
| `PMID:22958099` | Cross-comparison that reports no viriditoxin FtsZ GTPase inhibition under its protocols. |
| `PMID:23539337` | In vivo bacterial-cell follow-up reporting that viriditoxin perturbed membrane potential or permeability in `Bacillus subtilis`, a possible confounder for FtsZ-localization phenotypes. |
| `PMID:29037949` | Exact viriditoxin-congener antibacterial study against fish pathogens, with abnormal-cell-morphology evidence and FtsZ docking against `Staphylococcus aureus`. |
| `PMID:31045362` and `PMID:31304040` | Biosynthetic studies supporting the `vdt` gene cluster rather than a bacterial mechanism. |

## Findings

| Severity | Finding | Maintained owner |
|---|---|---|
| major | `mode_of_action`, `molecular_targets`, and `causal_graphs` are absent for a record with exact viriditoxin mechanism leads, so `CHEBI:146007` cannot pass the mechanism criterion for `REVIEWED`. The literature is not a simple fill-in: FtsZ-targeting is reported, but later biochemical and in vivo papers question or broaden that interpretation. | Curator-owned additions in `data/antibiotics/antibacterial/m-viriditoxin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blockers or minor findings were found.

## Recommended Edits

1. Inspect `PMID:21413107`, `PMID:22958099`, `PMID:23539337`, and
   `PMID:29037949` in full text. Decide whether viriditoxin supports an
   `OTHER` mode-of-action claim with an FtsZ molecular target, a membrane
   mechanism, a `MULTIPLE` or `UNKNOWN` mechanism, or an explicit curator veto
   with a `CURATOR:` note.
2. If the FtsZ mechanism is supportable, add an evidence-backed
   `molecular_targets` entry using a taxon-agnostic FtsZ family/function
   grounding rather than an organism-specific UniProt accession as the target
   identity. Keep strain or species context in the assertion slots.
3. Add any causal graph only after the mechanism has direct edge-level evidence;
   each `CausalEdge` needs its own primary-paper `EvidenceItem`.
4. Mine exact activity observations from the antibacterial papers only when a
   source reports the tested organism or strain, activity call, assay, and MIC
   with units.
5. Leave the MIBiG producer claim as source-owned unless `BGC0001866` itself
   changes upstream; its questionable quality and first-legacy-reference note
   are already preserved from the maintained `data/raw/mibig_producers.tsv`
   row.

## Follow-up Checks

- After any curator-owned edit, rerun
  `just validate-strict data/antibiotics/antibacterial/m-viriditoxin.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Rerun `just verify-corpus` to prove the generated record still reproduces from
  `data/raw/` plus curator inputs.
- Rerun `just worklist --limit 0 --tsv /tmp/m-viriditoxin-worklist.tsv` and
  confirm the `CHEBI:146007` `mechanism` and `review-readiness` rows changed as
  intended.
- Rerun `just lint`, `just test`, and `just render-check` through `just qc` if a
  later pass mutates the YAML rather than writing a review report only.

## Additional Notes

- `reports/` is gitignored. This report must be staged with `git add -f`.
- The local PubMed search was enough to expose unresolved mechanism literature;
  Semantic Scholar was rate-limited with HTTP 429 and was not treated as a
  negative source.
- The review found no matching local or remote
  `review-chebi-146007-m-viriditoxin` branch before creating this branch.
