# YAML Record Review: (±)-chartaceone C

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antiviral/chartaceone-c.yaml`
- Started UTC: `2026-09-24T06:21:22Z`
- Finished UTC: `2026-09-24T06:24:57Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/antibiotics/antiviral/chartaceone-c.yaml` |
| Identifier | `CHEBI:68935` |
| Label | `(±)-chartaceone C` |
| Class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv` through the ChEBI seeding path |
| Source concept | ChEBI `CHEBI:68935`, version `2026-08-30`, minted key `antibioticmech:chebi-f495f2917f` |
| Source role | `CHEBI:22587` antiviral agent |
| Parent compounds | `CHEBI:35692` dicarboxylic acid; `CHEBI:38749` dihydroxyflavanone |
| Structure source | ChEBI |
| Standard InChIKey | `RGZSPQYXJSDKAN-FWSOMWAYSA-N` |
| Same-structure xrefs | `reaxys:22123627` |
| Source literature lead | `PMID:22050318` |

This review covered exactly one generated ChEBI-seeded chartaceone C record. The
generated YAML was read in full and was not edited.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/antibiotics/antiviral/chartaceone-c.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/chartaceone-c.yaml --out /tmp/chartaceone-c-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2,939 records expected, 2,939 on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, and 0 stale lockfile rows. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/chartaceone-c-worklist.tsv` | Passed; exact `CHEBI:68935` appeared on `mechanism`, `activity-candidate`, and `review-readiness`. The `mechanism` row reported 0 CARD targets and 0 resistance edges to build on. The `activity-candidate` row captured a no-subject `active against` phrase from the ChEBI definition rather than a curated assay. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; at report selection time, `CHEBI:68935` was the first queue row without an exact one-record review report after matching prior report record paths to current YAML identifiers. |
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*chartaceone*' -print` | Passed; at report selection time, no prior chartaceone report was found. `find` included ignored files. |
| `rg --no-ignore --hidden -n "CHEBI:68935|chartaceone|chartaceone-c|PMID:22050318|22050318" data/antibiotics data/raw curation reports/yaml_record_review` | Passed; at report selection time, this found the exact generated record, the `PATHS.tsv` row, the `CHEBI:68935` ChEBI inventory row, sibling chartaceone D/E/F rows, and queue rows. It found no prior exact review report for this identifier. The search included ignored files. |
| `rg --no-ignore --hidden -n "CHEBI:68935|antibioticmech:chebi-f495f2917f|RGZSPQYXJSDKAN-FWSOMWAYSA-N|reaxys:22123627|22050318" data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed; after branching, this wider ignored/hidden-inclusive search found only the expected exact generated YAML, ChEBI raw row, review-queue row, sibling chartaceone D/E/F rows sharing `PMID:22050318`, and no curator-owned activity, target, resistance, producer, or causal-graph claim for exact `CHEBI:68935`. |
| `rg --no-ignore --hidden -n "RGZSPQYXJSDKAN-FWSOMWAYSA-N|reaxys:22123627" data/antibiotics -g '*.yaml'` | Passed; the exact Standard InChIKey and exact Reaxys xref appear only in `chartaceone-c.yaml` among generated YAML records. |
| EBI OLS4 API checks for `CHEBI:68935`, `CHEBI:35692`, `CHEBI:38749`, and `CHEBI:22587` | Passed; OLS resolved the exact chartaceone C term, the dicarboxylic-acid and dihydroxyflavanone parents, and the antiviral-agent role. |
| ChEBI REST lookup for `CHEBI:68935` | Passed; the current ChEBI JSON matched the generated label, definition, 3-star status, IUPAC synonym, formula, charge, average mass, monoisotopic mass, SMILES, Standard InChI, Standard InChIKey, Reaxys accession, PubMed citation, two parent terms, and antiviral-agent role. |
| PubMed `efetch` for `PMID:22050318` | Passed; PubMed resolved the chartaceone A-F primary paper, Journal of Natural Products 2011, DOI `10.1021/np200715v`. |
| Crossref lookup for DOI `10.1021/np200715v` | Passed; Crossref resolved the DOI to the same Allard et al. Journal of Natural Products paper. |
| PubMed/Semantic Scholar exact search for `"chartaceone C"`, `CHEBI:68935`, `RGZSPQYXJSDKAN-FWSOMWAYSA-N`, and `22123627` | Partial pass; PubMed returned `PMID:22050318` plus one unrelated `PMID:22123627` false positive caused by the numeric Reaxys accession, while Semantic Scholar returned an invalid response. |
| PubMed targeted search for `Cryptocarya chartacea`, dengue, NS5, and chartaceones | Passed; PubMed returned `PMID:22050318`. |
| PubMed direct search for chartaceone terms, `Cryptocarya chartacea`, and the Standard InChIKey | Passed; PubMed returned `PMID:22050318` plus one 2023 systematic review lead, `PMID:36818227`, that cites the 2011 primary chartaceone paper. |
| Reaxys xref `22123627` | Not independently checked: ChEBI lists this Reaxys registry number on exact `CHEBI:68935`, but Reaxys had no public resolver available in this workflow. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record in this repository; the full-corpus checks above were
the narrowest documented checks for those contracts.

## Identity and Grounding

The generated identity agrees with the local ChEBI inventory and the current
ChEBI term:

| Claim | Review |
| --- | --- |
| Label and identifier | Supported. OLS resolves `CHEBI:68935` to `(+-)-chartaceone C`, and the ChEBI REST compound endpoint resolves the same accession to `(±)-chartaceone C`. |
| Definition and IUPAC synonym | Supported. Current ChEBI reports the same dihydroxyflavanone definition and the same `rac-(5E,5'E)-...` IUPAC name as the generated YAML and `data/raw/chebi_antimicrobials.tsv`. |
| Structure | Supported. Current ChEBI reports SMILES, Standard InChI, Standard InChIKey `RGZSPQYXJSDKAN-FWSOMWAYSA-N`, formula `C41H40O8`, charge `0`, average mass `660.763`, and monoisotopic mass `660.27232`, matching the generated structure block. |
| Parent terms | Supported. Current ChEBI attaches exact `CHEBI:68935` to `CHEBI:35692` by `is a` and to `CHEBI:38749` by `is a`; OLS resolves those parents to `dicarboxylic acid` and `dihydroxyflavanone`. |
| Activity role and filing class | Supported as a ChEBI source claim. The generated record and committed source row carry `CHEBI:22587`, OLS resolves it as `antiviral agent`, and `conf/sources.yaml` maps that role to the `ANTIVIRAL` filing class. |
| ChEBI citation lead | Supported. Current ChEBI lists PubMed `22050318` on this term, matching the raw ChEBI row's `citations` field and the review-readiness literature lead. |
| Reaxys xref | Supported as a ChEBI assertion but not independently resolved. Current ChEBI lists `22123627` as a Reaxys registry number on exact `CHEBI:68935`, matching the generated `reaxys:22123627` xref. |
| Sibling boundaries | Supported. Chartaceone D, E, and F are adjacent ChEBI entries from the same PMID, but their raw ChEBI rows have different SMILES, Standard InChI strings, Standard InChIKeys, IUPAC names, and Reaxys identifiers, so the current record is not collapsed with its siblings. |

The record has one source concept, the exact ChEBI term. No ARO, CARD-only,
PubChem, or curator concept is merged into this record.

## Evidence

The generated YAML has no record-level `evidence`, `mode_of_action`,
`molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, or
`causal_graphs`. That is schema-valid because a ChEBI seed inherits its
classification from `data/raw/chebi_antimicrobials.tsv`, but no primary paper
has been promoted onto a claim-level mechanism, target, or assay observation.

The existing source-owned scientific assertions are:

| Assertion | Support |
| --- | --- |
| `(±)-chartaceone C` is a ChEBI-grounded exact record with the listed IUPAC name, structure, broader parents, Reaxys xref, and PubMed citation lead. | Supported by the committed ChEBI inventory row for `CHEBI:68935` and by the current ChEBI REST entry for the same term. |
| `(±)-chartaceone C` has a ChEBI antiviral-agent role. | Supported by the committed ChEBI row and by current ChEBI, both of which list `CHEBI:22587` on exact `CHEBI:68935`. |

The source-literature PMID is an actual primary lead. PubMed resolves
`PMID:22050318` to Allard et al. 2011, DOI `10.1021/np200715v`; Crossref
resolves that DOI to the same paper. PubMed's abstract reports that the
chemical investigation of `Cryptocarya chartacea` isolated chartaceones A-F as
racemic mixtures and that chartaceones C-F inhibited dengue virus NS5
RNA-dependent RNA polymerase with grouped IC50 values from 1.8 to 4.2 uM.

That abstract is enough to bound the missing curation work, but not to complete
it in this read-only review. It does not expose the exact chartaceone C compound
number, the exact chartaceone C IC50, the dengue virus serotype or protein
accession used in the assay, or whether the paper demonstrates direct binding
versus a measured target-associated polymerase effect. `PMID:22050318` should
therefore be inspected in full before a future curator sets
`mode_of_action: VIRAL_POLYMERASE_INHIBITION`, adds a dengue NS5 RdRp
`MolecularTarget`, or records a quantitative target measurement.

Bounded publication discovery found no stronger exact public lead. PubMed
returned `PMID:22050318` for the targeted chartaceone/`Cryptocarya chartacea`
query. The direct PubMed search also found `PMID:36818227`, a 2023 systematic
review whose reference list points back to `PMID:22050318`; it adds no primary
chartaceone C assay evidence. Searching the bare Reaxys registry number
`22123627` returned an unrelated dermatology PMID with the same number, so that
candidate was discarded as a numeric collision.

## Completeness

| Field family | Review |
| --- | --- |
| Identity and structure | Complete for a ChEBI exact seed. ChEBI and the committed raw row agree on exact identity, synonymy, formula, Standard InChI, Standard InChIKey, neutral charge, and masses. |
| Source concept | Complete. The only source concept is exact `CHEBI:68935`. |
| Xrefs | Incomplete only for independent external verification. `reaxys:22123627` is present on the current ChEBI term and in the committed ChEBI row, but Reaxys itself was not publicly resolvable here. |
| Record evidence | Empty. Acceptable for a raw seed, but no inspected source is attached to the record. |
| Mode of action and targets | Empty. Consequential gap; the review-readiness row is a `MECHANISM_REVIEW` row, and the mechanism worklist row reports 0 CARD targets. The primary PMID lead points to dengue NS5 RdRp inhibition but has not been curated into claim-level target evidence. |
| Target measurements and activity observations | Empty. The PubMed abstract for the primary paper reports grouped NS5 RdRp IC50 values for chartaceones C-F, but the exact chartaceone C value and enzyme assay context require full-text inspection before any `MolecularTarget.measurements` entry can be curated. A separate `ActivityObservation` would require whole-virus or cell-based activity for chartaceone C, not only a purified polymerase IC50. |
| Resistance mechanisms | Empty and expected for this ChEBI-only antiviral seed; there are 0 CARD resistance edges to build on. |
| Causal graph | Empty because no exact mechanism claim is curated yet. |
| Producer organisms | Empty and expected. Current ChEBI states that the compound was isolated from `Cryptocarya chartacea` trunk bark, which is a plant origin rather than a microbial producer claim for AntibioticMech. |
| Generated ownership | Correct. The reviewed YAML is emitted from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv`; future seeded-field corrections belong in those inputs or in the ChEBI extractor rather than as hand edits to the generated record. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, current ChEBI agrees with the exact identity and structure, and no sibling chartaceone row is being conflated with `CHEBI:68935`. | Not applicable. |
| Major | Exact `CHEBI:68935` remains a source-seeded antiviral structure with no curated mode of action, molecular target, target measurement, organismal activity observation, or causal graph despite having a promising dengue NS5 RdRp primary lead. | The generated record has 0 record evidence items and no `mode_of_action`, `molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, or `causal_graphs`; exact `CHEBI:68935` is on the `mechanism` and `review-readiness` queues with 0 CARD targets and 0 CARD resistance edges. PubMed confirms that `PMID:22050318` is the chartaceone A-F / dengue NS5 RdRp paper, but its abstract does not expose the exact chartaceone C IC50, target scope, or enzyme assay context required for claim-level curation. | Future curator-owned fields on `data/antibiotics/antiviral/chartaceone-c.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI changes this term's role, parentage, xrefs, or source PMIDs, `data/raw/chebi_antimicrobials.tsv` and the ChEBI extractor own the refresh. |
| Minor | `reaxys:22123627` is an unchecked direct Reaxys identity in this review. | The generated xref matches the committed ChEBI inventory row and current ChEBI's Reaxys registry number on exact `CHEBI:68935`, but Reaxys itself had no public resolver available here to verify same-structure semantics directly. | ChEBI extractor and `data/raw/chebi_antimicrobials.tsv`; future curation should either verify this source externally or preserve it as an upstream ChEBI assertion. |

## Recommended Edits

1. Inspect the full DOI `10.1021/np200715v` before curating chartaceone C
   mechanism or target fields. Extract the paper's exact compound number,
   chartaceone C IC50, enzyme assay, dengue virus context, and whether the
   assay supports `DIRECT_BINDING_TARGET` or only
   `MEASURED_TARGET_ASSOCIATION`.
2. Add a dengue virus NS5 RNA-dependent RNA polymerase `MolecularTarget` only
   if the full primary paper supports that exact target assertion for
   chartaceone C; use `VIRAL_PROTEIN`, `VIRAL_POLYMERASE_INHIBITION`, and a
   `MICROBIAL_TARGET` scope only after preserving the paper's virus and assay
   context.
3. Add a target measurement under the `MolecularTarget` only if the full paper
   maps a quantitative NS5 RdRp IC50 to exact chartaceone C, with the reported
   units and assay.
4. Add an `ActivityObservation` only if the full paper separately reports a
   whole-virus or cell-based activity observation with an exact taxon or virus
   label, units, and assay context.
5. Verify `reaxys:22123627` through Reaxys or a future ChEBI refresh before
   treating it as independently reviewed exact-identity evidence.

## Follow-up Checks

1. `just validate data/antibiotics/antiviral/chartaceone-c.yaml`
2. `just validate-strict data/antibiotics/antiviral/chartaceone-c.yaml --out /tmp/chartaceone-c-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/chartaceone-c-worklist.tsv`
5. `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv`
6. `just lint`

Rerun exact searches for `(±)-chartaceone C`, `CHEBI:68935`,
`RGZSPQYXJSDKAN-FWSOMWAYSA-N`, and DOI `10.1021/np200715v` before any future
curation, then inspect the full primary paper rather than promoting the grouped
PubMed abstract summary into exact compound-level measurements.

## Additional Notes

The absence checks above used `find` or `rg --no-ignore --hidden`, so generated
reports ignored by Git were included.

`PMID:22050318` is also the ChEBI literature lead for sibling exact records
`CHEBI:68936` chartaceone D, `CHEBI:68937` chartaceone E, and `CHEBI:68938`
chartaceone F. The grouped PubMed abstract language is useful for triage, but
future curation must not copy a chartaceone C measurement onto D, E, or F, or
vice versa, without exact full-text mapping.

Semantic Scholar was not required for the verdict. PubMed and direct ChEBI
lookups provided the identity and primary-literature boundaries needed for this
review.
