# YAML Record Review: (S)-linalool

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/unspecified/s-linalool.yaml
- Started UTC: 2026-09-23T23:17:00Z
- Finished UTC: 2026-09-23T23:21:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | AntibioticRecord |
| Identifier | CHEBI:98 |
| Label | (S)-linalool |
| Path | data/antibiotics/unspecified/s-linalool.yaml |
| Maintained status | Generated from `data/raw/chebi_antimicrobials.tsv` |
| Grounding | `EXACT` ChEBI source concept `CHEBI:98` |
| Curation status | `SEEDED` |

The target is the ChEBI-seeded `(S)` enantiomer. Exact ignored-inclusive
searches over `data/antibiotics/PATHS.tsv`, `data/raw`, `curation`, and
`reports/yaml_record_review` found one exact source row for `CHEBI:98`, one
PATHS row, one review-readiness row, one generated YAML record, and no prior
exact `CHEBI:98` review report. The same searches also surfaced the expected
siblings: `CHEBI:28` `(R)-linalool` and `CHEBI:17580` stereochemistry-unspecified
`linalool`.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/unspecified/s-linalool.yaml` | Pass; no LinkML validation issues. |
| `just validate-strict data/antibiotics/unspecified/s-linalool.yaml --out /tmp/s-linalool-validate-strict.tsv` | Pass; 1 file checked, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass; all 2,939 expected records were present on disk with no missing, unexpected, drift, path, or stale rows. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 xref refusal. |
| `just worklist --limit 0 --tsv /tmp/s-linalool-worklist.tsv` | Pass; wrote the full TSV. The anchored `CHEBI:98` rows are the `mechanism` and `review-readiness` backlog entries. |
| `just review-queue --limit 130` | Pass; listed `CHEBI:98` as `MECHANISM_REVIEW` with 0 source literature leads, 0 record evidence items, and 0 targets. |
| `just lint` | Pass. |
| `git diff --check` | Pass before adding this report. |
| `git diff --cached --check` | Pass after force-adding this ignored report. |

No narrower single-record term, reference, curation-history, or corpus-drift
validator is exposed for plain ChEBI-seeded records; `validate-strict` and
`verify-corpus --summary` are the narrowest documented checks for this case.

## Identity and Grounding

The local ChEBI inventory row for `CHEBI:98` contains label `(S)-linalool`, role
term `CHEBI:33281`, parent `CHEBI:17580`, SMILES `C=C[C@@](C)(O)CCC=C(C)C`,
Standard InChI
`InChI=1S/C10H18O/c1-5-10(4,11)8-6-7-9(2)3/h5,7,11H,1,6,8H2,2-4H3/t10-/m1/s1`,
InChIKey `CDOSHBSSFJOMGT-SNVBAGLBSA-N`, formula `C10H18O`, net charge `0`,
average mass `154.253`, monoisotopic mass `154.13577`, and four ChEBI xrefs.
The raw row has one IUPAC name, six ChEBI `SYNONYM` entries, and one
`UNIPROT NAME` that duplicates the preferred label. The generated YAML carries
the same preferred label, structure, parent, role term, source concept, the
seven alternate ChEBI names, and retained same-structure xrefs.

The EBI OLS4 term for `CHEBI:98` is active, non-obsolete, 3-star
`(S)-linalool` with the same definition, SMILES, Standard InChI, InChIKey,
formula, charge, mass values, Beilstein, CAS, KEGG Compound and KNApSAcK xrefs,
and ChEBI synonyms. Its only current OLS parent is `CHEBI:17580` `linalool`, and
its `is_enantiomer_of` relation points to the distinct active sibling
`CHEBI:28` `(R)-linalool` with InChIKey `CDOSHBSSFJOMGT-JTQLQIEISA-N`.

`CHEBI:33281` is active, non-obsolete `antimicrobial agent` in OLS4 and is the
generic in-scope ChEBI role root in `conf/sources.yaml`. It does not appear in
`role_to_class`, so the record correctly falls through to
`ANTIMICROBIAL_UNSPECIFIED` rather than a narrower antibacterial, antifungal,
antiprotozoal, antiviral, or biocide filing class.

Exact ignored-inclusive searches over `data/antibiotics`, `data/raw`,
`curation`, and `reports/yaml_record_review` found no second generated record
with InChIKey `CDOSHBSSFJOMGT-SNVBAGLBSA-N` and no second generated record with
source-concept minted identifier `antibioticmech:chebi-45d3a71ff9`.

## Evidence

The record has no record-level `evidence`, `mode_of_action`,
`molecular_targets`, `activity_observations`, `resistance_mechanisms`,
`producer_organisms`, or `causal_graphs`. That is an honest generated state for
a ChEBI seed whose only assertion is ChEBI's generic antimicrobial-agent role.

The ChEBI source row and review-readiness queue carry no source literature lead
for `CHEBI:98`. Bounded publication discovery left `NCBI_EMAIL` unset and
queried:

- `(S)-linalool`
- `CDOSHBSSFJOMGT-SNVBAGLBSA-N`
- `(3S)-3,7-dimethylocta-1,6-dien-3-ol`
- `S-(+)-linalool Aeromonas hydrophila antimicrobial`
- `(S)-(+)-Linalool antimicrobial`
- `126-90-9 linalool antimicrobial`

PubMed returned no candidates for the exact InChIKey or exact IUPAC-style
synonym, 20 candidates for the exact label, 1 candidate for the targeted
`S-(+)` antimicrobial query, and 20 candidates each for the broader
`(S)-(+)-Linalool antimicrobial` and CAS-number queries. Semantic Scholar was
HTTP 429 for all six searches.

Direct PubMed EFetch confirmed `PMID:28423080` is a 2017 paper comparing
`S-(+)` and `R-(-)-linalool` in fish. Its abstract reports a microdilution test
against *Aeromonas hydrophila* and says `S-(+)-linalool` was the only assayed
enantiomer active against that bacterium at 3.2 mg mL-1. This is a precise
activity lead for a future curator, but the abstract alone was not enough to
inspect the assayed strain, replicate table, or MIC qualifier.

The other inspected PubMed exact-label leads concerned non-antimicrobial
processes: beetle biosynthesis of floral mimics from `(S)-linalool`, microbial
or enzymatic production of `(S)-linalool`, plant linalool synthases, and
enantiomeric volatile profiles. The CAS-number and broad antimicrobial searches
mostly returned essential-oil, mixture, formulation, or stereochemistry-
unspecified linalool studies. Those are near misses for this exact ChEBI
structure unless the paper identifies the assayed material as the `(S)`
enantiomer.

## Completeness

The seeded identity, ChEBI grounding, generated structure, parent relation,
generic antimicrobial role, source concept, and retained ChEBI xrefs are
complete enough for an unreviewed ChEBI seed.

The record still lacks all reviewed antimicrobial biology:

- no `mode_of_action` or `mode_of_action_target_scope`;
- no `molecular_targets`;
- no structured activity observation for the exact `S-(+)` *A. hydrophila*
  result in `PMID:28423080`;
- no `resistance_mechanisms`;
- no `causal_graphs`.

`just worklist` therefore correctly queues `CHEBI:98` for mechanism curation,
and `just review-queue` correctly keeps it in `MECHANISM_REVIEW`.

## Findings

None found.

There are no blocker, major, or minor defects in the generated YAML. The absent
mechanism, target, activity, resistance, and graph sections are curation gaps,
not invalid seeded claims.

## Recommended Edits

None for the generated seed.

A future curator should only add a structured activity observation for
`PMID:28423080` after inspecting the full text for the assayed
*Aeromonas hydrophila* strain, the exact MIC qualifier, and method details.
Do not populate target, resistance, producer, or causal-graph fields from
essential-oil mixture papers or unspecified-linalool antimicrobial papers.

## Follow-up Checks

If a future curator adds exact `(S)-linalool` activity data or a supported
mechanism, rerun:

- `just validate-strict data/antibiotics/unspecified/s-linalool.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv`
- `just qc`
- `git diff --check`

No follow-up check is needed for this read-only review.

## Additional Notes

`PMID:28423080` is not currently part of the committed source rows or target
record; exact ignored-inclusive searches over `data/antibiotics`, `data/raw`,
`curation`, `reports`, `scripts`, `src`, `tests`, `docs`, and `.claude` found it
only in the prior sibling `(R)-linalool` review report.

The raw ChEBI row includes no PubMed xrefs for `CHEBI:98`, while the sibling
`CHEBI:28` `(R)-linalool` and parent `CHEBI:17580` rows do include PubMed xrefs.
Corpus verification passed, so the missing generated `evidence` section is
intentional seeded-state behavior rather than hand-edited drift.
