# YAML Record Review: (3S,6Z)-nerolidol

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antifungal/3s-6z-nerolidol.yaml`
- Started UTC: 2026-09-22T10:21:48Z
- Finished UTC: 2026-09-22T10:23:11Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:176337` |
| Label | `(3S,6Z)-nerolidol` |
| Path | `data/antibiotics/antifungal/3s-6z-nerolidol.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding | `EXACT` |
| Source concept | `CHEBI:176337`, version `2026-08-30`, minted key `antibioticmech:chebi-d47a7aaed8` |
| Source role | `CHEBI:35718` antifungal agent |
| Parent compounds | `CHEBI:173119` |
| Structure | `FQTLCLSUCSAZDY-QKXCFHHRSA-N`; formula `C15H26O`; charge `0` |
| Same-structure xrefs | `cas:142-50-7`, `chemspider:4512192`, `foodb.compound:FDB006275`, `hmdb:HMDB0035663` |
| Source literature lead | None |
| Evidence-bearing objects | None |

This review covered one generated ChEBI-seeded antifungal record. The YAML has
source-derived exact ChEBI grounding, a ChEBI definition, three exact ChEBI
synonyms, one strictly broader ChEBI parent, four ChEBI same-structure xrefs,
one antifungal role, structure, and source-concept metadata. It has no generated
or curator-owned mode of action, molecular target, activity observation,
resistance mechanism, producer, discussion, dataset, causal graph, or
claim-level evidence.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/3s-6z-nerolidol.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/3s-6z-nerolidol.yaml --out /tmp/antibioticmech-chebi-176337-validation.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Pass: 2939 expected records, 2939 on disk, no missing records, no unexpected records, no drifted fields, no identifiers absent from `PATHS.tsv`, no stale lockfile rows. The unrelated known `ARO:3000337` CARD cross-reference warning was emitted. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-176337.tsv` | Pass: the full worklist TSV was written. `CHEBI:176337` appears on `mechanism` and `review-readiness`; it was not found on any other queue in that TSV. |
| `just review-queue --limit 55` | Pass: `CHEBI:176337` is queued with `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |

No narrower single-record term, reference, or history validator is exposed for
this ChEBI-seeded record; the schema/strict checks, `verify-corpus`, and
worklist are the narrowest documented local gates.

## Identity and Grounding

`CHEBI:176337` resolves in official OLS as a current, non-obsolete ChEBI term
with label `(3S,6Z)-nerolidol`.

The live OLS annotations list the same formula, charge, average mass,
monoisotopic mass, Standard InChI, Standard InChIKey, SMILES, CAS
cross-reference, ChemSpider cross-reference, FoodDB cross-reference, and HMDB
cross-reference as the committed `data/raw/chebi_antimicrobials.tsv` row. The
committed raw ChEBI row has no `citations` values for exact `CHEBI:176337`, and
the live OLS term has no PubMed xrefs.

The generated `parent_compounds` value is exactly the live OLS hierarchical
parent of `CHEBI:176337`: `CHEBI:173119` `(6Z)-nerolidol`. The live OLS graph
also exposes `CHEBI:176336` `(3R,6Z)-nerolidol` as an enantiomer, not a parent
or same-structure xref; this record does not conflate the two stereoisomers.

The `ANTIFUNGAL` filing class and empty `mode_of_action` are consistent with
the ChEBI source role. The live `CHEBI:35718` role resolves in OLS as current
and non-obsolete `antifungal agent`, `data/raw/chebi_role_names.tsv` marks it
as a `class,scope` role, and `conf/sources.yaml` maps that role to
`ANTIFUNGAL` without a mechanism mapping.

An ignored-inclusive search for `CHEBI:176337`, the exact label, the filename
stem, the exact Standard InChIKey, and the CAS xref across `data/antibiotics`,
`data/raw`, `curation`, `reports`, and the exact `/tmp` worklist/publication
outputs found only the expected `PATHS.tsv` row, generated YAML, raw ChEBI row,
worklist/review-readiness rows, and mentions in the prior
`CHEBI:176336` sibling report. It found no prior dedicated review report,
curation decision, curator-inventory row, generated activity observation,
molecular target, resistance, producer, or causal-graph claim resolving
`CHEBI:176337`.

## Evidence

The record's only antimicrobial assertion is inherited from ChEBI role
`CHEBI:35718`, `antifungal agent`. That role is sufficient to file the record
under `ANTIFUNGAL`; it is not a compound-specific mechanism, target, producer,
or assay claim.

The committed raw ChEBI row for exact `CHEBI:176337` has no source-literature
leads. This is sparser than the sibling `CHEBI:176336` `(3R,6Z)-nerolidol`,
which carries one ChEBI PubMed xref; the live OLS graph treats those two
records as enantiomers, so the sibling source lead should not be moved onto
this record without exact stereochemical support.

A bounded repository publication search found zero PubMed candidates for the
exact Standard InChIKey `FQTLCLSUCSAZDY-QKXCFHHRSA-N` and zero PubMed
candidates for the exact ChEBI label. Direct PubMed all-fields checks also
found zero results for the exact Standard InChIKey, exact ChEBI label, and exact
`(S)-cis-nerolidol` synonym. Semantic Scholar returned HTTP 429 or an invalid
response for the repository exact searches.

Searching the CAS xref `142-50-7` through PubMed is too broad to support this
record by itself: it returns papers about generic nerolidol, `(E)-nerolidol`,
and other stereoisomeric scopes. Those hits may be useful leads for a future
full literature pass, but none of the inspected titles and abstracts supplied a
primary exact `(3S,6Z)-nerolidol` antifungal assay, producer, or mechanism
claim.

No NCBI Taxonomy verification was needed for this pass because no exact source
lead named a producing organism, susceptible fungal organism, or resistance
organism.

## Completeness

The record is incomplete as a reviewed antifungal natural-product record. It
has exact ChEBI identity, structure, same-structure xrefs, a strictly broader
ChEBI parent, and an antifungal role, but no exact source-paper lead, no
curator-owned activity observation, no producer assertion, no molecular target,
no mode of action, no dataset, and no causal graph.

The empty mechanism fields are correct for a ChEBI `antifungal agent` seed. No
inspected source maps exact `(3S,6Z)-nerolidol` to an ergosterol, cell-wall,
membrane, energy-metabolism, virulence, or other fungal mechanism.

The empty `producer_organisms` and `activity_spectrum` slots should remain
empty until a curator finds and checks an exact-stereoisomer primary source.
The full worklist currently puts exact `CHEBI:176337` on only the `mechanism`
and `review-readiness` queues; it does not surface a producer or activity
candidate from this ChEBI definition.

The empty `molecular_targets`, `resistance_mechanisms`,
`clinical_status_assertions`, `datasets`, `discussions`, and `causal_graphs`
slots are preferable to over-scoped filler. No molecular target, resistance
mechanism, clinical status, public dataset accession, or evidence-backed causal
edge was identified in the bounded checks.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Exact `CHEBI:176337` has only source-level antifungal classification; it has no source-literature lead, curator-owned activity observation, producer assertion, mechanism, or target claim. | The generated record has zero source-literature leads, zero record evidence items, zero `activity_spectrum` rows, zero `producer_organisms`, zero targets, and no `mode_of_action`. Bounded exact InChIKey, label, and `(S)-cis-nerolidol` PubMed searches found no exact primary literature candidates. | Future curator-owned fields on `data/antibiotics/antifungal/3s-6z-nerolidol.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI later changes this term's role, xrefs, parent, or citations, the ChEBI inventory extractor owns that refresh. |

No blocker identity, structure, schema, xref, class, parentage, or audit-history
findings were found.

No minor findings.

## Recommended Edits

1. Search for exact `(3S,6Z)-nerolidol`, exact `(S)-cis-nerolidol`, and exact
   `142-50-7` primary sources before adding `activity_spectrum` rows. Require
   exact fungal organisms, assay methods, and MIC or other antimicrobial
   readouts instead of using generic nerolidol bioactivity as a proxy.
2. Preserve stereochemical scope while checking the broader nerolidol
   literature. Evidence for racemic `nerolidol`, `(6Z)-nerolidol`,
   `(E)-nerolidol`, `(3R,6Z)-nerolidol`, or `(3R,6E)-nerolidol` should not be
   attached to `CHEBI:176337` unless the source actually measured this exact
   stereoisomer or makes an explicit stereoisomer-independent claim.
3. Add `producer_organisms` only from a primary source or MIBiG accession that
   supports biosynthesis of exact `(3S,6Z)-nerolidol`; do not convert plant,
   food, or metabolomics detections into producer claims without a clear
   exact-structure scope.
4. Add `mode_of_action`, `mode_of_action_target_scope`, a fungal
   `MolecularTarget`, or a `CausalGraph` only if an inspected primary source
   maps exact `(3S,6Z)-nerolidol` to a biochemical effect or target. Do not
   infer a mechanism from the generic ChEBI antifungal role alone.
5. Keep future article identifiers out of same-structure `xrefs`; cite them
   from assertion-level `evidence` blocks only if their full text supports
   future producer, activity, dataset, mechanism, target, or causal-graph rows.

## Follow-up Checks

After any exact-term curation or source refresh, rerun:

1. `just validate data/antibiotics/antifungal/3s-6z-nerolidol.yaml`
2. `just validate-strict data/antibiotics/antifungal/3s-6z-nerolidol.yaml --out /tmp/antibioticmech-chebi-176337-validation.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-176337.tsv`
5. `just review-queue --limit 55`
6. `just qc`

Manually compare any future producer, activity, mechanism, or target assertion
against exact stereochemical identity. Confirm that claim-level evidence is
attached to `CHEBI:176337`, not only to a broader nerolidol stereochemical set.

## Additional Notes

All absence checks in this report used `rg --no-ignore --hidden`, so they
included ignored `reports/` files.

`runoak` was not available in this shell, so official ChEBI term checks were
performed against the EBI OLS4 HTTPS API.
