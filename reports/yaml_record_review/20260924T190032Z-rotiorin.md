# YAML Record Review: (−)-rotiorin

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antifungal/rotiorin.yaml`
- Started UTC: 2026-09-24T19:00:32Z
- Finished UTC: 2026-09-24T19:00:39Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `CHEBI:68699` |
| Label | `(−)-rotiorin` |
| Path | `data/antibiotics/antifungal/rotiorin.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concepts | `CHEBI:68699` from ChEBI 2026-08-30 |
| Maintained owner | Generated from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv`; source-owned identity, structure, class, role, parent, xref, and source-concept changes must go through the ChEBI extractor, raw inventory, `curation/decisions.tsv`, or the seeder rather than a hand edit to this YAML. |

The record resolves unambiguously. `data/antibiotics/PATHS.tsv` maps
`CHEBI:68699` to `ANTIFUNGAL/rotiorin`, and an ignored-inclusive search for
`CHEBI:68699`, `(−)-rotiorin`, and
`CJMOMVNHRUTOJX-SEAFAFSMSA-N` across `data/antibiotics`, `data/raw`,
`curation`, `reports/yaml_record_review`, `.git`, and the full `/tmp`
worklist/review TSVs found the expected ChEBI raw row, path row, worklist rows,
and target YAML, with no prior exact `CHEBI:68699` report or duplicate
`CJMOMVNHRUTOJX-SEAFAFSMSA-N` structure in the searched corpus.

## Validation

| Check | Result |
| --- | --- |
| `rg --no-ignore --hidden -n 'CHEBI:68699\|rotiorin\|CJMOMVNHRUTOJX' ...` | Pass: resolved the expected ChEBI raw row, path row, target record, worklist rows, and rotiorin-family sibling records; no prior exact `CHEBI:68699` report and no duplicate `CJMOMVNHRUTOJX-SEAFAFSMSA-N` record were found. Hidden and ignored files were included. |
| `just validate data/antibiotics/antifungal/rotiorin.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/rotiorin.yaml --out /tmp/rotiorin-validate-strict.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass: 2,939 records expected and present; 0 missing, unexpected, or drifted records; 0 `PATHS.tsv` discrepancies. The only diagnostic was the known unrelated `iclaprim`/`CHEBI:31724` CARD cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Pass: wrote the full TSV. `CHEBI:68699` appears on `mechanism`, `producer-candidate`, and `review-readiness`. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Pass: regenerated the full `review-readiness` queue and listed `CHEBI:68699` as `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| ChEBI OLS4 lookups for `CHEBI:68699` | Pass: the official exact search, term lookup, direct parent lookup, and direct `has role` lookup resolve and agree with the generated identity, structure, parent, xref, and antifungal role fields. |
| NCBI EFetch for `PMID:16792406` | Pass: resolved the ChEBI literature lead and its DOI. |
| NCBI PubMed ESearch | Pass: exact `rotiorin` title/abstract search found 4 rows on 2026-09-24, including `PMID:16792406`. The other three rows concern a `Penicillium expansum` metabolomics survey, host CETP inhibition by azaphilones, and chemical synthesis of a rotiorin analogue rather than an antimicrobial mode-of-action result for `(−)-rotiorin`. |

No narrower single-record term, reference, or history validator is exposed for
plain generated ChEBI records. The full-corpus `worklist` and `verify-corpus`
checks above are the documented repository checks for those concerns.

## Identity and Grounding

The YAML denotes one exact, ChEBI-grounded structure: neutral `(−)-rotiorin`,
ChEBI's `(9aR)` azaphilone.

| Claim | Review |
| --- | --- |
| ChEBI identity | The official OLS4 exact lookup for `CHEBI:68699` returned one ChEBI class labelled `(-)-rotiorin`, matching the YAML label apart from minus-glyph normalization. The OLS4 term endpoint reports the same identifier as active and non-obsolete. |
| Structure | OLS4, `data/raw/chebi_antimicrobials.tsv`, and the YAML agree on SMILES `CC[C@H](C)/C=C(C)/C=C/C1=CC2=CC3=C(C(C)=O)C(=O)O[C@@]3(C)C(=O)C2=CO1`, Standard InChI `InChI=1S/C23H24O5/c1-6-13(2)9-14(3)7-8-17-10-16-11-19-20(15(4)24)22(26)28-23(19,5)21(25)18(16)12-27-17/h7-13H,6H2,1-5H3/b8-7+,14-9+/t13-,23+/m0/s1`, Standard InChIKey `CJMOMVNHRUTOJX-SEAFAFSMSA-N`, formula `C23H24O5`, neutral charge, average mass `380.44`, and monoisotopic mass `380.16237`. |
| Synonym | OLS4 and the raw ChEBI row report the same exact IUPAC synonym retained in the record: `(9aR)-3-acetyl-6-[(1E,3E,5S)-3,5-dimethylhepta-1,3-dien-1-yl]-9a-methyl-2H-furo[3,2-g]isochromene-2,9(9aH)-dione`. |
| ChEBI parents | The OLS4 parent lookup returns exactly the five broader classes in `parent_compounds`: `CHEBI:26979` organic heterotricyclic compound, `CHEBI:37581` gamma-lactone, `CHEBI:50941` azaphilone, `CHEBI:51689` enone, and `CHEBI:51867` methyl ketone. |
| Activity role and filing class | OLS4 returns `CHEBI:35718` antifungal agent as a direct `has role` target for `CHEBI:68699`; filing the record as `ANTIFUNGAL` follows the retained ChEBI antifungal role. OLS4 also reports the non-antimicrobial `CHEBI:76960` Chaetomium metabolite role, which the generated `activity_roles` field correctly omits. |
| Xrefs | OLS4 and `data/raw/chebi_antimicrobials.tsv` both list the exact-equivalence `chemspider:255838` and `reaxys:15840534` xrefs retained in the YAML. The remaining database cross-reference on the OLS term is the PubMed literature identifier and correctly does not appear in the structure-identity `xrefs` field. |

No wrong identity, grounding, xref, parent, or class conflict was found.

## Evidence

The record has no `evidence` array and no object-level evidence-bearing
assertions. That is honest for a generated ChEBI-only seed: identity, role,
parents, xrefs, and structure reproduce from ChEBI, while there are no molecular
targets, resistance mechanisms, MIC measurements, producer claims, clinical
assertions, datasets, or causal edges that would need primary-paper evidence.

`CHEBI:68699` carries one source literature lead:

| PMID | DOI | Review |
| --- | --- | --- |
| `PMID:16792406` | `10.1021/np060051v` | PubMed resolves this to Kanokmedhakul et al. 2006, `Antifungal azaphilones from the fungus Chaetomium cupreum CC3003.` The abstract supports isolation of `(−)-rotiorin` as one of six azaphilones from *Chaetomium cupreum* CC3003 and reports an anti-*Candida albicans* IC50 value of 24.3 microg/mL for compound 4, but it does not expose the assay method, a molecular target, or a mode of action. |

The bounded PubMed title/abstract search for `rotiorin` also returned three
records that should not be promoted into antimicrobial mechanism or activity
curation without full-text evidence:

| PMID | DOI | Review |
| --- | --- | --- |
| `PMID:26863302` | `10.1371/journal.pone.0149012` | Kim et al. 2016 detected rotiorin as a polyketide metabolite in a *Penicillium expansum* fermentation/transcriptomics study. The abstract gives producer-side fermentation context, not direct antimicrobial assay or mechanism evidence for `(−)-rotiorin`. |
| `PMID:10344570` | `10.7164/antibiotics.52.160` | Tomoda et al. 1999 assayed fungal azaphilones against host cholesteryl ester transfer protein. Its rotiorin result is a mammalian CETP assay, not an antifungal target claim. |
| `PMID:5167264` | `10.1039/j39710003566` | Chong et al. 1971 report chemistry and synthesis of a rotiorin analogue. The PubMed record exposes no antimicrobial activity or mechanism abstract. |

## Completeness

`CHEBI:68699` remains intentionally thin. It has no CARD source concept, no CARD
target, no CARD resistance edge, no curator-authored `mode_of_action`, no
molecular target, no activity observation, no producer organism, and no causal
graph.

The full worklist places this identifier only on:

- `mechanism`: absent mode of action, with `0 CARD target(s), 0 resistance edge(s) to build on`
- `producer-candidate`: `Chaetomium cupreum — "isolated from", SOURCE only — may not be the producer`
- `review-readiness`: `MECHANISM_REVIEW`, with 1 source literature lead, 0 top-level record evidence items, and 0 targets

The same complete worklist has no exact `CHEBI:68699` row for
`target-evidence`, `minted`, `unknown-mech`, `moa-scope`, `aro-class`,
`xref-unverified`, `xref-name-conflict`, `xref-span-conflict`,
`multi-component`, `activity-candidate`, `unnamed-producer`, or
`structure-unreviewed`.

An NCBI PubMed title/abstract search for `rotiorin` found 4 rows on
2026-09-24. That bounded search rediscovers ChEBI's own `PMID:16792406` lead
and several rotiorin-family papers, but none of the inspected abstracts exposes
an antifungal molecular target, MIC-style measurement, or causal mechanism for
the exact `CHEBI:68699` structure.

Empty slots for `mode_of_action`, `mode_of_action_target_scope`,
`molecular_targets`, `activity_observations`, `resistance_mechanisms`,
`producer_organisms`, `clinical_status_assertions`, `datasets`, and
`causal_graphs` are preferable to inferring a target, measured activity, or
biosynthetic organism from the ChEBI antifungal role and isolation statement.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The record still needs compound-specific mechanism review before it can become `REVIEWED`. | The YAML has no `mode_of_action`, no molecular targets, no activity observations, and no causal graph. `just worklist` keeps `CHEBI:68699` on `mechanism` and `review-readiness` even though ChEBI supplies `PMID:16792406` as a source literature lead. The PubMed abstract for that lead supports antifungal activity against *Candida albicans* but does not expose a compound-specific molecular target or mode of action for `(−)-rotiorin`. | Future curator-owned fields on `data/antibiotics/antifungal/rotiorin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |
| Minor | The species-level producer lead should be checked before any optional `producer_organisms` row is added. | `just worklist` places `CHEBI:68699` on `producer-candidate` because the ChEBI definition says the compound was isolated from *Chaetomium cupreum*. `PMID:16792406` resolves to a paper about azaphilones from *C. cupreum* CC3003, and `PMID:26863302` reports later rotiorin detection in *P. expansum* KACC 40815. Those are useful producer leads, but the abstract alone does not expose NCBITaxon identifiers or enough isolate and strain context for a schema-ready producer claim. | Future curator-owned `producer_organisms` rows on `data/antibiotics/antifungal/rotiorin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blocker findings found.

## Recommended Edits

1. Do not edit the source-owned identity, structure, synonym, class, role,
   parent, xref, or source-concept fields for `CHEBI:68699`: they reproduce
   from the official ChEBI row, `data/raw/chebi_antimicrobials.tsv`, and
   `data/antibiotics/PATHS.tsv`.
2. Inspect the full text and supplementary data for Kanokmedhakul et al. 2006,
   DOI `10.1021/np060051v`, before adding any `activity_observations`. Add the
   *Candida albicans* observation only when the source states assay method,
   value semantics, and exact units in a schema-ready way; an abstract IC50
   alone is not an MIC and is not sufficient to infer assay context.
3. Leave `producer_organisms` empty unless the full text, supplementary data,
   a future ChEBI-adjacent source, or an exact biosynthetic source identifies a
   producing organism with enough taxon and strain context for a schema-ready
   claim.
4. Add `mode_of_action`, `molecular_targets`, or `causal_graphs` only after a
   primary source identifies how `(−)-rotiorin` inhibits *Candida* or another
   microbial system.
5. Keep `curation_status: SEEDED` until identity, structure, class, and the
   antimicrobial mode of action have all been checked and any target claims
   have primary citations.

## Follow-up Checks

If a future curator changes this record:

- Run `just validate-strict data/antibiotics/antifungal/rotiorin.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Run `just verify-corpus` to prove source-owned generated fields still match
  `data/raw/`.
- Run `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` and
  confirm `CHEBI:68699` leaves `mechanism`, `producer-candidate`, and
  `review-readiness` only after exact mechanism review and producer triage have
  actually been curated.
- Re-run `just lint`, `git diff --check`, and `git diff --cached --check`
  before opening a PR.

## Additional Notes

The same `PMID:16792406` source also supports ChEBI seed rows for
`CHEBI:68785` rotiorinol A, `CHEBI:68786` rotiorinol C, and `CHEBI:68787`
rubrorotiorin. It should be reviewed per exact compound because the source
abstract reports activity for compounds 1, 3, 4, and 6 and leaves at least one
isolated azaphilone inactive or unreported in the abstract.
