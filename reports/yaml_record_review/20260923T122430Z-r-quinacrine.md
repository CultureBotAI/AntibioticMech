# YAML Record Review: (R)-quinacrine

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antiprotozoal/r-quinacrine.yaml
- Started UTC: 2026-09-23T12:24:30Z
- Finished UTC: 2026-09-23T12:24:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | CHEBI:49845 |
| Label | (R)-quinacrine |
| Path | data/antibiotics/antiprotozoal/r-quinacrine.yaml |
| Class | ANTIPROTOZOAL |
| Status | SEEDED |
| Grounding | EXACT |
| Source concept | CHEBI:49845, source version 2026-08-30 |
| Standard InChIKey | GPKJTRJOBQGKQK-MRXNPFEDSA-N |
| Maintained owner | Generated from data/raw/chebi_antimicrobials.tsv plus data/antibiotics/PATHS.tsv; do not hand-edit the generated YAML |

The target resolves unambiguously to `data/antibiotics/antiprotozoal/r-quinacrine.yaml`.
An ignored-file-inclusive search for `CHEBI:49845`, `(R)-quinacrine`, `r-quinacrine`,
`GPKJTRJOBQGKQK-MRXNPFEDSA-N`, `pdb-ccd:QUN`, and
`antibioticmech:chebi-cfb3905c56` found only:

- `data/raw/chebi_antimicrobials.tsv`
- `data/antibiotics/PATHS.tsv`
- `data/antibiotics/antiprotozoal/r-quinacrine.yaml`
- `curation/record_review_queue.tsv`

The same search found no prior `reports/yaml_record_review/` report for this stem.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiprotozoal/r-quinacrine.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/r-quinacrine.yaml --out /tmp/antibioticmech-r-quinacrine-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed; 2939 records expected, 2939 on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-r-quinacrine-worklist.tsv` | Passed; `CHEBI:49845` appears only in `mechanism` and `review-readiness`. |
| `just review-queue --limit 120` | Passed; `CHEBI:49845` is the first queue entry after the already reviewed `(R)-oct-1-en-3-ol` row. |

The repository exposes `validate`, `validate-strict`, `verify-corpus`,
`worklist`, `review-queue`, and `lint`; it does not expose narrower
single-record term, reference, or history validators for a plain ChEBI-seeded
record with no record-level citations.

## Identity and Grounding

The ChEBI identity is exact. OLS4 resolves `CHEBI:49845` to `(R)-quinacrine`
with the same SMILES, Standard InChI, Standard InChIKey, formula, charge,
average mass, monoisotopic mass, and `pdb-ccd:QUN` xref carried by the local
record.

The stereochemical boundary is also correct:

- `CHEBI:8711` is the achiral parent `quinacrine` and has the non-stereospecific
  InChIKey `GPKJTRJOBQGKQK-UHFFFAOYSA-N`.
- `CHEBI:49845` is the `(R)` enantiomer and OLS4 reports its
  `RO:0018039` enantiomer relation to `CHEBI:37597`, `(S)-quinacrine`.
- The local sibling `data/antibiotics/antiprotozoal/s-quinacrine.yaml` carries
  the mirror-image InChIKey `GPKJTRJOBQGKQK-INIZCTEOSA-N`.

PDB CCD component `QUN` is an exact chemical xref, not a macromolecular PDB
structure accession. Its official CIF uses the same formula, formal charge,
canonical `CCN(CC)CCC[C@@H](C)...` SMILES, Standard InChI, InChIKey, and
`(4R)` systematic name, so the generated `pdb-ccd:QUN` xref is scoped correctly.

The `parent_compounds` value `CHEBI:8711` is strictly broader than this record;
none of the achiral record's xrefs or drug xrefs were inappropriately copied
onto the enantiomer-specific record.

One synonym is over-scoped. In ChEBI's OBO synonym annotations,
`QUINACRINE` on `CHEBI:49845` has `scope: hasRelatedSynonym`, while the local
generated record emits it as an `EXACT_SYNONYM`. The unsuffixed `Quinacrine`
name is also a related synonym on achiral `CHEBI:8711`, so this should not be
presented as exact identity for the `(R)` enantiomer.

## Evidence

This record has no record-level `evidence`, no `activity_spectrum`, no
`molecular_targets`, no `resistance_mechanisms`, and no `causal_graphs`.
That is structurally valid for a ChEBI-seeded entry, but it means the record has
only upstream database provenance and no primary-paper support for a
compound-specific antimicrobial mechanism.

The queue-derived source leads agree with the record shape:

- `/tmp/antibioticmech-r-quinacrine-worklist.tsv` lists `CHEBI:49845` under
  `mechanism` with `0 CARD target(s), 0 resistance edge(s) to build on`.
- The same TSV lists it under `review-readiness` with
  `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record
  evidence item(s), 0 target(s)`.

Bounded publication checks did not identify an immediately supportable exact
mechanism for this `(R)` enantiomer:

- Exact PubMed/Semantic Scholar query:
  `"(R)-quinacrine" OR "GPKJTRJOBQGKQK-MRXNPFEDSA-N" OR "CHEBI:49845"`.
  PubMed returned PMID:12808118, an antiprion enantiomer paper in ScN2a cells;
  it is outside the antiprotozoal mechanism modeled by this record.
- Broader PubMed query:
  `quinacrine mepacrine antiprotozoal antimicrobial mechanism`.
  This found Giardia treatment and clinical-efficacy leads, including
  PMID:42323544, but not a primary source resolving a specific quinacrine
  target.
- Focused PubMed query:
  `quinacrine Giardia mechanism`.
  This found PMID:7131192, which reports rapid quinacrine-HCl effects on Giardia
  trophozoite attachment but explicitly left quinacrine's attachment mechanism
  unresolved.
- Focused PubMed query:
  `mepacrine giardiasis mechanism OR quinacrine antiprotozoal mechanism`.
  This mostly duplicated achiral quinacrine or treatment-review leads.

Semantic Scholar returned HTTP 429 for the two provider searches, so PubMed was
the only successful literature provider. Google Scholar was not used.

## Completeness

The generated identity, source concept, ChEBI grounding, parent boundary,
PDB CCD xref, structure fields, and history are sufficient for a ChEBI-seeded
entry after the synonym-scope defect is corrected upstream.

Consequential gaps:

- `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, and
  `causal_graphs` are absent. This record should remain below `REVIEWED` until
  a curator either adds an evidence-backed mechanism or adds a curator-owned
  veto explaining why the known antiprotozoal mechanism is still unresolved.
- `activity_spectrum` is absent. A future activity observation should use the
  actual measured organism, strain or isolate, assay, value, qualifier, units,
  and claim-level evidence rather than generalizing clinical giardiasis use to
  Giardia species-wide potency.

Empty optional slots that are acceptable in this generated seed:

- `resistance_mechanisms`: no CARD target or resistance edge was available.
- `producer_organisms`: no producer-candidate worklist row was present.
- `clinical_status_assertions`: no regulatory source was imported for this
  enantiomer-specific record.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | A related ChEBI synonym was emitted as an exact synonym. | OLS4 lists `QUINACRINE` on `CHEBI:49845` with `scope: hasRelatedSynonym`, but `r-quinacrine.yaml` marks it `EXACT_SYNONYM`; the same unsuffixed lexical form belongs to achiral `CHEBI:8711`, so the generated record overstates exact identity for the isolated `(R)` enantiomer. | Preserve synonym scope in the ChEBI extraction/seeding path, then refresh `data/raw/chebi_antimicrobials.tsv` and re-run `just seed-apply`. The local flattening currently enters through `scripts/seed_from_sources.py`. |
| Major | The record has no reviewed antiprotozoal mechanism. | `just worklist --limit 0 --tsv /tmp/antibioticmech-r-quinacrine-worklist.tsv` lists `CHEBI:49845` in `mechanism` and `review-readiness`; the exact-enantiomer search found only an antiprion lead, while bounded PubMed searches for achiral quinacrine/Giardia leads did not find support for a representable exact mechanism. | Add an evidence-backed curator-owned mechanism to `data/antibiotics/antiprotozoal/r-quinacrine.yaml` through `write_validated_antibiotic`, or add a curator-owned veto explaining why the mechanism remains unknown. |

No blockers found. No minor findings found.

## Recommended Edits

1. Preserve ChEBI synonym scope in the import path so `hasRelatedSynonym`
   values remain related or are omitted from `CompoundSynonym` until the schema
   can represent them. Re-extract ChEBI and re-seed; the regenerated
   `(R)-quinacrine` record should no longer label `QUINACRINE` as exact.
2. Curate the mode of action only from a source that supports a specific
   `(R)-quinacrine` claim, or explicitly document that only achiral or racemic
   quinacrine leads were found. If an achiral mechanism is adopted for both
   enantiomers, state why that stereochemical projection is justified.
3. Add only measured `activity_spectrum` rows with complete method, organism,
   measurement, and evidence. Clinical success in refractory giardiasis is
   useful as a lead, but it does not carry MIC or target claims.

## Follow-up Checks

- Re-run `just seed-apply`, then `just validate data/antibiotics/antiprotozoal/r-quinacrine.yaml`
  and `just verify-corpus --summary`; confirm the record no longer emits
  ChEBI's `hasRelatedSynonym` value as `EXACT_SYNONYM`.
- Run the ChEBI exact-term OLS4 check for `CHEBI:49845` and confirm the
  generated synonym list reflects the OBO synonym scopes.
- After any mechanism or activity curation, run
  `just validate-strict data/antibiotics/antiprotozoal/r-quinacrine.yaml --out /tmp/antibioticmech-record-validation.tsv`,
  `just verify-corpus --summary`, `just worklist --limit 0 --tsv /tmp/antibioticmech-r-quinacrine-worklist.tsv`,
  and `just lint`.

## Additional Notes

- The first resolution search accidentally included nonexistent `inputs/`,
  but it still returned the relevant `data/` matches before exiting with code 2;
  a second ignored-inclusive search over existing `data/`, `curation/`, and
  `reports/` paths confirmed the target and found no prior report.
- The repository exposes only full-corpus worklist categories for mechanism,
  cross-reference, producer, target-evidence, and review-readiness checks.
  `CHEBI:49845` was absent from all inspected queues except `mechanism` and
  `review-readiness`.
- The CARD self-contradictory cross-reference warning printed by `just
  verify-corpus` and `just worklist` concerns `ARO:3000337` / iclaprim and does
  not affect this ChEBI-only record.
