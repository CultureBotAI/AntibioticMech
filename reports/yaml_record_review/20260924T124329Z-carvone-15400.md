# YAML Record Review: (−)-carvone

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/carvone-15400.yaml`
- Started UTC: 2026-09-24T12:43:29Z
- Finished UTC: 2026-09-24T12:43:29Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antifungal/carvone-15400.yaml` |
| Class | `ANTIFUNGAL` |
| ID | `CHEBI:15400` |
| Label | `(−)-carvone` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

`carvone-15400.yaml` is a generated exact ChEBI record for `CHEBI:15400`.
ChEBI contributes the individual neutral `(−)-carvone` / `(R)-carvone`
structure, the antifungal-agent activity role `CHEBI:35718`, the generic
`carvone` parent `CHEBI:38265`, nine registry or structure xrefs, one source
literature lead, and no curator-owned `mode_of_action`, target scope,
record-level evidence, molecular target, activity observation, resistance
mechanism, producer organism, clinical assertion, dataset, discussion, or
causal graph.

## Validation

| Command | Result |
|---|---|
| `rg --no-ignore --hidden -n 'CHEBI:15400|15400|carvone' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed with ignored and hidden files included; it found the generated `CHEBI:15400` YAML, `PATHS.tsv` lock row, exact ChEBI inventory row, current review-queue rows, and the earlier exact `(+)-carvone` report, but no prior exact `CHEBI:15400` review report. |
| `rg --no-ignore --hidden -n 'CHEBI:15400|carvone' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv` | Passed with ignored and hidden files included; no exact adopted ARO, BindingDB, Drugs@FDA, MIBiG, PHI-base, or grounding-decision row exists for `CHEBI:15400` or exact `(−)-carvone`. |
| `just validate data/antibiotics/antifungal/carvone-15400.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/carvone-15400.yaml --out /tmp/carvone-15400-validate-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed with the known unrelated CARD iclaprim/CHEBI:31724 xref refusal; all 2,939 expected records were on disk, with no missing records, no unexpected records, no drifted fields, and no `PATHS.tsv` issues. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Passed; exact `CHEBI:15400` appears only in the `mechanism` queue and the derived `review-readiness` queue. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; exact `CHEBI:15400` is row 145 with `MECHANISM_REVIEW`, 1 committed source-literature lead, 0 record evidence items, and 0 targets. |
| Live OLS4 lookup for `CHEBI:15400` | Passed; OLS resolved one non-obsolete ChEBI class with `obo_id` `CHEBI:15400`, label `(-)-carvone`, the generated formula, SMILES, Standard InChI, Standard InChIKey, CAS xref, and PubMed xref. |
| Live ChEBI page lookup for `CHEBI:15400` | Passed; current ChEBI agreed with the generated Unicode label, ASCII label, definition, formula, charge, average mass, monoisotopic mass, SMILES, InChI, InChIKey, CAS xref, `CHEBI:35718` role, `CHEBI:50904` allergen role, `CHEBI:76924` plant-metabolite role, `CHEBI:38265` parent, and `PMID:11380545` citation. |
| PubMed efetch for `PMID:11380545` and `PMID:27682121` | Passed; both PMIDs resolved. `PMID:11380545` is the committed ChEBI literature lead, and `PMID:27682121` is an exact R/S-carvone antifungal activity lead found during review. |
| PMC full-text lookup for `PMID:27682121` / `PMC5023262` | Passed; the article reports exact `R-carvone` and `S-carvone` MIC testing against `Fusarium verticillioides` M3125. |
| Europe PMC full-text query `"R-carvone" MIC Fusarium` | Passed; the first exact primary research hit was `PMID:27682121`, amid review hits and less direct records. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no generated YAML mutation.

## Identity and Grounding

| Claim | Assessment |
|---|---|
| Identifier and label | Supported. `PATHS.tsv`, the generated YAML, the committed ChEBI row, OLS, and current ChEBI all resolve `CHEBI:15400` to exact `(−)-carvone` / `(-)-carvone`. |
| Structure | Supported. The generated SMILES, Standard InChI, Standard InChIKey `ULDHMXUKGWMISQ-SECBINFHSA-N`, formula `C10H14O`, charge `0`, average mass, and monoisotopic mass match `data/raw/chebi_antimicrobials.tsv`, OLS, and current ChEBI for exact `CHEBI:15400`. |
| Stereochemistry | Supported. The record denotes the R enantiomer, not `(+)-carvone` `CHEBI:15399` or achiral/generic `carvone` `CHEBI:38265`; the distinct R-enantiomer InChIKey suffix `SECBINFHSA-N` agrees with the ChEBI source. |
| Parent compound | Supported. Exact `(−)-carvone` is an `is_a` child of the broader generic carvone term `CHEBI:38265` in current ChEBI and in the committed source row. |
| Xrefs | Supported as exact same-structure rows by ChEBI for the bounded review. OLS and current ChEBI still list CAS `6485-40-1`, KEGG `C01767`, KNApSAcK `C00000807`, LIPID MAPS `LMPR0102090007`, PPDB `125`, UM-BBD `c0627`, Beilstein `3588497`, Gmelin `83213`, and Reaxys `2206714`. |
| Source concept | Supported. The only exact source concept is ChEBI `CHEBI:15400` at version `2026-08-30`, with minted key `antibioticmech:chebi-4d4e9598a2`; exact ignored/hidden-inclusive searches found no adopted external inventory or local curator decision that changes this record's identity. |

## Evidence

| Claim | Assessment |
|---|---|
| Exact `CHEBI:15400` denotes neutral `(−)-carvone`. | Supported by the committed ChEBI inventory row, OLS, and current ChEBI. |
| Exact `(−)-carvone` has role `CHEBI:35718`, antifungal agent. | Supported as a ChEBI database assertion. The record correctly retains this in `activity_roles` and uses it only to choose `ANTIFUNGAL` as the filing class. |
| Exact `(−)-carvone` is broader-carvone child `is_a CHEBI:38265` and enantiomeric sibling of `(+)-carvone` `CHEBI:15399`. | Supported by current ChEBI. The generic record and the opposite enantiomer stay out of `xrefs`, so the YAML does not overstate either relation as same-structure identity. |
| `PMID:11380545` is useful stereochemistry literature but not antifungal evidence. | PubMed resolves the ChEBI source PMID to Nilsson et al. 2001, which tested antigen formation and cross-reactivity of R/S-carvone analogues in the allergy/dermatitis context. It does not report a fungal assay, MIC, molecular antifungal target, or antimicrobial mode of action. |
| `PMID:27682121` is an exact activity lead for future curation. | Pizzolitto et al. 2015 tested exact `R-carvone` and `S-carvone` among ketone terpenes against `Fusarium verticillioides` M3125 and reported R-carvone MIC `4.56 mM`. The full text gives a 7-day incubation context and the article states experiments were performed twice in triplicate, but the record has no `ActivityObservation` for this result yet. |

The record makes no molecular-target, activity-observation,
resistance-mechanism, producer, clinical, dataset, or causal-edge claims, so
there are no unsupported claim-level citations in the YAML.

## Completeness

| Area | Assessment |
|---|---|
| Identity and structure | Complete enough for read-only review. Exact `CHEBI:15400`, the generated record, OLS, current ChEBI, `data/raw/chebi_antimicrobials.tsv`, and `PATHS.tsv` agree. |
| Filing class and activity role | Complete enough for source-seeded filing. The `ANTIFUNGAL` directory and `CHEBI:35718` activity role faithfully reproduce ChEBI's current antifungal-agent assertion for exact `(−)-carvone`. |
| Mode of action | Incomplete. No antimicrobial mode is curator-reviewed or source-seeded. |
| Molecular targets and causal graphs | Complete as empty for the bounded exact-compound review. The inspected exact R-carvone antifungal lead is an activity/QSAR paper, not a molecular-target paper, and it does not identify a specific fungal target or enough directed mechanism steps for a causal graph. |
| Activity observations | Incomplete. Exact R-carvone MIC evidence exists in `PMID:27682121` for `F. verticillioides` M3125, but the YAML has no `activity_spectrum` entry. |
| Resistance mechanisms | Complete as empty for the adopted sources. Exact ignored/hidden-inclusive searches found no CARD or PHI-base row for `CHEBI:15400`. |
| Producers and clinical status | Complete as empty for the adopted sources. Exact ignored/hidden-inclusive searches found no MIBiG producer or Drugs@FDA status row for `(−)-carvone`. |
| Datasets and discussions | Complete as empty for the bounded review. The inspected sources did not expose a public exact `(−)-carvone` dataset accession or a concrete unresolved identity conflict that needs a `Discussion` before activity/mechanism curation. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record cannot satisfy the `REVIEWED` gate because it has no curator-owned antimicrobial mode of action. | `data/antibiotics/antifungal/carvone-15400.yaml` has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, or `causal_graphs`; `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` keeps exact `CHEBI:15400` on the `mechanism` queue. | Future curator-owned mechanism, target, discussion, and causal-graph additions on `data/antibiotics/antifungal/carvone-15400.yaml`, written with `record_curation_event` and `write_validated_antibiotic`. |
| Minor | The ChEBI source-literature lead is adjacent rather than direct antifungal support. | `PMID:11380545` supports carvone antigen formation and stereochemistry in animals and allergic patients, but it does not support an antifungal activity observation or mode of action for exact `(−)-carvone`. | No generated record edit needed for a read-only review; future curator-owned claim evidence on `data/antibiotics/antifungal/carvone-15400.yaml` should cite primary antifungal assay literature instead. |
| Minor | The record omits an exact MIC observation that has a primary source. | `PMID:27682121` tested R-carvone against `Fusarium verticillioides` M3125 and reported a 4.56 mM MIC, but `activity_spectrum` is empty. | Future curator-owned activity additions on `data/antibiotics/antifungal/carvone-15400.yaml`, written with `record_curation_event` and `write_validated_antibiotic`. |

No blocker findings.

## Recommended Edits

1. Add an `ActivityObservation` for exact R-carvone against `Fusarium
   verticillioides` M3125 from `PMID:27682121`, preserving the 7-day assay
   context, MIC `4.56 mM`, and tested strain.

   Maintained owner: `data/antibiotics/antifungal/carvone-15400.yaml`.

2. Leave `PMID:11380545` as ChEBI source provenance only; do not use it as
   claim-level evidence for antifungal activity or an antimicrobial mechanism.

   Maintained owner: source-faithful generated provenance in
   `data/raw/chebi_antimicrobials.tsv`, plus future curator-owned evidence on
   `data/antibiotics/antifungal/carvone-15400.yaml` when claims are added.

3. Keep `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`,
   and `causal_graphs` empty until a full-text primary source supports an exact
   R-carvone fungal target or directed antifungal mechanism.

   Maintained owner: future curator-owned fields on
   `data/antibiotics/antifungal/carvone-15400.yaml`.

## Follow-up Checks

- Rerun `just validate data/antibiotics/antifungal/carvone-15400.yaml`.
- Rerun `just validate-strict data/antibiotics/antifungal/carvone-15400.yaml --out /tmp/carvone-15400-validate-strict.tsv`.
- Rerun `just verify-corpus`.
- Rerun `just worklist --limit 0 --tsv /tmp/carvone-15400-worklist.tsv` and
  confirm exact `CHEBI:15400` leaves `activity-candidate` only after any future
  exact R-carvone activity edit, and leaves `mechanism` only after a
  curator-reviewed mechanism edit.
- Rerun `just review-queue --limit 0 --tsv /tmp/carvone-15400-review-queue.tsv`
  and confirm exact `CHEBI:15400` advances past `MECHANISM_REVIEW` only after a
  supported mechanism is present.
- Repeat an ignored/hidden-inclusive exact search for `CHEBI:15400`,
  `antibioticmech:chebi-4d4e9598a2`, `ULDHMXUKGWMISQ-SECBINFHSA-N`,
  `6485-40-1`, and any PMID/DOI cited in future claim-level evidence.
- Manually recheck current ChEBI for `CHEBI:15400`, `CHEBI:15399`, and
  `CHEBI:38265` so a future curation PR preserves the R/S/generic carvone
  boundary.

## Additional Notes

- Searches used to establish local absence included ignored files.
- The related reports and source rows for `CHEBI:15399` and `CHEBI:38265` were
  treated as leads only. They are sibling structures, not evidence that
  `(−)-carvone` itself has every generic or opposite-enantiomer activity.
- `PMID:27682121` also measured repellent effects against `Sitophilus zeamais`,
  an insect vector of `F. verticillioides`; that is out of scope for
  `ActivityObservation`, which names microbial targets.
- This report is read-only review output. No generated record, upstream
  inventory, decision row, page, queue, or curation-history entry was edited.
