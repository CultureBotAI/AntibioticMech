# YAML Record Review: (Z)-tomaymycin

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/z-tomaymycin.yaml`
- Started UTC: `2026-09-24T05:09:01Z`
- Finished UTC: `2026-09-24T05:10:06Z`
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Path | `data/antibiotics/antibacterial/z-tomaymycin.yaml` |
| Identifier | `CHEBI:167318` |
| Label | `(Z)-tomaymycin` |
| Class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source provenance | ChEBI `CHEBI:167318`, version `2026-08-30` |
| Source role | `CHEBI:33282` antibacterial agent |
| Parent compound | `CHEBI:157681` tomaymycin |
| Structure source | ChEBI |
| Standard InChIKey | `UQVNRKBFAXNOGA-OHLDGCSVSA-N` |

This review covered exactly one generated ChEBI-seeded record. The generated
YAML was read in full and was not edited.

## Validation

| Check | Result |
| --- | --- |
| `just validate data/antibiotics/antibacterial/z-tomaymycin.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/z-tomaymycin.yaml --out /tmp/z-tomaymycin-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2,939 records expected, 2,939 on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, and 0 stale lockfile rows. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/z-tomaymycin-worklist.tsv` | Passed; exact `CHEBI:167318` appeared only on `mechanism` and `review-readiness`, with 0 CARD targets and 0 resistance edges to build on. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; at report selection time, `CHEBI:167318` was the first queue row without an exact one-record review report after matching prior report identifiers and normalized report headers. |
| `find reports/yaml_record_review -maxdepth 1 -type f -name '*z-tomaymycin*' -print` | Passed; no prior exact `z-tomaymycin` report was found. `find` included ignored files. |
| `rg --no-ignore --hidden -n "CHEBI:167318|\(Z\)-tomaymycin|z-tomaymycin|tomaymycin" data/antibiotics data/raw curation reports/yaml_record_review` | Passed; found the exact generated record, `PATHS.tsv` lock row, ChEBI inventory row, review-queue row, adjacent `(E)-tomaymycin` and broad tomaymycin rows, and the prior `(E)-tomaymycin` review report. The search included ignored files. |
| OLS4 ChEBI API checks for `CHEBI:167318`, `CHEBI:167317`, `CHEBI:157681`, and `CHEBI:33282` | Passed; OLS resolved the exact `(Z)-tomaymycin` term, sibling `(E)-tomaymycin`, parent `tomaymycin`, and `antibacterial agent`. |
| PubMed/Semantic Scholar exact search for `(Z)-tomaymycin`, `Z-tomaymycin`, `CHEBI:167318`, and `UQVNRKBFAXNOGA-OHLDGCSVSA-N` | Partial pass; PubMed returned exact `PMID:6833147`; Semantic Scholar did not return a usable exact response. |
| PubMed lookup for `6833147` | Passed; resolved Tozuka et al. 1983, DOI `10.7164/antibiotics.36.276`. |
| PubMed/Semantic Scholar broad search for `Z-tomaymycin` or `tomaymycin` | Partial pass; PubMed returned 20 parent-compound and pyrrolobenzodiazepine leads, while Semantic Scholar returned HTTP 429. |

No narrower single-record term, reference, or history validator is exposed for a
plain ChEBI-seeded record in this repository; the full-corpus checks above were
the narrowest documented checks for those contracts.

## Identity and Grounding

The record identity is internally consistent with ChEBI's exact
`CHEBI:167318` entry:

| Claim | Review |
| --- | --- |
| Label and identifier | Supported. OLS4 resolves `CHEBI:167318` to `(Z)-tomaymycin` and describes it as the `(Z)` isomer of tomaymycin. |
| Structure | Supported. OLS4 reports the same formula `C16H20N2O4`, Standard InChI, Standard InChIKey `UQVNRKBFAXNOGA-OHLDGCSVSA-N`, zero charge, average mass `304.346`, monoisotopic mass `304.14231`, and SMILES as the generated record. |
| E/Z boundary | Supported. OLS4 resolves sibling `CHEBI:167317` as `(E)-tomaymycin`; its Standard InChI carries `/b9-4+/` and InChIKey `UQVNRKBFAXNOGA-LWTNMJDUSA-N`, while exact `CHEBI:167318` carries `/b9-4-/` and InChIKey `UQVNRKBFAXNOGA-OHLDGCSVSA-N`. |
| Parent relation | Supported. The generated `CHEBI:157681` parent resolves to the broader `tomaymycin` record rather than either exact olefinic isomer. |
| Antibacterial role | Supported as a ChEBI source claim. The source inventory row and generated `activity_roles` contain `CHEBI:33282`, and OLS4 resolves that term as `antibacterial agent`. |

The record has one upstream source concept, `CHEBI:167318`, and its minted
source key is used only as ChEBI provenance. No ARO, CARD-only, PubChem, or
curator source concept is merged into this record.

## Evidence

The generated YAML has no record-level `evidence`, `mode_of_action`,
`molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, or
`causal_graphs`. That is structurally allowed because the record inherits its
classification from the committed ChEBI seed row, but it means there is no
curator-owned claim-level evidence yet.

`PMID:6833147` is a direct lead for this exact term. PubMed and the J-STAGE
landing page resolve it to Tozuka et al. 1983, "Studies on tomaymycin. II.
Total syntheses of the antitumor antibiotics, E-and Z-tomaymycins"; the
inspected abstract reports synthesis of naturally occurring
`(E)-tomaymycin` and olefinic `(Z)-tomaymycin`, and states that the `Z` isomer
showed the same antibacterial activity as the `E` isomer. That supports ChEBI's
source-level antibacterial lead for exact `(Z)-tomaymycin`, but the inspected
HTML did not expose organism, strain, assay, or endpoint values specific enough
to curate an `ActivityObservation`.

The broad tomaymycin search returned mechanistic and biosynthetic leads,
including parent-tomaymycin, synthetic pyrrolobenzodiazepine DNA-adduct, and
tomaymycin biosynthetic-gene-cluster papers. Those papers are leads rather than
support for this record: their abstracts discuss parent tomaymycin, synthetic
analogs, or biosynthesis without saying that exact `(Z)-tomaymycin` is the
compound whose antibacterial mechanism was measured.

## Completeness

| Field family | Review |
| --- | --- |
| Identity and structure | Complete for a generated ChEBI exact seed. The exact identifier, label, stereochemistry, formula, InChI, InChIKey, mass fields, parent, and ChEBI antibacterial role agree with OLS4 and the committed ChEBI inventory row. |
| Source concept | Complete. The only contributing source is the exact ChEBI term. |
| Record evidence | Empty. Acceptable for a raw seed, but no inspected source is attached to the record. |
| Mode of action and targets | Empty. Consequential gap; the review-readiness row is a `MECHANISM_REVIEW` row, and the mechanism worklist row reports 0 CARD targets. |
| Activity observations | Empty. The exact 1983 abstract supports antibacterial activity qualitatively but does not expose assay, taxon, strain, or MIC values in the inspected HTML. |
| Resistance mechanisms | Empty and expected for this ChEBI-only seed; there are 0 CARD resistance edges to build on. |
| Causal graph | Empty because no exact mechanism claim is curated yet. |
| Generated ownership | Correct. The reviewed YAML is emitted from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv`; future seeded-field corrections belong in those inputs or in the ChEBI extractor rather than as hand edits to the generated record. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, and exact ChEBI identity and structure agree with OLS4. | Not applicable. |
| Major | Exact `CHEBI:167318` remains a source-seeded antibacterial structure with no reviewed mechanism, no molecular target, no activity observation, and no causal graph. | The generated record has 0 record evidence items and no `mode_of_action`, `molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, or `causal_graphs`; exact `CHEBI:167318` is on the `mechanism` and `review-readiness` worklists with 0 CARD targets and 0 CARD resistance edges. `PMID:6833147` is a direct exact-compound activity lead, but the inspected PubMed/J-STAGE abstract does not expose an assay method or taxon-level endpoint for curation. | Future curator-owned fields on `data/antibiotics/antibacterial/z-tomaymycin.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI changes this term's identity, role, parentage, or source PMIDs, `data/raw/chebi_antimicrobials.tsv` and the ChEBI extractor own the refresh. |
| Minor | None found. | The seed is sparse but not internally inconsistent, and the exact term has no same-structure xrefs or extra generated fields requiring a narrower style fix. | Not applicable. |

## Recommended Edits

1. Inspect the full Tozuka et al. 1983 PDF manually or with a local PDF text
   extractor and add source-reported `(Z)-tomaymycin` activity only if the
   article gives exact organism, assay, and measurement details. If it does,
   write one `ActivityObservation` per supported organism through
   `record_curation_event` and `write_validated_antibiotic`.
2. Keep `mode_of_action`, `molecular_targets`, and `causal_graphs` empty until
   primary evidence directly measures exact `(Z)-tomaymycin` or a paper makes
   an explicit scope-preserving generalization over the E/Z tomaymycin pair.
   Parent tomaymycin and synthetic pyrrolobenzodiazepine DNA-adduct papers
   should remain discovery leads unless their full text supports the exact
   `CHEBI:167318` compound.
3. Leave `resistance_mechanisms` empty unless a future source reports a
   route-level or organism-specific resistance observation for exact
   `(Z)-tomaymycin`.

## Follow-up Checks

1. `just validate data/antibiotics/antibacterial/z-tomaymycin.yaml`
2. `just validate-strict data/antibiotics/antibacterial/z-tomaymycin.yaml --out /tmp/z-tomaymycin-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/z-tomaymycin-worklist.tsv`
5. `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv`

Rerun targeted PubMed/J-STAGE or DOI checks for `PMID:6833147` after any future
activity curation from that paper, and manually verify exact E/Z scope before
promoting parent-tomaymycin or pyrrolobenzodiazepine mechanism leads to
`mode_of_action`, `molecular_targets`, or `causal_graphs`.

## Additional Notes

The absence checks above used `find` or `rg --no-ignore --hidden`, so generated
reports ignored by Git were included.

The J-STAGE article page exposes a 3.5 MB PDF for the Tozuka et al. 1983 paper,
but this local environment had neither the `pdftotext` binary nor `pypdf`,
`pdfplumber`, or `PyMuPDF` installed in the project Python environment. The PDF
was therefore retained as a future follow-up source rather than treated as
inspected table evidence.

Semantic Scholar was not required for the verdict: PubMed resolved the exact
ChEBI PMID, and the official J-STAGE landing page resolved its DOI and abstract.
