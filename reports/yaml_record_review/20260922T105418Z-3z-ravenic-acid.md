# YAML Record Review: (3Z)-ravenic acid

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/3z-ravenic-acid.yaml`
- Started UTC: 2026-09-22T10:52:50Z
- Finished UTC: 2026-09-22T10:54:25Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:66291` |
| Label | `(3Z)-ravenic acid` |
| Path | `data/antibiotics/antibacterial/3z-ravenic-acid.yaml` |
| Class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding | `EXACT` |
| Source concept | `CHEBI:66291`, version `2026-08-30`, minted key `antibioticmech:chebi-78504814d2` |
| Source roles | `CHEBI:33281` antimicrobial agent; `CHEBI:33282` antibacterial agent |
| Parent compounds | `CHEBI:26177`, `CHEBI:33823`, `CHEBI:74223` |
| Structure | `JJFGJQNWIJPCAN-AJOOJNDDSA-N`; formula `C15H17NO3`; charge `0` |
| Same-structure xrefs | `reaxys:9275104` |
| Source literature leads | `PMID:12350167`, `PMID:20066698` |
| Evidence-bearing objects | None |

This review covered one generated ChEBI-seeded antibacterial record. The YAML
has source-derived exact ChEBI grounding, a ChEBI definition, two exact ChEBI
synonyms, three strictly broader ChEBI parents, one Reaxys xref, antimicrobial
and antibacterial roles, structure, and source-concept metadata. It has no
generated or curator-owned mode of action, molecular target, activity
observation, resistance mechanism, producer, discussion, dataset, causal graph,
or claim-level evidence.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/3z-ravenic-acid.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/3z-ravenic-acid.yaml --out /tmp/antibioticmech-chebi-66291-validation.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Pass: 2939 expected records, 2939 on disk, no missing records, no unexpected records, no drifted fields, no identifiers absent from `PATHS.tsv`, no stale lockfile rows. The unrelated known `ARO:3000337` CARD cross-reference warning was emitted. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-66291.tsv` | Pass: the full worklist TSV was written. `CHEBI:66291` appears on `mechanism`, `producer-candidate`, and `review-readiness`; it was not found on any other queue in that TSV. |
| `just review-queue --limit 58` | Pass: `CHEBI:66291` is queued with `MECHANISM_REVIEW: mechanism is absent; 2 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |

No narrower single-record term, reference, or history validator is exposed for
this ChEBI-seeded record; the schema/strict checks, `verify-corpus`, and
worklist are the narrowest documented local gates.

## Identity and Grounding

`CHEBI:66291` resolves in official OLS as a current, non-obsolete ChEBI term
with label `(3Z)-ravenic acid`.

The live OLS annotations list the same formula, charge, average mass,
monoisotopic mass, Standard InChI, Standard InChIKey, Reaxys cross-reference,
and PubMed cross-references as the committed
`data/raw/chebi_antimicrobials.tsv` row. The current live OLS SMILES omits one
isomeric slash that is present in the committed ChEBI inventory, but the
Standard InChI and InChIKey still match exactly and preserve the same
stereochemical identity.

The generated `parent_compounds` values are exactly the live OLS hierarchical
parents of `CHEBI:66291`: `CHEBI:26177` `polyene antibiotic`, `CHEBI:33823`
`enol`, and `CHEBI:74223` `pyrrolidin-2-ones`.

The `ANTIBACTERIAL` filing class and empty `mode_of_action` are consistent with
the ChEBI source roles. The live `CHEBI:33282` term resolves in OLS as current
and non-obsolete `antibacterial agent`; the live `CHEBI:33281` term resolves as
current and non-obsolete `antimicrobial agent`. `data/raw/chebi_role_names.tsv`
marks `CHEBI:33281` as a generic `scope` role and `CHEBI:33282` as a
`class,scope` role, and `conf/sources.yaml` maps `CHEBI:33282` to
`ANTIBACTERIAL` without a mechanism mapping.

Live OLS also gives `CHEBI:66291` the direct role `CHEBI:76964` `Penicillium
metabolite`. The generated record does not store that role in `activity_roles`,
which is correct because this corpus imports ChEBI antimicrobial roles there,
not taxonomic metabolite roles.

An ignored-inclusive search for `CHEBI:66291`, the exact label, the filename
stem, the exact Standard InChIKey, both source PMIDs, and `ravenic` across
`data/antibiotics`, `data/raw`, `curation`, `reports`, and the exact `/tmp`
worklist/publication outputs found only the expected `PATHS.tsv` row, generated
YAML, raw ChEBI row, worklist/review-readiness rows, and publication-search
outputs. It found no prior dedicated review report, curation decision,
curator-inventory row, generated activity observation, molecular target,
resistance, producer, or causal-graph claim resolving `CHEBI:66291`.

## Evidence

The record's antibacterial assertion is inherited from ChEBI role
`CHEBI:33282`, `antibacterial agent`, with the generic antimicrobial
`CHEBI:33281` role retained beside it. Those roles are sufficient to file the
record under `ANTIBACTERIAL`; they are not compound-specific mechanism, target,
producer, organism, or assay claims.

The two ChEBI-linked source leads resolve in PubMed and match the committed raw
ChEBI row. `PMID:12350167` is Michael, Grace, Kotiw, and Barrow's Journal of
Natural Products paper `Ravenic acid, a new tetramic acid isolated from a
cultured microfungus, Penicillium sp.` with DOI `10.1021/np0200358`.
`PMID:20066698` is Schlenk, Diestel, Sasse, and Schobert's Chemistry paper `A
selective 3-acylation of tetramic acids and the first synthesis of ravenic
acid` with DOI `10.1002/chem.200902544`.

The PubMed abstract for `PMID:12350167` supports exact identity and a
production lead: ravenic acid was isolated from the mycelia of a cultured
`Penicillium` sp., its structure was determined by detailed spectroscopy, and
the major isomer was identified with `(3Z,7E,9E,11E,13E)` stereochemistry. It
names neither a Penicillium species/strain nor a susceptible bacterial
organism, assay value, MIC unit, or biochemical target.

The PubMed abstract for `PMID:20066698` supports a synthesis and activity lead,
not a claim-level activity assertion for exact `(3Z)-ravenic acid`. It says
3-acyl tetramic acids including ravenic acid were prepared and that the new
3-oligoenoyl tetramic acids had structure-dependent antimicrobial and cytotoxic
activity, but it does not state whether ravenic acid itself was active, which
organisms were tested, or any measured value.

A bounded repository publication search found zero PubMed candidates for the
exact Standard InChIKey `JJFGJQNWIJPCAN-AJOOJNDDSA-N`. A PubMed all-fields
check likewise found no exact Standard InChIKey hits. An exact PubMed search
for `"ravenic acid"` found the two ChEBI-linked source papers plus one
near-match paper about `ravynic acid`, a distinct `Penicillium` natural product
that should not be attached to this record without exact structure matching. A
direct PubMed all-fields check found no candidates for the exact label
`(3Z)-ravenic acid`.

No NCBI Taxonomy verification was needed for this pass because the inspected
source abstracts and ChEBI definition name only `Penicillium sp.` rather than a
species or strain. The full worklist carries the definition-derived
`producer-candidate` row as `(no binomial)` and describes that `Penicillium`
phrase as source-only.

## Completeness

The record is incomplete as a reviewed antibacterial natural-product record. It
has exact ChEBI identity, structure, a same-structure xref, strictly broader
ChEBI parents, antibacterial source classification, and two source-paper leads,
but no curator-owned activity observation, no species- or strain-level producer
assertion, no molecular target, no mode of action, no dataset, and no causal
graph.

The empty mechanism fields are correct for a ChEBI `antibacterial agent` seed.
No inspected source maps exact `(3Z)-ravenic acid` to a bacterial cell-wall,
protein-synthesis, nucleic-acid-synthesis, membrane, metabolic, or other
antibacterial mechanism.

The empty `producer_organisms` and `activity_spectrum` slots should remain
empty until a curator checks the full source papers. The abstracts do not supply
a species/strain-level producer or a bacterial assay with exact MIC or other
activity measurements. The full worklist currently puts exact `CHEBI:66291` on
`mechanism`, `producer-candidate`, and `review-readiness`; it does not surface a
claim-ready activity candidate from the ChEBI definition.

The empty `molecular_targets`, `resistance_mechanisms`,
`clinical_status_assertions`, `datasets`, `discussions`, and `causal_graphs`
slots are preferable to over-scoped filler. No molecular target, resistance
mechanism, clinical status, public dataset accession, or evidence-backed causal
edge was identified in the bounded checks.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Exact `CHEBI:66291` has only source-level antibacterial classification; it has no curator-owned activity observation, producer assertion, mechanism, or target claim. | The generated record has two source-literature leads but zero record evidence items, zero `activity_spectrum` rows, zero `producer_organisms`, zero targets, and no `mode_of_action`. `PMID:12350167` supports an exact isolation/structure lead and `PMID:20066698` supports a synthesis/activity lead, but neither PubMed abstract provides a species/strain-level producer, exact organism-specific MIC, or molecular target for a curator field. | Future curator-owned fields on `data/antibiotics/antibacterial/3z-ravenic-acid.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI later changes this term's roles, xrefs, parents, or citations, the ChEBI inventory extractor owns that refresh. |

No blocker identity, structure, schema, xref, class, parentage, or audit-history
findings were found.

No minor findings.

## Recommended Edits

1. Inspect full DOI `10.1021/np0200358` before adding a Penicillium
   `producer_organisms` row. Add a producer only if the paper identifies a
   species, strain, or other source organism precisely enough for this corpus;
   do not turn abstract-level `Penicillium sp.` into an unsupported species
   claim.
2. Inspect full DOI `10.1002/chem.200902544` and any primary papers it cites
   for exact ravenic acid antibacterial data before adding `activity_spectrum`
   rows. Require organism, strain when reported, assay, value, and unit fields;
   broad 3-acyl tetramic acid antimicrobial summaries are not enough.
3. Add `mode_of_action`, `mode_of_action_target_scope`, a bacterial
   `MolecularTarget`, or a `CausalGraph` only if an inspected primary source
   maps exact `(3Z)-ravenic acid` to a biochemical effect or target. Do not
   infer a mechanism from the ChEBI antibacterial role or the polyene-antibiotic
   parent class alone.
4. Keep `PMID:12350167` and `PMID:20066698` out of same-structure `xrefs`;
   cite them from assertion-level `evidence` blocks only if their full text
   supports future producer, activity, dataset, mechanism, target, or
   causal-graph rows.
5. Treat `ravynic acid` as an adjacent `Penicillium` natural-product lead, not
   as a ravenic-acid synonym, until structure matching proves identity.

## Follow-up Checks

After any exact-term curation or source refresh, rerun:

1. `just validate data/antibiotics/antibacterial/3z-ravenic-acid.yaml`
2. `just validate-strict data/antibiotics/antibacterial/3z-ravenic-acid.yaml --out /tmp/antibioticmech-chebi-66291-validation.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-66291.tsv`
5. `just review-queue --limit 58`
6. `just qc`

Manually compare any future producer, activity, mechanism, or target assertion
against exact `(3Z)-ravenic acid` identity. Confirm that every claim-level
evidence block attaches to the specific object it supports, not only to the
whole ChEBI term or to a tetramic-acid chemical class.

## Additional Notes

All absence checks in this report used `rg --no-ignore --hidden`, so they
included ignored `reports/` files.

`runoak` was not available in this shell, so official ChEBI term checks were
performed against the EBI OLS4 HTTPS API.
