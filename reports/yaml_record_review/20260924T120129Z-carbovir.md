# YAML Record Review: (−)-carbovir

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antiviral/carbovir.yaml`
- Started UTC: `2026-09-24T11:57:00Z`
- Finished UTC: `2026-09-24T12:01:29Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/carbovir.yaml` |
| Identifier | `CHEBI:421843` |
| Label | `(−)-carbovir` |
| Class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | ChEBI `CHEBI:421843`, version `2026-08-30`, minted key `antibioticmech:chebi-da9966f4ba` |
| Structure key | `XSSYCIGJYCVRRK-RQJHMYQMSA-N` |

`carbovir.yaml` is a generated exact ChEBI record for `CHEBI:421843`.
ChEBI contributes the individual neutral `(−)-carbovir` structure, CAS xref
`cas:120443-30-3`, parent `CHEBI:202940`, the `CHEBI:53756` HIV-1 reverse
transcriptase inhibitor role, and three source-literature PMIDs. The seeder maps
that role to `ANTIVIRAL`, `VIRAL_POLYMERASE_INHIBITION`, and
`MICROBIAL_TARGET`; no CARD/ARO, curator, BindingDB, Drugs@FDA, MIBiG, or
PHI-base slice contributes an exact assertion.

## Validation

| Check | Result |
|---|---|
| `rg -uu -n 'CHEBI:421843\|carbovir\|421843' reports/yaml_record_review data/antibiotics data/raw curation .claude docs README.md conf src scripts tests` | Passed before this report was written; the ignored/hidden-inclusive search found the generated YAML, the exact ChEBI row, the `PATHS.tsv` row, and review-queue rows, but no prior exact `CHEBI:421843` review report. |
| `rg -uu -n 'CHEBI:421843\|carbovir\|421843' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv` | Passed with ignored/hidden files included; no exact adopted ARO, BindingDB, Drugs@FDA, MIBiG, PHI-base, or grounding-decision row exists for `CHEBI:421843` or exact `(−)-carbovir`. |
| `just validate data/antibiotics/antiviral/carbovir.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/carbovir.yaml --out /tmp/carbovir-validate-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2,939 expected records, 2,939 on disk, no missing or unexpected records, 0 drifted fields, and 0 stale lockfile rows. It repeated the known unrelated CARD iclaprim / `CHEBI:31724` cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Passed; exact `CHEBI:421843` appears on `mechanism` and `review-readiness`. The separate racemate `CHEBI:202940` appears on `no-structure`, but it is only a broader parent of this record. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; exact `CHEBI:421843` is row 144 with `MECHANISM_REVIEW`, 3 committed source-literature leads, 0 record evidence items, and 0 targets. |
| Live OLS4 lookup for `CHEBI:421843` | Passed; OLS resolved one non-obsolete ChEBI class with `obo_id` `CHEBI:421843`, label `(-)-carbovir`, the generated formula, SMILES, Standard InChI, Standard InChIKey, CAS xref, and three PubMed xrefs. |
| Live ChEBI page lookup for `CHEBI:421843` | Passed; current ChEBI agreed with the generated Unicode label, ASCII label, definition, formula, charge, average mass, monoisotopic mass, SMILES, InChI, InChIKey, CAS xref, `CHEBI:53756` role, parent `CHEBI:202940`, and PMIDs `15887959`, `17373782`, and `18549801`. |
| PubMed efetch for committed ChEBI PMIDs `15887959`, `17373782`, and `18549801` | Passed; all three identifiers resolved. |
| PubMed Central ID conversion for committed ChEBI PMIDs | Passed; `PMID:17373782` resolves to open `PMC2533426`; `PMID:15887959` and `PMID:18549801` had no PMC match. |
| PubMed exact discovery searches for carbovir, CBV, and carbovir triphosphate with HIV-1 reverse transcriptase terms | Passed; exact carbovir-triphosphate and HIV reverse-transcriptase queries returned primary mechanism leads, including `PMID:2472136`, `PMID:1371285`, `PMID:1703154`, and `PMID:11955063`. |

## Identity and Grounding

| Claim | Assessment |
|---|---|
| Identifier and label | Supported. OLS and current ChEBI resolve `CHEBI:421843` to ASCII `(-)-carbovir`; the generated label uses the equivalent Unicode minus spelling `(−)-carbovir`. |
| Structure | Supported. The generated SMILES, Standard InChI, Standard InChIKey, formula, charge, average mass, and monoisotopic mass match both the committed ChEBI row and current ChEBI for exact `CHEBI:421843`. |
| Xrefs | Supported. The only exact xref, `cas:120443-30-3`, is the CAS registry number on the exact ChEBI term. The related racemate `CHEBI:202940` has a different CAS, `118353-05-2`, and is not asserted as an exact xref. |
| Parent compound | Supported. Exact `(−)-carbovir` is a child of the broader cis/racemic `carbovir` term `CHEBI:202940` in current ChEBI. |
| Filing class and activity role | Supported as a seeded ChEBI assertion. The sole local role is `CHEBI:53756`, `HIV-1 reverse transcriptase inhibitor`, which `conf/sources.yaml` maps to `ANTIVIRAL`. |
| Source concept | Supported. The only exact source concept is `CHEBI:421843`; exact ignored/hidden-inclusive searches found no local curator decision or adopted external inventory row that alters this record's identity. |

## Evidence

| Record claim | Support |
|---|---|
| Exact `CHEBI:421843` denotes neutral `(−)-carbovir`. | Supported by the committed ChEBI inventory row, OLS, and current ChEBI. |
| Exact `(−)-carbovir` has role `CHEBI:53756`, HIV-1 reverse transcriptase inhibitor. | Supported as a ChEBI database assertion. The seeded `mode_of_action` and `mode_of_action_target_scope` honestly say they were assigned from the ChEBI role and are not a curator's mechanistic review. |
| Exact `(−)-carbovir` is broader-racemate child `is_a CHEBI:202940`. | Supported by the committed ChEBI row and current ChEBI. This remains a parent relation, not an exact identity assertion. |
| `PMID:15887959` is an exact carbovir source-literature lead. | Supported as a useful activity lead. Its abstract reports a synthesized phosphoramidate of the guanine nucleoside analogue carbovir and compares the antiviral profile of parent carbovir and its ProTide in HIV and HBV assays. |
| `PMID:17373782` is a carbovir-adjacent lead, not direct exact-carbovir mechanism evidence. | The paper synthesized 2′,3′-unsaturated 3′-fluoro carbocyclic nucleosides and modeled one D-series guanosine analogue in wild-type and M184V HIV reverse transcriptase. Exact carbovir is background and a resistance comparator rather than the compound under test. |
| `PMID:18549801` is about abacavir hypersensitivity, not exact carbovir antiviral activity. | The article tests HLA-B*5701-restricted CD8+ T-cell activation by abacavir. Its abstract mentions abacavir as a reverse-transcriptase inhibitor but does not inspect exact carbovir inhibition of HIV-1 reverse transcriptase. |
| Primary exact-carbovir triphosphate mechanism papers exist. | Supported as future curation leads. `PMID:2472136` and `PMID:1703154` evaluated carbovir 5′-triphosphate against HIV-1 reverse transcriptase and human polymerases; `PMID:1371285` examined `(-)-enantiomeric carbovir triphosphate` inhibition of HIV-1 reverse transcriptase and DNA chain termination; `PMID:11955063` reported transient-kinetic studies on carbovir triphosphate inhibition and drug resistance for wild-type and mutant HIV-1 reverse transcriptase. |

## Completeness

| Area | Assessment |
|---|---|
| Identity and structure | Complete enough for read-only review. Exact `CHEBI:421843`, the generated record, OLS, current ChEBI, `data/raw/chebi_antimicrobials.tsv`, and `PATHS.tsv` all agree. |
| Classification and source role | Complete enough for seeded provenance. The record carries the one exact current ChEBI biological role. |
| Mode of action | Incomplete. The current `VIRAL_POLYMERASE_INHIBITION` value is plausible and traceable to ChEBI, but it is not curator-reviewed and lacks primary evidence on a target or causal edge. |
| Molecular targets | Incomplete. HIV-1 reverse transcriptase is the ChEBI-named target, and exact carbovir-triphosphate primary studies exist, but the YAML has no `molecular_targets` entry. |
| Activity observations | Incomplete. The YAML has no exact HIV-1, HIV-2, or HBV activity observation despite exact activity leads in `PMID:15887959` and older carbovir in-vitro HIV literature. |
| Resistance mechanisms | Complete as empty for the adopted sources. CARD and PHI-base contribute no exact row for `CHEBI:421843`. Abacavir/carbovir resistance papers are future mechanism leads, not adopted resistance assertions. |
| Producers and clinical status | Complete as empty. Exact ignored/hidden-inclusive searches found no MIBiG producer or Drugs@FDA status row for this experimental compound; abacavir, not carbovir, is the approved prodrug represented in ChEBI as `CHEBI:421707`. |
| Causal graphs | Incomplete. The record could express intracellular phosphorylation to carbovir triphosphate, HIV-1 reverse transcriptase inhibition, and DNA chain termination, but has no graph. |
| Datasets and discussions | Complete as empty for the bounded review. The inspected evidence did not expose a public exact-carbovir dataset accession or a concrete local unresolved conflict that requires a `Discussion` before mechanistic curation. |
| Audit trail | Supported. The history is seed-only and matches a generated exact ChEBI record with no curator-owned claim. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, and exact live ChEBI still agrees with the generated identity, structure, xref, role, and parent. | Not applicable. |
| Major | The record has no curator-reviewed molecular target, activity claim, or causal graph for the exact HIV-1 reverse transcriptase mechanism. | The record has only source-seeded `mode_of_action` text and no `molecular_targets`, `activity_spectrum`, or `causal_graphs`; `just worklist` queues `CHEBI:421843` for `mechanism`; older exact carbovir-triphosphate studies directly tested HIV-1 reverse transcriptase inhibition and DNA chain termination. | Future curator-owned fields on `data/antibiotics/antiviral/carbovir.yaml`, written with `record_curation_event` and `write_validated_antibiotic`. |
| Minor | One committed ChEBI source-literature lead is activity/mechanism-adjacent rather than exact carbovir evidence. | `PMID:18549801` is an abacavir HLA-B*5701 hypersensitivity paper. It does not support an exact carbovir antiviral mechanism or target claim, and the generated record correctly leaves it as upstream ChEBI provenance rather than local claim-level evidence. | No record edit needed for a read-only review; a future ChEBI citation refresh or manual claim curation should prefer exact carbovir and carbovir-triphosphate primary papers. |
| Minor | The record's committed source-literature leads omit older direct carbovir-triphosphate mechanism papers. | Current ChEBI lists only PMIDs `15887959`, `17373782`, and `18549801`; exact bounded PubMed searches found primary biochemical papers such as `PMID:2472136`, `PMID:1371285`, `PMID:1703154`, and `PMID:11955063` that are more direct leads for HIV-1 reverse transcriptase target curation. | Future curator-owned evidence additions on `data/antibiotics/antiviral/carbovir.yaml`; ChEBI inventory rows can stay source-faithful until an upstream refresh changes them. |

## Recommended Edits

1. Add curator-owned HIV-1 reverse transcriptase target evidence for exact `(−)-carbovir` / carbovir triphosphate.

   Maintained owner: `data/antibiotics/antiviral/carbovir.yaml`.

   Inspect `PMID:1371285`, `PMID:1703154`, and `PMID:11955063` far enough to capture the exact enzyme form, activated compound form, inhibition relation, and evidence snippet. Add a target term for HIV-1 reverse transcriptase if a local family/function term is available, otherwise add the narrowest representable target and protein examples without using an organism-specific UniProt accession as the target identity.

2. Add direct antiviral activity observations for exact carbovir.

   Maintained owner: `data/antibiotics/antiviral/carbovir.yaml`.

   Start with `PMID:15887959` for exact HIV/HBV assay leads and `PMID:2847711` for the original in-vitro HIV carbovir activity paper cited by `PMC2533426`. Preserve cell-line and viral-species context and use `EC50`/`IC50` only if the schema can represent them without forcing a MIC.

3. Add a causal graph only after the mechanism papers have been inspected.

   Maintained owner: `data/antibiotics/antiviral/carbovir.yaml`.

   A likely graph is `(−)-carbovir -> carbovir triphosphate -> HIV-1 reverse transcriptase inhibition -> viral DNA chain termination -> decreased HIV replication`, but every conversion and inhibition edge needs its own primary evidence item.

## Follow-up Checks

- Rerun `just validate data/antibiotics/antiviral/carbovir.yaml`.
- Rerun `just validate-strict data/antibiotics/antiviral/carbovir.yaml --out /tmp/carbovir-validate-strict.tsv`.
- Rerun `just verify-corpus --summary`.
- Rerun `just worklist --limit 0 --tsv /tmp/carbovir-worklist.tsv` and confirm exact `CHEBI:421843` leaves the `mechanism` queue only after any future curator-reviewed mechanism and target edit.
- Rerun `just review-queue --limit 0 --tsv /tmp/carbovir-review-queue.tsv` and confirm exact `CHEBI:421843` moves from `MECHANISM_REVIEW` to the next unresolved evidence gate only after primary target evidence is attached.
- Repeat an ignored/hidden-inclusive exact search for `CHEBI:421843`, `XSSYCIGJYCVRRK-RQJHMYQMSA-N`, `120443-30-3`, and the PMIDs used in any new activity, target, or causal edge.
- Manually recheck OLS/ChEBI for `CHEBI:421843` and the broader racemate `CHEBI:202940` so a future curation PR preserves the enantiomer boundary.

## Additional Notes

- The first exact PubMed discovery query was intentionally broader than the
  three committed ChEBI leads and returned many abacavir-metabolite,
  pharmacokinetic, and hypersensitivity results. It was useful for discovery
  but too broad to support curation directly.
- Carbovir is an active abacavir metabolite, but abacavir has its own exact
  record at `CHEBI:421707`. Future evidence should not copy clinical status,
  activity, or resistance claims from abacavir onto `CHEBI:421843` unless the
  source actually measures carbovir or carbovir triphosphate.
