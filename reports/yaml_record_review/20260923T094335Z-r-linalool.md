# YAML Record Review: (R)-linalool

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/unspecified/r-linalool.yaml
- Started UTC: 2026-09-23T09:40:45Z
- Finished UTC: 2026-09-23T09:43:35Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Class | AntibioticRecord |
| Identifier | CHEBI:28 |
| Label | (R)-linalool |
| Path | data/antibiotics/unspecified/r-linalool.yaml |
| Maintained status | Generated from `data/raw/chebi_antimicrobials.tsv` |
| Grounding | `EXACT` ChEBI source concept `CHEBI:28` |
| Curation status | `SEEDED` |

The target is the ChEBI-seeded `(R)` enantiomer. Exact ignored-inclusive
searches over `data/antibiotics`, `data/raw`, `curation`, and
`reports/yaml_record_review` found one exact source row for `CHEBI:28`, one
PATHS row, one queue row, one generated YAML record, and no prior exact review
report for this identifier.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/unspecified/r-linalool.yaml` | Pass; no LinkML validation issues. |
| `just validate-strict data/antibiotics/unspecified/r-linalool.yaml --out /tmp/r-linalool-validate-strict.tsv` | Pass; 1 file checked, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass; all 2,939 expected records were present on disk with no missing, unexpected, drift, path, or stale rows. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 xref refusal. |
| `just worklist --limit 0 --tsv /tmp/r-linalool-worklist.tsv` | Pass; wrote the full TSV. The anchored `CHEBI:28` rows are the `mechanism` and `review-readiness` backlog entries. |
| `just review-queue --limit 101` | Pass; listed `CHEBI:28` as `MECHANISM_REVIEW` with 1 source literature lead, 0 record evidence items, and 0 targets. |
| `just lint` | Pass. |
| `git diff --check` | Pass before adding this report. |
| `git diff --cached --check` | Pass after force-adding this ignored report. |

No narrower single-record term, reference, curation-history, or corpus-drift
validator is exposed for plain ChEBI-seeded records; `validate-strict` and
`verify-corpus --summary` are the narrowest documented checks for this case.

## Identity and Grounding

The local ChEBI inventory row for `CHEBI:28` contains label `(R)-linalool`, role
term `CHEBI:33281`, parent `CHEBI:17580`, SMILES `C=C[C@](C)(O)CCC=C(C)C`,
Standard InChI
`InChI=1S/C10H18O/c1-5-10(4,11)8-6-7-9(2)3/h5,7,11H,1,6,8H2,2-4H3/t10-/m0/s1`,
InChIKey `CDOSHBSSFJOMGT-JTQLQIEISA-N`, formula `C10H18O`, net charge `0`,
average mass `154.253`, monoisotopic mass `154.13577`, ChEBI synonyms, and
ChEBI xrefs. The generated YAML carries the same preferred label, structure,
parent, role term, and retained same-structure xrefs.

`CHEBI:33281` is the generic `antimicrobial agent` scope root in
`conf/sources.yaml`, so the record is correctly filed as
`ANTIMICROBIAL_UNSPECIFIED` rather than as a narrower antibacterial,
antifungal, antiprotozoal, antiviral, or biocide class.

Exact ignored-inclusive searches over `data/antibiotics`, `data/raw`,
`curation`, and `reports/yaml_record_review` found no second generated record
with InChIKey `CDOSHBSSFJOMGT-JTQLQIEISA-N` and no second generated record with
source-concept minted identifier `antibioticmech:chebi-c863dc8043`. Sibling
records are intentionally distinct: `CHEBI:17580` is racemic or
stereochemistry-unspecified `linalool` with InChIKey
`CDOSHBSSFJOMGT-UHFFFAOYSA-N`, and `CHEBI:98` is `(S)-linalool` with InChIKey
`CDOSHBSSFJOMGT-SNVBAGLBSA-N`.

## Evidence

The record has no record-level `evidence`, `molecular_targets`,
`activity_observations`, `resistance_mechanisms`, `producer_organisms`, or
`causal_graphs`. That is an honest generated state for a ChEBI seed whose only
assertion is an upstream antimicrobial-agent role.

The single literature lead imported from ChEBI, `PMID:18579302`, resolves to a
mouse antinociception study of `(-)-linalool` and glutamatergic nociception. It
does not support an antimicrobial mode of action, activity observation, target,
resistance mechanism, producer claim, or causal edge for `(R)-linalool`.

Bounded publication discovery left `NCBI_EMAIL` unset and queried:

- `(R)-linalool`
- `CDOSHBSSFJOMGT-JTQLQIEISA-N`
- `(3R)-3,7-dimethylocta-1,6-dien-3-ol`
- `R-linalool antimicrobial`
- `18579302`

PubMed returned exact-enantiomer aroma, synthesis, insect, plant-chemistry,
mouse antinociception, and essential-oil activity leads, plus one comparative
fish-pathogen paper where `S-(+)-linalool`, not `R-(-)-linalool`, was active
against *Aeromonas hydrophila* at 3.2 mg/mL. Semantic Scholar was HTTP 429 for
the exact label, exact InChIKey, and antimicrobial queries; the IUPAC-style
synonym query returned off-target chemistry and flavor papers. No inspected
lead yielded compound-specific antimicrobial mechanism or MIC evidence for the
exact `CHEBI:28` structure.

## Completeness

The seeded identity, ChEBI grounding, generated structure, parent relation,
generic antimicrobial role, source concept, and retained ChEBI xrefs are
complete enough for an unreviewed ChEBI seed.

The record still lacks all reviewed antimicrobial biology:

- no `mode_of_action` or `mode_of_action_target_scope`;
- no `molecular_targets`;
- no `activity_observations`;
- no `resistance_mechanisms`;
- no `causal_graphs`;
- no exact-enantiomer antimicrobial evidence beyond ChEBI's generic role.

`just worklist` therefore correctly queues `CHEBI:28` for mechanism curation,
and `just review-queue` correctly keeps it in `MECHANISM_REVIEW`.

## Findings

None found.

There are no blocker, major, or minor defects in the generated YAML. The absent
mechanism, target, activity, resistance, and graph sections are curation gaps,
not invalid seeded claims, because the bounded searches above did not find
evidence precise enough to populate them for `(R)-linalool`.

## Recommended Edits

None for the generated seed.

Do not populate activity, target, resistance, or causal-graph fields from the
current ChEBI lead or from essential-oil mixture papers. A future curator should
only edit `data/antibiotics/unspecified/r-linalool.yaml` through
`write_validated_antibiotic` if a primary source reports antimicrobial
activity or mechanism for the exact `(R)` enantiomer, with every activity
measurement carrying the assayed organism, method, value, and units.

## Follow-up Checks

If a future curator finds exact `(R)-linalool` antimicrobial data and edits the
record, rerun:

- `just validate-strict data/antibiotics/unspecified/r-linalool.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv`
- `just qc`
- `git diff --check`

No follow-up check is needed for this read-only review.

## Additional Notes

The raw ChEBI row includes `knapsack:C00003047`, while the generated YAML
retains the Beilstein, CAS, KEGG Compound, LIPID MAPS, and Reaxys xrefs. Corpus
verification passed, so this is generator behavior rather than hand-edited
drift.

`PMID:18579302` is present in the raw ChEBI row and the review-readiness queue,
but not in the generated record. Record-level evidence is optional for
ChEBI-seeded identity and role assertions, and the PMID does not provide
claim-level antimicrobial support.
