# YAML Record Review: (−)-usnic acid

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/usnic-acid.yaml`
- Started UTC: 2026-09-24T20:15:00Z
- Finished UTC: 2026-09-24T20:34:49Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:122` |
| Label | `(−)-usnic acid` |
| Path | `data/antibiotics/antifungal/usnic-acid.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | `CHEBI:122` / `(−)-usnic acid` |
| Minted source key | `antibioticmech:chebi-75e0639ef6` |
| Generated or maintained | Generated from `data/raw/chebi_antimicrobials.tsv` plus `data/antibiotics/PATHS.tsv`; future seeded identity, structure, class, role, synonym, or xref changes belong upstream rather than as hand edits. |

Resolution:

- `curation/record_review_queue.tsv:155` names `CHEBI:122`, label
  `(−)-usnic acid`, and the review hint
  `MECHANISM_REVIEW: mechanism is absent`.
- `data/antibiotics/PATHS.tsv:27` maps `CHEBI:122` to the `ANTIFUNGAL`
  directory and `usnic-acid` slug.
- `data/raw/chebi_antimicrobials.tsv:5` is the ChEBI source row that seeds the
  target record.
- The whole 57-line target YAML was read before judging generated identity,
  structure, xrefs, source concept, and curation history.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/usnic-acid.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/usnic-acid.yaml --out /tmp/usnic-acid-122-validate-strict.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2939 expected records, 2939 on disk, no missing, unexpected, drifted, `PATHS.tsv`-absent, or stale-lockfile records. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Passed after emitting the known unrelated `iclaprim` / `CHEBI:31724` CARD diagnostic; `CHEBI:122` appears in `mechanism` and `review-readiness`. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed and confirmed `CHEBI:122` remains in the review-readiness queue as `MECHANISM_REVIEW`, with zero source literature leads, zero record evidence items, and zero targets. |

No narrower term, reference, or curation-history validator is exposed for this
single ChEBI-seeded record. The focused strict validator covered closed schema
shape, and `verify-corpus` covered drift from the maintained raw and `PATHS.tsv`
inputs.

## Identity and Grounding

The seeded identity is consistent with the authoritative ChEBI record:

| Assertion | Record value | Check |
|---|---|---|
| ChEBI identity | `CHEBI:122`, `(−)-usnic acid` | Official ChEBI resolved `CHEBI:122` to `(-)-usnic acid`. |
| Definition | `The (−)-enantiomer of usnic acid.` | Matched ChEBI. |
| Formula and charge | `C18H16O7`, charge `0` | Matched ChEBI. |
| Masses | average `344.319`, monoisotopic `344.0896` | Matched ChEBI. |
| SMILES | `CC(=O)C1=C(O)C=C2Oc3c(C(C)=O)c(O)c(C)c(O)c3[C@]2(C)C1=O` | Matched ChEBI. |
| Standard InChI | `InChI=1S/C18H16O7/c1-6-14(22)12(8(3)20)16-13(15(6)23)18(4)10(25-16)5-9(21)11(7(2)19)17(18)24/h5,21-23H,1-4H3/t18-/m1/s1` | Matched ChEBI. |
| InChIKey | `WEYVVCKOOFYHRW-GOSISDBHSA-N` | Matched ChEBI. |
| Parent compound | `CHEBI:38319` | ChEBI asserts `(-)-usnic acid is a usnic acid`. |
| Xrefs | `beilstein:4719209`, `beilstein:96698`, `cas:6159-66-6`, `kegg.compound:C10101`, `knapsack:C00002413`, `lipidmaps:LMPK13060002`, `metacyc.compound:CPD-7411` | Matched the official ChEBI page. |

The target is specifically the `(9bS)` and levorotatory enantiomer of usnic
acid, not the sibling `CHEBI:38320` / `(+)-usnic acid` record at
`data/antibiotics/antifungal/usnic-acid-38320.yaml` and not the
stereochemically-unspecified parent `CHEBI:38319` / `usnic acid` record at
`data/antibiotics/antifungal/usnic-acid-38319.yaml`. Those three records have
different ChEBI identifiers, xrefs, and Standard InChIKeys.

Official ChEBI asserts a direct `has role` edge from `CHEBI:122` to
`CHEBI:38317`, `EC 1.13.11.27 (4-hydroxyphenylpyruvate dioxygenase)
inhibitor`, and relates `CHEBI:122` to the deprotonated
`CHEBI:57266` sibling. The generated record correctly omits that herbicidal
role and preserves only the antimicrobial `CHEBI:35718` antifungal role
inherited from the parent `usnic acid` ChEBI concept in
`data/raw/chebi_antimicrobials.tsv:5`.

## Evidence

The target record has no record-level `evidence`. That is allowed for a
ChEBI-seeded record because the ChEBI source concept supplies provenance, and
there are no `activity_spectrum`, `molecular_targets`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status_assertions`,
`datasets`, or `causal_graphs` that would require claim-local evidence.

`data/raw/chebi_antimicrobials.tsv:5` carries no PubMed or DOI leads for the
exact `CHEBI:122` source concept. A local exact-string scan over the fetched
PubMed XML for 738 `usnic acid` title/abstract records found:

| Exact string | Title/abstract hits |
|---|---:|
| `(-)-usnic acid` | 47 |
| `(−)-usnic acid` | 0 |
| `(S)-usnic acid` | 3 |
| `(S)-usnate` | 0 |
| `(9bS)` | 0 |
| `WEYVVCKOOFYHRW-GOSISDBHSA-N` | 0 |
| `6159-66-6` | 0 |
| `C10101` | 0 |
| `C00002413` | 0 |
| `LMPK13060002` | 0 |
| `CPD-7411` | 0 |

The PubMed phrase parser broadened signed enantiomer searches to
`usnic acid`, so the local scan was used instead of trusting the parsed query.
The exact and broader searches surfaced record-relevant curation leads:

| PMID | Public abstract support |
|---|---|
| `PMID:8900875` | The indexed title is `(-)-Usnic acid and its derivatives. Their inhibition of fungal growth and enzyme activity.`, but PubMed exposes no abstract. Full text is needed before curating an exact activity or mechanism claim. |
| `PMID:15241936` | Reports the antimicrobial activity of lichen extracts and pure `(-)-usnic acid`, atranorin, and fumarprotocetraric acid against bacteria, *Candida albicans*, and *Candida glabrata*, and states that MICs were determined. The abstract supports an exact `(-)-usnic acid` yeast lead but does not expose isolate-level MIC values. |
| `PMID:40689027` | Reports semisynthetic compounds made from `(R)-` and `(S)-UA` and screened against *Candida tropicalis* and *Trichophyton rubrum*. The public abstract supports `(S)-usnic acid` as a scaffold lead, not the antifungal activity of unmodified exact `CHEBI:122`. |
| `PMID:25647298` | Reports `(-)-usnic acid` and extracts from *Cladonia uncialis* tested against *Staphylococcus aureus*, *Escherichia coli*, and *Candida albicans*. The public abstract says the *C. uncialis* extracts lacked activity against *E. coli* and *C. albicans*, but does not expose the pure `(-)-usnic acid` Candida result. |
| `PMID:22006006` | Reports `usnic acid` activity against *Candida orthopsilosis* and *Candida parapsilosis* under planktonic and biofilm conditions. The abstract does not identify the tested enantiomer. |
| `PMID:26411891` | Reports usnic acid inhibition of *Candida albicans* biofilm formation and the yeast-to-hyphal switch. The abstract does not identify the tested enantiomer. |

## Completeness

Consequential gaps:

- `mode_of_action`, `mode_of_action_target_scope`, `mode_of_action_notes`,
  `molecular_targets`, and `causal_graphs` are absent, so the record has no
  curator-owned antifungal mechanism.
- No `activity_spectrum` entry captures an exact fungus, strain, assay, value,
  qualifier, unit, and per-observation evidence for exact `(-)-usnic acid`.
- No `producer_organisms` entry captures the lichen or partner organism that
  biosynthesizes exact `CHEBI:122`.

Correctly empty optional slots:

- No `resistance_mechanisms`: the record is ChEBI-only and CARD resistance
  routes do not apply.
- No `clinical_status` or `clinical_status_assertions`: the ChEBI-only source
  concept and inspected metadata do not assert regulatory use.
- No `datasets`: inspected ChEBI and PubMed metadata did not surface a public
  screening, omics, or structural dataset accession for this exact structure.

Ignored-inclusive absence search:

- Ran `rg --hidden --no-ignore` over `curation/`, `data/raw/`,
  `data/antibiotics/`, `reports/yaml_record_review/`, the full exported
  worklist and review queue under `/tmp/`, and `.git` for `CHEBI:122`,
  `(−)-usnic acid`, `(-)-Usnic acid`, `usnic-acid.yaml`,
  `WEYVVCKOOFYHRW-GOSISDBHSA-N`, `6159-66-6`, `C10101`, `C00002413`,
  `LMPK13060002`, and `CPD-7411`.
- The search included ignored files and found only the expected generated
  target, `PATHS.tsv`, ChEBI raw row, exported worklist and review-queue rows,
  the maintained review-queue row, and the sibling prior
  `(+)-usnic acid` report mentioning the `CHEBI:122` record. It found no prior
  exact `CHEBI:122` review report, `curation/decisions.tsv` row, curated
  mechanism decision, or curated activity/provenance addition for this exact
  ChEBI concept.

## Findings

| Severity | ID | Finding | Evidence | Maintained owner |
|---|---|---|---|---|
| Major | USNIC-122-M1 | The record is still a seeded exact-enantiomer stub with a ChEBI-derived antifungal role but no curator-owned activity spectrum, producer assertion, mode of action, mechanism scope, molecular target, or causal graph. | `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` reports `MECHANISM_REVIEW: mechanism is absent` with zero source literature leads; `/tmp/antibioticmech-worklist.tsv` lists `CHEBI:122` only on `mechanism` and `review-readiness`. The PubMed searches surfaced exact `(-)-usnic acid` antifungal activity leads, but no exact-compound activity or mechanism claim has been curated onto the record. | `data/antibiotics/antifungal/usnic-acid.yaml`, via guarded curator-owned record mutation plus a new `CurationEvent`; or `curation/decisions.tsv` if full-text inspection cannot verify exact `(-)-usnic acid` antifungal activity. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect the full text of source papers behind the exact and broad
   usnic-acid antifungal leads, starting with `PMID:8900875`,
   `PMID:15241936`, `PMID:40689027`, `PMID:22006006`, and `PMID:26411891`,
   to decide which studies tested isolated `(-)-usnic acid`,
   stereochemically-unspecified usnic acid, `(+)-usnic acid`, mixtures, or
   derivatives.
2. If exact `(-)-usnic acid` antifungal primary evidence is found, add
   claim-local `activity_spectrum` observations with fungus, strain, assay,
   value, unit, and evidence; do not generalize extract-level, derivative, or
   stereochemically-unspecified activity onto the exact enantiomer.
3. Curate `mode_of_action`, `mode_of_action_target_scope`,
   `mode_of_action_notes`, molecular targets, and a causal graph only to the
   precision directly supported by inspected exact-compound mechanism sources.
4. If a lichen-biosynthesis source names the exact `(-)-usnic acid` enantiomer
   and a species or strain, add a `producer_organisms` assertion with
   claim-local evidence.
5. If exact `(-)-usnic acid` antifungal activity cannot be verified, leave the
   record `SEEDED` and add either a curator discussion for the ChEBI-derived
   role or a source-concept decision in `curation/decisions.tsv` if the ChEBI
   role is clearly over-scoped from stereochemically unspecified usnic acid.
6. Preserve the seeded ChEBI identity, `(9bS)` structure, parent, xrefs, and
   source concept. This review found no grounded-identity correction that
   belongs in `data/raw/chebi_antimicrobials.tsv` or
   `data/antibiotics/PATHS.tsv`.

## Follow-up Checks

- `just validate-strict data/antibiotics/antifungal/usnic-acid.yaml --out /tmp/antibioticmech-record-validation.tsv`
- `just verify-corpus --summary`
- `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-usnic-acid.tsv`
- `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue-usnic-acid.tsv`
- `just qc`
- Manual diff review of
  `data/antibiotics/antifungal/usnic-acid.yaml` and
  `curation/decisions.tsv` to confirm every new exact-structure claim is
  claim-local and directly supported by an inspected primary source.

## Additional Notes

- `NCBI_EMAIL` was unset in the local environment before direct PubMed E-Utilities
  fetches; no contact email was sent as NCBI API metadata.
- The review avoided SerpAPI/Google Scholar because that path can be billable
  and was not necessary to establish the bounded finding above.
- The official ChEBI graph for exact `(-)-usnic acid` lists the out-of-scope
  4-hydroxyphenylpyruvate dioxygenase inhibitor role, and the
  stereochemically-unspecified `usnic acid` parent lists `acaricide` and
  `lichen metabolite` roles in addition to `antifungal agent`. The extra ChEBI
  roles did not alter the record's `ANTIFUNGAL` filing class.
