# YAML Record Review: (S)-famoxadone

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antifungal/s-famoxadone.yaml
- Started UTC: 2026-09-23T19:35:59Z
- Finished UTC: 2026-09-23T19:37:07Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antifungal/s-famoxadone.yaml` |
| Identifier | `CHEBI:106738` |
| Label | `(S)-famoxadone` |
| Filing class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:106738` from ChEBI release `2026-08-30` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, mode, target-scope, role, structure, xref, and queue claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/s-famoxadone.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/antifungal/s-famoxadone.yaml --out /tmp/s-famoxadone-validate-strict.tsv` | Passed: 1 file checked, 0 files with `ERROR`, and 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed: all 2,939 expected records were on disk, no unexpected records were present, no generated fields drifted, no identifiers were absent from `PATHS.tsv`, and no stale lockfile rows were present. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-famoxadone-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:106738` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 1 source literature lead, 0 record evidence items, and 0 targets. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just review-queue --limit 123` | Passed and listed `(S)-famoxadone` as `MECHANISM_REVIEW: mechanism is source-seeded and not curator-checked; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:106738`, `label: (S)-famoxadone`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `PCCSBWNGDMYFCW-QFIPXVFZSA-N`, formula, charge, masses, synonyms, xrefs, PMID `10843216`, roles, and mode-source terms. |
| Live ChEBI term | EBI OLS returned active, non-obsolete `CHEBI:106738` with the same label, formula `C22H18N2O4`, structure, CAS, DrugBank, PDB-CCD, PubMed, and Reaxys xrefs, and ChEBI 3-star subset membership. |
| Structure | SMILES `C[C@@]1(c2ccc(Oc3ccccc3)cc2)OC(=O)N(Nc2ccccc2)C1=O`, Standard InChI, InChIKey, formula `C22H18N2O4`, charge `0`, average mass `374.396`, and monoisotopic mass `374.12666` are the exact ChEBI-imported values. |
| Parent boundary | OLS lists exactly one hierarchical parent, `CHEBI:83250` `5-methyl-5-(4-phenoxyphenyl)-3-(phenylamino)-1,3-oxazolidine-2,4-dione`; the parent is broader than the exact reviewed `(S)` enantiomer. |
| Enantiomer boundary | OLS exposes `CHEBI:83252` `(R)-famoxadone` as the opposite enantiomer and labels it inactive, while the reviewed term is the active `(S)` enantiomer. |
| Filing class | ChEBI roles `CHEBI:141153` `quinone outside inhibitor` and `CHEBI:24127` `fungicide` both map to `ANTIFUNGAL` at priority 3 in `conf/sources.yaml`; filing the record under `data/antibiotics/antifungal/` follows those role mappings. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:106738` to `ANTIFUNGAL` / `s-famoxadone`, matching the current record path. |

An ignored-inclusive search across `data/raw`, `curation`, `reports/yaml_record_review`, and `data/antibiotics` for `CHEBI:106738`, `PCCSBWNGDMYFCW-QFIPXVFZSA-N`, `cas:132584-12-4`, `reaxys:8433247`, `pdb-ccd:FMX`, and `CHEBI:83250` found only the expected ChEBI raw row, path-lock row, review-queue row, and exact YAML record. The same search including the broader `famoxadone` substring also found the separate racemic ChEBI seed row, as expected. An ignored-inclusive `find` scan of `reports/yaml_record_review` found no prior `famoxadone` report.

## Evidence

The mode-of-action fields are source-seeded and internally traceable:

| Assertion | Support |
|---|---|
| `(S)-famoxadone` is a ChEBI-grounded exact record with the listed synonyms and structure. | Supported by the committed ChEBI inventory row for `CHEBI:106738` and by the live OLS term. |
| The record has `ANTIFUNGAL` activity roles and a source-seeded `ENERGY_METABOLISM_INHIBITION` mode. | The raw ChEBI row lists `CHEBI:141153|CHEBI:24127` as roles and `CHEBI:141153|CHEBI:38499` as mode-source terms; `conf/sources.yaml` maps `CHEBI:141153` and `CHEBI:38499` to `ENERGY_METABOLISM_INHIBITION`. |
| The target-scope mapper marks the source-seeded mode as `HOST_SHARED_TARGET`. | `conf/sources.yaml` maps both `CHEBI:141153` and `CHEBI:38499` to `HOST_SHARED_TARGET`; the seeded YAML preserves this scope. |
| The record has CAS, PDB-CCD, Reaxys, and DrugBank xrefs. | Supported by the committed ChEBI inventory row and by live OLS. |

The seeded PubMed lead is on point. `PMID:10843216` is titled `Synthesis and structural analysis of the active enantiomer of famoxadone, a potent inhibitor of cytochrome bc1`; its abstract identifies `S-(-)-famoxadone` as the active component and famoxadone as a Qo-site inhibitor of cytochrome bc1. That primary publication is consistent with the ChEBI-derived `ENERGY_METABOLISM_INHIBITION` mode and the `HOST_SHARED_TARGET` cytochrome-bc1 scope.

Bounded publication discovery found mechanism-adjacent papers but did not find a source that would justify populating target, activity, resistance, producer, or causal-graph fields during this read-only review:

| Query | Result |
|---|---|
| Exact `(S)-famoxadone`, `PCCSBWNGDMYFCW-QFIPXVFZSA-N`, `132584-12-4`, and PDB-CCD `FMX` search | PubMed returned 20 candidates, mostly noise from `FMX` acronym collisions plus one 2024 study of stereoselective mammalian famoxadone toxicity. Semantic Scholar was rate-limited with HTTP 429. |
| `famoxadone cytochrome bc1 QoI 10843216` | PubMed returned two broader QoI respiration papers: `PMID:17586672` on QoI resistance in `Plasmopara viticola` and `PMID:11561403` on `Venturia inaequalis` mitochondrial respiration sensitivity to QoI inhibitors, including famoxadone. Semantic Scholar was rate-limited with HTTP 429. |
| `10843216[uid] OR famoxadone Complex III` | PubMed found 17 Complex III/Qo-site/famoxadone candidates, including the seeded `PMID:10843216`, `PMID:12269811` on mitochondrial cytochrome bc1 crystallized with famoxadone, `PMID:12525495` on famoxadone effects on cytochrome bc1 electron transfer, `PMID:15708371` on Qo-site inhibition differences between famoxadone and methoxyacrylate stilbene, and `PMID:21719298` on oxazolidine-dione cytochrome bc1 inhibitors. Semantic Scholar was rate-limited with HTTP 429. |

## Completeness

The generated identity, role, source-concept, structure, parent, xref, source-seeded mode, target-scope, and curation-history fields are complete enough for a ChEBI seed.

The record remains short of a curator-owned `REVIEWED` state because the ChEBI-derived `mode_of_action` has not been converted into record-level evidence, no concrete cytochrome-bc1 `molecular_targets` item is asserted, and no direct antifungal `activity_observations`, pathogen-specific resistance mechanisms, producer claims, causal graphs, datasets, or discussions are present. The exact derived worklist rows keep `CHEBI:106738` in `mechanism` and `review-readiness` because it has one source literature lead but no record evidence or targets.

The absent activity, resistance, producer, causal-graph, dataset, and discussion slots are acceptable for a generated seed: the bounded searches did not produce an inspected exact source that would justify adding any of those optional sections during this read-only review.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The source-seeded energy-metabolism mode has primary support but is not yet curator-owned or tied to target/evidence slots, so the record cannot qualify as `REVIEWED`. | The YAML has ChEBI-derived `mode_of_action` and `mode_of_action_target_scope` fields but lacks `evidence`, `molecular_targets`, `activity_observations`, and `causal_graphs`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:106738` in `MECHANISM_REVIEW` with one literature lead, 0 record evidence items, and 0 targets. | Future curator-owned additions to `data/antibiotics/antifungal/s-famoxadone.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded identity, structure, ChEBI role terms, source-seeded mode, target scope, parent, xrefs, and path unchanged.
2. Promote the cytochrome-bc1/Qo-site mechanism only with curator-owned evidence anchored to `PMID:10843216` or an equivalent exact source for active `(S)-famoxadone`.
3. Add a concrete `molecular_targets` entry only after selecting an organism-aware cytochrome-bc1 target representation that distinguishes fungal Complex III biology from host-shared mitochondrial Complex III biology.
4. Do not transfer resistance or activity observations from broad QoI or racemic famoxadone papers to this exact `(S)` enantiomer unless the source demonstrates that the measurement applies to the same stereochemical form.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/antifungal/s-famoxadone.yaml`
2. `just validate-strict data/antibiotics/antifungal/s-famoxadone.yaml --out /tmp/s-famoxadone-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/s-famoxadone-worklist.tsv`
5. `just review-queue --limit 123`
6. `just lint`

Also re-run exact publication searches for `(S)-famoxadone`, `CHEBI:106738`, `PMID:10843216`, `PCCSBWNGDMYFCW-QFIPXVFZSA-N`, CAS `132584-12-4`, and the exact ChEBI synonyms before concluding that target, activity, or resistance evidence remains unresolved.

## Additional Notes

- The record has only seed and reseed curation-history events from `2026-08-30` and `2026-09-10`, accurately matching its generated `SEEDED` state.
- The ChEBI source includes `CHEBI:141153` as a direct role and as a mode-source term, so the local source-seeding path consistently imported the same ChEBI assertion as both an antifungal class signal and an energy-metabolism mechanism signal.
- Semantic Scholar returned HTTP 429 for all three bounded searches, so PubMed and local derived queues carried the exact publication checks in this review.
