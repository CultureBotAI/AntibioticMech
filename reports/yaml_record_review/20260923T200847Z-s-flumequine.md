# YAML Record Review: (S)-flumequine

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/unspecified/s-flumequine.yaml
- Started UTC: 2026-09-23T20:08:47Z
- Finished UTC: 2026-09-23T20:08:47Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/unspecified/s-flumequine.yaml` |
| Identifier | `CHEBI:85272` |
| Label | `(S)-flumequine` |
| Filing class | `ANTIMICROBIAL_UNSPECIFIED` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:85272` from ChEBI release `2026-08-30` |
| Minted source key | `antibioticmech:chebi-c865f7348d` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, role, structure, xref, and queue claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/unspecified/s-flumequine.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/unspecified/s-flumequine.yaml --out /tmp/s-flumequine-validate-strict.tsv` | Passed and wrote a strict validation TSV with no record-level errors. |
| `just verify-corpus --summary` | Passed: generated records on disk matched the committed source inventories, with no unexpected records, generated-field drift, missing `PATHS.tsv` identifiers, or stale lockfile rows. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-flumequine-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:85272` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 0 source literature leads, 0 record evidence items, and 0 targets. |
| `just review-queue --limit 117` | Passed and listed `(S)-flumequine` as `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed before writing this report. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:85272`, `label: (S)-flumequine`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. EBI OLS4 confirms that `CHEBI:85272` is active, non-obsolete, ChEBI-defining, and in the 3-star subset. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `DPSPPJIUMHPXMA-ZETCQYMHSA-N`, formula, charge, masses, IUPAC name, and `reaxys:8273774` xref. |
| Structure | SMILES `C[C@H]1CCc2cc(F)cc3c(=O)c(C(=O)O)cn1c23`, Standard InChI, InChIKey, formula `C14H12FNO3`, charge `0`, average mass `261.252`, and monoisotopic mass `261.08012` are the exact ChEBI-imported values. |
| Parent and sibling boundary | OLS and the local raw ChEBI row agree that `CHEBI:85269` is the broader unspecific flumequine structure. OLS relates the reviewed `CHEBI:85272` term only to `CHEBI:85273` by `RO_0018039`, matching the local `(R)-flumequine` sibling record. |
| Filing class | The sole ChEBI role is `CHEBI:33281`, the generic antimicrobial-agent root; no more specific antibacterial, antifungal, antiprotozoal, antiviral, or disinfectant role was present in the ChEBI row, so `ANTIMICROBIAL_UNSPECIFIED` is the expected filing class. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:85272` to `ANTIMICROBIAL_UNSPECIFIED` / `s-flumequine`, matching the current record path under `data/antibiotics/unspecified/`. |

An ignored-inclusive `find` check in `reports/yaml_record_review/` for `*flumequine*` found only the prior `(R)-flumequine` report and no previous exact `(S)-flumequine` report.

## Evidence

There is no claim-level evidence to audit because this seeded record has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_observations`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, datasets, discussions, or record-level `evidence`.

The existing scientific assertions are seed-owned ChEBI database assertions:

| Assertion | Support |
|---|---|
| `(S)-flumequine` is a ChEBI-grounded exact record with the listed IUPAC synonym and structure. | Supported by the committed ChEBI inventory row for `CHEBI:85272`, and independently consistent with the live OLS4 `CHEBI:85272` term. |
| The record carries the generic ChEBI `antimicrobial agent` role. | Supported by the same committed ChEBI row, which lists `CHEBI:33281` as the sole role term. |
| The record has a Reaxys xref. | Supported by the committed ChEBI row and live OLS4 term, both of which list `reaxys:8273774`. |

Bounded publication discovery did not find a source that could justify adding mechanism or activity claims for the exact reviewed structure:

| Query | Result |
|---|---|
| Exact `(S)-flumequine`, `DPSPPJIUMHPXMA-ZETCQYMHSA-N`, or `8273774` | PubMed returned 2 candidates; Semantic Scholar was rate-limited with HTTP 429. `PMID:21574550` concerns enantioselective quinoline hydrogenation and mentions synthesis of an intermediate for `(S)-flumequine`, but not antimicrobial activity, MIC, resistance, DNA gyrase inhibition, or another mechanism claim. `PMID:8273774` is a false-positive numeric collision on the Reaxys ID. |
| `flumequine enantiomer antibacterial MIC gyrase` | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Broader parent `flumequine DNA gyrase antibacterial MIC` query | PubMed returned 24 candidates, including parent-flumequine and quinolone-resistance leads plus recent DNA-gyrase screens of unrelated synthetic series, but the normalized PubMed titles and abstracts did not name `(S)-flumequine`, `CHEBI:85272`, or the exact InChIKey. |

## Completeness

The generated identity, source-concept, structure, parent, xref, and curation-history fields are complete enough for a ChEBI seed.

The record remains scientifically incomplete because it has only a generic `CHEBI:33281` activity role and does not yet have a direct activity observation, a specific antimicrobial class assignment, or an evidence-backed mode of action for the exact `(S)` enantiomer. The derived worklist keeps it in both the `mechanism` and `review-readiness` queues with no source literature leads, record evidence, or targets. That gap is consequential and blocks `REVIEWED` status under the local gate because the mode of action has not been checked.

The optional target, activity, resistance, producer, causal-graph, dataset, and discussion slots are correctly absent in the sense that the bounded exact searches did not produce inspected support that would justify populating them during this read-only review.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The record has no specific evidence-backed antimicrobial class, direct activity observation, or mode of action for exact `(S)-flumequine`, so it cannot qualify as `REVIEWED`. | The YAML only imports the generic ChEBI role `CHEBI:33281` and lacks `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_observations`, and `causal_graphs`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:85272` in `MECHANISM_REVIEW` with 0 literature leads, 0 record evidence items, and 0 targets. Bounded exact searches found only one synthesis mention and no exact biological or mechanism paper. | Future curator-owned additions to `data/antibiotics/unspecified/s-flumequine.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded identity, structure, generic ChEBI role term, parent, Reaxys xref, and path unchanged.
2. Before adding a specific filing class, `mode_of_action`, `mode_of_action_target_scope`, target, activity observation, resistance mechanism, producer claim, or causal graph, inspect primary evidence for the exact `CHEBI:85272` enantiomer or an explicitly structure-equivalent source.
3. Do not transfer flumequine mechanism, DNA gyrase inhibition, MIC, or quinolone-resistance claims from the unspecific `CHEBI:85269` parent or the `(R)` enantiomer to this `(S)` enantiomer unless the source itself demonstrates that the measurement or mechanism applies to the exact same stereochemical form.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/unspecified/s-flumequine.yaml`
2. `just validate-strict data/antibiotics/unspecified/s-flumequine.yaml --out /tmp/s-flumequine-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/s-flumequine-worklist.tsv`
5. `just review-queue --limit 117`
6. `just lint`

Also re-run exact publication searches for `(S)-flumequine`, `CHEBI:85272`, `DPSPPJIUMHPXMA-ZETCQYMHSA-N`, and the exact ChEBI IUPAC name before concluding that mechanism evidence remains unresolved.

## Additional Notes

- The record has only seed and reseed curation-history events from `2026-08-30`, accurately matching its generated `SEEDED` state.
- Parent `CHEBI:85269` is filed as `ANTIBACTERIAL` and has one review-queue PMID lead, but this exact `(S)` enantiomer does not inherit the parent's ARO concept, ARO structural class, parent PubChem xref, or source-literature lead.
- Semantic Scholar returned HTTP 429 for all bounded publication searches, so PubMed and local derived queues carried the no-exact-mechanism result for those searches.
