# YAML Record Review: (−)-gynuraone

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antibacterial/gynuraone.yaml`
- Started UTC: 2026-09-24T15:34:18Z
- Finished UTC: 2026-09-24T15:56:45Z
- Verdict: needs curation

## Target

| Field | Value |
| --- | --- |
| Identifier | `CHEBI:68289` |
| Label | `(−)-gynuraone` |
| Path | `data/antibiotics/antibacterial/gynuraone.yaml` |
| Class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concepts | `CHEBI:68289` from ChEBI 2026-08-30 |
| Maintained owner | Generated from `data/raw/chebi_antimicrobials.tsv` and `data/antibiotics/PATHS.tsv`; source-owned identity, structure, class, role, and parent changes must go through the ChEBI extractor, raw inventory, `curation/decisions.tsv`, or the seeder rather than a hand edit to this YAML. |

The record resolves unambiguously. `data/antibiotics/PATHS.tsv` maps
`CHEBI:68289` to `ANTIBACTERIAL/gynuraone`, and an ignored-inclusive search for
`CHEBI:68289`, `gynuraone`, and `COBAEYCUCRUGRP-MLUIRONXSA-N` across
`data/antibiotics`, `data/raw`, `curation`, `reports`, and `.git` refs/logs
found the expected raw ChEBI row, path row, review-queue row, target YAML, and
current branch checkout log, with no prior `gynuraone` report.

## Validation

| Check | Result |
| --- | --- |
| `rg --no-ignore --hidden -n -e 'CHEBI:68289' -e 'gynuraone' ...` | Pass: resolved only the expected ChEBI raw row, path row, `review-readiness` queue row, target YAML, and current branch checkout log; no prior report or duplicate record was found. Hidden and ignored files were included. |
| `find reports/yaml_record_review -maxdepth 1 -type f -iname '*gynuraone*'` and `find reports/yaml_record_review -maxdepth 1 -type f -iname '*68289*'` | Pass: no prior `gynuraone` or `68289` review report file was found. |
| `just validate data/antibiotics/antibacterial/gynuraone.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antibacterial/gynuraone.yaml --out /tmp/gynuraone-validate-strict.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Pass: 2,939 records expected and present; 0 missing, unexpected, or drifted records; 0 `PATHS.tsv` discrepancies. The only diagnostic was the known unrelated `iclaprim`/`CHEBI:31724` CARD cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` | Pass: wrote the full TSV. `CHEBI:68289` appears on `mechanism` and `review-readiness` only. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Pass: regenerated the full `review-readiness` queue and listed `CHEBI:68289` as `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |

No narrower single-record term, reference, or history validator is exposed for
plain generated ChEBI records. The full-corpus `worklist` and `verify-corpus`
checks above are the documented repository checks for those concerns.

## Identity and Grounding

The YAML denotes one exact, ChEBI-grounded structure: neutral
`(−)-gynuraone`, ChEBI's `(2R,3R)` stereoisomer of
3,5-dihydroxy-2-methyl-2,3-dihydro-4H-chromen-4-one.

| Claim | Review |
| --- | --- |
| ChEBI identity | The official OLS4 term lookup for `CHEBI:68289` returned one active, non-obsolete, 3-star ChEBI class labelled `(-)-gynuraone`, matching the YAML label apart from ASCII minus normalization. |
| Structure | OLS4, `data/raw/chebi_antimicrobials.tsv`, and the YAML agree on SMILES `C[C@H]1Oc2cccc(O)c2C(=O)[C@@H]1O`, Standard InChI `InChI=1S/C10H10O4/c1-5-9(12)10(13)8-6(11)3-2-4-7(8)14-5/h2-5,9,11-12H,1H3/t5-,9-/m1/s1`, Standard InChIKey `COBAEYCUCRUGRP-MLUIRONXSA-N`, formula `C10H10O4`, neutral charge, average mass `194.186`, and monoisotopic mass `194.05791`. |
| Synonym | OLS4 reports the same exact IUPAC synonym retained in the record: `(2R,3R)-3,5-dihydroxy-2-methyl-2,3-dihydro-4H-chromen-4-one`. |
| ChEBI parents | The OLS4 parent lookup returns exactly the four broader classes in `parent_compounds`: `CHEBI:2468` secondary alpha-hydroxy ketone, `CHEBI:33853` phenols, `CHEBI:35681` secondary alcohol, and `CHEBI:38763` chromanone. |
| Activity role and filing class | OLS4 returns `CHEBI:33282` as a direct `has role` target for `CHEBI:68289`, and the `CHEBI:33282` role itself resolves as active, non-obsolete `antibacterial agent`; filing the record as `ANTIBACTERIAL` follows that retained ChEBI antimicrobial role. OLS4 also reports a non-antimicrobial `CHEBI:76946` fungal metabolite role, which the generated `activity_roles` field correctly omits. |
| Xrefs | The generated record has no `xrefs`, and `data/raw/chebi_antimicrobials.tsv` has no raw exact-equivalence database identifiers for `CHEBI:68289` beyond `PMID:21244021` as a literature reference. |

No wrong identity or grounding conflict was found.

## Evidence

The record has no `evidence` array and no object-level evidence-bearing
assertions. That is honest for a generated ChEBI-only seed: identity, role,
parents, and structure reproduce from ChEBI, while there are no molecular
targets, resistance mechanisms, MIC measurements, producer claims, clinical
assertions, datasets, or causal edges that would need primary-paper evidence.

`CHEBI:68289` carries one source literature lead, `PMID:21244021`. NCBI
ESummary and EFetch both resolve that identifier to Siddiqui et al.,
Journal of Natural Products, 2011, DOI `10.1021/np100730b`, titled
"Diversonol and blennolide derivatives from the endophytic fungus
Microdiplodia sp.: absolute configuration of diversonol." The abstract says a
chemical investigation of a `Microdiplodia` strain isolated from
*Lycium intricatum* produced four new compounds including a
2,3-dihydrochroman-4-one and that most metabolites had antibacterial activity
against *Legionella pneumophila* and/or antifungal activity against
*Microbotryum violaceum*. That supports the paper as a plausible ChEBI source
for an endophytic fungal chromanone with antibacterial activity, but the PubMed
abstract does not expose a compound-specific MIC, target, or mode of action for
`(−)-gynuraone`.

## Completeness

`CHEBI:68289` remains intentionally thin. It has no CARD source concept, no
CARD target, no CARD resistance edges, no MIBiG producer import, and no
curator-authored mechanism evidence to audit. The complete worklist places this
identifier only on:

- `mechanism`: absent mode of action, with `0 CARD target(s), 0 resistance edge(s) to build on`
- `review-readiness`: `MECHANISM_REVIEW`, with 1 source literature lead, 0 record evidence items, and 0 targets

The same complete worklist has no exact `CHEBI:68289` row for
`target-evidence`, `minted`, `xref-unverified`, `xref-name-conflict`,
`xref-span-conflict`, `multi-component`, `producer-candidate`, or
`activity-candidate`.

The `producer-candidate` absence is a worklist false negative for this record:
ChEBI's definition says "Isolated form the endophytic fungus", so the
`isolated from` detector does not catch the `Microdiplodia` source lead. That
does not authorize a `producer_organisms` assertion, but it keeps
`PMID:21244021` as a bounded full-text check for strain/source context and
compound-specific activity.

Empty slots for `mode_of_action`, `mode_of_action_target_scope`,
`molecular_targets`, `activity_observations`, `resistance_mechanisms`,
`producer_organisms`, `clinical_status_assertions`, `datasets`, and
`causal_graphs` are preferable to inferring a target or measured activity from
the ChEBI role alone.

## Findings

| Severity | Finding | Evidence | Maintained owner |
| --- | --- | --- | --- |
| Major | The record still needs compound-specific mechanism review before it can become `REVIEWED`. | The YAML has no `mode_of_action`, no molecular targets, no activity observations, and no causal graph. `just worklist` keeps `CHEBI:68289` on `mechanism` and `review-readiness` even though ChEBI supplies `PMID:21244021` as a source literature lead. The PubMed abstract for that lead confirms the Microdiplodia natural-products context but does not expose a compound-specific MIC, molecular target, or mode of action for `(−)-gynuraone`. | Future curator-owned fields on `data/antibiotics/antibacterial/gynuraone.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |

No blocker findings found.

No minor findings found.

## Recommended Edits

1. Do not edit the source-owned identity, structure, synonym, class, role,
   parent, or source-concept fields for `CHEBI:68289`: they reproduce from the
   official ChEBI row and from `PATHS.tsv`.
2. Inspect the full text or supplementary data for Siddiqui et al. 2011,
   DOI `10.1021/np100730b`, and curate only the compound-specific claims it
   supports. Add `mode_of_action`, `molecular_targets`, or `causal_graphs`
   only when the paper gives direct target or mechanism evidence for
   `(−)-gynuraone`; add `activity_observations` for compound-specific organism
   assay results, with MIC value and units only when a MIC is actually
   reported, and do not promote the abstract's pooled "most metabolites"
   activity statement into a per-compound observation.
3. Leave `producer_organisms` empty unless an inspected primary source or MIBiG
   record identifies the biosynthetic organism and enough strain context for a
   schema-ready producer claim. The abstract-level `Microdiplodia sp.` lead is
   not enough on its own.

## Follow-up Checks

If a future curator changes this record:

- Run `just validate-strict data/antibiotics/antibacterial/gynuraone.yaml --out /tmp/antibioticmech-record-validation.tsv`.
- Run `just verify-corpus` to prove source-owned generated fields still match
  `data/raw/`.
- Run `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist.tsv` and
  confirm `CHEBI:68289` leaves `mechanism` and `review-readiness` only after an
  exact mechanism review has actually been curated.
- Re-run `just lint`, `git diff --check`, and `git diff --cached --check`
  before opening a PR.

## Additional Notes

The official ChEBI definition contains the wording "Isolated form the
endophytic fungus"; the YAML correctly mirrors that upstream text rather than
normalizing the likely `from` typo in generated output.
