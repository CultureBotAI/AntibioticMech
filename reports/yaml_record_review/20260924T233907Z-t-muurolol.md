# YAML Record Review: (−)-Τ-muurolol

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/τ-muurolol.yaml`
- Started UTC: 2026-09-24T23:21:00Z
- Finished UTC: 2026-09-24T23:39:07Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:132906` |
| Label | `(−)-Τ-muurolol` |
| Path | `data/antibiotics/antifungal/τ-muurolol.yaml` |
| Record class | `AntibioticRecord` |
| Generated or maintained | Generated from `data/raw/chebi_antimicrobials.tsv` plus `data/antibiotics/PATHS.tsv` |
| `antimicrobial_class` | `ANTIFUNGAL` |
| `curation_status` | `SEEDED` |
| `grounding_status` | `EXACT` |
| Source concepts | `CHEBI:132906`, ChEBI `2026-08-30`, role `CHEBI:24127` |
| Structure key | `LHYHMMRYTDARSZ-AJNGGQMLSA-N` |

The full 71-line YAML record was read before reviewing claims. The record is a
ChEBI-seeded antifungal with a ChEBI structure, no record-level `evidence`, no
`mode_of_action`, no `molecular_targets`, no `activity_spectrum`, no
`producer_organisms`, no `resistance_mechanisms`, no `causal_graphs`, no
`datasets`, no `discussions`, and only the three seed/reseed history events
from 2026-08-30.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/τ-muurolol.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/τ-muurolol.yaml --out /tmp/t-muurolol-132906-validate-strict.tsv` | Passed: scanned 1 file with 0 ERROR rows. |
| `just verify-corpus --summary` | Passed: 2,939 records expected and on disk; 0 missing, unexpected, drifted, `PATHS.tsv`, or stale-lockfile rows. The only diagnostic printed was the known unrelated `iclaprim` / `CHEBI:31724` CARD cross-reference refusal. |
| `just worklist` | Passed and rebuilt the curation backlog. `CHEBI:132906` is on `review-readiness` and owes `MECHANISM_REVIEW`. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed and wrote the full 2,859-row review-readiness queue. |
| Hidden-inclusive target/xref search | Passed for the bounded review. `rg --no-ignore --hidden` over `data/antibiotics`, `data/raw`, `curation`, `reports/yaml_record_review`, `/tmp/antibioticmech-review-queue.tsv`, and `.git` resolved the generated record and raw ChEBI row. Exact xrefs `cas:19912-62-0`, `knapsack:C00020154`, and `reaxys:2210160` were found only on the raw ChEBI row and target YAML in the searched non-git trees. |
| OLS4 lookup for `CHEBI:132906` | Passed. Current OLS/ChEBI returned `obo_id` `CHEBI:132906`, label `(-)-Tau-muurolol`, the matching definition, formula, charge, masses, SMILES, Standard InChI, exact InChIKey `LHYHMMRYTDARSZ-AJNGGQMLSA-N`, all three xrefs, and all six PubMed xrefs from the committed ChEBI row. |
| OLS4 parent lookup for `CHEBI:132906` | Passed. Current OLS direct parents are the four generated `parent_compounds`: `CHEBI:138397`, `CHEBI:22975`, `CHEBI:26878`, and `CHEBI:36785`. |
| OLS4 lookup for role `CHEBI:24127` | Passed. `CHEBI:24127` resolves to `fungicide`; it is mapped to `ANTIFUNGAL` in `conf/sources.yaml`. |
| OLS4 search for exact InChIKey `LHYHMMRYTDARSZ-AJNGGQMLSA-N` | Returned 0 search hits. The term lookup by CURIE is authoritative for the exact ChEBI record; the OLS free-text search did not find by InChIKey. |
| PubMed `efetch` for the six ChEBI literature xrefs | Passed. `PMID:18206367`, `PMID:19072130`, `PMID:22129092`, `PMID:22888533`, `PMID:23177818`, and `PMID:24079193` all resolved to PubMed records. |
| PubMed exact-name antimicrobial search | Passed as a bounded discovery check. The query over `Tau-muurolol`, `T-muurolol`, `τ-muurolol`, `10-epi-α-muurolol`, and `epi-α-muurolol` with antimicrobial/MIC/Candida terms returned 27 PubMed hits. |
| PubMed CAS/KNApSAcK/InChIKey search | Passed as a negative bounded check. NCBI reported 0 hits for `19912-62-0`, `C00020154`, and `LHYHMMRYTDARSZ`. |
| PubMed exact-name target/mechanism search | Passed as a bounded discovery check. The query returned 14 hits; no inspected public abstract exposed wet-lab evidence for a direct T-muurolol molecular target or enough causal evidence to fill `mode_of_action`. |
| PubMed exact-name producer/biosynthesis search | Passed as a bounded discovery check. The query returned 8 hits; they are producer or sesquiterpene-synthase leads that need source-level organism, strain, and exact stereochemistry review before any `ProducerOrganism` assertion. |
| KNApSAcK lookup for `C00020154` | Partial pass. `C00020154` resolves to `tau-Muurolol` / `epi-alpha-Muurolol` / `T-Muurolol`, formula `C15H26O`, and CAS `19912-62-0`, but the public page displays a stereochemical InChIKey and InChI that do not match ChEBI's exact structure. |
| CAS Common Chemistry lookup for `19912-62-0` | Not independently resolved. The public detail page returned an Angular shell with `Get detail failed`, and the JSON endpoint returned `Unauthorized`. |
| Reaxys `2210160` | Not checked directly: no public Reaxys resolver was available in this workflow. |

No narrower term, reference, or history validator is exposed in `justfile` for a
single generated ChEBI record. `just validate-strict` and `just verify-corpus
--summary` are the narrowest documented structural and generated-provenance
checks for this read-only review.

## Identity and Grounding

The record denotes the ChEBI structure `CHEBI:132906` exactly. The generated
label, definition, SMILES, Standard InChI, Standard InChIKey, formula, charge,
average mass, monoisotopic mass, xrefs, source role, and six source PubMed IDs
match row 2506 of `data/raw/chebi_antimicrobials.tsv`. Current OLS agrees with
the same structure, and the generated `source_concepts` entry preserves the
native ChEBI ID, ChEBI label, ChEBI `2026-08-30` source version, and
`fungicide` role term.

`CHEBI:132905` / `α-cadinol` is a local same-connectivity sibling with the same
formula and InChIKey first block, but it has the stereochemical InChIKey
`LHYHMMRYTDARSZ-BYNSBNAKSA-N` and a separate ChEBI record at
`data/antibiotics/antifungal/α-cadinol.yaml`. No local record or existing review
report in the hidden-inclusive search carried KNApSAcK's displayed
`LHYHMMRYTDARSZ-MLPSCTRZNA-N` stereochemical block.

The ChEBI role `CHEBI:24127` resolves to `fungicide`, and `conf/sources.yaml`
maps that role to `ANTIFUNGAL`; the record has no additional roles to preserve
in `activity_roles`.

## Evidence

The record has no curator-owned evidence objects. That is acceptable for
ChEBI-seeded identity and class metadata, because the source concept supplies
database provenance, but it leaves every organismal activity, target, mechanism,
producer, and causal assertion absent.

| Reference | Review |
|---|---|
| `PMID:18206367` | Direct pure-compound lead. The public abstract reports that *Calocedrus macrolepis* var. *formosana* leaf oil constituents were evaluated in vitro against six plant pathogenic fungi and that `T-muurolol` and `α-cadinol` strongly inhibited *Rhizoctonia solani* and *Fusarium oxysporum* with `IC50` values `< 50 microg ml(-1)`, with additional mycelial-growth inhibition of *Colletotrichum gloeosporioides*, *Pestalotiopsis funerea*, *Ganoderma australe*, and *Fusarium solani*. A curator needs the paper's organism-by-compound table before writing discrete `ActivityObservation` values. |
| `PMID:22129092` | Direct pure-compound lead. The public abstract reports bioactivity-guided isolation of `T-cadinol`, cedrol, `T-muurolol`, `(-)-epi-cedrol`, and three diterpenes from *Cunninghamia konishii* wood; it further states that `T-muurolol` inhibited four wood-rot fungi at 100 microg/mL with antifungal indices from 39.5-100.0%. A curator needs the source table for the organism-level values. |
| `PMID:15609830` | Not a ChEBI xref on the current row, but an exact public-abstract lead. It reports that two leaf-oil constituents, `alpha-cadinol` and `T-muurolol`, exhibited the strongest antifungal activity against four fungi in a *Calocedrus formosana* leaf-oil study. The termite mortality measurement is not antimicrobial. |
| `PMID:19072130` | Chemical and producer discovery lead. The PubMed title is a *Journal of Natural Products* report of T-muurolol sesquiterpenes from marine *Streptomyces* sp. M491; the PubMed record has no public abstract. KNApSAcK maps `C00020154` in `Streptomyces sp.` to a Ding 2009 *Journal of Natural Products* citation, but KNApSAcK's public structure for this accession is not stereochemically identical to `CHEBI:132906`. |
| `PMID:22888533` | Essential-oil/extract context only in the public abstract. It names `T-muurolol` as 13.85% of *Saurauia lantsangensis* root oil, then reports activity for the oil and solvent extracts rather than for pure exact T-muurolol. |
| `PMID:23177818` | Essential-oil context only in the public abstract. It names `T-muurolol` as 5.32% of *Homalomena aromatica* rhizome essential oil, then reports antimicrobial activity for the whole oil. |
| `PMID:24079193` | Essential-oil composition lead. It reports `t-muurolol` as a 5.1% constituent of *Chiliadenus lopadusanus* flower oil and then makes a nonspecific statement about roles of several identified compounds in antibacterial, antifungal, allelopathic, and spasmolytic activity. |
| `PMID:39273596` | Molecular-target near miss. The exact PubMed search found a 2024 in silico *Staphylococcus aureus* screening study of T-muurolol, but docking against bacterial proteins is not primary wet evidence for direct binding or growth inhibition. |
| `PMID:21276937` | Stereochemical near miss. The exact PubMed search found a *Streptomyces clavuligerus* sesquiterpene-synthase paper that produced `(+)‑T-muurolol`, not exact `(−)-Τ-muurolol`. |

## Completeness

`mode_of_action`, `molecular_targets`, `causal_graphs`, and `activity_spectrum`
are consequentially absent. The ChEBI source row and exact PubMed searches
surface direct antifungal assays for pure T-muurolol, but the public abstracts
do not expose enough per-organism numeric values and assay conditions to write
claim-level `ActivityObservation` objects during this read-only review.

No inspected source supplied a wet-lab molecular target or causal graph for
exact `(−)-Τ-muurolol`. The 2024 docking screen can help future discovery but
does not establish a direct target for curation. The exact producer trail is
also not yet record-ready: KNApSAcK links `C00020154` to `Streptomyces sp.`, and
the ChEBI source PMID names marine *Streptomyces* sp. M491 in the title, but the
KNApSAcK structure mismatch and absent public PubMed abstract leave exact
stereochemistry and strain-scoped taxon grounding unresolved.

Empty `resistance_mechanisms`, `clinical_status_assertions`, `datasets`, and
`discussions` are not immediate validation defects for this ChEBI-only natural
product. The bounded exact searches over PubMed, local raw data, curated records,
and prior reports did not find a CARD/ARO resistance slice, public dataset, or
clinical/regulatory assertion for `CHEBI:132906`.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Exact pure-compound antifungal activity is uncurated, and the record remains a `SEEDED` stub with no checked mechanism or activity observations. | The YAML has no `mode_of_action`, no `activity_spectrum`, no `molecular_targets`, and no `causal_graphs`. `PMID:18206367` and `PMID:22129092` are exact T-muurolol pure-compound antifungal assay leads, and `PMID:15609830` is a third exact antifungal lead, but none of those data are represented as claim-level organism/activity assertions. | Curator-owned additions in `data/antibiotics/antifungal/τ-muurolol.yaml`, written only through `record_curation_event` plus `write_validated_antibiotic`. |
| Major | `knapsack:C00020154` is unresolved as an exact xref because the public KNApSAcK structure is stereochemically different from `CHEBI:132906`. | ChEBI and the generated record use `LHYHMMRYTDARSZ-AJNGGQMLSA-N`; KNApSAcK `C00020154` resolves the expected names, formula, and CAS but displays InChIKey `LHYHMMRYTDARSZ-MLPSCTRZNA-N` and a Standard InChI with a different stereochemistry layer. Exact xrefs must denote the same structure. | Seeded from the ChEBI xrefs in `data/raw/chebi_antimicrobials.tsv`; future cleanup belongs in the ChEBI extractor/seeder xref filter or a source-owned xref decision layer, not a hand edit to generated YAML. |

## Recommended Edits

1. Curate exact T-muurolol organism-level antifungal observations from the full
   text or tables of `PMID:18206367`, `PMID:22129092`, and, if it reports
   record-ready per-organism values for the exact compound, `PMID:15609830`.
   Preserve the reported endpoint (`IC50`, percent mycelial inhibition, or MIC
   only where the paper actually reports MIC), assay method, organism names, and
   units rather than converting a 100 microg/mL screen into an MIC.

2. Keep `mode_of_action` empty until a primary source establishes a
   compound-specific fungal or bacterial target. If a future curator reviews the
   docking-only *S. aureus* paper, cite it as an in silico dataset or discussion
   lead rather than as direct binding evidence.

3. Re-check `knapsack:C00020154` against the ChEBI xref during any future xref
   cleanup. If KNApSAcK continues to publish `LHYHMMRYTDARSZ-MLPSCTRZNA-N`,
   withhold it from exact `xrefs` through a reproducible source-owned filter or
   decision so a seed run does not reintroduce the stereochemical mismatch.

4. Inspect the Ding 2009 *Streptomyces* sp. M491 paper behind `PMID:19072130`
   before curating a producer. Any retained producer needs a meaningful
   NCBITaxon grounding, the source strain if reported, and evidence that exact
   `(−)-Τ-muurolol` rather than the KNApSAcK-displayed stereoisomer was produced.

## Follow-up Checks

| After | Check |
|---|---|
| Any activity, mechanism, producer, or xref curation | `just validate-strict data/antibiotics/antifungal/τ-muurolol.yaml --out /tmp/t-muurolol-132906-validate-strict.tsv` |
| Any source-owned xref filter, ChEBI extractor change, or seed-affecting decision | `just seed`, then `just seed-canary CHEBI:132906`, then `just verify-corpus --summary` |
| Any curated YAML mutation | Re-read `data/antibiotics/antifungal/τ-muurolol.yaml` and confirm all `ActivityObservation`, `ProducerOrganism`, `MolecularTarget`, and `CausalEdge` items carry claim-level evidence on the object rather than record-level evidence. |
| Any KNApSAcK xref decision | Re-fetch official OLS/ChEBI and KNApSAcK `C00020154`, then compare the displayed Standard InChIKey against `LHYHMMRYTDARSZ-AJNGGQMLSA-N`. |

## Additional Notes

- The generated label uses a Greek capital tau, `(−)-Τ-muurolol`; ChEBI's OLS
  label renders the same concept in ASCII as `(-)-Tau-muurolol`, and the exact
  synonym list includes lower-case Greek `(−)-τ-muurolol`.
- Current `review-readiness` prints only the first five source literature leads
  in its `source_id` display field, so the `CHEBI:132906` queue row shows
  `PMID:18206367|PMID:19072130|PMID:22129092|PMID:22888533|PMID:23177818`
  while its hint correctly counts six leads. The sixth is `PMID:24079193`, and
  it is present in the committed ChEBI raw row and current OLS xrefs.
- `reaxys:2210160` is consistent with current ChEBI as an inherited database
  cross-reference, but Reaxys was not directly checkable because no public
  resolver was available.
