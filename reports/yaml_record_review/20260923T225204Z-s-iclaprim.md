# YAML Record Review: (S)-iclaprim

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antibacterial/s-iclaprim.yaml
- Started UTC: 2026-09-23T22:52:04Z
- Finished UTC: 2026-09-23T22:52:04Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antibacterial/s-iclaprim.yaml` |
| Identifier | `CHEBI:131753` |
| Label | `(S)-iclaprim` |
| Filing class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:131753` from ChEBI release `2026-08-30` |
| Minted source key | `antibioticmech:chebi-8c18708f2a` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, role, structure, xref, and source-owned mechanism claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/s-iclaprim.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/antibacterial/s-iclaprim.yaml --out /tmp/s-iclaprim-validate-strict.tsv` | Passed: 1 file checked, 0 files with `ERROR`, and 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed: all 2,939 expected records were on disk, no unexpected records were present, no generated fields drifted, no identifiers were absent from `PATHS.tsv`, and no stale lockfile rows were present. The only diagnostic was the pre-existing CARD `ARO:3000337`/CHEBI:31724 iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-iclaprim-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:131753` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 1 source literature lead, 0 record evidence items, and 0 targets. The only diagnostic was the pre-existing CARD `ARO:3000337`/CHEBI:31724 iclaprim cross-reference refusal. |
| `just review-queue --limit 130` | Passed and listed `(S)-iclaprim` as `MECHANISM_REVIEW: mechanism is source-seeded and not curator-checked; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed before writing this report. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:131753`, `label: (S)-iclaprim`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `HWJPWWYTGBZDEG-CQSZACIVSA-N`, formula, charge, masses, IUPAC name, synonyms, Reaxys xref, antibacterial role, and dihydrofolate-reductase-inhibitor role. |
| Live ChEBI/OLS | OLS confirmed an active, non-obsolete, 3-star `CHEBI:131753` term with the same label, definition, formula, SMILES, InChI, InChIKey, Reaxys `20535807`, PubMed `19622858`, and synonyms `AR 102` and `AR-102`. |
| Structure | SMILES `[H][C@]1(C2CC2)C=Cc2c(Cc3cnc(N)nc3N)cc(OC)c(OC)c2O1`, Standard InChI, InChIKey, formula `C19H22N4O3`, charge `0`, average mass `354.41`, and monoisotopic mass `354.16919` are the exact ChEBI-imported values. |
| Parent boundary | OLS and the local seed both place `CHEBI:131751` as the one broader iclaprim parent. OLS links the sibling `(R)-iclaprim` / `CHEBI:131752` by `is_enantiomer_of` with the different InChIKey `HWJPWWYTGBZDEG-AWEZNQCLSA-N`, so this record denotes the intended individual stereoisomer. |
| Filing class | The sole ChEBI activity role is `CHEBI:36047` / antibacterial drug, which `conf/sources.yaml` maps to `ANTIBACTERIAL` at priority 2; filing the record under `data/antibiotics/antibacterial/` is consistent with that mapping. |
| Source-owned mode | ChEBI also imports the mode-of-action role `CHEBI:50683`, locally mapped to `FOLATE_PATHWAY_INHIBITION` and `HOST_SHARED_TARGET`; the YAML notes correctly say the assignment is source-seeded and not a curator's mechanistic review. |
| Xrefs | `reaxys:20535807` is a direct ChEBI row import and is exact to the `(S)` record. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:131753` to `ANTIBACTERIAL` / `s-iclaprim`, matching the current record path. |

Before this report, an ignored-inclusive search across `data/raw`, `curation`, `reports/yaml_record_review`, and `data/antibiotics` for `CHEBI:131753` and `iclaprim` found the expected ChEBI raw rows, ARO raw rows for the refused CARD `ARO:3000337` cross-reference, path-lock rows for both enantiomers, review-queue rows, the exact YAML records, and the prior `(R)-iclaprim` sibling report. An ignored-inclusive `find` for `*iclaprim*` reports found only `reports/yaml_record_review/20260923T090849Z-r-iclaprim.md` before writing this report, so there was no earlier exact `(S)-iclaprim` review.

## Evidence

There is no claim-level literature evidence to audit because this seeded record has no `molecular_targets`, `activity_observations`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, datasets, discussions, or record-level `evidence`.

The existing scientific assertions are seed-owned ChEBI database assertions:

| Assertion | Support |
|---|---|
| `(S)-iclaprim` is a ChEBI-grounded exact record with the listed IUPAC name, `AR-102` synonyms, Reaxys xref, and structure. | Supported by the committed ChEBI inventory row for `CHEBI:131753` and by live OLS for the same term. |
| The record carries the ChEBI antibacterial-drug role term. | Supported by the same ChEBI row, which lists `CHEBI:36047` as its activity role term. |
| The record carries `FOLATE_PATHWAY_INHIBITION` and `HOST_SHARED_TARGET`. | Supported only as a source-seeded ChEBI role transform from `CHEBI:50683`; no primary paper has been normalized into the record for this exact enantiomer. |

Bounded publication discovery found exact or near-exact sources that a future curator should inspect, but no claim-level evidence has been curated into the record yet:

| Query | Result |
|---|---|
| Exact `(S)-iclaprim` label through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Exact `HWJPWWYTGBZDEG-CQSZACIVSA-N` InChIKey through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Exact ChEBI IUPAC name through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar returned an invalid response. |
| `AR-102 iclaprim` through PubMed and Semantic Scholar | PubMed and Semantic Scholar found `PMID:19622858`, the ChEBI source lead that compares AR-101, AR-102, and racemic iclaprim against wild-type and F98Y trimethoprim-resistant *Staphylococcus aureus* dihydrofolate reductase and reports ternary DHFR/NADPH inhibitor structures. Semantic Scholar also returned exact AR-102 PDB entries for wild-type DHFR and F98Y DHFR. That paper is the best candidate for future exact-enantiomer target evidence, but full-text curation is still needed to capture the enzyme forms, species, resistance mutant, assay values, and X-ray evidence without overgeneralizing from the racemate. |
| Direct PubMed summary fetch for `PMID:19622858` and `PMID:20446707` | `PMID:19622858` is the ChEBI source lead for DHFR inhibition and structures of AR-101, AR-102, and racemic iclaprim. `PMID:20446707` reports enantioselective synthesis of `(R)` and `(S)` iclaprim from cyclopropyl homoallyl alcohols and is useful for exact-enantiomer identity rather than antimicrobial mechanism curation. |

## Completeness

The generated identity, source-concept, structure, parent, xref, ChEBI-derived mode, antibacterial role, and curation-history fields are complete enough for a ChEBI seed.

The record remains scientifically incomplete because no curator has checked the source-seeded DHFR mechanism against evidence for exact `(S)-iclaprim`, and the record has no explicit molecular target, resistance mechanism, direct activity observation, causal graph, or claim-level evidence. The derived worklist keeps it in both the `mechanism` and `review-readiness` queues with 1 ChEBI source literature lead, no record evidence, and no targets. That gap blocks `REVIEWED` status under the local gate because the mode of action has not been checked.

The optional target, activity, resistance, producer, causal-graph, dataset, and discussion slots should stay absent until a curator reads `PMID:19622858` and any linked exact-enantiomer structural entries in full, chooses the right bacterial target entities for wild-type and mutant DHFR, and adds claim-level evidence through the validated writer.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The record has a source-seeded folate-pathway mode and host-shared target scope but no primary evidence for exact `(S)-iclaprim`, so the mechanism cannot yet qualify as curator-checked and the record cannot qualify as `REVIEWED`. | `mode_of_action_notes` explicitly mark the mechanism as not curator-reviewed; the YAML lacks `molecular_targets`, `resistance_mechanisms`, `activity_observations`, and `causal_graphs`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:131753` in `MECHANISM_REVIEW` with 1 source literature lead, 0 record evidence items, and 0 targets. | Future curator-owned additions to `data/antibiotics/antibacterial/s-iclaprim.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded ChEBI identity, structure, antibacterial role, parent, Reaxys xref, source concept, ChEBI-derived mode, and path unchanged.
2. Inspect `PMID:19622858` before promoting the source-seeded `FOLATE_PATHWAY_INHIBITION` assertion or adding target, activity, resistance, or causal-graph claims for `CHEBI:131753`.
3. Represent AR-101 and AR-102 as distinct enantiomer-specific measurements whenever the primary paper distinguishes them; do not merge their enzyme-inhibition or structural results into the racemic `CHEBI:131751` parent unless the source reports a racemate result.
4. Keep the refused CARD `ARO:3000337` to CHEBI:31724 cross-reference out of this record unless a future CARD or curator source maps ARO iclaprim to an exact, non-conflicting ChEBI identifier.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/antibacterial/s-iclaprim.yaml`
2. `just validate-strict data/antibiotics/antibacterial/s-iclaprim.yaml --out /tmp/s-iclaprim-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/s-iclaprim-worklist.tsv`
5. `just review-queue --limit 130`
6. `just lint`

Also re-run exact publication searches for `(S)-iclaprim`, `AR-102`, `CHEBI:131753`, `HWJPWWYTGBZDEG-CQSZACIVSA-N`, and the exact ChEBI IUPAC name before deciding the final evidence model.

## Additional Notes

- The record has only seed and reseed curation-history events from `2026-08-30`, accurately matching its generated `SEEDED` state.
- `ARO:3000337` / iclaprim appears in the generated worklist only as a refused CARD cross-reference to `CHEBI:31724` / Isoaminile citrate; the refusal is pre-existing, self-contained, and correctly prevents that conflicting CARD identity from adding structure, role, target, or resistance claims here.
- The sibling `(R)-iclaprim` record carries the `AR 101`/`AR-101` synonyms and `reaxys:20535806`; it must remain distinct from this `(S)`/AR-102 record.
- Semantic Scholar returned HTTP 429 or an invalid response for exact-name, exact-structure, and exact-IUPAC searches; PubMed plus Semantic Scholar carried the `AR-102` candidate discovery used in this report.
