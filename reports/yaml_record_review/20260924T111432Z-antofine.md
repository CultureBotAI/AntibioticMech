# YAML Record Review: (−)-antofine

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/antofine.yaml`
- Started UTC: 2026-09-24T10:50:00Z
- Finished UTC: 2026-09-24T11:14:32Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/antofine.yaml` |
| Class | `AntibioticRecord` |
| Identifier | `CHEBI:143911` |
| Label | `(−)-antofine` |
| Antimicrobial class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Source concept | ChEBI `CHEBI:143911`, version `2026-08-30`, minted key `antibioticmech:chebi-af62c3ea84` |
| Standard InChIKey | `NCVWJDISIZHFQS-CQSZACIVSA-N` |
| Xrefs | `cas:32671-82-2`, `knapsack:C00035244` |

`antofine.yaml` is a generated exact ChEBI record for `CHEBI:143911`. It
contains ChEBI-owned identity, definition, synonyms, structure, xrefs, parentage,
and the two retained in-scope ChEBI activity roles `antiviral agent`
`CHEBI:22587` and generic `antimicrobial agent` `CHEBI:33281`. It has no
record-level `evidence`, curated `mode_of_action`,
`mode_of_action_target_scope`, `molecular_targets`, `activity_spectrum`,
`resistance_mechanisms`, `producer_organisms`, `clinical_status`, `datasets`,
`discussions`, or `causal_graphs`.

## Validation

| Command or check | Result |
|---|---|
| `git fetch origin main --prune` | Passed; `main` was current before branching. |
| `rg -uu -n '(^\|[^0-9])CHEBI:143911([^0-9]\|$)\|antofine\|antofin' reports/yaml_record_review data/antibiotics data/raw curation .claude docs README.md conf src scripts tests` | Passed before this report was written; the ignored/hidden-inclusive search found the generated YAML, its `PATHS.tsv` row, the exact raw ChEBI row, and the review-queue row, but no prior exact `CHEBI:143911` report. |
| `just validate data/antibiotics/antiviral/antofine.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/antofine.yaml --out /tmp/antofine-validate-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2939 expected records were present on disk with no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` drift, and the known unrelated `ARO:3000337` iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/antofine-worklist.tsv` | Passed; exact `CHEBI:143911` appeared only on `mechanism` and `review-readiness`. |
| `just review-queue --limit 170 --tsv /tmp/antofine-review-queue.tsv` | Passed; exact `CHEBI:143911` listed as `MECHANISM_REVIEW`, with 10 committed source literature leads, 0 record evidence items, and 0 targets. |
| `rg -uu -n 'CHEBI:143911\|antibioticmech:chebi-af62c3ea84\|NCVWJDISIZHFQS-CQSZACIVSA-N\|32671-82-2\|C00035244\|PMID:12350151\|PMID:12868894\|PMID:17049857\|PMID:20151387\|PMID:21739223\|PMID:22753104\|PMID:22880628\|PMID:24934681\|PMID:28666888\|PMID:28805975' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed; the ignored/hidden-inclusive search found only the generated YAML, the raw ChEBI row, `PATHS.tsv`, and the checkpoint review-queue row for exact `CHEBI:143911`, its generated structure key, its source-concept key, its generated xrefs, and its committed source PMIDs. |
| `rg -uu -n 'CHEBI:143911\|NCVWJDISIZHFQS-CQSZACIVSA-N\|32671-82-2\|C00035244\|antofine' data/raw/aro_antibiotics.tsv data/raw/aro_resistance_edges.tsv data/raw/aro_target_edges.tsv data/raw/bindingdb_target_measurements.tsv data/raw/fda_clinical_status.tsv data/raw/mibig_producers.tsv data/raw/phibase_amr.tsv curation/decisions.tsv` | Passed; no ARO, BindingDB, Drugs@FDA, MIBiG, PHI-base, or grounding-decision row exists for exact `CHEBI:143911`, its structure key, or its generated xrefs. The search included ignored/hidden files. |
| OLS4 lookup for `CHEBI:143911` | Passed; OLS resolved one non-obsolete ChEBI class with `obo_id` `CHEBI:143911`, label `(-)-antofine`, the generated definition, the generated synonyms, and the generated formula, charge, SMILES, Standard InChI, Standard InChIKey, and xrefs. |
| ChEBI page lookup for `CHEBI:143911` | Passed; current ChEBI agreed with the generated label, ASCII label, definition, formula, average mass, monoisotopic mass, SMILES, Standard InChI, Standard InChIKey, KNApSAcK and CAS xrefs, parent terms, and `antiviral agent` plus `antimicrobial agent` roles. |
| KNApSAcK lookup for `C00035244` | Partial pass; KNApSAcK resolved `C00035244` to `Antofine` / `(-)-Antofine` with formula `C23H25NO3` and CAS `32671-82-2`, but its listed InChIKey was `NCVWJDISIZHFQS-YQTOOIBONA-N`, not ChEBI's `NCVWJDISIZHFQS-CQSZACIVSA-N`. |
| PubMed efetch for committed ChEBI PMIDs `12350151`, `12868894`, `17049857`, `20151387`, `21739223`, `22753104`, `22880628`, `24934681`, `28666888`, and `28805975` | Passed; all 10 identifiers resolved. The set covers synthesis, cytotoxicity, anti-inflammatory, anti-adipogenic, angiogenesis, phytotoxicity, tobacco mosaic virus, and antofine-analogue papers, but the most direct `Fusarium` and `Penicillium` antifungal papers on current ChEBI are not in the committed raw row. |
| PubMed efetch for current-ChEBI-only PMIDs `30691129` and `31186319` | Passed; both identifiers resolved and both are exact antimicrobial leads for 7-demethoxytylophorine / antofine. `PMID:30691129` has PMCID `PMC6406921`; `PMID:31186319` has PMCID `PMC6561021`. |
| PubMed exact discovery search for `(antofine OR 7-demethoxytylophorine OR NCVWJDISIZHFQS-CQSZACIVSA-N) AND (antiviral OR antimicrobial OR antibacterial OR antifungal OR mechanism OR target OR virus)` | Passed; PubMed returned 31 candidates. The exact InChIKey was not present in PubMed, but exact-name hits included direct antifungal papers against `Fusarium graminearum`, `Penicillium italicum`, and `Phytophthora capsici`; tobacco mosaic virus analogue papers; anticancer papers; and later reviews. |
| PubMed Central full-text lookups for `PMC6406921`, `PMC6561021`, `PMC9039625`, and `PMC11085548` | Passed; all four exact antofine / 7-demethoxytylophorine antimicrobial papers were open and inspectable. |

No separate focused term, reference, or history validator is exposed in
`justfile`; the single-record strict validator, full-corpus `verify-corpus`, and
full-corpus `worklist` are the documented narrow checks for seeded-field drift
and queue placement.

## Identity and Grounding

| Assertion | Review |
|---|---|
| Label and identifier | Supported. OLS and current ChEBI resolve `CHEBI:143911` to ASCII `(-)-antofine`; the generated label uses the equivalent Unicode minus form `(−)-antofine`. |
| Structure | Supported by the committed raw ChEBI row, OLS, and current ChEBI. The generated SMILES, Standard InChI, InChIKey `NCVWJDISIZHFQS-CQSZACIVSA-N`, formula `C23H25NO3`, neutral charge, average mass `363.457`, and monoisotopic mass `363.18344` agree across those three sources. |
| Source concept | Supported. The only source concept is exact ChEBI `CHEBI:143911`; no CARD/ARO, BindingDB, MIBiG, Drugs@FDA, PHI-base, or curator grounding-decision row is attached locally. |
| Filing class and roles | Supported for the seeded record. The raw ChEBI row gives `CHEBI:143911` the in-scope roles `CHEBI:22587` `antiviral agent` and `CHEBI:33281` `antimicrobial agent`; lower `priority` wins in `conf/sources.yaml`, but no retained ChEBI antifungal or antibacterial role exists, so the record is filed as `ANTIVIRAL`. |
| Parent terms | Supported. Current ChEBI resolves the generated parents as `CHEBI:22315` `alkaloid`, `CHEBI:35618` `aromatic ether`, `CHEBI:38164` `organic heteropentacyclic compound`, and `CHEBI:86322` `alkaloid antibiotic`. |
| Xrefs | Mostly supported as ChEBI assertions. Current ChEBI lists CAS `32671-82-2` and KNApSAcK `C00035244`; the KNApSAcK page resolves the same names, formula, and CAS but displays a stereochemical InChIKey that differs from ChEBI's. |

Current ChEBI also lists non-antimicrobial roles for anti-inflammatory,
anti-angiogenic, antineoplastic, plant-metabolite, and phytotoxin activity.
Those roles are correctly absent from `activity_roles`, which only preserves
roles inside this repository's antimicrobial scope.

## Evidence

| Claim or candidate claim | Support |
|---|---|
| Exact `CHEBI:143911` is a ChEBI-grounded record for neutral `(-)-antofine` / `7-demethoxytylophorine`. | Supported by the committed ChEBI inventory row, OLS, and current ChEBI. |
| Exact `CHEBI:143911` has ChEBI antiviral and generic antimicrobial roles. | Supported as ChEBI database assertions by the committed raw row and current ChEBI. No primary antiviral paper directly attached to this record was inspected deeply enough to curate a virus-specific activity observation. |
| Exact `CHEBI:143911` has antifungal activity against `Penicillium italicum`. | Supported as a future activity lead. `PMID:30691129` tested 7-demethoxytylophorine against `P. italicum` and reported mycelial-growth MIC `1.56 µg/mL` and MFC `6.25 µg/mL`. `PMID:35495682` reused that 6.25 mg/L MFC to test antofine-triggered resistance against `P. italicum` in Ponkan fruit. |
| Exact `CHEBI:143911` has activity against `Fusarium graminearum`. | Supported as a future activity and target lead. `PMID:31186319` purified antofine, showed dose-dependent `F. graminearum` germination and growth suppression with an approximate IC50 of `100 µg/mL`, and reduced wheat-head infection when `F. graminearum` spores were pretreated with antofine. |
| Exact `CHEBI:143911` targets the `Fusarium graminearum` TOR signaling pathway through `FgRRD2` / the Tap42-phosphatase complex. | Supported as a future molecular-target and causal-graph lead by `PMID:31186319`: the paper used yeast haploinsufficiency profiling, complementation with `F. graminearum` homologs, purified-protein thermal shift assays against `FgGDH1` and `FgRRD2`, `F. graminearum` gene deletion/overexpression experiments, and yeast two-hybrid assays of the `FgRRD2`-`FgTap42` interaction. |
| Exact `CHEBI:143911` inhibits `Phytophthora capsici` mycelial growth. | Supported as a future activity lead but not MIC evidence. `PMID:38731455` reported an EC50 of `5.0795 μg/mL`, with ROS and mitochondrial respiratory-chain perturbations after treatment. The paper frames that as a likely energy-metabolism effect rather than as a direct single-target assignment. |

The existing YAML does not assert any of the specific activity, target, or
mechanism claims above, so no claim-level evidence is currently misplaced. The
main defect is the opposite: exact, open, primary antimicrobial mechanism and
activity papers exist but have not yet been represented.

## Completeness

| Area | Status |
|---|---|
| Identity and structure | Complete enough for read-only review; OLS, current ChEBI, the committed ChEBI row, `PATHS.tsv`, and the generated YAML agree on exact `CHEBI:143911` and `NCVWJDISIZHFQS-CQSZACIVSA-N`. |
| Source concept | Complete. The only source concept is exact ChEBI `CHEBI:143911`, and the ignored/hidden-inclusive search found no curator override for `CHEBI:143911` or `antibioticmech:chebi-af62c3ea84`. |
| Activity roles and filing class | Complete for the committed source row. `activity_roles` retains the ChEBI antiviral and generic antimicrobial roles that put the record under `ANTIVIRAL`; new antifungal activity from primary papers should be represented as curator-owned `activity_spectrum` observations unless ChEBI itself adds an antifungal role and the raw ChEBI row is refreshed. |
| Mechanism | Incomplete. The record has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, or causal graph even though `PMID:31186319` can support a bounded TOR-pathway mechanism in `F. graminearum`. |
| Activity observations | Incomplete. `PMID:30691129` can support `P. italicum` MIC/MFC curation, `PMID:31186319` can support `F. graminearum` approximate IC50 activity, and `PMID:38731455` can support `P. capsici` EC50 activity if the current MIC-specific numeric slot is broadened or the value is preserved qualitatively. |
| Resistance mechanisms | Complete as empty. CARD and PHI-base contribute no exact row for antofine, its InChIKey, or its exact local xrefs. |
| Producers | Complete as empty for the inspected local sources. The live ChEBI page lists plant metabolite species of origin, but this record has no microbial producer candidate and MIBiG has no exact row for the generated InChIKey. |
| Clinical status | Complete as empty. Drugs@FDA contributes no exact GSRS/UNII row for this InChIKey or the inspected xrefs. |
| Datasets | Complete as empty for the inspected evidence. The record has no dataset assertion and the bounded checks did not expose a public exact-antofine dataset accession to curate. |
| Discussions | Incomplete only if mechanism curation is intentionally deferred. The existing `mechanism` and `review-readiness` queues expose the blank mechanism, but a future curator may want a `MECHANISM_GAP` discussion if the direct `FgRRD2` evidence is considered insufficient for first-class target curation. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, current OLS and ChEBI agree with exact `CHEBI:143911`, and the generated record has no populated target, mechanism, activity, resistance, producer, or causal assertion that overclaims the inspected literature. | Not applicable. |
| Major | Exact `CHEBI:143911` has no curated mechanism, molecular target, or causal graph despite direct primary antifungal target evidence. | The record has no `mode_of_action`, `molecular_targets`, or `causal_graphs`; `just worklist` queues `CHEBI:143911` for `mechanism`; `PMID:31186319` identified `FgRRD2` as an antofine-bound TOR-pathway component in `Fusarium graminearum` and linked antofine to disruption of the `FgRRD2`-`FgTap42` interaction. | Future curator-owned fields on `data/antibiotics/antiviral/antofine.yaml`, written through `record_curation_event` and `write_validated_antibiotic`. |
| Minor | Exact antifungal and anti-oomycete activities are absent from `activity_spectrum`. | The record has no activity observations. Open primary papers report `P. italicum` MIC and MFC values, an approximate `F. graminearum` IC50, and a `P. capsici` EC50 for exact antofine / 7-demethoxytylophorine. | Future `activity_spectrum` entries on `data/antibiotics/antiviral/antofine.yaml`, with a schema/writer extension first if EC50 or IC50 values need numeric representation outside the MIC-specific `mic_value` slot. |
| Minor | The committed ChEBI literature leads lag current ChEBI's exact citation list for the most relevant mechanism papers. | Current ChEBI lists PMIDs `30691129` and `31186319` on `CHEBI:143911`, but `data/raw/chebi_antimicrobials.tsv` contains only ten older PMIDs and omits those two exact antifungal citations. | A future ChEBI raw inventory refresh, if the ChEBI flat files also contain these current citations; otherwise a curator can cite the papers directly on target and activity claims. |
| Minor | The generated `knapsack:C00035244` xref is supported by name, formula, and CAS but has an unresolved stereochemical-key discrepancy on KNApSAcK's public page. | Current ChEBI lists `knapsack:C00035244`, and KNApSAcK resolves it to `Antofine` / `(-)-Antofine` with formula `C23H25NO3` and CAS `32671-82-2`; however, KNApSAcK displays InChIKey `NCVWJDISIZHFQS-YQTOOIBONA-N` while ChEBI and the generated record use `NCVWJDISIZHFQS-CQSZACIVSA-N`. | No record edit required unless a curator disproves the xref; exact xref assertions are seeded from `data/raw/chebi_antimicrobials.tsv`, or a future `curation/decisions.tsv` row can override a source concept if needed. |

## Recommended Edits

1. Curate an exact `Fusarium graminearum` `molecular_targets` entry from
   `PMID:31186319`, scoped to `FgRRD2` / the TOR-pathway Tap42-phosphatase
   complex rather than to every TOR component.

   Maintained owner: future curator-owned fields on
   `data/antibiotics/antiviral/antofine.yaml`.

2. Add a causal graph only for the source-supported `F. graminearum` mechanism:
   antofine interacts with `FgRRD2`, perturbs the `FgRRD2`-`FgTap42` branch of
   TOR signaling, inhibits germination and mycelial growth, and reduces wheat
   head infection in the tested model. Avoid a broader pan-fungal TOR target
   until another source demonstrates it.

   Maintained owner: future `causal_graphs` entries on
   `data/antibiotics/antiviral/antofine.yaml`.

3. Curate `P. italicum` and `F. graminearum` activity observations from primary
   assay values. Preserve EC50 and IC50 reports qualitatively or extend
   `ActivityObservation` before converting those values into MIC-specific
   fields.

   Maintained owner: `data/antibiotics/antiviral/antofine.yaml` for qualitative
   activity, or `src/antibioticmech/schema/antibioticmech.yaml` plus writer and
   renderer support for quantitative EC50/IC50 observations.

4. Re-check the ChEBI flat files and KNApSAcK before any future source-owned
   identity/xref cleanup. Update the raw ChEBI extractor only if the current
   ChEBI citations or xrefs are reproducibly present in the downloaded source
   files and missing because of extractor behavior.

   Maintained owner: `data/raw/chebi_antimicrobials.tsv` and
   `scripts/extract_source_inventory.py`, or `curation/decisions.tsv` for any
   explicit xref veto.

## Follow-up Checks

- Rerun `just validate-strict data/antibiotics/antiviral/antofine.yaml --out /tmp/antofine-validate-strict.tsv`.
- Rerun `just verify-corpus --summary` after any generated-source or
  curator-owned record edit.
- Rerun `just worklist --limit 0 --tsv /tmp/antofine-worklist.tsv` and confirm
  exact `CHEBI:143911` leaves any queue that a curation edit claims to resolve.
- Rerun exact ignored/hidden-inclusive searches for `CHEBI:143911`,
  `antibioticmech:chebi-af62c3ea84`, `NCVWJDISIZHFQS-CQSZACIVSA-N`, CAS
  `32671-82-2`, KNApSAcK `C00035244`, `antofine`, and
  `7-demethoxytylophorine` before asserting that any local source path is still
  absent.
- Confirm every proposed causal edge cites `PMID:31186319` or another primary
  experiment that directly supports the source and target nodes in that exact
  direction.

## Additional Notes

- The bounded PubMed search returned many exact antofine leads, but several are
  cytotoxicity, anticancer, anti-inflammatory, host angiogenesis, or
  plant-virus analogue papers. They should be mined for references, not used as
  evidence for fungal activity or the `FgRRD2` target.
- `PMID:22753104` and `PMID:22880628` show useful tobacco mosaic virus
  structure-activity and RNA-binding leads for antofine analogues. This review
  did not find an exact-antofine plant-virus activity row precise enough to
  curate from the inspected abstracts alone.
- The current schema names its quantitative `ActivityObservation` value slot
  `mic_value`, with a MIC-specific description and rule. That is why `P. capsici`
  EC50 and `F. graminearum` IC50 values need a representation decision before
  normalization as numeric activity rows.
