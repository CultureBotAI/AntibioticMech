# YAML Record Review: (3R,6Z)-nerolidol

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antifungal/3r-6z-nerolidol.yaml`
- Started UTC: 2026-09-22T09:49:11Z
- Finished UTC: 2026-09-22T09:52:30Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:176336` |
| Label | `(3R,6Z)-nerolidol` |
| Path | `data/antibiotics/antifungal/3r-6z-nerolidol.yaml` |
| Class | `ANTIFUNGAL` |
| Curation status | `SEEDED` |
| Grounding | `EXACT` |
| Source concept | `CHEBI:176336`, version `2026-08-30`, minted key `antibioticmech:chebi-1268419c90` |
| Source role | `CHEBI:35718` antifungal agent |
| Parent compounds | `CHEBI:173119` |
| Structure | `FQTLCLSUCSAZDY-SZGZABIGSA-N`; formula `C15H26O`; charge `0` |
| Same-structure xrefs | `cas:132958-73-7` |
| Source literature lead | `PMID:1602301` |
| Evidence-bearing objects | None |

This review covered one generated ChEBI-seeded antifungal record. The YAML has
source-derived exact ChEBI grounding, a ChEBI definition, three exact ChEBI
synonyms, one strictly broader ChEBI parent, one CAS xref, one antifungal role,
structure, and source-concept metadata. It has no generated or curator-owned
mode of action, molecular target, activity observation, resistance mechanism,
producer, discussion, dataset, causal graph, or claim-level evidence.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antifungal/3r-6z-nerolidol.yaml` | Pass: `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/3r-6z-nerolidol.yaml --out /tmp/antibioticmech-chebi-176336-validation.tsv` | Pass: 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Pass: 2939 expected records, 2939 on disk, no missing records, no unexpected records, no drifted fields, no identifiers absent from `PATHS.tsv`, no stale lockfile rows. The unrelated known `ARO:3000337` CARD cross-reference warning was emitted. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-176336.tsv` | Pass: the full worklist TSV was written. `CHEBI:176336` appears on `mechanism` and `review-readiness`; it was not found on any other queue in that TSV. |
| `just review-queue --limit 54` | Pass: `CHEBI:176336` is queued with `MECHANISM_REVIEW: mechanism is absent; 1 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |

No narrower single-record term, reference, or history validator is exposed for
this ChEBI-seeded record; the schema/strict checks, `verify-corpus`, and
worklist are the narrowest documented local gates.

## Identity and Grounding

`CHEBI:176336` resolves in official OLS as a current, non-obsolete ChEBI term
with label `(3R,6Z)-nerolidol`.

The live OLS annotations list the same formula, charge, average mass,
monoisotopic mass, Standard InChI, Standard InChIKey, SMILES, PubMed
cross-reference, and CAS cross-reference as the committed
`data/raw/chebi_antimicrobials.tsv` row. The raw inventory exposes
`PMID:1602301` provenance in its `citations` column and `cas:132958-73-7` as
the same-structure `xrefs` row; the generated record correctly preserves only
the CAS chemical identity xref.

The generated `parent_compounds` value is exactly the live OLS hierarchical
parent of `CHEBI:176336`: `CHEBI:173119` `(6Z)-nerolidol`. The live OLS graph
also exposes `CHEBI:176337` `(3S,6Z)-nerolidol` as an enantiomer, not a parent
or same-structure xref; this record does not conflate the two stereoisomers.

The `ANTIFUNGAL` filing class and empty `mode_of_action` are consistent with
the ChEBI source role. The live `CHEBI:35718` role resolves in OLS as current
and non-obsolete `antifungal agent`, `data/raw/chebi_role_names.tsv` marks it
as a `class,scope` role, and `conf/sources.yaml` maps that role to
`ANTIFUNGAL` without a mechanism mapping.

An ignored-inclusive search for `CHEBI:176336`, the exact label, the filename
stem, the exact Standard InChIKey, and the exact source PMID across
`data/antibiotics`, `data/raw`, `curation`, `reports`, and the exact `/tmp`
worklist/publication outputs found only the expected PATHS row, generated YAML,
raw ChEBI row, worklist/review-readiness rows, and publication-search output.
It found no prior dedicated review report, curation decision, curator-inventory
row, generated activity observation, molecular target, resistance, producer, or
causal-graph claim resolving `CHEBI:176336`.

## Evidence

The record's only antimicrobial assertion is inherited from ChEBI role
`CHEBI:35718`, `antifungal agent`. That role is sufficient to file the record
under `ANTIFUNGAL`; it is not a compound-specific mechanism, target, producer,
or assay claim.

The committed raw ChEBI row for `CHEBI:176336` has one source-literature lead:
`PMID:1602301`. PubMed resolves that PMID to Anderson, Ma, Smith, Chang, and
McLaughlin's Journal of Natural Products paper `Biologically active
gamma-lactones and methylketoalkenes from Lindera benzoin`; Crossref resolves
DOI `10.1021/np50079a011` to the same paper.

The PubMed abstract supports an isolation and broad bioactivity lead, not an
antifungal assertion. It reports brine-shrimp-lethality-directed fractionation
of `Lindera benzoin` ripe berries, isolation of known `(+)-(Z)-nerolidol` among
several lactones and methylketoalkenes, and reported structural elucidation and
biological activities. It does not name a fungal organism, an MIC or fungal
growth assay, or a biochemical fungal target.

The ChEBI-linked source paper still needs a full-paper check before it can
support future producer or activity fields. ChEBI grounds its exact
`(3R,6Z)-nerolidol` term to that publication, but the abstract does not state
the absolute configuration assignment for the isolated `(+)-(Z)-nerolidol`, the
full tested organism panel, exact assay readouts, or whether any assay in the
paper was antifungal rather than brine-shrimp or cultured-tumor-cell
bioactivity.

NCBI Taxonomy resolves `Lindera benzoin` to current species
`NCBITaxon:55958`, matching the isolation-source plant named in the
ChEBI-linked source paper.

A bounded repository publication search found zero PubMed candidates for the
exact Standard InChIKey `FQTLCLSUCSAZDY-SZGZABIGSA-N` and zero PubMed
candidates for the exact ChEBI label. A search for the exact ChEBI-linked
source-paper title found one candidate, `PMID:1602301`. Semantic Scholar
returned HTTP 429 for all three exact searches.

## Completeness

The record is incomplete as a reviewed antifungal natural-product record. It
has exact ChEBI identity, structure, a CAS xref, a strictly broader ChEBI
parent, an antifungal role, and a source-paper lead, but no curator-owned
activity observation, producer assertion, molecular target, mode of action,
dataset, or causal graph.

The empty mechanism fields are correct for a ChEBI `antifungal agent` seed. No
inspected source maps exact `(3R,6Z)-nerolidol` to an ergosterol, cell-wall,
membrane, energy-metabolism, virulence, or other fungal mechanism.

The empty `producer_organisms` and `activity_spectrum` slots should remain
empty until a curator checks the full Anderson, Ma, Smith, Chang, and
McLaughlin paper or another exact-stereoisomer primary source. The full
worklist currently puts exact `CHEBI:176336` on only the `mechanism` and
`review-readiness` queues; it does not surface a producer or activity candidate
from this ChEBI definition.

The empty `molecular_targets`, `resistance_mechanisms`,
`clinical_status_assertions`, `datasets`, `discussions`, and `causal_graphs`
slots are preferable to over-scoped filler. No molecular target, resistance
mechanism, clinical status, public dataset accession, or evidence-backed causal
edge was identified in the bounded checks.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | Exact `CHEBI:176336` has only source-level antifungal classification; it has no curator-owned activity observation, producer assertion, mechanism, or target claim. | The only source-literature lead is `PMID:1602301`; the PubMed abstract supports isolation of known `(+)-(Z)-nerolidol` from `Lindera benzoin` ripe berries during brine-shrimp-lethality-directed fractionation, but it does not report fungal organisms, antifungal assay readouts, MIC values, or a fungal target. The generated record has zero record evidence items, zero `activity_spectrum` rows, zero `producer_organisms`, zero targets, and no `mode_of_action`. | Future curator-owned fields on `data/antibiotics/antifungal/3r-6z-nerolidol.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI later changes this term's role, xrefs, parent, or citations, the ChEBI inventory extractor owns that refresh. |

No blocker identity, structure, schema, xref, class, parentage, or audit-history
findings were found.

No minor findings.

## Recommended Edits

1. Inspect the full DOI `10.1021/np50079a011` before curating
   `Lindera benzoin` / `NCBITaxon:55958` as a `producer_organisms` assertion.
   Add a producer row only if the full paper supports the plant-to-exact
   `(3R,6Z)-nerolidol` production claim directly enough for this corpus.
2. Search the full Anderson, Ma, Smith, Chang, and McLaughlin paper for any
   exact antifungal assay before adding `activity_spectrum` rows. Do not
   convert brine-shrimp lethality or cultured-tumor-cell activity into a fungal
   activity observation.
3. Add `mode_of_action`, `mode_of_action_target_scope`, a fungal
   `MolecularTarget`, or a `CausalGraph` only if an inspected primary source
   maps exact `(3R,6Z)-nerolidol` to a biochemical effect or target. Do not
   infer a mechanism from the generic ChEBI antifungal role alone.
4. Preserve stereochemical scope while checking the broader nerolidol
   literature. Evidence for racemic `nerolidol`, `(6Z)-nerolidol`, or the
   sibling `(3S,6Z)-nerolidol` should not be attached to `CHEBI:176336` unless
   the source actually measured this exact stereoisomer or makes an explicit
   stereoisomer-independent claim.
5. Keep `PMID:1602301` out of same-structure `xrefs`; it should be cited from
   assertion-level `evidence` blocks only if its full text supports future
   producer, activity, or dataset rows.

## Follow-up Checks

After any exact-term curation or source refresh, rerun:

1. `just validate data/antibiotics/antifungal/3r-6z-nerolidol.yaml`
2. `just validate-strict data/antibiotics/antifungal/3r-6z-nerolidol.yaml --out /tmp/antibioticmech-chebi-176336-validation.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-chebi-176336.tsv`
5. `just review-queue --limit 54`
6. `just qc`

Manually compare any future producer, activity, mechanism, or target assertion
against exact stereochemical identity. Confirm that claim-level evidence is
attached to `CHEBI:176336`, not only to a broader nerolidol stereochemical set.

## Additional Notes

All absence checks in this report used `rg --no-ignore --hidden`, so they
included ignored `reports/` files.

`runoak` was not available in this shell, so official ChEBI term checks were
performed against the EBI OLS4 HTTPS API.

The OLS full-text search API returned zero results for the exact Standard
InChIKey even though the direct live `CHEBI:176336` term annotation carries
that InChIKey. The direct term, hierarchical-parent, graph, and antifungal-role
endpoints resolved.

Semantic Scholar returned HTTP 429 for the exact InChIKey, exact label, and
exact title searches; the PubMed exact-title search resolved `PMID:1602301`.
