# YAML Record Review: (+)-albicanol

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/albicanol.yaml`
- Started UTC: 2026-09-21T07:16:42Z
- Finished UTC: 2026-09-21T07:16:42Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antifungal/albicanol.yaml` |
| Class | `ANTIFUNGAL` |
| ID | `CHEBI:155904` |
| Label | `(+)-albicanol` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded `(+)-albicanol` with InChIKey
`ZPTSRWNMMWXEHX-KCQAQPDRSA-N`. It has one ChEBI source concept, one antifungal
activity role, four broader ChEBI parent compounds, CAS and KNApSAcK xrefs, and
no record evidence, mode of action, molecular target, activity observation,
resistance mechanism, clinical assertion, causal graph, dataset, or
`Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antifungal/albicanol.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/albicanol.yaml --out /tmp/antibioticmech-albicanol-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just review-queue --limit 10` | Confirmed `(+)-albicanol` remains in `review-readiness`: `MECHANISM_REVIEW`, no mechanism, nine source literature leads, zero record evidence items, and zero targets. |
| `uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "(+)-albicanol antifungal mechanism" --limit 20 --output /tmp/antibioticmech-albicanol-pubs.jsonl` | Wrote 21 candidates. The useful hit from the title and abstract review was `PMID:32548177`, a 2020 drimane-sesquiterpenoid mechanism paper that names `(+)-albicanol`. The remaining visible candidates were discovery noise for other antifungal compounds or mixtures. |
| `uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query "CHEBI:155904 OR albicanol Diplophyllum albicans antifungal" --limit 20 --output /tmp/antibioticmech-albicanol-identity-pubs.jsonl` | PubMed wrote 0 candidates; Semantic Scholar returned an invalid response. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`albicanol.yaml` resolves unambiguously under `data/antibiotics/antifungal/`.
The generated lock row in `data/antibiotics/PATHS.tsv` maps `CHEBI:155904` to
`ANTIFUNGAL/albicanol`, and `source_concepts` carries only the expected ChEBI
upstream identity with minted ID `antibioticmech:chebi-bbbdca00b8`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:155904`: SMILES, Standard InChI, InChIKey
`ZPTSRWNMMWXEHX-KCQAQPDRSA-N`, formula `C15H26O`, charge `0`, average mass,
and monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`. The
official ChEBI page for `CHEBI:155904` also reports the same label, formula,
SMILES, Standard InChI, and InChIKey.

The record's antifungal filing is explained by the generated source concept:
ChEBI asserts `CHEBI:35718`, which this repo's raw role-name inventory maps to
`antifungal agent`.

No local grounding decision was found for `CHEBI:155904` or
`antibioticmech:chebi-bbbdca00b8` in a gitignore-independent search over
`curation/decisions.tsv` with `rg --hidden --no-ignore`.

## Evidence

The generated identity, label, definition, exact synonyms, exact structure,
parent compounds, xrefs, filing class, antifungal activity role, source
concept, and grounding status reproduce from the committed ChEBI inventory with
no `verify-corpus` drift. ChEBI links nine PMID source rows and one PMC source
row for this compound, but none are curated into record-level evidence yet.

The 2020 paper surfaced as `PMID:32548177` is a promising primary lead: its
abstract states that the authors synthesized eight drimane sesquiterpenoids,
including `(+)-albicanol`, evaluated their antifungal activities, and pursued
mechanistic work on the strongest analogue, `(-)-drimenol`. The abstract does
not by itself support a molecular target for exact `(+)-albicanol`.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

The committed BindingDB, Drugs@FDA, MIBiG, and PHI-base inventories contain no
local row for `CHEBI:155904`, InChIKey `ZPTSRWNMMWXEHX-KCQAQPDRSA-N`, CAS
`54632-04-1`, KNApSAcK `C00020283`, or the label `albicanol`.

## Completeness

The exact identity, ChEBI grounding, source concept, antifungal filing class,
activity role, parent compounds, structure, and same-structure xrefs are
defensible.

The consequential gap is mechanism sign-off. The YAML has no
`mode_of_action`, no `mode_of_action_target_scope`, no `molecular_targets`, and
no `causal_graphs`; it also has no record-level evidence even though the
committed ChEBI row names several source-literature leads. `just review-queue
--limit 10` therefore keeps the record in `MECHANISM_REVIEW`.

No activity observations, producer claims, public datasets, structural
observations, resistance assertions, clinical assertions, or discussions are
present. The review found no specific local evidence that any of those optional
sections must be populated before the absent mechanism is resolved.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has no curated antifungal mechanism and cannot satisfy the `REVIEWED` gate. | The target YAML has no `record_evidence`, `mode_of_action`, `molecular_targets`, or `causal_graphs`; the ChEBI inventory row lists nine source-literature rows for `CHEBI:155904`; `just review-queue --limit 10` reports `MECHANISM_REVIEW: mechanism is absent; 9 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | Curator-owned evidence, mechanism, target, discussion, or causal-graph additions written to `data/antibiotics/antifungal/albicanol.yaml` through `write_validated_antibiotic`, if primary literature supports them. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Review the ChEBI-linked source literature, starting with `PMID:32548177` /
   `PMC:PMC7278516`, for measured antifungal activity of exact
   `(+)-albicanol` and for whether the drimane-sesquiterpenoid mechanism can be
   asserted at the albicanol, analogue, or class level.
2. If only `(-)-drimenol` has direct mechanistic support, keep `(+)-albicanol`
   targetless and add a scoped `Discussion` explaining that the related
   drimane-sesquiterpenoid evidence was inspected but not promoted.
3. If a mode of action or target is supported for `(+)-albicanol`, write it
   with a guarded mutator that asserts `identifier == "CHEBI:155904"`, updates
   only the curator-owned mechanism fields, appends a `codex` `CurationEvent`
   with `llm_assisted: true`, and emits via `write_validated_antibiotic`.
4. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the newly curated mechanism or unresolved-mechanism discussion
   satisfy the `docs/CURATION.md` sign-off criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antifungal/albicanol.yaml --out /tmp/antibioticmech-albicanol-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antifungal/albicanol.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `rg --hidden --no-ignore` over `curation/`, `data/antibiotics/`,
  `data/raw/`, and `reports/yaml_record_review/` for `CHEBI:155904`,
  `albicanol`, `ZPTSRWNMMWXEHX`, `54632-04-1`, and `C00020283`.
- `rg --hidden --no-ignore` over
  `data/raw/bindingdb_target_measurements.tsv`,
  `data/raw/fda_clinical_status.tsv`, `data/raw/mibig_producers.tsv`, and
  `data/raw/phibase_amr.tsv` for `CHEBI:155904`,
  `ZPTSRWNMMWXEHX`, `54632-04-1`, `albicanol`, and `C00020283`.
- `rg --hidden --no-ignore` over `curation/decisions.tsv` for `CHEBI:155904`
  and `antibioticmech:chebi-bbbdca00b8`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned an
invalid response for the exact identity query, so that provider's exact-label
literature leads were not available in this pass.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
