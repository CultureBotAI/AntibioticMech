# YAML Record Review: (±)-chartaceone F

- Repository: `CultureBotAI/AntibioticMech`
- Record: `data/antibiotics/antiviral/chartaceone-f.yaml`
- Started UTC: `2026-09-24T08:27:54Z`
- Finished UTC: `2026-09-24T08:27:54Z`
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/chartaceone-f.yaml` |
| Class | `AntibioticRecord` |
| Identifier | `CHEBI:68938` |
| Label | `(±)-chartaceone F` |
| Antimicrobial class | `ANTIVIRAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/chebi_antimicrobials.tsv` |
| Source concept | ChEBI `CHEBI:68938`, version `2026-08-30`, minted key `antibioticmech:chebi-f51dc1c74c` |
| Standard InChIKey | `VDGBAYIBHQEDHS-BQASJOSNSA-N` |
| Xrefs | `reaxys:22123630` |
| Parent compounds | `CHEBI:35692`, `CHEBI:38749` |
| Activity roles | `CHEBI:22587` |
| Source literature lead | `PMID:22050318` |

`chartaceone-f.yaml` is a generated exact ChEBI record for the antiviral
dihydroxyflavanone `(±)-chartaceone F`. It has ChEBI-owned identity, class,
role, structure, parent, synonym, source-concept, and Reaxys fields; it has no
record-level evidence, curated `mode_of_action`, `mode_of_action_target_scope`,
`molecular_targets`, `activity_spectrum`, `resistance_mechanisms`,
`producer_organisms`, `datasets`, `discussions`, or `causal_graphs`.

## Validation

| Check | Result |
|---|---|
| `find . -name AGENTS.md -print` | Passed before review; no local `AGENTS.md` files were present. `find` included hidden and ignored paths. |
| `just review-queue --limit 0 --tsv /tmp/antibioticmech-review-queue.tsv` | Passed; `CHEBI:68938` was the first queue row without an exact one-record review report after matching current YAML paths and identifiers against prior reports. |
| `just validate data/antibiotics/antiviral/chartaceone-f.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/chartaceone-f.yaml --out /tmp/chartaceone-f-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2939 expected records were present on disk with no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` drift, and the known unrelated `ARO:3000337` iclaprim cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/chartaceone-f-worklist.tsv` | Passed; exact `CHEBI:68938` appeared on `mechanism`, `activity-candidate`, and `review-readiness`. The `mechanism` row reported 0 CARD targets and 0 resistance edges to build on. The `activity-candidate` row captured a no-subject `active against` phrase from the ChEBI definition rather than a curated assay. The only diagnostic was the known unrelated `ARO:3000337`/iclaprim cross-reference refusal. |
| `rg --no-ignore --hidden -n -e 'CHEBI:68938' -e 'chartaceone F' -e 'chartaceone-f' -e 'VDGBAYIBHQEDHS-BQASJOSNSA-N' -e 'reaxys:22123630' -e '22123630' data/antibiotics data/raw curation reports/yaml_record_review .claude docs README.md conf src scripts tests` | Passed; this wider ignored/hidden-inclusive search found the exact generated YAML, ChEBI raw row, review-queue row, and sibling notes in the chartaceone C/D/E review reports. It found no curator-owned activity, target, resistance, producer, or causal-graph claim for exact `CHEBI:68938`. |
| `rg --no-ignore --hidden -n -e '^CHEBI:68938\b' -e 'chartaceone F' -e 'VDGBAYIBHQEDHS-BQASJOSNSA-N' -e 'reaxys:22123630' data/antibiotics data/raw curation reports/yaml_record_review -g '*.yaml' -g '*.tsv' -g '*.md'` | Passed; the exact identifier, exact Standard InChIKey, exact Reaxys xref, and exact current path were present only where expected, plus non-curating sibling mentions in the chartaceone C/D/E review reports. |
| EBI OLS4 lookup for `CHEBI:68938`, `CHEBI:35692`, `CHEBI:38749`, and `CHEBI:22587` | Passed; OLS resolved the exact chartaceone F term, both ChEBI parents, and the antiviral-agent role. |
| ChEBI REST lookup for `CHEBI:68938` | Passed; the current ChEBI JSON matched the generated label, definition, 3-star status, IUPAC synonym, formula, charge, average mass, monoisotopic mass, SMILES, Standard InChI, Standard InChIKey, Reaxys accession, PubMed citation, two parent terms, and antiviral-agent role. |
| PubMed `efetch` for `PMID:22050318` | Passed; PubMed resolved the chartaceone A-F primary paper, Journal of Natural Products 2011, DOI `10.1021/np200715v`. |
| Crossref lookup for `DOI:10.1021/np200715v` | Passed; Crossref resolved the same Allard et al. article in Journal of Natural Products. |
| PubMed/Semantic Scholar exact search for `"chartaceone F"`, `CHEBI:68938`, `VDGBAYIBHQEDHS-BQASJOSNSA-N`, and `22123630` | Partial pass; PubMed returned `PMID:22050318` plus one unrelated `PMID:22123630` false positive caused by the numeric Reaxys accession, while Semantic Scholar returned HTTP 429. |
| PubMed targeted search for `Cryptocarya chartacea`, dengue, NS5, and chartaceones | Passed; PubMed returned `PMID:22050318` plus one 2023 systematic review lead, `PMID:36818227`, that cites the 2011 primary chartaceone paper. |
| PubMed direct search for chartaceone terms, `Cryptocarya chartacea`, and the Standard InChIKey | Passed; PubMed returned `PMID:22050318` plus the 2023 systematic review lead, `PMID:36818227`. |
| Reaxys xref `22123630` | Not independently checked: ChEBI lists this Reaxys registry number on exact `CHEBI:68938`, but Reaxys had no public resolver available in this workflow. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`.

## Identity and Grounding

| Claim | Review |
|---|---|
| Label and identifier | Supported. OLS resolves `CHEBI:68938` to `(+-)-chartaceone F`, and the current ChEBI compound endpoint resolves the same accession to `(±)-chartaceone F`. |
| Structure | Supported. The generated SMILES, Standard InChI, Standard InChIKey `VDGBAYIBHQEDHS-BQASJOSNSA-N`, formula `C41H40O8`, charge `0`, average mass `660.763`, and monoisotopic mass `660.27232` match both `data/raw/chebi_antimicrobials.tsv` and current ChEBI for `CHEBI:68938`. |
| Source concept | Supported. The only source concept is ChEBI `CHEBI:68938`; no ARO, curator, BindingDB, MIBiG, FDA, or PHI-base source row is attached to this record. |
| Filing class and role | Supported as a ChEBI role. `activity_roles` contains `CHEBI:22587`, and current ChEBI classifies exact `CHEBI:68938` as an antiviral agent. `ANTIVIRAL` is the expected filing class for a ChEBI antiviral role. |
| Parent terms | Supported. Current ChEBI attaches exact `CHEBI:68938` to `CHEBI:35692` by `is a` and to `CHEBI:38749` by `is a`; OLS resolves those parents to `dicarboxylic acid` and `dihydroxyflavanone`. |
| Citation lead | Supported as a ChEBI citation. Current ChEBI lists PubMed `22050318` on this term, matching the raw ChEBI row's `citations` field and the review-readiness literature lead. |
| Natural-product origin | Supported as plant provenance only. Current ChEBI records `Cryptocarya chartacea` trunk bark from `PMID:22050318`, so this is not a microbial `producer_organism` candidate. |
| Reaxys xref | Supported as a ChEBI assertion but not independently resolved. Current ChEBI lists `22123630` as a Reaxys registry number on exact `CHEBI:68938`, matching the generated `reaxys:22123630` xref. |

The record identity is therefore coherent: the file denotes the exact
ChEBI-grounded chartaceone F racemate, not sibling chartaceone C, D, or E. The
four sibling chartaceones are distinct ChEBI accessions with distinct Standard
InChIKeys and consecutive Reaxys registry numbers in the committed ChEBI
inventory.

## Evidence

| Assertion | Support |
|---|---|
| `(±)-chartaceone F` is a ChEBI-grounded exact record with the listed IUPAC name, structure, broader parents, Reaxys xref, and PubMed citation lead. | Supported by the committed ChEBI inventory row for `CHEBI:68938` and by the current ChEBI REST entry for the same term. |
| `(±)-chartaceone F` has a ChEBI antiviral-agent role. | Supported by the committed ChEBI row and by current ChEBI, both of which list `CHEBI:22587` on exact `CHEBI:68938`. |
| The cited PubMed lead is the chartaceone A-F primary paper. | Supported. PubMed resolves `PMID:22050318` to Allard et al. 2011, DOI `10.1021/np200715v`; Crossref resolves the same DOI and title. |
| Chartaceones C-F inhibit dengue virus NS5 RNA-dependent RNA polymerase in vitro. | Supported by the `PMID:22050318` abstract at cohort scope; the abstract names chartaceones C-F and dengue NS5 RdRp inhibition. |

No existing object-level mechanism, target, activity, resistance, producer,
dataset, discussion, or causal graph claims require support review, because none
are populated on `chartaceone-f.yaml`.

The Allard et al. abstract supports chartaceone F as part of the chartaceone
C-F NS5 RdRp-active cohort but does not expose F's exact `IC50`, whether all C-F
measurements used the same DENV serotype or NS5 construct, or whether the data
establish direct binding versus a target-associated polymerase effect.
`PMID:22050318` should therefore be inspected beyond the PubMed abstract before
a future curator writes a `VIRAL_POLYMERASE_INHIBITION` `mode_of_action`,
`MolecularTarget`, target measurement, or causal graph for exact `CHEBI:68938`.

Bounded publication discovery did not find a second primary exact-compound lead.
PubMed returned `PMID:22050318` and one unrelated `PMID:22123630` false positive
for the exact chartaceone F / identifier / Standard InChIKey / Reaxys query. The
targeted chartaceone/`Cryptocarya chartacea`/dengue NS5 search returned
`PMID:22050318` and `PMID:36818227`. The latter is a 2023 systematic review
whose reference list points back to the 2011 primary paper; it adds no primary
support for an exact chartaceone F curation claim. A bare `22123630` search hit
unrelated `PMID:22123630`, which is why the Reaxys registry number cannot be
treated as a PubMed lead.

## Completeness

| Area | Assessment |
|---|---|
| Identity and structure | Complete enough for read-only review; current ChEBI, the committed ChEBI row, `PATHS.tsv`, and the generated YAML agree on `CHEBI:68938` and `VDGBAYIBHQEDHS-BQASJOSNSA-N`. |
| Source concept | Complete. The only source concept is exact `CHEBI:68938`. |
| Antimicrobial class | Complete as a source-seeded filing decision. The only retained antimicrobial role is ChEBI antiviral agent. |
| Mode of action | Incomplete. The record has no curated mode despite a source PubMed lead for dengue NS5 RdRp inhibition. |
| Target scope | Incomplete with the absent mode. A viral NS5 target would be `MICROBIAL_TARGET`, but no curator-owned mechanism has been added. |
| Molecular targets | Incomplete. The exact record has no NS5 RdRp target assertion or target `IC50` measurement. |
| Activity observations | Not populated, and the ChEBI definition is insufficient. The reviewed lead is an NS5 enzyme assay lead; add an `ActivityObservation` only if the paper separately reports a whole-virus or cell-based organismal activity assay for chartaceone F. |
| Resistance mechanisms | Complete as empty. CARD contributes no ARO resistance edges to this antiviral ChEBI-only record, and no chartaceone F resistance lead appeared in the bounded searches. |
| Producer organisms | Complete as empty for the inspected claims. ChEBI names plant bark as the natural-product source, not a microbial producer. |
| Causal graph | Incomplete until the mechanism and target claims are curated at edge-level evidence granularity. |
| Datasets | Complete as empty for the inspected evidence. The generated record has no dataset assertion, and the ChEBI/PubMed/Crossref checks did not expose a specific chartaceone F dataset to add. |
| Discussions | No discussion is required for the review itself. A later curation pass could add a `CURATION_TODO` if it cannot inspect the full Allard et al. paper. |
| Audit | Accurate for a generated ChEBI seed. The last history event is a `seed_from_sources` reseed on 2026-08-30; no curator event claims review. |

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Blocker | None found. | The YAML validates, the corpus reproduces exactly, current ChEBI agrees with the exact identity and structure, and the record does not conflate chartaceone F with sibling chartaceone C, D, or E. | Not applicable. |
| Major | Exact `CHEBI:68938` remains a source-seeded antiviral structure with no curated mode of action, molecular target, target measurement, organismal activity observation, or causal graph despite having a promising dengue NS5 RdRp primary lead. | The generated record has 0 record evidence items and no `mode_of_action`, `molecular_targets`, `activity_spectrum`, `resistance_mechanisms`, or `causal_graphs`; exact `CHEBI:68938` is on the `mechanism` and `review-readiness` queues with 0 CARD targets and 0 CARD resistance edges. PubMed confirms that `PMID:22050318` is the chartaceone A-F / dengue NS5 RdRp paper, but its abstract does not expose the exact chartaceone F IC50, target scope, or enzyme assay context required for claim-level curation. | Future curator-owned fields on `data/antibiotics/antiviral/chartaceone-f.yaml`, written through `record_curation_event` and `write_validated_antibiotic`; if ChEBI changes this term's role, parentage, xrefs, or source PMIDs, `data/raw/chebi_antimicrobials.tsv` and the ChEBI extractor own the refresh. |
| Minor | `reaxys:22123630` is an unchecked direct Reaxys identity in this review. | The generated xref matches the committed ChEBI inventory row and current ChEBI's Reaxys registry number on exact `CHEBI:68938`, but Reaxys itself had no public resolver available here to verify same-structure semantics directly. | ChEBI extractor and `data/raw/chebi_antimicrobials.tsv`; future curation should either verify this source externally or preserve it as an upstream ChEBI assertion. |

## Recommended Edits

1. Leave seeded identity fields untouched unless a ChEBI refresh changes them.
   `identifier`, `label`, `definition`, `definition_source`, `synonyms`,
   `parent_compounds`, `activity_roles`, `chemical_structure`, `xrefs`,
   `source_concepts`, `grounding_status`, and the file path are generated from
   ChEBI/PATHS and currently agree with current ChEBI for exact `CHEBI:68938`.

2. Inspect Allard et al. `PMID:22050318` / `DOI:10.1021/np200715v` beyond the
   PubMed abstract. If full-paper tables map exact `(±)-chartaceone F` to a
   dengue NS5 RdRp assay, add curator-owned `mode_of_action:
   VIRAL_POLYMERASE_INHIBITION`, `mode_of_action_target_scope:
   MICROBIAL_TARGET`, and one viral NS5 RNA-dependent RNA polymerase
   `MolecularTarget`. Preserve any chartaceone F-specific enzyme `IC50` in the
   target evidence notes or `experimental_context` unless `TargetMeasurement` is
   extended to support non-BindingDB primary-literature values.

3. Add a `causal_graph` only after the target assertion is curated and only for
   source-supported directed steps. The graph must keep DENV NS5 RdRp inhibition
   distinct from unsupported direct-binding, whole-virus inhibition, uptake, or
   host-response edges.

4. Add `ActivityObservation` entries only if the Allard et al. paper or another
   primary source reports whole-virus or cell-based chartaceone F activity.
   Enzyme `IC50` is target-level context, not organismal `activity_spectrum`;
   do not force primary-literature values into `MolecularTarget.measurements`
   while that shape remains BindingDB-specific.

## Follow-up Checks

After any future mutation, rerun:

1. `just validate data/antibiotics/antiviral/chartaceone-f.yaml`
2. `just validate-strict data/antibiotics/antiviral/chartaceone-f.yaml --out /tmp/chartaceone-f-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/chartaceone-f-worklist.tsv`
5. `just qc`
6. `git diff --check`

Rerun exact ignored-inclusive searches for `(±)-chartaceone F`, `CHEBI:68938`,
`VDGBAYIBHQEDHS-BQASJOSNSA-N`, and `reaxys:22123630` to confirm future evidence
still lands on the exact chartaceone F structure and not on sibling chartaceone
C, D, or E.

## Additional Notes

This review did not edit generated YAML, add a curation event, promote
`curation_status`, or regenerate products.

The only Semantic Scholar check attempted for this record was rate-limited with
HTTP 429. That failure is not material to the verdict because ChEBI and PubMed
both point to `PMID:22050318`, and bounded PubMed searches found the same primary
paper plus a later review.

`PMID:22050318` is also the ChEBI literature lead for sibling exact records
`CHEBI:68935` chartaceone C, `CHEBI:68936` chartaceone D, and `CHEBI:68937`
chartaceone E. Evidence from that paper still needs to be mapped compound by
compound before any one sibling receives an exact target `IC50` or causal graph.
