# YAML Record Review: (S)-quinacrine

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antiprotozoal/s-quinacrine.yaml
- Started UTC: 2026-09-24T01:45:01Z
- Finished UTC: 2026-09-24T01:45:01Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | CHEBI:37597 |
| Label | (S)-quinacrine |
| Path | data/antibiotics/antiprotozoal/s-quinacrine.yaml |
| Class | ANTIPROTOZOAL |
| Status | SEEDED |
| Grounding | EXACT |
| Source concept | CHEBI:37597, source version 2026-08-30 |
| Standard InChIKey | GPKJTRJOBQGKQK-INIZCTEOSA-N |
| Maintained owner | Generated from data/raw/chebi_antimicrobials.tsv plus data/antibiotics/PATHS.tsv; do not hand-edit the generated YAML |

The target resolves unambiguously to
`data/antibiotics/antiprotozoal/s-quinacrine.yaml`. An
ignored-file-inclusive search for `CHEBI:37597`, `(S)-quinacrine`,
`s-quinacrine`, and `quinacrine` covered `data/antibiotics/PATHS.tsv`,
`data/raw`, `curation`, and `reports/yaml_record_review`; it found the target,
the achiral `CHEBI:8711` sibling, the `(R)-quinacrine` sibling
`CHEBI:49845`, the matching raw ChEBI inventory rows, the regenerated
review-queue row, and the prior `(R)-quinacrine` review. A separate
ignored-inclusive filename search found no prior exact
`s-quinacrine` report.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antiprotozoal/s-quinacrine.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antiprotozoal/s-quinacrine.yaml --out /tmp/antibioticmech-s-quinacrine-strict.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed; 2939 records expected, 2939 on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, 0 stale lockfile rows. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-s-quinacrine-worklist.tsv` | Passed; `CHEBI:37597` appears only in `mechanism` and `review-readiness`. |
| `just review-queue --limit 140` | Passed; `CHEBI:37597` remains queued as `MECHANISM_REVIEW: mechanism is absent; 0 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed; Ruff reported `All checks passed!`. |

The repository exposes `validate`, `validate-strict`, `verify-corpus`,
`worklist`, `review-queue`, and `lint`; it does not expose narrower
single-record term, reference, or history validators for a plain ChEBI-seeded
record with no record-level citations.

## Identity and Grounding

The ChEBI identity is exact. EBI OLS4 resolves `CHEBI:37597` to the current
`(S)-quinacrine` term with the same SMILES, Standard InChI, Standard InChIKey,
formula, charge, average mass, and monoisotopic mass carried by the generated
record.

The stereochemical boundary is correct:

- `CHEBI:37597` has stereospecific InChIKey
  `GPKJTRJOBQGKQK-INIZCTEOSA-N`.
- `CHEBI:8711` is the achiral parent `quinacrine`, has direct-parent status in
  OLS4, and has non-stereospecific InChIKey
  `GPKJTRJOBQGKQK-UHFFFAOYSA-N`.
- `CHEBI:49845` is the `(R)-quinacrine` sibling and OLS4 reports the
  mirror-image InChIKey `GPKJTRJOBQGKQK-MRXNPFEDSA-N`.

OLS4 lists no `database_cross_reference` annotations on `CHEBI:37597`; the
generated record therefore correctly has no exact chemical `xrefs`, drug
`xrefs`, or document `xrefs`. The only local synonym is the stereospecific
`(4S)` IUPAC name from ChEBI and is safe as an exact synonym.

An ignored-inclusive search over `data/antibiotics/**/*.yaml` for
`GPKJTRJOBQGKQK-INIZCTEOSA-N`, `antibioticmech:chebi-ea7254f536`, and
`CHEBI:37597` found only `s-quinacrine.yaml`, so no generated sibling currently
duplicates this exact ChEBI identity, structure, or source concept.

## Evidence

This record has no record-level `evidence`, no `mode_of_action`, no
`mode_of_action_target_scope`, no `activity_spectrum`, no
`molecular_targets`, no `resistance_mechanisms`, no `producer_organisms`, no
`clinical_status_assertions`, no `datasets`, and no `causal_graphs`. That is
structurally valid for a ChEBI-only seeded record, but it leaves the record
without a reviewed primary-paper mechanism.

Bounded publication checks did not identify an immediately supportable exact
antiprotozoal mechanism for this `(S)` enantiomer:

- Exact PubMed/Semantic Scholar query:
  `"(S)-quinacrine" OR "GPKJTRJOBQGKQK-INIZCTEOSA-N" OR "CHEBI:37597"`.
  PubMed found PMID:12808118, an antiprion enantiomer paper in ScN2a cells;
  that is outside the antiprotozoal mechanism modeled by this record. Semantic
  Scholar returned HTTP 429.
- Achiral PubMed query:
  `quinacrine mepacrine antiprotozoal antimicrobial mechanism`. The candidates
  mostly concerned clinical giardiasis use, repurposing outside antimicrobial
  scope, or antimalarial/acridine analogs rather than the exact
  `(S)-quinacrine` target.
- Focused PubMed query:
  `quinacrine Giardia mechanism`. PMID:7131192 reported rapid
  quinacrine-HCl effects on Giardia trophozoite attachment while leaving the
  attachment mechanism unresolved. PMID:41756833 is a Giardia preprint in which
  achiral quinacrine is a probe for autophagic-structure accumulation, but it
  is not enough to assign a stereospecific antiprotozoal mode of action.

Google Scholar was not used.

## Completeness

The generated identifier, label, parent boundary, activity role, structure
fields, source concept, and curation history are sufficient for a ChEBI-only
seed after the official OLS identity check. No generated synonym or xref needs
immediate correction.

Consequential gaps:

- `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, and
  `causal_graphs` are absent. The record should remain below `REVIEWED` until
  a future curator either adds an evidence-backed mechanism or writes a
  curator-owned veto explaining why the antiprotozoal mechanism remains
  unknown.
- `activity_spectrum` is absent. A future activity observation should be copied
  only from a measured assay with a taxon or strain, value, qualifier, units,
  method, and claim-level evidence; clinical giardiasis efficacy is not an MIC.

Empty optional slots that are acceptable in this generated seed:

- `resistance_mechanisms`: no CARD target or resistance route was available,
  and the worklist reports zero resistance edges to build on.
- `producer_organisms`: no producer-candidate worklist row names
  `CHEBI:37597`.
- `clinical_status_assertions`: no regulatory source was imported for this
  enantiomer-specific ChEBI term.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has no reviewed antiprotozoal mechanism. | `just worklist --limit 0 --tsv /tmp/antibioticmech-s-quinacrine-worklist.tsv` lists `CHEBI:37597` only in `mechanism` and `review-readiness`; the exact `(S)-quinacrine` publication search found only an antiprion lead, and the achiral quinacrine/Giardia searches did not find a supportable exact target or mode of action. | Add an evidence-backed curator-owned mechanism to `data/antibiotics/antiprotozoal/s-quinacrine.yaml` through `write_validated_antibiotic`, or add a curator-owned veto explaining why the mechanism remains unknown. |

No blockers found. No minor findings found.

## Recommended Edits

1. Curate `mode_of_action` only from a source that supports a specific
   `(S)-quinacrine` or deliberately projected achiral quinacrine antiprotozoal
   claim. If an achiral mechanism is adopted for both enantiomers, state why
   the stereochemical projection is justified.
2. If no representable mechanism can be established, add a `CURATOR:` veto
   note through `write_validated_antibiotic` explaining which exact and achiral
   literature searches failed to resolve it.
3. Add measured `activity_spectrum` rows only with complete organism, strain or
   isolate, assay, value, qualifier, units, and evidence. Do not generalize
   clinical refractory-giardiasis efficacy into species-wide potency.

## Follow-up Checks

- After any mechanism or activity curation, run
  `just validate data/antibiotics/antiprotozoal/s-quinacrine.yaml`,
  `just validate-strict data/antibiotics/antiprotozoal/s-quinacrine.yaml --out /tmp/antibioticmech-record-validation.tsv`,
  `just verify-corpus --summary`, `just worklist --limit 0 --tsv /tmp/antibioticmech-s-quinacrine-worklist.tsv`,
  and `just lint`.
- Repeat the EBI OLS4 term check for `CHEBI:37597` if any upstream ChEBI
  refresh adds new synonyms or cross-references.

## Additional Notes

- A repository-local `AGENTS.md` search used `find` over the full checkout,
  including ignored and hidden paths, and found no repo-local override.
- The CARD self-contradictory cross-reference warning printed by
  `just verify-corpus` and `just worklist` concerns `ARO:3000337` / iclaprim
  and does not affect this ChEBI-only record.
- The prior `(R)-quinacrine` report found a generated related-synonym scoping
  defect. Official OLS4 did not expose the analogous `QUINACRINE` related
  synonym on `CHEBI:37597`, so that sibling defect is not present here.
