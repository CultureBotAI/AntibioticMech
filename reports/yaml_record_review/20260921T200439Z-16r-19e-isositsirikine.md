# YAML Record Review: (16R,19E)-isositsirikine

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antifungal/16r-19e-isositsirikine.yaml`
- Started UTC: 2026-09-21T19:59:00Z
- Finished UTC: 2026-09-21T20:04:39Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Identifier | `CHEBI:70508` |
| Label | `(16R,19E)-isositsirikine` |
| Path | `data/antibiotics/antifungal/16r-19e-isositsirikine.yaml` |
| Class | `ANTIFUNGAL` |
| Grounding | `EXACT` |
| Status | `SEEDED` |
| Source concept | `CHEBI:70508`, source version `2026-08-30`, minted source identifier `antibioticmech:chebi-5bfb8ce56a` |
| Structure key | `RGXKJLTVROJBKZ-LZNZQLKFSA-N` |

This review resolved one curated YAML record. An ignored-inclusive search over `data/antibiotics`, `data/raw`, `curation`, and `reports` for `CHEBI:70508`, `(16R,19E)-isositsirikine`, `16r-19e-isositsirikine`, and `isositsirikine` found the target record, its `data/antibiotics/PATHS.tsv` lockfile row, its `data/raw/chebi_antimicrobials.tsv` ChEBI seed row, and its `curation/record_review_queue.tsv` worklist row. The same ignored-inclusive search found the adjacent `CHEBI:70507` queue/raw rows because they share `PMID:21043460`, but it found no prior `reports/yaml_record_review/*isositsirikine*` report.

The maintained upstream row is the ChEBI line in `data/raw/chebi_antimicrobials.tsv`; the YAML record is generated from that committed inventory and `data/antibiotics/PATHS.tsv`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antifungal/16r-19e-isositsirikine.yaml` | Passed; `linkml-validate` reported `No issues found`. |
| `just validate-strict data/antibiotics/antifungal/16r-19e-isositsirikine.yaml --out /tmp/antibioticmech-isositsirikine-70508-validation.tsv` | Passed; 1 file scanned, 0 files with `ERROR`, 0 total error rows. |
| `just verify-corpus --summary` | Passed; 2,939 records expected and on disk, 0 missing, 0 unexpected, 0 drifted fields, 0 identifiers absent from `PATHS.tsv`, and 0 stale lockfile rows. |
| `just review-queue --limit 32` | Listed `CHEBI:70508` at row 31 with `MECHANISM_REVIEW: mechanism is absent; 7 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-isositsirikine-70508.tsv` | Passed and wrote the full TSV; `CHEBI:70508` appears only in `mechanism` and `review-readiness`. |

No narrower single-record term, reference, or history validator is exposed for this ChEBI-seeded record category. The full-corpus worklist also repeated the known unrelated CARD conflict for `ARO:3000337` / iclaprim versus isoaminile citrate; that conflict is queued separately and does not name `CHEBI:70508`.

## Identity and Grounding

The YAML record agrees with the committed ChEBI inventory row for all source-owned fields:

| Claim | Status |
|---|---|
| Exact ChEBI identity | Coherent. The identifier is `CHEBI:70508`, the label is `(16R,19E)-isositsirikine`, `grounding_status` is `EXACT`, and the source concept points back to `CHEBI:70508`. |
| Structure | Coherent. SMILES, Standard InChI, InChIKey `RGXKJLTVROJBKZ-LZNZQLKFSA-N`, formula `C21H26N2O3`, neutral charge, average mass `354.45`, and monoisotopic mass `354.19434` match the current ChEBI page. |
| Synonyms | Coherent. The IUPAC name and five exact synonyms in YAML are the ChEBI `IUPAC NAME` and `SYNONYM` entries; the ChEBI-only `UNIPROT NAME` duplicate is correctly absent from YAML synonyms. |
| Parent compounds | Coherent. `CHEBI:15734`, `CHEBI:25248`, `CHEBI:38163`, and `CHEBI:38958` match ChEBI's primary alcohol, methyl ester, organic heterotetracyclic compound, and indole alkaloid `is a` parents. |
| Activity role | Coherent with upstream. The record's only imported role, `CHEBI:35718`, is ChEBI's `antifungal agent` role. ChEBI also lists plant-metabolite/metabolite roles that are not antimicrobial roles and are correctly not imported into `activity_roles`. |
| Exact xrefs | Coherent. ChEBI lists CAS `6519-27-3`, ChemSpider `4941438`, and MetaCyc `CPD-21555`; MetaCyc `CPD-21555` resolves to `(16R,19E)-isositsirikine` with formula `C21H26N2O3` and mass `354.45`. |

The record denotes an individual neutral `16R,19E` chemical structure, not the same entity as the 16-epi, 16S, 19Z, oxindole, or unspecific `isositsirikine` leads returned by exact-name PubMed searches.

## Evidence

The record has no `evidence`, `activity_spectrum`, `molecular_targets`, `resistance_mechanisms`, `producer_organisms`, or `causal_graphs` entries, so there are no claim-level citations to verify in the YAML itself.

The ChEBI seed row lists seven PubMed leads:

| Lead | What it supports after inspection |
|---|---|
| `PMID:17340304` / `DOI:10.1055/s-2007-969688` | Isolation and structure determination of 16R-19,20-E-isositsirikine from `Catharanthus roseus` cell-suspension cultures. It does not describe antifungal activity or a microbial mechanism in the PubMed abstract. |
| `PMID:21043460` / `DOI:10.1021/np100552b` | Isolation and structure work for alkaloids from `Alstonia spatulata`; the abstract reports multidrug-resistance reversal in vincristine-resistant KB cells for other numbered alkaloids. It is a plant/natural-products and mammalian-cell lead, not an antifungal mechanism citation for `CHEBI:70508`. |
| `PMID:28827772` / `DOI:10.1038/s41467-017-00154-x` | Plant MIA biosynthesis from strictosidine toward akuammicine in a `Catharanthus` system; no antifungal or antimicrobial assay was apparent from the PubMed abstract. |
| `PMID:29511102` / `DOI:10.1073/pnas.1719979115` | Exact full-text support for 16R,19E-isositsirikine as a `Catharanthus roseus` MIA intermediate detected in VIGS-geissoschizine-oxidase plants and for Redox2 catalyzing 19E/19Z-geissoschizine reduction to 16R-isositsirikine. It supports plant biosynthesis, not an antifungal mechanism. |
| `PMID:33477682` / `DOI:10.3390/molecules26020488` | A review of monoterpene indole alkaloid chemistry and bioactivities across Apocynaceae genera. Useful for citation chasing, but not primary evidence for exact antifungal activity. |
| `PMID:35865593` / `DOI:10.1039/d2ra01791a` | A review of `Kopsia` phytochemistry and pharmacology. Useful for citation chasing, but not primary evidence for exact antifungal activity. |
| `PMID:38001233` / `DOI:10.1038/s42003-023-05574-8` | `Rauvolfia tetraphylla` genome and yohimbane-biosynthesis work. It is plant biosynthesis, not an antifungal mechanism citation for the exact 16R,19E record. |

Additional PubMed searches were run without `NCBI_EMAIL`:

| Query class | Output | Result |
|---|---|---|
| Exact and variant names | `/tmp/antibioticmech-isositsirikine-70508-exact-quoted.jsonl`, 15 candidates | Found plant isolation, synthesis, biosynthesis, antineoplastic, anti-inflammatory/docking, antimalarial, and broader `isositsirikine` or epimer leads. |
| Activity terms | `/tmp/antibioticmech-isositsirikine-70508-antimicrobial.jsonl`, 2 candidates | Found `PMID:26560981`, an antimalarial `Aspidosperma ramiflorum` study that refers to purified `isositsirikine`, and `PMID:31140017`, where the abstract says 16-demethoxycarbonylvoacamine, not co-isolated isositsirikine, had the reported antiplasmodial/cytotoxic activities. |
| Identifiers plus source plants | `/tmp/antibioticmech-isositsirikine-70508-ids-plants.jsonl`, 2 candidates | Recovered `PMID:17340304` and the adjacent 16-epi-Z-isositsirikine antineoplastic lead `PMID:6619887`; neither resolves the exact antifungal claim. |

The official ChEBI page also lists AGRICOLA accession `IND607176727`, but the committed raw inventory did not carry that citation and no inspected PubMed or full-text lead resolved it during this review. That Agricola lead remains the most obvious upstream citation to inspect before deciding whether the ChEBI `antifungal agent` role is simply unsupported or is supported by a non-PubMed source.

## Completeness

- `mode_of_action` is absent, there are no `molecular_targets`, and there is no `causal_graph`; the review-readiness queue correctly keeps the record open for mechanism review.
- `activity_spectrum` is absent. No inspected source during this pass provided an exact `(16R,19E)-isositsirikine` MIC, `IC50`, fungal strain, or antifungal assay that could be represented as a claim-level observation.
- `resistance_mechanisms` is absent and `just worklist` has no `CHEBI:70508` target or resistance row.
- `producer_organisms` is absent. ChEBI has plant-origin statements for this natural product, including `Catharanthus roseus` and `Alstonia spatulata`; this review did not find a microbial producer claim.
- `clinical_status`, `clinical_status_assertions`, and `datasets` are absent; no inspected source suggested that any of these omissions are consequential for this plant indole alkaloid record.

A gitignore-independent search over `data/raw`, `curation`, `data/antibiotics`, and `reports` for `CHEBI:70508`, `RGXKJLTVROJBKZ`, `6519-27-3`, `4941438`, `CPD-21555`, and all seven ChEBI PubMed CURIEs found only the target raw/YAML/PATHS rows and review-queue metadata described above. No curator decision, local source overlay, prior review report, activity candidate, or mechanism draft was present under that searched scope.

## Findings

| Severity | ID | Finding |
|---|---|---|
| Major | ISOSITSIRIKINE-70508-M1 | The record is an exact ChEBI seed with a coherent structure and xrefs, but it has no inspected primary source that supports exact `(16R,19E)-isositsirikine` antifungal activity, no activity observation, and no mechanism. |

### ISOSITSIRIKINE-70508-M1

`CHEBI:70508` enters the corpus because the ChEBI seed asserts `CHEBI:35718` antifungal-agent activity. The generated record preserves that assertion as `antimicrobial_class: ANTIFUNGAL` plus `activity_roles: [CHEBI:35718]`, but it has 0 record evidence items and 0 targets in `curation/record_review_queue.tsv`. The seven committed PubMed leads on the ChEBI raw row support plant isolation, plant biosynthesis, or secondary review context rather than an exact fungal assay or a microbial molecular mechanism. Targeted exact-name and activity searches likewise found adjacent epimers, unspecified `isositsirikine`, antimalarial leads, and non-microbial mammalian-cell phenotypes, not a primary antifungal assay for this exact `16R,19E` structure.

Owner: the exact YAML record at `data/antibiotics/antifungal/16r-19e-isositsirikine.yaml` for claim-level curator additions written through `write_validated_antibiotic`; `data/raw/chebi_antimicrobials.tsv`, the ChEBI extractor, or `curation/decisions.tsv` if further review shows the imported ChEBI antifungal role should be suppressed or challenged rather than supported.

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect ChEBI's non-PubMed AGRICOLA citation `IND607176727` and backwards citations from the ChEBI/PubMed leads for a primary paper that tests exact `(16R,19E)-isositsirikine` against a fungal organism.
2. If exact antifungal primary evidence exists, add a claim-level `activity_spectrum` entry with organism or strain, assay, value, qualifier, unit, and citation. Add `mode_of_action`, `molecular_targets`, and `causal_graphs` only if the same or follow-on sources identify a compound-specific fungal mechanism.
3. If the only activity evidence is for an unspecified `isositsirikine` mixture, a 16-epi/16S/19Z sibling, a non-fungal assay, or a co-isolated alkaloid, leave those data out of `CHEBI:70508` and record a curator decision or discussion that the ChEBI role remains unsupported for this exact structure.
4. Keep `curation_status: SEEDED` until identity, structure, the imported antifungal class/role, and a mechanism disposition have all been checked to the repository's `REVIEWED` gate.

## Follow-up Checks

- Re-run `just validate data/antibiotics/antifungal/16r-19e-isositsirikine.yaml`.
- Re-run `just validate-strict data/antibiotics/antifungal/16r-19e-isositsirikine.yaml --out /tmp/antibioticmech-isositsirikine-70508-validation.tsv`.
- Re-run `just verify-corpus --summary` after any seeded-field or curator-owned addition.
- Re-run `just worklist --limit 0 --tsv /tmp/antibioticmech-worklist-isositsirikine-70508.tsv` and confirm `CHEBI:70508` leaves `mechanism` only after a supported mechanism disposition is present.
- Re-run `just review-queue --limit 32` or the full review-readiness checkpoint and confirm the row changes only after real review state changes, not by direct TSV editing.

## Additional Notes

- The exact searched local scope included ignored files. `reports/` is gitignored, so this new review report must be staged with `git add -f`.
- ChEBI's current page includes plant-origin assertions for `Alstonia spatulata`, `Catharanthus roseus`, `Kopsia griffithii`, `Kopsia pauciflora`, and `Rhazya stricta`; these are natural-product origin metadata and do not by themselves establish antifungal activity.
- MetaCyc `CPD-21555` resolved as an exact label/formula/mass cross-reference but did not add activity or mechanism evidence during this pass.
