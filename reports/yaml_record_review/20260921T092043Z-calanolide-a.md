# YAML Record Review: (+)-calanolide A

- Repository: CultureBotAI/AntibioticMech
- Record: `data/antibiotics/antiviral/calanolide-a.yaml`
- Started UTC: 2026-09-21T09:20:43Z
- Finished UTC: 2026-09-21T09:20:43Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antiviral/calanolide-a.yaml` |
| Class | `ANTIVIRAL` |
| ID | `CHEBI:65552` |
| Label | `(+)-calanolide A` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Maintained status | Generated from `data/raw/` inventories |

The record denotes exact ChEBI-grounded `(+)-calanolide A` with InChIKey
`NIDRYBLTWYFCFV-FMTVUPSXSA-N`. It has one ChEBI source concept, a ChEBI
HIV-1 reverse-transcriptase-inhibitor activity role, a seeded
`VIRAL_POLYMERASE_INHIBITION` mode of action with `MICROBIAL_TARGET` scope, four
broader ChEBI parent compounds, four same-structure xrefs, and no record
evidence, molecular target, activity observation, resistance mechanism, producer
organism, clinical assertion, causal graph, dataset, or `Discussion`.

## Validation

| Command | Result |
|---|---|
| `just validate data/antibiotics/antiviral/calanolide-a.yaml` | Passed: `No issues found`. |
| `just validate-strict data/antibiotics/antiviral/calanolide-a.yaml --out /tmp/antibioticmech-calanolide-a-validation.tsv` | Passed: 1 file scanned, 0 files with errors, 0 total error rows. |
| `just verify-corpus --summary` | Passed: 2,939 expected records, 2,939 records on disk, no missing records, no unexpected records, no drifted fields, no `PATHS.tsv` issues. |
| `just review-queue --limit 12` | Confirmed `(+)-calanolide A` remains in `review-readiness`: `MECHANISM_REVIEW`, source-seeded mechanism not curator-checked, five source literature leads, zero record evidence items, and zero targets. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --query '10901131[uid] OR 12501127[uid] OR 14980631[uid] OR 15340243[uid] OR 9784162[uid]' --limit 20 --output /tmp/antibioticmech-calanolide-a-source-pmids.jsonl` | Wrote the five exact ChEBI source-PMID candidates. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query '"calanolide A" "HIV-1 reverse transcriptase"' --limit 20 --output /tmp/antibioticmech-calanolide-a-mechanism-publications.jsonl` | PubMed wrote 14 candidates; Semantic Scholar returned HTTP 429. |
| `env -u NCBI_EMAIL uv run python scripts/search_publications.py --provider pubmed --provider semantic-scholar --query 'NIDRYBLTWYFCFV OR 142632-32-4 OR C09147 OR C00002454' --limit 20 --output /tmp/antibioticmech-calanolide-a-ids-publications.jsonl` | PubMed wrote 20 broad candidates; Semantic Scholar returned HTTP 429. |

No separate single-record term, reference, or history validator is documented in
the local `justfile`. The full `just qc` gate is left for the PR-level check
because this review made no YAML mutation.

## Identity and Grounding

`calanolide-a.yaml` resolves unambiguously under
`data/antibiotics/antiviral/`. The generated lock row in
`data/antibiotics/PATHS.tsv` maps `CHEBI:65552` to
`ANTIVIRAL/calanolide-a`, and `source_concepts` carries only the expected ChEBI
upstream identity with minted ID `antibioticmech:chebi-3ecb7c88bf`.

The exact structure block matches the committed ChEBI antimicrobial inventory
row for `CHEBI:65552`: SMILES, Standard InChI, InChIKey
`NIDRYBLTWYFCFV-FMTVUPSXSA-N`, formula `C22H26O5`, charge `0`, average mass,
and monoisotopic mass agree with `data/raw/chebi_antimicrobials.tsv`. The
official ChEBI page for `CHEBI:65552` reports the same label, definition,
formula, SMILES, Standard InChI, InChIKey, HIV-1 reverse transcriptase
inhibitor role, CAS `142632-32-4`, KEGG `C09147`, KNApSAcK `C00002454`, and
Reaxys `5856917` xrefs.

The target is distinct from `CHEBI:65553` `(−)-calanolide B`: that sibling has
a different label, stereochemical layers in its Standard InChI, xrefs, and
InChIKey `NIDRYBLTWYFCFV-PZROIBLQSA-N`.

No local grounding decision or prior review report was found for `CHEBI:65552`,
`antibioticmech:chebi-3ecb7c88bf`, `NIDRYBLTWYFCFV-FMTVUPSXSA-N`,
`142632-32-4`, KEGG `C09147`, KNApSAcK `C00002454`, Reaxys `5856917`, or
`(+)-calanolide A` in a gitignore-independent search over `curation/`,
`data/raw/`, and `reports/yaml_record_review/`.

## Evidence

The generated identity, label, definition, exact synonyms, exact structure,
parent compounds, same-structure xrefs, filing class, HIV-1 reverse
transcriptase inhibitor role, source concept, and grounding status reproduce
from the committed ChEBI inventory with no `verify-corpus` drift.

The seeded `mode_of_action` and `mode_of_action_target_scope` are honest about
their provenance: the note states they are assigned from ChEBI role
`CHEBI:53756`, that ChEBI asserts that role on the compound, and that the field
is not a curator's mechanistic review. The corresponding ChEBI page still lists
the same HIV-1 reverse-transcriptase-inhibitor role.

The exact-PMID PubMed lookup resolved all five ChEBI literature leads:
`PMID:15340243` reports anti-HIV dipyranocoumarins, including calanolides A and
B and soulattrolide, from `Calophyllum brasiliense` leaves;
`PMID:9784162` reports the Calophyllum chemotaxonomic search and frames
`(+)-calanolide A` as an HIV-1-specific nonnucleoside reverse transcriptase
inhibitor; `PMID:14980631` reports antimycobacterial activity and preliminary
RNA/DNA/protein-synthesis observations; and `PMID:10901131` plus `PMID:12501127`
are pharmacokinetic or clinical-safety leads.

The targeted mechanism search found additional primary candidates about
`(+)-calanolide A` and HIV-1 reverse transcriptase. `PMID:15961674` reports
Thr139 substitutions that alter reverse transcriptase sensitivity to the
compound, and `PMID:10723499` reports sensitivity of wild-type and mutated
HIV-1 reverse-transcriptase molecules to `(+)-calanolide A` in primer-extension
assays. These are promising target-evidence leads, not claim-level curation in
the YAML.

The identifier query did not find a registry-identifier-only source that
changed the record identity or introduced a more exact upstream structure
source; most candidates were calanolide analog, safety, or
reverse-transcriptase papers already discoverable by name.

The record makes no molecular-target, activity, resistance, producer, clinical,
or causal-edge claims, so there are no unsupported claim-level citations in the
YAML.

## Completeness

The exact ChEBI identity, stereochemistry, structure, ChEBI grounding, source
concept, antiviral filing class, ChEBI HIV-1 reverse-transcriptase-inhibitor
role, parent compounds, and same-structure xrefs are defensible.

The consequential gap is mechanism sign-off. The YAML carries only a
source-seeded mode of action and scope, has no `molecular_targets` item for
HIV-1 reverse transcriptase, and has no target-specific `EvidenceItem`.
`just review-queue --limit 12` therefore keeps the record in
`MECHANISM_REVIEW`.

No activity observations, producer claims, resistance assertions, clinical
assertions, or public datasets are present. The PubMed source leads include
activity, producer, and safety literature that could support future optional
additions, but this read-only pass found no local evidence that any of those
optional sections must be populated before the unreviewed mechanism is
resolved.

## Findings

| Severity | Finding | Evidence | Maintained owner |
|---|---|---|---|
| Major | The record has only a source-seeded antiviral mechanism and cannot satisfy the `REVIEWED` gate until a curator reviews the HIV-1 reverse transcriptase claim and adds target-level evidence. | The target YAML has a ChEBI-seeded `mode_of_action` note ending `Not a curator's mechanistic review`, no `record_evidence`, no `molecular_targets`, and no `causal_graphs`; `just review-queue --limit 12` reports `MECHANISM_REVIEW: mechanism is source-seeded and not curator-checked; 5 source literature lead(s), 0 record evidence item(s), 0 target(s)`. | Curator-owned evidence, molecular target, activity observation, producer, discussion, or causal-graph additions written to `data/antibiotics/antiviral/calanolide-a.yaml` through `write_validated_antibiotic`, if primary literature supports them. |

No blocker findings.

No minor findings.

## Recommended Edits

1. Inspect primary HIV-1 reverse-transcriptase papers for exact
   `(+)-calanolide A`, prioritizing `PMID:15961674` and `PMID:10723499`, then
   add a curator-owned `MolecularTarget` for HIV-1 reverse transcriptase only if
   the source supports the target identity, direction, evidence status, and
   any resistance context at claim level.
2. Promote the source-seeded `VIRAL_POLYMERASE_INHIBITION` block to a
   curator-owned mechanism only when the selected paper supports inhibition of
   HIV-1 reverse transcriptase by this exact stereoisomer; keep
   `mode_of_action_target_scope: MICROBIAL_TARGET` because the target is viral.
3. Review `PMID:15340243`, `PMID:9784162`, and `PMID:14980631` for optional
   activity and producer claims. Keep anti-HIV reverse-transcriptase claims
   separate from the antimycobacterial RNA/DNA/protein-synthesis observations
   in `PMID:14980631`.
4. Do not promote evidence from `(-)-calanolide B`, racemic calanolide A,
   11-demethyl-calanolide A, 12-oxocalanolide A, F18, or other analogs unless
   the inspected source explicitly supports the same assertion for
   `(+)-calanolide A`.
5. If a mode of action, target, activity observation, or producer claim is
   supported, write it with a guarded mutator that asserts
   `identifier == "CHEBI:65552"`, updates only curator-owned fields, appends a
   `codex` `CurationEvent` with `llm_assisted: true`, and emits via
   `write_validated_antibiotic`.
6. Move `curation_status` to `REVIEWED` only after identity, structure, filing
   class, and the newly curator-checked mechanism satisfy the
   `docs/CURATION.md` sign-off criteria.

## Follow-up Checks

After a future mechanism curation edit:

- `just validate-strict data/antibiotics/antiviral/calanolide-a.yaml --out /tmp/antibioticmech-calanolide-a-validation.tsv`
- `just verify-corpus`
- `just qc`
- `git diff -- data/antibiotics/antiviral/calanolide-a.yaml curation/decisions.tsv src scripts tests`
- `just review-queue --limit 0 --tsv curation/record_review_queue.tsv`

## Additional Notes

Searches used to establish absence included ignored files:

- `rg --hidden --no-ignore` over `curation/record_review_queue.tsv`,
  `data/antibiotics/PATHS.tsv`, and `data/raw/` for `CHEBI:65552` and
  `calanolide`.
- `rg --hidden --no-ignore` over `curation/`, `data/raw/`, and
  `reports/yaml_record_review/` for `CHEBI:65552`,
  `antibioticmech:chebi-3ecb7c88bf`, `NIDRYBLTWYFCFV-FMTVUPSXSA-N`,
  `142632-32-4`, `kegg.compound:C09147`, `knapsack:C00002454`,
  `reaxys:5856917`, and `(+)-calanolide A`.

`NCBI_EMAIL` was unset for the PubMed searches. Semantic Scholar returned HTTP
429 for both non-PMID searches, so that provider's literature leads were not
available in this pass.

This report is read-only review output. No generated record, upstream
inventory, decision row, page, queue, or curation-history entry was edited.
