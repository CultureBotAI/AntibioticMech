# YAML Record Review: (S)-glaucine

- Repository: CultureBotAI/AntibioticMech
- Record: data/antibiotics/antibacterial/s-glaucine.yaml
- Started UTC: 2026-09-23T21:49:14Z
- Finished UTC: 2026-09-23T21:49:14Z
- Verdict: needs curation

## Target

| Field | Value |
|---|---|
| Path | `data/antibiotics/antibacterial/s-glaucine.yaml` |
| Identifier | `CHEBI:5373` |
| Label | `(S)-glaucine` |
| Filing class | `ANTIBACTERIAL` |
| Curation status | `SEEDED` |
| Grounding status | `EXACT` |
| Generated or maintained | Generated record, emitted from `data/raw/chebi_antimicrobials.tsv` through the ChEBI seeding path |
| Source concept | `CHEBI:5373` from ChEBI release `2026-08-30` |
| Minted source key | `antibioticmech:chebi-f9b3f51006` |

This review inspected the complete YAML record and treated `data/raw/chebi_antimicrobials.tsv`, `data/antibiotics/PATHS.tsv`, `conf/sources.yaml`, and `curation/record_review_queue.tsv` as the maintained local inputs for seeded ChEBI identity, class, role, structure, and source-literature-lead claims.

## Validation

| Check | Result |
|---|---|
| `just validate data/antibiotics/antibacterial/s-glaucine.yaml` | Passed with no LinkML validation issues. |
| `just validate-strict data/antibiotics/antibacterial/s-glaucine.yaml --out /tmp/s-glaucine-validate-strict.tsv` | Passed: 1 file checked, 0 files with `ERROR`, and 0 total `ERROR` rows. |
| `just verify-corpus --summary` | Passed: all 2,939 expected records were on disk, no unexpected records were present, no generated fields drifted, no identifiers were absent from `PATHS.tsv`, and no stale lockfile rows were present. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just worklist --limit 0 --tsv /tmp/s-glaucine-worklist.tsv` | Passed and regenerated a full worklist. Exact rows for `CHEBI:5373` report `mechanism` and `review-readiness` gaps with 0 CARD targets, 0 resistance edges, 10 source literature leads, 0 record evidence items, and 0 targets. The only diagnostic was the pre-existing unrelated CARD iclaprim/CHEBI:31724 cross-reference refusal. |
| `just review-queue --limit 125` | Passed and listed `(S)-glaucine` as `MECHANISM_REVIEW: mechanism is absent; 10 source literature lead(s), 0 record evidence item(s), 0 target(s)`. |
| `just lint` | Passed before writing this report. |
| `git diff --check` | Passed before writing this report. |
| `git diff --cached --check` | Passed after staging this report. |

No narrower single-record term, reference, or curation-history validator is exposed for this plain ChEBI-seeded record, so the review used the available single-record schema checks plus full-corpus reproducibility and queue/worklist checks.

## Identity and Grounding

The record identity is internally consistent:

| Claim | Review |
|---|---|
| ChEBI grounding | `identifier: CHEBI:5373`, `label: (S)-glaucine`, `grounding_status: EXACT`, and the sole `source_concepts` entry all agree. EBI OLS4 confirms that `CHEBI:5373` is active, non-obsolete, ChEBI-defining, and in the 3-star subset. |
| Raw ChEBI seed | `data/raw/chebi_antimicrobials.tsv` carries the same identifier, label, definition, 3-star status, SMILES, Standard InChI, Standard InChIKey `RUZIUYOSRDWYQF-HNNXBMFYSA-N`, formula, charge, masses, IUPAC name, activity role, and xrefs. |
| Structure | SMILES `[H][C@]12Cc3cc(OC)c(OC)cc3-c3c(OC)c(OC)cc(c31)CCN2C`, Standard InChI, InChIKey, formula `C21H25NO4`, charge `0`, average mass `355.434`, and monoisotopic mass `355.17836` are the exact ChEBI-imported values. |
| Parent boundary | Live OLS and the local raw ChEBI row agree that `CHEBI:5373` has exactly four hierarchical parents: `CHEBI:134209` aporphine alkaloid, `CHEBI:38163` organic heterotetracyclic compound, `CHEBI:46774` polyether, and `CHEBI:50996` tertiary amino compound. |
| Role and filing class | The sole local role is `CHEBI:33282` / antibacterial agent. Live OLS also relates `CHEBI:5373` to `CHEBI:33282`, and `conf/sources.yaml` maps that role to `ANTIBACTERIAL`, matching `data/antibiotics/antibacterial/`. |
| Related exact ChEBI terms | Live OLS exposes `CHEBI:134212` `(S)-glaucine(1+)` as a protonated-form relation with a different formula, charge, InChI, InChIKey, and SMILES. The current neutral `CHEBI:5373` record does not conflate those structures. |
| Path lock | `data/antibiotics/PATHS.tsv` maps `CHEBI:5373` to `ANTIBACTERIAL` / `s-glaucine`, matching the current record path. |

An ignored-inclusive search across `data/antibiotics/PATHS.tsv`, `data/raw`, `curation`, and `reports/yaml_record_review` for `CHEBI:5373` and `glaucine` found the expected path-lock row, ChEBI raw row, exact review-queue row, target YAML, and no previous exact `(S)-glaucine` review report.

## Evidence

There is no claim-level evidence to audit because this seeded record has no `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `activity_observations`, `resistance_mechanisms`, `producer_organisms`, `causal_graphs`, datasets, discussions, or record-level `evidence`.

The existing scientific assertions are seed-owned ChEBI database assertions:

| Assertion | Support |
|---|---|
| `(S)-glaucine` is a ChEBI-grounded exact record with the listed IUPAC synonym, non-PubMed xrefs, and structure. | Supported by the committed ChEBI inventory row for `CHEBI:5373`, and independently consistent with the live OLS4 `CHEBI:5373` term. |
| The record carries the ChEBI antibacterial-agent role. | Supported by the committed ChEBI row, which lists `CHEBI:33282` as its sole local activity role, and by live OLS, which still relates `CHEBI:5373` to `CHEBI:33282`. |

Bounded publication discovery did not find a source that could justify adding a mechanism, molecular target, resistance mechanism, or direct activity claim for exact isolated `(S)-glaucine`:

| Query or lead set | Result |
|---|---|
| Exact `(S)-glaucine` | PubMed returned 9 candidates; Semantic Scholar was rate-limited with HTTP 429. They cover 2025 chemoenzymatic aporphine synthesis, a 2025 *Mahonia napaulensis* bark extract profiling paper that identifies `(S)-glaucine` as one LC-MS/MS component while testing only the crude extract, 2018 serotonin and adrenergic receptor testing, human bronchodilator and smooth-muscle pharmacology, HPLC use of `(+)-glaucine` as an internal standard, rat uterus relaxation, NMR characterization, and microbial glaucine oxidation. None provides antibacterial MIC, target, mechanism, or resistance evidence for isolated exact `(S)-glaucine`. |
| Exact `RUZIUYOSRDWYQF-HNNXBMFYSA-N` InChIKey through PubMed and Semantic Scholar | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| Exact `(6aS)-1,2,9,10-tetramethoxy-6-methyl-5,6,6a,7-tetrahydro-4H-dibenzo[de,g]quinoline` IUPAC name | PubMed returned 0 candidates; Semantic Scholar was rate-limited with HTTP 429. |
| `glaucine antibacterial MIC mechanism` | The top 20 candidates were unrelated broad antibacterial mechanism papers and did not name glaucine in normalized titles or abstracts. |
| Ten pinned ChEBI PubMed source leads | Direct PubMed summaries show cancer P-glycoprotein/MRP1 inhibition, rat and human glaucine metabolism, *Glaucium flavum* anticancer alkaloid quantification, *Nandina domestica* alkaloid discovery, yellow hornpoppy micropropagation, isoquinoline neuraminidase inhibitors, Caco-2 P-glycoprotein transport of psychoactive substances, breast cancer migration/NF-kappaB work, and Yuanhu Zhitong quality-control HPLC. None is direct antibacterial mechanism or activity evidence for exact `(S)-glaucine`. |

## Completeness

The generated identity, source-concept, structure, xref, antibacterial role, parent, and curation-history fields are complete enough for a ChEBI seed.

The record remains scientifically incomplete because it has a ChEBI antibacterial role but no evidence-backed mode of action, molecular target, resistance mechanism, direct MIC or other activity observation, causal graph, or exact antibacterial literature lead. The derived worklist keeps it in both the `mechanism` and `review-readiness` queues with 10 source literature leads, no record evidence, and no targets. That gap blocks `REVIEWED` status under the local gate because the antibacterial source leads have not been promoted into evidence-backed mechanism or activity claims.

The optional target, activity, resistance, producer, causal-graph, dataset, and discussion slots are correctly absent in the sense that the bounded exact searches and inspected ChEBI source leads did not produce support that would justify populating them during this read-only review.

## Findings

### Blocker

None found.

### Major

| Finding | Evidence | Maintained owner |
|---|---|---|
| The record has only a seed-owned ChEBI antibacterial role and no evidence-backed mode of action, target, resistance mechanism, direct activity observation, or causal graph for exact `(S)-glaucine`, so it cannot qualify as `REVIEWED`. | The YAML lacks `mode_of_action`, `mode_of_action_target_scope`, `molecular_targets`, `resistance_mechanisms`, `activity_observations`, `causal_graphs`, and record-level `evidence`; `curation/record_review_queue.tsv` and the regenerated worklist both place `CHEBI:5373` in `MECHANISM_REVIEW` with 10 ChEBI source literature leads, 0 record evidence items, and 0 targets. Exact publication searches and the pinned ChEBI PubMed lead titles did not find direct antibacterial mechanism evidence for isolated `(S)-glaucine`. | Future curator-owned additions to `data/antibiotics/antibacterial/s-glaucine.yaml`, written only through `write_validated_antibiotic` with a `record_curation_event`. |

### Minor

None found.

## Recommended Edits

1. Keep the seeded identity, structure, antibacterial ChEBI role, parent terms, xrefs, drug xref, and path unchanged.
2. Before adding `mode_of_action`, `mode_of_action_target_scope`, target, activity observation, resistance mechanism, or causal graph fields, inspect primary antibacterial evidence for isolated exact `CHEBI:5373` or an explicitly structure-equivalent S-(+)-glaucine source.
3. Do not transfer weak crude-extract MIC values from *Mahonia napaulensis* bark, cancer-cell P-glycoprotein effects, human receptor pharmacology, calcium-channel effects, or PDE4 inhibition into this antibacterial record unless a source itself tests exact isolated `(S)-glaucine` in a microbial system.

## Follow-up Checks

Run the focused and full-corpus gates after any future curation:

1. `just validate data/antibiotics/antibacterial/s-glaucine.yaml`
2. `just validate-strict data/antibiotics/antibacterial/s-glaucine.yaml --out /tmp/s-glaucine-validate-strict.tsv`
3. `just verify-corpus --summary`
4. `just worklist --limit 0 --tsv /tmp/s-glaucine-worklist.tsv`
5. `just review-queue --limit 125`
6. `just lint`

Also re-run exact publication searches for `(S)-glaucine`, `CHEBI:5373`, `RUZIUYOSRDWYQF-HNNXBMFYSA-N`, and the exact ChEBI IUPAC name before concluding that isolated-compound antibacterial evidence remains unresolved.

## Additional Notes

- The record has only seed and reseed curation-history events from `2026-08-30` and `2026-09-10`, accurately matching its generated `SEEDED` state.
- Live OLS lists the same CAS, KEGG compound, KEGG drug, KNApSAcK, and Reaxys xrefs imported locally, and additional PubMed xrefs beyond the ten currently pinned source literature leads.
- Live OLS relates the term to several non-antimicrobial roles, including antineoplastic agent, platelet aggregation inhibitor, antitussive, muscle relaxant, NF-kappaB inhibitor, plant metabolite, and rat metabolite; the local seed transform correctly ignores those for AntibioticMech class filing.
- Semantic Scholar returned HTTP 429 for the exact label, InChIKey, and IUPAC publication searches.
